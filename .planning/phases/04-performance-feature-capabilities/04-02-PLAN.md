---
phase: 04-performance-feature-capabilities
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/field/field.ts
  - test/components/field.test.ts
  - src/components/textarea/textarea.ts
  - test/components/textarea.test.ts
  - src/components/number-field/number-field.ts
  - test/components/number-field.test.ts
  - src/components/input-otp/input-otp.ts
  - test/components/input-otp.test.ts
autonomous: true
requirements: [FEAT-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "In the error state am-field hides the slotted am-hint-text and shows the slotted am-error-text; when the error clears the hint returns (D-02 — one message line, single describedby target)."
    - "am-textarea, am-number-field, and am-input-otp each surface the native validationMessage in a same-shadow-root aria-live region with aria-describedby/aria-invalid, on D-01 blur/submit timing (FEAT-01)."
    - "Each of the three controls exposes setCustomError(message) with D-03 custom-wins precedence, adopting the tracer's ValidationController identically."
    - "EDGE: am-field's hint<->error swap reacts to the control's reflected invalid attribute without a module-level singleton or global state (per-instance MutationObserver, torn down on disconnect)."
  artifacts:
    - src/components/field/field.ts
    - src/components/textarea/textarea.ts
    - src/components/number-field/number-field.ts
    - src/components/input-otp/input-otp.ts
  key_links:
    - "am-field default-slot control.invalid attribute -> am-field hint/error slot visibility swap (D-02)"
    - "each control -> ValidationController (src/internal/controllers/validation.ts) reuse"
  prohibitions:
    - "NO new module-level singleton / global state for the D-02 swap — per-instance observer only (no-global-state constraint)."
    - "NO auto-show on first paint (D-01) — inherited from the ValidationController timing gate."
    - "NO raw-HTML sink for messages — text binding only."
    - "NO hardcoded colors — reuse --am-* tokens (--am-danger)."
---

<objective>
Expand the proven tracer validation pattern to the text-input family (am-textarea, am-number-field, am-input-otp) and add the am-field D-02 visual hint-to-error swap. These three controls mirror am-input's inner-focusable structure, so they adopt the `ValidationController` with the least structural variation — the natural first expansion step.

Purpose: Deliver FEAT-01 for the text-entry controls and the Material-style D-02 supporting-text-to-error-text swap that gives one message line and a single describedby target.
Output: Three validated controls plus am-field visual orchestration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md
</context>

## Artifacts this phase produces (added by this plan)

- am-field D-02 hint↔error visual swap (light-DOM orchestration; no new public event).
- `setCustomError(message: string)` public method on am-textarea, am-number-field, am-input-otp (NEW public surface — captured by plan 04-10).
- Same-shadow-root validation message region wired on each of the three controls (internal DOM).

<tasks>

<task type="auto">
  <name>Implement am-field D-02 hint↔error visual swap</name>
  <files>src/components/field/field.ts, test/components/field.test.ts</files>
  <read_first>
    - src/components/field/field.ts (current structural wrapper: slots label/default/hint/error at :44-52; static styles :29-42 — this is the file to extend)
    - src/internal/helpers/teardown-scope.ts (per-instance abortable teardown discipline — reuse for the observer lifecycle, torn down on disconnect)
    - .planning/phases/04-performance-feature-capabilities/04-CONTEXT.md D-02 (error replaces hint; hint returns on clear) and D-04
    - .planning/research/FEATURES.md §"Target #2" (Material supporting-text→error-text model)
  </read_first>
  <action>
    Extend am-field so that, per D-02, the slotted hint is hidden and the slotted error is shown while the associated control is invalid, and the reverse when valid. Do NOT introduce a new public event or a module-level singleton. Resolve the form control from the default slot via `@queryAssignedElements()` (default slot). Observe that control's reflected `invalid` attribute using a per-instance `MutationObserver` (attributeFilter: `['invalid']`); on change, toggle visibility of the `hint` and `error` slot wrappers (wrap each named slot so visibility can be driven without removing the slot from the tree — hidden hint via `hidden`/display none). Connect the observer in `connectedCallback`/on slotchange and disconnect it in `disconnectedCallback` (mirror the teardown discipline). Keep a single `aria-describedby` target semantic: the control owns the SR-authoritative message (from 04-01); am-field's swap is presentational only. Preserve existing `part="field"` and `--am-field-gap`.

    Extend `test/components/field.test.ts` (jsdom): mount am-field with a slotted control, a slotted am-hint-text, and a slotted am-error-text. Assert that toggling the control's `invalid` attribute hides the hint and reveals the error; clearing it reverses. Assert the observer is disconnected on am-field disconnect (no swap after removal).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run field` passes: invalid → hint hidden + error shown; valid → hint shown + error hidden; observer torn down on disconnect.
    - field.ts contains no module-level mutable state (grep confirms the swap state lives on the instance, not at module scope).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run field</automated>
  </verify>
  <done>am-field swaps slotted hint↔error on the control's invalid state (D-02), per-instance and torn down cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-textarea and am-number-field</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to two more controls; binds at freeze.</reversibility>
  <files>src/components/textarea/textarea.ts, test/components/textarea.test.ts, src/components/number-field/number-field.ts, test/components/number-field.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (the ValidationController created in 04-01 — the reuse target)
    - src/components/input/input.ts (the now-proven tracer analog: how ValidationController is instantiated, same-root message region, markTouched on blur/submit, setCustomError facade)
    - src/components/textarea/textarea.ts and src/components/number-field/number-field.ts (the controls to wire — locate their inner focusable, formAssociated/internals, blur handler, and invalid reflection)
    - .planning/phases/04-performance-feature-capabilities/04-PATTERNS.md §"input.ts + 14 form-associated controls"
  </read_first>
  <action>
    In am-textarea and am-number-field, adopt the ValidationController exactly as am-input does: instantiate with `{ internals: () => this.internals, anchor: () => <inner focusable>, describedById: uniqueId(...) }`; render a same-shadow-root `aria-live` message region (polite per-field, role=alert on submit — D-04) shown only when `controller.invalid`; set the inner focusable's `aria-describedby` to the message id when invalid and keep `aria-invalid` driven by `this.invalid`; call `this.internals.setValidity(...)` from the host mirroring the inner control's native `.validity`; hook `markTouched()` into the blur handler and the submit path; add the public `setCustomError(message: string): void` facade with JSDoc (`@param message`, `''` clears to native). Reuse existing `--am-danger` invalid styling; do not add hardcoded colors. Do not introduce any raw-HTML sink — text binding only.

    Write/extend jsdom tests first (TDD): textarea.test.ts and number-field.test.ts each assert no first-paint error, setCustomError override + '' fallback, and markTouched gating.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run textarea number-field` passes: no first-paint error; setCustomError precedence (D-03); markTouched timing (D-01).
    - Both controls' setCustomError carries a JSDoc block (captured into dist/custom-elements.json on build:manifest).
    - Neither control re-exports ValidationController; both import it from src/internal.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run textarea number-field</automated>
  </verify>
  <done>am-textarea and am-number-field surface native validationMessage + setCustomError with D-03 precedence and D-01/D-04 timing, reusing the tracer ValidationController.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-input-otp</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to am-input-otp; binds at freeze.</reversibility>
  <files>src/components/input-otp/input-otp.ts, test/components/input-otp.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog)
    - src/components/input-otp/input-otp.ts (multi-cell OTP control — identify the form-associated internals, the aggregate value, and the focusable that should carry aria-describedby/aria-invalid; the message must live in this control's own shadow root)
  </read_first>
  <action>
    Adopt the ValidationController in am-input-otp following the am-input pattern, accounting for its multi-cell structure: the SR-authoritative message and aria-describedby/aria-invalid attach to the control's primary focusable (the active/first OTP cell or the group container that receives focus). Wire setValidity from the aggregate OTP value, markTouched on blur of the group + submit, same-root aria-live region (D-04 politeness), and the public setCustomError facade with JSDoc. Reuse --am-danger; text binding only.

    Write jsdom tests first (TDD) in input-otp.test.ts: no first-paint error, setCustomError override + '' fallback, markTouched gating on the aggregate value.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run input-otp` passes: no first-paint error; setCustomError D-03 precedence; markTouched timing.
    - setCustomError carries a JSDoc block; ValidationController imported from src/internal (not re-exported).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run input-otp</automated>
  </verify>
  <done>am-input-otp surfaces native validationMessage + setCustomError with D-03 precedence on its aggregate value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer/server → setCustomError on 3 controls | Server error strings rendered into control DOM |
| slotted content → am-field observer | Consumer-slotted hint/error toggled by control state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering / Info-disclosure | setCustomError on textarea/number-field/input-otp | high | mitigate | Message rendered via Lit text binding only; no raw-HTML sink. jsdom test asserts text content, not markup. |
| T-04-05 | Denial-of-control | am-field MutationObserver lifecycle | low | mitigate | Per-instance observer disconnected in disconnectedCallback; no leak, no global state. |
</threat_model>

<verification>
- `npm test -- --run field textarea number-field input-otp` green.
- `npx tsc --noEmit` exits 0.
- No ValidationController re-export on the public surface.
</verification>

<success_criteria>
The three text-entry controls surface native validation messages and setCustomError with D-03 precedence on D-01/D-04 timing, and am-field performs the D-02 hint↔error swap — all reusing the tracer ValidationController.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-02-SUMMARY.md` when done.
</output>
