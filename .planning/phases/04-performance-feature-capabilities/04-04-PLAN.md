---
phase: 04-performance-feature-capabilities
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/select/select.ts
  - test/components/select.test.ts
  - src/components/combobox/combobox.ts
  - test/components/combobox.test.ts
  - src/components/rich-select/rich-select.ts
  - test/components/rich-select.test.ts
  - src/components/date-picker/date-picker.ts
  - test/components/date-picker.test.ts
  - src/components/time-picker/time-picker.ts
  - test/components/time-picker.test.ts
autonomous: true
requirements: [FEAT-01, FEAT-02]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "am-select, am-combobox, am-rich-select, am-date-picker, and am-time-picker each surface the native validationMessage in a same-shadow-root aria-live region with aria-describedby/aria-invalid on D-01 blur/submit timing (FEAT-01)."
    - "Each of the five controls exposes setCustomError(message) with D-03 custom-wins precedence, reusing the tracer ValidationController (FEAT-02)."
    - "Adding validation to combobox/select does NOT reintroduce the Phase 3 FIX-02 clamp behavior (ListboxNavController still does not re-clamp _highlightedIndex on option replace) — existing combobox/select tests stay green."
    - "EDGE: on the overlay controls the aria-describedby message anchors to the control's primary focusable (combobox input / select combobox-role wrapper), never to a node in am-field light DOM (Pitfall 3)."
  artifacts:
    - src/components/select/select.ts
    - src/components/combobox/combobox.ts
    - src/components/rich-select/rich-select.ts
    - src/components/date-picker/date-picker.ts
    - src/components/time-picker/time-picker.ts
  key_links:
    - "each composite control -> ValidationController (src/internal/controllers/validation.ts) reuse"
    - "combobox/select primary focusable -> same-root validation message (does not touch the listbox nav path)"
  prohibitions:
    - "NO auto-show on first paint (D-01)."
    - "NO reintroduction of the FIX-02 stale-index re-clamp or FIX-03 focus regressions."
    - "NO raw-HTML sink for messages — text binding only."
    - "NO hardcoded colors — reuse --am-* tokens."
---

<objective>
Complete validation-message wiring for the five composite/overlay form controls: am-select, am-combobox, am-rich-select, am-date-picker, am-time-picker. This finishes FEAT-01/FEAT-02 across all 14 named form-associated validation targets (am-input from 04-01; text family from 04-02; choice/range from 04-03; composites here). The validation wiring touches only the control's primary focusable + a same-root message region — it deliberately does not modify the listbox navigation or floating-position seams (those are PERF-03/PERF-04 in later waves).

Purpose: Deliver FEAT-01/FEAT-02 for the overlay-bearing controls without disturbing the Phase 3 async-index/focus fixes.
Output: Five validated composite controls; validation wiring complete for all 14 controls.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md
</context>

## Artifacts this phase produces (added by this plan)

- `setCustomError(message: string)` public method on am-select, am-combobox, am-rich-select, am-date-picker, am-time-picker (NEW public surface — captured by plan 04-10).
- Same-shadow-root validation message region + aria-describedby/aria-invalid wiring on each (internal DOM), anchored to the control's primary focusable.

<tasks>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-select and am-combobox</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to select and combobox; binds at freeze.</reversibility>
  <files>src/components/select/select.ts, test/components/select.test.ts, src/components/combobox/combobox.ts, test/components/combobox.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog: instantiation, same-root message region, markTouched, setCustomError facade)
    - src/components/combobox/combobox.ts (combobox-mode input focus/blur handlers at :600-602; select-mode combobox-role wrapper at :646-657; invalid class in wrapper at :641 — anchor the message to the primary focusable; do NOT touch the repeat() listbox at :611-625/:678-694)
    - src/internal/controllers/listbox-nav.ts:33-43 (FIX-02 docstring: does NOT re-clamp _highlightedIndex on option replace — must remain untouched)
  </read_first>
  <action>
    Adopt the ValidationController in am-select and am-combobox. Anchor the same-root aria-live message and aria-describedby/aria-invalid to the control's PRIMARY FOCUSABLE (combobox-mode input; select-mode combobox-role wrapper) — never a node in am-field light DOM (Pitfall 3). Wire setValidity from the value/required state, markTouched on the input/wrapper blur handlers + submit, D-04 politeness, public setCustomError facade with JSDoc, reuse existing --am-danger `.wrapper.invalid` styling, :host([invalid]) reflection, text binding only. Do NOT modify the repeat() listbox render, the ListboxNavController wiring, or the floating-position seam — validation is orthogonal to those (PERF-03/PERF-04 handle them). Confirm the existing combobox/select tests still pass unchanged (no FIX-02/FIX-03 regression).

    TDD: extend select.test.ts and combobox.test.ts — no first-paint error; setCustomError override + '' fallback; markTouched gating; existing option-nav/index-clamp tests remain green.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run select combobox` passes: no first-paint error; setCustomError D-03 precedence; markTouched timing; pre-existing option-nav/index-clamp assertions still green.
    - Both setCustomError methods carry JSDoc; ValidationController imported from src/internal.
    - `git diff --stat src/internal/controllers/listbox-nav.ts` shows no change (FIX-02 seam untouched).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run select combobox</automated>
  </verify>
  <done>am-select and am-combobox surface native validationMessage + setCustomError with D-03 precedence; the listbox-nav / floating seams are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-rich-select and am-date-picker</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to rich-select and date-picker; binds at freeze.</reversibility>
  <files>src/components/rich-select/rich-select.ts, test/components/rich-select.test.ts, src/components/date-picker/date-picker.ts, test/components/date-picker.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog)
    - src/components/rich-select/rich-select.ts and src/components/date-picker/date-picker.ts (identify the form-associated internals, the value, and the primary focusable/trigger to anchor aria-describedby/aria-invalid; do not touch the floating-position seam)
  </read_first>
  <action>
    Adopt the ValidationController in am-rich-select and am-date-picker, anchoring the same-root aria-live message + aria-describedby/aria-invalid to each control's primary focusable/trigger. Wire setValidity from value/required, markTouched on blur + submit, D-04 politeness, public setCustomError facade with JSDoc, --am-danger styling, :host([invalid]) reflection, text binding only. Do not modify the floating-position seam (PERF-04) or any listbox render (PERF-03).

    TDD: extend rich-select.test.ts and date-picker.test.ts — no first-paint error; setCustomError precedence; markTouched gating; existing tests green.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run rich-select date-picker` passes: no first-paint error; setCustomError D-03 precedence; markTouched timing; existing suites green.
    - setCustomError methods carry JSDoc; ValidationController imported from src/internal.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run rich-select date-picker</automated>
  </verify>
  <done>am-rich-select and am-date-picker surface native validationMessage + setCustomError with D-03 precedence.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-time-picker</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to am-time-picker; binds at freeze.</reversibility>
  <files>src/components/time-picker/time-picker.ts, test/components/time-picker.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog)
    - src/components/time-picker/time-picker.ts (form-associated internals + value + primary focusable; time-picker has no floating-ui per Phase 2 finding — a plain composite)
  </read_first>
  <action>
    Adopt the ValidationController in am-time-picker, anchoring the same-root aria-live message + aria-describedby/aria-invalid to its primary focusable. Wire setValidity from value/required, markTouched on blur + submit, D-04 politeness, public setCustomError facade with JSDoc, --am-danger styling, :host([invalid]) reflection, text binding only.

    TDD: extend time-picker.test.ts — no first-paint error; setCustomError precedence; markTouched gating.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run time-picker` passes: no first-paint error; setCustomError D-03 precedence; markTouched timing.
    - setCustomError carries JSDoc; ValidationController imported from src/internal.
    - All 14 named form-associated controls now expose setCustomError: `node -e "const fs=require('fs');const c=['input','textarea','checkbox','switch','radio','slider','color-picker','number-field','input-otp','select','combobox','rich-select','date-picker','time-picker'];const miss=c.filter(n=>!/setCustomError/.test(fs.readFileSync('src/components/'+n+'/'+n+'.ts','utf8')));process.stdout.write(miss.join(',')); process.exit(miss.length?1:0)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run time-picker</automated>
  </verify>
  <done>am-time-picker surfaces native validationMessage + setCustomError with D-03 precedence; validation wiring is complete across all 14 named controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer/server → setCustomError on 5 composite controls | Server error strings rendered into control DOM |
| overlay listbox content → validation render | Validation render must not perturb the listbox/floating seams |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering / Info-disclosure | setCustomError on select/combobox/rich-select/date-picker/time-picker | high | mitigate | Message rendered via Lit text binding only; no raw-HTML sink. Tests assert text content. |
| T-04-09 | Tampering | accidental FIX-02/FIX-03 regression via validation edit | medium | mitigate | Validation touches only the primary-focusable + message region; listbox-nav.ts diff asserted empty; existing option-nav/focus tests must stay green. |
</threat_model>

<verification>
- `npm test -- --run select combobox rich-select date-picker time-picker` green.
- `npx tsc --noEmit` exits 0.
- listbox-nav.ts unchanged; all 14 controls expose setCustomError.
</verification>

<success_criteria>
The five composite/overlay controls surface native validation messages and setCustomError with D-03 precedence on D-01/D-04 timing; validation is complete across all 14 named form-associated controls without disturbing the Phase 3 fixes.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-04-SUMMARY.md` when done.
</output>
