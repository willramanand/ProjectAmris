---
phase: 07-measurement-baselines-budgets
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - test/perf/_spike.cdp.test.ts
  - test/perf/_spike.lit-markers.test.ts
autonomous: true
requirements: [MEAS-02, MEAS-04, MEAS-05]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "cdp() returns a usable Playwright CDPSession in the new Chromium-only perf project; Emulation.setCPUThrottlingRate + Network.emulateNetworkConditions both apply (throttled wall-clock is measurably greater than unthrottled for the same work) — proving research assumption A2."
    - "The exact vitest config key path that grants cdp() write+exec is pinned in vitest.config.ts with a comment; if in-test cdp() cannot be enabled, the ctx.page.context().newCDPSession(ctx.page) custom-command fallback is wired instead and documented."
    - "The three inlined-Lit version-marker strings (reactiveElementVersions, litHtmlVersions, litElementVersions) are confirmed present in a deliberately-bundled Lit fixture and absent from the real externalized dist — validating research assumption A3 for the Plan 02 no-bundled-Lit assertion."
    - "rollup-plugin-visualizer@^7.1.1, @size-limit/esbuild-why@^13.0.3, and tachometer@^0.7.2 are installed as devDependencies with pinned version ranges; the lockfile updates; consumer dependencies/peerDependencies are unchanged (no bundled Lit, no new consumer-facing runtime dep)."
    - "package.json gains ALL the phase's report-only script entrypoints as stubs (test:perf, size:baseline, perf:diff, assert:no-lit, size:why, visualize, attribution:check) so every downstream plan wires to stable names WITHOUT re-editing package.json — Plan 00 is the sole Wave-1 owner of package.json (avoids same-wave file conflicts)."
  prohibitions:
    - "MUST NOT enable cdp() write+exec on any project other than the Chromium-only perf project (never on the shipped build)."
    - "MUST NOT add any of the three tools to dependencies or peerDependencies — devDependencies only."
    - "MUST NOT import test/setup.ts or any mock symbol into the perf project (native Chromium APIs only)."
    - "MUST NOT patch @floating-ui/dom or Lit exports to obtain metrics."
  artifacts:
    - test/perf/_spike.cdp.test.ts
    - test/perf/_spike.lit-markers.test.ts
    - vitest.config.ts (perf project + writeMetrics command stub)
    - package.json (3 devDependencies + script stubs)
  key_links:
    - "cdp() privilege key path → unblocks Plan 03 perf harness (A2)."
    - "Confirmed Lit marker strings → unblocks Plan 02 no-bundled-Lit assertion (A3)."
    - "Installed devDependencies → unblock Plan 05 attribution report (MEAS-05)."
---

<objective>
De-risk the two MEDIUM-confidence research assumptions (A2: the cdp() write+exec privilege key path; A3: the inlined-Lit version-marker strings) before any harness or assertion is built on them, and install the three pinned dev-only measurement tools.

Purpose: A2 and A3 are the only research unknowns in this phase. Proving them on a one-line spike now converts a possible mid-harness dead-end into a five-minute fix, and pins the exact config shape the perf harness (Plan 03) and no-bundled-Lit assertion (Plan 02) depend on.
Output: A confirmed, commented cdp() throttle path (or its custom-command fallback), a validated set of Lit marker strings, the new Chromium-only perf project scaffold + writeMetrics command stub, and three installed devDependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Spike the cdp() throttle privilege path (A2) and scaffold the Chromium-only perf project</name>
  <files>vitest.config.ts, test/perf/_spike.cdp.test.ts</files>
  <read_first>
    - vitest.config.ts (existing `browser` project block, lines 18-32 — mirror its provider/headless/instances shape; the setupFiles-omitted comment at 22-24 is the rule to preserve)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Pattern 1" (cdp() signature, THROTTLE_PROFILE, NETWORK_TIERS, applyThrottle) and §"Pitfall 1" (exact key-path uncertainty + fallback) and §"Pattern 4" (writeMetrics command)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"writeMetrics custom command + perf project"
  </read_first>
  <action>
    Add a new `perf` project to vitest.config.ts alongside `jsdom` and `browser`, mirroring the `browser` project: provider playwright(), headless true, instances [{ browser: 'chromium' }] (D-11 Chromium-only), include ['test/perf/**/*.perf.test.ts', 'test/perf/**/*.spike.test.ts'], and NO setupFiles (native APIs only). Add the browser-server write+exec grant needed by cdp() — research assumption A2 names it api.allowWrite / api.allowExec; determine the exact nesting empirically in this task and pin it with a comment citing A2. Register a Node-side writeMetrics command stub (BrowserCommand from vitest/node doing writeFileSync of JSON) under test.browser.commands so Plan 03 has a stable persistence channel. Write test/perf/_spike.cdp.test.ts that imports cdp from 'vitest/browser', obtains a CDPSession, sends Emulation.setCPUThrottlingRate {rate:6} and Network.emulateNetworkConditions with the Slow-3G tier constants, then times a small busy loop throttled vs unthrottled to prove the throttle actually applied. If in-test cdp() cannot be granted, wire the documented fallback (ctx.page.context().newCDPSession(ctx.page) via a custom command) and pin that shape instead. This spike is the end-to-end tracer for the entire perf lane: config grant → CDP session → throttle command → observable slowdown, one path.
  </action>
  <verify>
    <automated>npx vitest run --project perf test/perf/_spike.cdp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project perf test/perf/_spike.cdp.test.ts` exits 0.
    - The spike asserts cdp() returned a session and that throttled elapsed time for a fixed busy loop is strictly greater than unthrottled (proves the CPU throttle is live, not a silent no-op).
    - vitest.config.ts contains the pinned write+exec key path with an inline comment referencing assumption A2, or the newCDPSession custom-command fallback if the in-test path was unavailable.
    - A writeMetrics BrowserCommand stub is registered under the perf project's test.browser.commands.
    - The perf project has no setupFiles and imports no mock/setup.ts symbol.
  </acceptance_criteria>
  <reversibility rating="reversible">New test project + spike file; deletable config additions.</reversibility>
  <done>The Chromium-only perf project exists, cdp() throttling is proven to apply under the pinned config key (or fallback), and writeMetrics is stubbed — the perf lane spine is de-risked for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the inlined-Lit version-marker strings (A3)</name>
  <files>test/perf/_spike.lit-markers.test.ts</files>
  <read_first>
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Pitfall 4" (the three marker globals + "validate against a deliberately-bundled fixture" note) and Assumptions log A3
    - vite.config.ts (the `external` array at line 220 — what keeps Lit out of dist today)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"scripts/assert-no-bundled-lit.mjs" (the guard this validates for)
  </read_first>
  <action>
    Write test/perf/_spike.lit-markers.test.ts (jsdom lane is fine here — this is a string/bundling check, not a throttle check; place it so it runs under the existing verify lane or perf lane as convenient, but keep it mock-free). Prove that the three Lit version-marker identifiers reactiveElementVersions, litHtmlVersions, litElementVersions appear verbatim in a deliberately-bundled Lit source blob (import a small piece of Lit source text or construct a fixture that inlines it) and do NOT appear in the current externalized build output. The goal is to confirm these three identifiers are reliable positive markers of an inlined Lit copy before Plan 02 greps dist for them. If any of the three proves unreliable, record the corrected marker set in the test and in a note for Plan 02. Do not modify vite.config.ts's external array — it is snapshotted by the Plan 02 guard and must stay byte-stable.
  </action>
  <verify>
    <automated>npx vitest run test/perf/_spike.lit-markers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/perf/_spike.lit-markers.test.ts` exits 0.
    - The test asserts each confirmed marker string is present in the deliberately-bundled fixture.
    - The test asserts the confirmed marker strings are absent from the real externalized dist output (or documents that dist must be built first).
    - The final confirmed marker set is recorded in the test file for Plan 02 to consume.
    - vite.config.ts external array is unchanged (no diff).
  </acceptance_criteria>
  <reversibility rating="reversible">Spike test file only; no production change.</reversibility>
  <done>The three Lit version-marker strings are validated (or corrected) against a bundled fixture, giving Plan 02's no-bundled-Lit grep a proven signal.</done>
</task>

<task type="auto">
  <name>Task 3: Install the three pinned dev-only measurement tools and add report-only script stubs</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (devDependencies block lines 85-105; scripts block lines 59-79; files array lines 48-51 — must stay ["dist","README.md"]; dependencies/peerDependencies must not change)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Standard Stack" (version pins) and §"Package Legitimacy Audit" (all three Approved, dev-only)
    - MEMORY.md worktree note (run `npm install` after a dep-adding plan in each active worktree)
  </read_first>
  <action>
    Add exactly three devDependencies with the pinned ranges from research: rollup-plugin-visualizer@^7.1.1, @size-limit/esbuild-why@^13.0.3 (must match installed size-limit major 13), tachometer@^0.7.2. Run the install so package-lock.json updates. Do NOT touch dependencies or peerDependencies (lit stays the only peer; nothing bundles Lit). Add ALL of the phase's report-only npm script entrypoints now (Plan 00 is the sole Wave-1 owner of package.json so no downstream plan re-edits it): "test:perf" (runs the perf vitest project — e.g. `vitest run --project perf test/perf`), "size:baseline" (`node scripts/size-baseline.mjs`), "perf:diff" (`node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json`), "assert:no-lit" (`node scripts/assert-no-bundled-lit.mjs`), "size:why" (size-limit with the esbuild-why breakdown), "visualize" (VISUALIZE=1 build for the dev attribution report), and "attribution:check" (`node scripts/attribution-check.mjs`). These are inert stubs where their target script does not yet exist (npm does not validate script targets) — downstream plans create the script files, not the package.json entries. Keep the package.json files array as ["dist","README.md"] so no dev tool ships. Per the legitimacy audit all three packages are Approved (no [ASSUMED]/[SUS]), so no blocking human-verify checkpoint is required.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.devDependencies; if(!d['rollup-plugin-visualizer']||!d['@size-limit/esbuild-why']||!d['tachometer']) throw new Error('missing devDep'); if((p.dependencies||{})['rollup-plugin-visualizer']||(p.dependencies||{})['tachometer']) throw new Error('leaked into dependencies'); for(const s of ['test:perf','size:baseline','perf:diff','assert:no-lit','size:why','visualize','attribution:check']){ if(!p.scripts[s]) throw new Error('missing script stub '+s); } if(JSON.stringify(p.files)!==JSON.stringify(['dist','README.md'])) throw new Error('files array changed');"</automated>
  </verify>
  <acceptance_criteria>
    - The three devDependencies are present with the pinned ranges; none appears in dependencies or peerDependencies.
    - package-lock.json is updated (installed resolutions present).
    - All seven report-only script stubs exist (test:perf, size:baseline, perf:diff, assert:no-lit, size:why, visualize, attribution:check).
    - package.json files array is unchanged (["dist","README.md"]).
    - The verify node one-liner exits 0.
  </acceptance_criteria>
  <precondition>npm is available and the lockfile is writable; run `npm install` after this task in any active worktree (dep-adding plan, per MEMORY.md).</precondition>
  <reversibility rating="reversible">devDependency additions + script stubs; removable.</reversibility>
  <done>The three pinned dev tools are installed (dev-only, nothing bundled), and the report-only script names exist for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Three new build/measurement packages enter the toolchain |
| perf test project → browser server | cdp() requests elevated write+exec on the test browser API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | rollup-plugin-visualizer / @size-limit/esbuild-why / tachometer install | low | mitigate | Pinned version ranges from the HIGH-confidence STACK research; lockfile committed; all three Approved in the Package Legitimacy Audit (mature, official repos, dev-only). Kept in devDependencies; package.files stays ["dist","README.md"] so none ships. |
| T-07-EP | Elevation of Privilege | cdp() write+exec grant | low | mitigate | Scope allowWrite/allowExec to the Chromium-only perf project only; never on the shipped build; CI perf job inherits the repo's read-only `permissions: contents: read`. |
| T-07-ID | Info disclosure | dev tool leaking into published tarball | low | mitigate | Env-gate the visualizer (default off); keep tachometer config outside package.files; devDependencies only. |
</threat_model>

<verification>
- `npx vitest run --project perf test/perf/_spike.cdp.test.ts` exits 0 (A2 proven).
- `npx vitest run test/perf/_spike.lit-markers.test.ts` exits 0 (A3 proven).
- Three devDependencies installed, none leaked to dependencies/peerDependencies; files array intact.
</verification>

<success_criteria>
A2 and A3 are both de-risked with passing spikes, the Chromium-only perf project + writeMetrics stub exist, and the three pinned dev tools are installed dev-only — Plans 02, 03, and 05 can now build on proven ground.
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-00-SUMMARY.md` when done. Record: the pinned cdp() config key path (or fallback), the confirmed Lit marker set, and the installed versions.
</output>
