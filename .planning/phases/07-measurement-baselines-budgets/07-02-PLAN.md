---
phase: 07-measurement-baselines-budgets
plan: 02
type: execute
wave: 2
depends_on: [07-00, 07-01]
files_modified:
  - scripts/assert-no-bundled-lit.mjs
  - test/no-bundled-lit.test.ts
autonomous: true
requirements: [MEAS-04]

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A no-bundled-Lit assertion independent of size-limit proves Lit is never inlined into any shipped chunk: `scripts/assert-no-bundled-lit.mjs` greps every dist/**/*.js for the confirmed Lit version-marker globals and finds zero, and asserts `lit` appears only as a bare external specifier (MEAS-04, D-03)."
    - "A unit test on the jsdom verify lane snapshots the vite.config.ts external array and asserts it still externalizes every lit / @lit / @lit-labs pattern and @floating-ui/dom — a chunking refactor cannot silently drop the externalization (D-03, Pitfall 5)."
    - "The dist-grep guard runs report-only this phase (exit 0), consistent with D-08; the enforcing flip is Phase 11."
    - "The marker grep filters out comment lines / uses exact identifier matching so header prose or a comment mentioning a marker name cannot self-invalidate the count (grep hygiene)."
  prohibitions:
    - "MUST NOT rely on size-limit for the no-Lit check — size-limit ignores `lit`, so an inlined copy is invisible to it (Pitfall 5)."
    - "MUST NOT modify the vite.config.ts external array — it is the snapshot source and must stay byte-stable."
    - "MUST NOT flip this guard to enforcing this phase — report-only exit 0 (D-08)."
    - "MUST NOT bundle Lit — it is a peer dependency and stays external."
  artifacts:
    - scripts/assert-no-bundled-lit.mjs
    - test/no-bundled-lit.test.ts
  key_links:
    - "Confirmed Lit marker set (Plan 00 A3 spike) → the grep signal this guard uses."
    - "vite.config.ts external array → the snapshot the unit test freezes."
---

<objective>
Prove Lit is never bundled into the shipped output — the second, size-limit-independent half of MEAS-04 — via a dist-grep marker guard plus an external-list snapshot unit test.

Purpose: size-limit `ignore`s `lit`, so it would happily pass a build that accidentally inlined a full Lit copy (Pitfall 5). D-03 requires two independent guards so a future chunking refactor cannot smuggle Lit into the payload undetected.
Output: `scripts/assert-no-bundled-lit.mjs` (report-only dist grep) and a jsdom-lane unit test snapshotting the vite external array.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
@scripts/cem-diff.mjs
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/assert-no-bundled-lit.mjs (report-only dist grep)</name>
  <files>scripts/assert-no-bundled-lit.mjs</files>
  <read_first>
    - test/perf/_spike.lit-markers.test.ts (Plan 00 output — the CONFIRMED marker set to grep for)
    - scripts/cem-diff.mjs (isMain guard + report-only exit shape; INVERT to exit 0 per D-08)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Pitfall 4" and §"Code Examples"
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"scripts/assert-no-bundled-lit.mjs"
    - vite.config.ts (external array line 220)
  </read_first>
  <action>
    Create scripts/assert-no-bundled-lit.mjs as a zero-dependency Node script. Recursively read every dist/**/*.js file and assert none contains the confirmed Lit version-marker global identifiers (reactiveElementVersions, litHtmlVersions, litElementVersions — or the corrected set recorded by the Plan 00 spike); these appear only when Lit source is inlined. Additionally assert that where dist references Lit it does so as a bare external specifier (an import from the "lit" module string), not an inlined implementation. Use exact-identifier matching and strip/ignore sourcemap-comment noise so an incidental textual mention cannot produce a false positive or false negative (grep hygiene — do not count on unfiltered lines). Keep the isMain guard from cem-diff and exit 0 unconditionally this phase (report-only, D-08; usage error exits 2). The `assert:no-lit` npm script already exists as a Plan 00 stub pointing at this file — do NOT edit package.json (Plan 00 owns it).
  </action>
  <verify>
    <automated>npm run build && node scripts/assert-no-bundled-lit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/assert-no-bundled-lit.mjs` after a clean build reports zero inlined-Lit markers and exits 0.
    - The script greps every dist/**/*.js (recursively), not just top-level bundles.
    - Marker detection uses exact-identifier matching and ignores sourcemap/comment noise.
    - The script exits 0 report-only even if a marker were found this phase (prints the offending files), and exits 2 only on usage error.
  </acceptance_criteria>
  <precondition>`npm run build` has produced dist/**/*.js; the Plan 00 spike recorded the confirmed marker set.</precondition>
  <reversibility rating="reversible">New standalone script; removable.</reversibility>
  <done>An independent, report-only dist-grep guard proves no shipped chunk inlines Lit.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot the vite external array in a jsdom-lane unit test</name>
  <files>test/no-bundled-lit.test.ts</files>
  <read_first>
    - vite.config.ts (external array: lit, /^lit\//, /^@lit\//, /^@lit-labs\//, @floating-ui/dom, /^@floating-ui\//)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"scripts/assert-no-bundled-lit.mjs" (external-list snapshot half → jsdom verify lane, RESEARCH Wave 0 Gaps line 453)
    - test/helpers.ts (jsdom-lane conventions)
  </read_first>
  <action>
    Add test/no-bundled-lit.test.ts running on the jsdom verify lane (it is a static config assertion, no browser needed). Import or read the vite.config resolved external list and assert it still externalizes every Lit pattern (the literal "lit" plus the /^lit\//, /^@lit\//, /^@lit-labs\// regexes) and @floating-ui/dom. Snapshot the external array so any future edit that drops a Lit externalization pattern fails the test. Assert the externalization of lit specifically remains present (peer dep must never be bundled). This is the second D-03 guard, independent of both size-limit and the dist grep.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/no-bundled-lit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project jsdom test/no-bundled-lit.test.ts` exits 0.
    - The test asserts the external list externalizes lit and every @lit / @lit-labs pattern plus @floating-ui/dom.
    - The test fails if the lit externalization pattern is removed from the external array (snapshot guard).
  </acceptance_criteria>
  <reversibility rating="reversible">New unit test; removable.</reversibility>
  <done>The vite external array is frozen by a snapshot test, so no chunking refactor can silently stop externalizing Lit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/** build output → assert-no-bundled-lit.mjs | Grep guard reads in-repo built artifacts only |
| vite.config.ts external array → snapshot test | Config value frozen against silent regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | Accidentally-inlined Lit copy invisible to size-limit | medium | mitigate | Two independent guards (dist marker grep + external-array snapshot), neither relying on size-limit's lit ignore (Pitfall 5, D-03). |
| T-07-04 | Spoofing | A comment mentioning a marker string faking a positive/negative | low | mitigate | Exact-identifier matching + sourcemap/comment filtering (grep hygiene). |
</threat_model>

<verification>
- `npm run build && node scripts/assert-no-bundled-lit.mjs` exits 0 with zero markers.
- `npx vitest run --project jsdom test/no-bundled-lit.test.ts` exits 0.
</verification>

<success_criteria>
MEAS-04 is fully closed: size-limit now counts @floating-ui/dom (Plan 01) AND two independent, size-limit-independent guards prove Lit is never bundled.
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-02-SUMMARY.md` when done. Record the confirmed marker set used and note the guard is report-only (enforcing flip is Phase 11).
</output>
