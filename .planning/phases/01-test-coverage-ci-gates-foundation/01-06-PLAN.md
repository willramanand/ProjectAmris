---
phase: 01-test-coverage-ci-gates-foundation
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/browser/form-association.test.ts
autonomous: true
requirements: [TEST-02]
estimate:
  tokens: 50000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every form-associated control participates in a real `<form>` in Chromium: its value appears in `FormData` on submit via native `ElementInternals.setFormValue` (TEST-02)."
    - "Validity participation is asserted where the control supports it: `required`/empty makes `form.checkValidity()` false, and a satisfied control makes it true (real setValidity, not the jsdom mock)."
    - "No assertion in the browser form suite uses getMockInternals or any test/setup.ts mock (browser project omits setupFiles)."
    - "Bugs surfaced (e.g. a control that fails to submit) are captured as findings for the owning phase, not fixed here."
  artifacts:
    - test/browser/form-association.test.ts (expanded to all formAssociated controls)
  key_links:
    - "each control mounted inside a real <form>; new FormData(form) reflects native setFormValue"
    - "expands the tracer's checkbox seed; single file grown across two tasks"
---

<objective>
Expand `test/browser/form-association.test.ts` (seeded with am-checkbox by the tracer) to cover every form-associated control against a real `<form>` and native `ElementInternals` in Chromium: real `setFormValue` participation and real validity (TEST-02). This is the fidelity that a form-heavy v1.0 freeze cannot rest on a jsdom mock for.

Purpose: Prove the load-bearing form-association contract for the whole control set with the real browser API.
Output: A comprehensive `test/browser/form-association.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real-form submission for the simple controls (input, textarea, radio, switch, number-field, search-field, slider)</name>
  <files>test/browser/form-association.test.ts</files>
  <behavior>
    - Each of input, textarea, radio, switch, number-field, search-field, slider, mounted with a `name` inside a real `<form>`, contributes its value to `new FormData(form)` via native setFormValue.
    - For controls that support constraint validation, `required` + empty makes `form.checkValidity()` false and a filled value makes it true.
  </behavior>
  <read_first>
    - test/browser/form-association.test.ts (the checkbox seed from plan 01 — extend it; keep the native-API guard)
    - test/components/input.test.ts, textarea.test.ts, slider.test.ts, number-field.test.ts, search-field.test.ts, radio.test.ts, switch.test.ts (existing jsdom tests — reuse fixture shapes; but assert real FormData, NOT getMockInternals)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (Pattern 2 real ElementInternals; Component->Lane map TEST-02 list)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§form-association analog — swap mock assertion for real API)
  </read_first>
  <action>
    Extend `test/browser/form-association.test.ts` with a describe/it per simple control (input, textarea, radio, switch, number-field, search-field, slider). For each: mount inside `<form>` with a `name`, set/enter a value, and assert `new FormData(form).get(name)` equals the real submitted value (native `ElementInternals.setFormValue`). For controls with constraints, additionally assert `required`+empty -> `form.checkValidity()===false` and a valid value -> true. Import only `fixture` (and interaction helpers) from `'../helpers'`; do NOT import getMockInternals. Radio: verify group value selection reflects in FormData. If any control fails to participate, capture it as a finding (does not block — it is a real bug for the owning phase), and mark that assertion clearly.
  </action>
  <verify>
    <automated>npx vitest run --project browser test/browser/form-association.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test/browser/form-association.test.ts` covers input, textarea, radio, switch, number-field, search-field, slider with real `FormData` assertions.
    - No getMockInternals / setup.ts mock reference anywhere in the file.
    - The browser suite exits 0 (findings for any genuinely non-participating control are documented in the SUMMARY).
  </acceptance_criteria>
  <done>The simple form controls are proven to participate in a real form via native ElementInternals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real-form submission for the complex controls (select, combobox, rich-select, input-otp, date-picker, time-picker, color-picker, file-upload)</name>
  <files>test/browser/form-association.test.ts</files>
  <behavior>
    - Each complex control (select, combobox, rich-select, input-otp, date-picker, time-picker, color-picker, file-upload), mounted with a `name` inside a real `<form>` and given a value, contributes that value to `new FormData(form)` via native setFormValue.
    - Where the control exposes required/validity, `required`+empty -> invalid form and a chosen value -> valid.
  </behavior>
  <read_first>
    - test/browser/form-association.test.ts (as extended in Task 1)
    - test/components/select.test.ts, combobox.test.ts, rich-select.test.ts, input-otp.test.ts, date-picker.test.ts, time-picker.test.ts, color-picker.test.ts, file-upload.test.ts (existing jsdom fixture shapes — reuse the mounting/value-selection patterns; assert real FormData)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (Component->Lane map TEST-02 full formAssociated set)
  </read_first>
  <action>
    Add a describe/it per complex control. For each: mount inside `<form>` with a `name`, drive the control to a value using its real API (choose an option, type the OTP, pick a date, choose a color, attach a file via a real DataTransfer in the browser), and assert `new FormData(form).get(name)` reflects it (native setFormValue). Assert required/validity where supported. Reuse the jsdom fixture shapes for how to set each value but assert the REAL FormData — never getMockInternals. file-upload: use the real browser `DataTransfer` (available natively, unlike the jsdom mock). Capture any non-participating control as a finding without blocking.
  </action>
  <verify>
    <automated>npx vitest run --project browser test/browser/form-association.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The file now additionally covers select, combobox, rich-select, input-otp, date-picker, time-picker, color-picker, file-upload with real `FormData` assertions.
    - No getMockInternals / setup.ts mock references anywhere in the file.
    - The full browser suite exits 0; any real participation bug is recorded as a finding.
  </acceptance_criteria>
  <done>The complex form controls are proven to participate in a real form via native ElementInternals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> CI browser runner | This suite executes in the PR-triggered headless-Chromium job (read-only, from plan 01) |
| test authoring only | No runtime code, network, or input surface is changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06a | Tampering | New browser test assertions executing in CI | low | accept | No runtime code or new deps; runs in the read-only browser CI job from plan 01 |
| T-01-06b | Tampering | Temptation to fix a control's form participation to make a test pass | low | mitigate | Capture non-participation as a finding for the owning phase; do not edit component source this phase |
</threat_model>

<verification>
- `vitest run --project browser` passes with all form-associated controls covered by real `FormData` assertions.
- No mock symbol appears in the browser form suite.
</verification>

<success_criteria>
Every form-associated control is verified for real form submission and validity participation against native ElementInternals — TEST-02 is satisfied for the whole control set.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- Modified/expanded: `test/browser/form-association.test.ts` (all formAssociated controls: input, textarea, checkbox, radio, switch, select, combobox, rich-select, slider, number-field, search-field, input-otp, date-picker, time-picker, color-picker, file-upload).
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-06-SUMMARY.md` when done.
</output>
