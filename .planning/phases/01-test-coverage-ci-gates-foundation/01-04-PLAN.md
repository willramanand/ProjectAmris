---
phase: 01-test-coverage-ci-gates-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/components/progress-ring.test.ts
  - test/components/side-nav.test.ts
  - test/components/stat.test.ts
  - test/components/status-dot.test.ts
  - test/components/timeline.test.ts
  - test/components/visually-hidden.test.ts
  - test/components/split-view.test.ts
  - test/components/breadcrumb.test.ts
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 48000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "progress-ring, side-nav, stat, status-dot, timeline, visually-hidden each have a dedicated 1:1 smoke file (TEST-01, D-04)."
    - "side-nav.test.ts covers both am-side-nav and am-side-nav-item; timeline.test.ts covers both am-timeline and am-timeline-item (multi-element directories)."
    - "split-view has a dedicated behavioral file (the one interactive component of the 23, D-04) asserting orientation dynamics, position-prop clamping, named slots/part, role=separator, and am-resize firing — NOT keyboard resize (the component has none)."
    - "The am-breadcrumb-item block moves out of display-trivial into the existing breadcrumb.test.ts (its parent already has a dedicated file)."
    - "display-trivial.test.ts is NOT deleted here (deferred to plan 08)."
  artifacts:
    - test/components/progress-ring.test.ts
    - test/components/side-nav.test.ts
    - test/components/stat.test.ts
    - test/components/status-dot.test.ts
    - test/components/timeline.test.ts
    - test/components/visually-hidden.test.ts
    - test/components/split-view.test.ts
  key_links:
    - "split-view behavioral assertions target what the source actually implements (pointer drag + clamp), not assumed keyboard support"
    - "breadcrumb-item assertions merge into breadcrumb.test.ts, which already imports the breadcrumb barrel"
---

<objective>
Split the remaining six display components out of `display-trivial.test.ts` into dedicated smoke files (progress-ring, side-nav, stat, status-dot, timeline, visually-hidden), author the one genuinely behavioral file among the 23 (split-view, per D-04), and fold the `am-breadcrumb-item` block into the existing `breadcrumb.test.ts`. This plan does NOT delete the grouped file (plan 08 does).

Purpose: Close the last display-file gaps and give split-view real behavioral coverage that matches its actual implementation.
Output: Six new smoke files, one behavioral split-view file, and an expanded breadcrumb.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split progress-ring, side-nav, stat into dedicated files (D-04 smoke; side-nav covers item too)</name>
  <files>test/components/progress-ring.test.ts, test/components/side-nav.test.ts, test/components/stat.test.ts</files>
  <read_first>
    - test/components/display-trivial.test.ts (am-progress-ring block lines 112-129, am-side-nav-item 131-139, am-stat 170-175 — lift verbatim)
    - test/helpers.ts (fixture signature)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§Smoke test files ARIA-reflection pattern; §Multi-element directories note)
  </read_first>
  <action>
    Create `test/components/progress-ring.test.ts` (lift the two blocks: `svg[role="progressbar"]` aria-valuenow/aria-valuemax reflection, and the indeterminate omits-aria-valuenow case), `test/components/stat.test.ts` (trend reflection), and `test/components/side-nav.test.ts`. side-nav's single barrel import (`'../../src/components/side-nav/side-nav'`) registers BOTH `am-side-nav` and `am-side-nav-item`; carry over the existing am-side-nav-item block (active reflection + anchor href) and keep it smoke per D-04. Import only used helpers.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/progress-ring.test.ts test/components/side-nav.test.ts test/components/stat.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist; `vitest run --project jsdom` over them exits 0.
    - `side-nav.test.ts` asserts on `am-side-nav-item` (active reflection + anchor href).
    - `progress-ring.test.ts` asserts both the valued and indeterminate cases.
  </acceptance_criteria>
  <done>progress-ring, side-nav (+item), stat have passing dedicated files.</done>
</task>

<task type="auto">
  <name>Task 2: Split status-dot, timeline, visually-hidden; move breadcrumb-item into breadcrumb.test.ts</name>
  <files>test/components/status-dot.test.ts, test/components/timeline.test.ts, test/components/visually-hidden.test.ts, test/components/breadcrumb.test.ts</files>
  <read_first>
    - test/components/display-trivial.test.ts (am-status-dot 177-186, am-timeline-item 188-193, am-visually-hidden 195-202, am-breadcrumb-item 21-34 — lift verbatim)
    - test/components/breadcrumb.test.ts (existing dedicated file — append the breadcrumb-item describe block; it already imports the breadcrumb barrel which registers am-breadcrumb-item)
  </read_first>
  <action>
    Create `test/components/status-dot.test.ts` (variant/size/pulse reflection), `timeline.test.ts` (single barrel import registers `am-timeline` + `am-timeline-item`; lift the am-timeline-item variant-reflection block), and `visually-hidden.test.ts` (off-screen slot). Then append the `am-breadcrumb-item` describe block (anchor-when-href, plain-when-current) into the EXISTING `test/components/breadcrumb.test.ts` — do not create a new breadcrumb-item file (the parent barrel already registers the item). Do NOT delete display-trivial.test.ts.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/status-dot.test.ts test/components/timeline.test.ts test/components/visually-hidden.test.ts test/components/breadcrumb.test.ts 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - status-dot/timeline/visually-hidden files exist and pass.
    - `test/components/breadcrumb.test.ts` now contains an `am-breadcrumb-item` describe block and still passes.
    - No standalone breadcrumb-item file is created.
  </acceptance_criteria>
  <done>status-dot, timeline (+item), visually-hidden have dedicated files; breadcrumb-item lives with breadcrumb.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the behavioral split-view test — real implementation surface only (D-04)</name>
  <files>test/components/split-view.test.ts</files>
  <behavior>
    - orientation reflects to the attribute and updates `aria-orientation` on the `role="separator"` divider when the `orientation` prop changes.
    - Setting `position` beyond [0,100] clamps the applied inline pane sizing (the source clamps to `Math.max(0, Math.min(100, position))` in `updated()`) — e.g. position=150 yields the 100%-clamped layout, position=-10 yields 0%.
    - A pointer drag sequence on the divider dispatches an `am-resize` CustomEvent (detail carries a `position`); the drag ratio itself depends on real layout, so assert the event fires and that reported position stays within the drag clamp bounds, not exact geometry (jsdom has no layout).
    - The `start`/`end` named slots and `part="divider"` are present.
  </behavior>
  <read_first>
    - src/components/split-view/split-view.ts (READ the whole file: `_apply`/`updated()` clamp at ~111-119, `_handlePointerDown/Move/Up` at ~122-153 emitting `am-resize` clamped to [5,95], render divider with `role="separator"` + `aria-orientation` at ~157-167 — there is NO keydown/Arrow handler and NO aria-valuenow)
    - test/components/display-trivial.test.ts (am-split-view seed block lines 141-168 — orientation, named slots, dynamic position — carry these over)
    - test/components/slider.test.ts (event-dispatch + oneEvent style reference only; split-view has NO keyboard resize, so do NOT copy slider's keyboard assertions)
    - test/helpers.ts (fixture, oneEvent, waitForUpdate)
  </read_first>
  <action>
    Create `test/components/split-view.test.ts`. Carry over the seed assertions (orientation reflection, start/end named slots, dynamic `position` prop) and add behavioral depth that matches the ACTUAL component: (1) change `orientation` at runtime and assert the divider's `aria-orientation` follows and the attribute reflects; (2) set `position` to an out-of-range value and assert the applied clamped layout per the source's `updated()` clamp; (3) drive a `pointerdown`->`pointermove`->`pointerup` sequence on the `part="divider"` element and assert an `am-resize` event fires (use `oneEvent`), asserting the reported `position` is finite and within the drag clamp — do NOT assert exact pixel geometry (jsdom returns zero rects). Assert `role="separator"` and `part="divider"` exist.
    Prohibition: split-view has no keyboard resize and no `aria-valuenow` — do NOT assert them and do NOT add keyboard/ARIA-value support to the component (that would be an API change, out of scope this phase). If keyboard resize / separator value semantics are desirable for a11y, capture it as a finding for the owning phase in the SUMMARY; do not implement it here.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/split-view.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test/components/split-view.test.ts` exists and passes under the jsdom project.
    - It asserts orientation dynamics (aria-orientation follows), position-prop clamping, an `am-resize` event on pointer drag, and slot/part presence.
    - It contains NO keyboard-resize (Arrow) assertions and NO `aria-valuenow` assertions.
    - `src/components/split-view/split-view.ts` is unchanged (no component edit).
  </acceptance_criteria>
  <done>split-view has a behavioral test that matches its real pointer-drag + clamp implementation, with no component API change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test authoring only | No runtime code, network, or input surface is changed; component source stays untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04a | Tampering | New jsdom test files executing in CI | low | accept | Test files add no runtime code and no new deps; run in the read-only CI established by plan 01 |
| T-01-04b | Tampering | Temptation to add keyboard/ARIA support to split-view to make a test pass | low | mitigate | Assert only the real implementation surface; capture any a11y gap as a finding for the owning phase — no component edit |
</threat_model>

<verification>
- Seven dedicated files (six smoke + split-view behavioral) exist and pass under `vitest run --project jsdom`.
- `breadcrumb.test.ts` covers am-breadcrumb-item.
- `display-trivial.test.ts` remains present for plan 08's deletion.
- `src/components/split-view/split-view.ts` is unmodified.
</verification>

<success_criteria>
The remaining display components have dedicated smoke files; split-view has honest behavioral coverage; breadcrumb-item lives with its parent — all with no component-source change.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- New: `test/components/progress-ring.test.ts`, `side-nav.test.ts`, `stat.test.ts`, `status-dot.test.ts`, `timeline.test.ts`, `visually-hidden.test.ts`, `split-view.test.ts`.
- Modified: `test/components/breadcrumb.test.ts` (adds am-breadcrumb-item block).
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-04-SUMMARY.md` when done.
</output>
