---
phase: 01-test-coverage-ci-gates-foundation
plan: 08
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06", "01-07"]
files_modified:
  - .size-limit.json
  - vitest.config.ts
  - .github/workflows/ci.yml
  - test/components/display-trivial.test.ts
autonomous: true
requirements: [TEST-01, TEST-07, PERF-01]
estimate:
  tokens: 52000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "size-limit budgets the core bundle, the full bundle, a light deep import (button), and a heavy deep import (data-grid), each min+gzipped, with limits at the measured size + ~5-10% headroom (PERF-01, D-07/D-08/D-09)."
    - "A tree-shaking assertion fails CI if importing one component transitively pulls in the whole library (a barrel/`import *` regression), set against the per-component deep entry with lit/@floating-ui ignored (D-08, Pitfall 10)."
    - "Coverage thresholds are ratcheted up to the final measured floor now that all 23 splits + TEST-04/05 assertions have landed, still green-on-arrival (TEST-07, D-01/D-02/D-03, D-05)."
    - "All 66 components have a dedicated 1:1 test file and NO grouped multi-component file remains (display-trivial.test.ts deleted; layout-primitives + misc-display already gone) (TEST-01, OQ-3)."
    - "All four CI gates (coverage, browser incl. a11y, size) pass on existing code and hard-block on regression; ci.yml stays PR-triggered and read-only (no packages:write)."
  artifacts:
    - .size-limit.json (full budget set + tree-shaking canary)
    - vitest.config.ts (final ratcheted thresholds)
  key_links:
    - "size-limit entries point at real dist/*.js (lit/@floating-ui external -> shipped size)"
    - "tree-shaking canary based on per-component deep entry, not the self-registering full barrel"
    - "final thresholds == measured post-split floor -> green-on-arrival, blocks future regression"
---

<objective>
Finalize and lock the Phase 1 gates once all test files exist: complete the `size-limit` budget set with a tree-shaking canary (PERF-01), ratchet the coverage thresholds to the final measured floor (TEST-07), retire the last grouped file and verify the 1:1 invariant (TEST-01), and prove the whole four-gate pipeline green on existing code.

Purpose: Convert the tracer's single-entry budget and baseline thresholds into the full, ratcheted safety net that guards all downstream breaking work.
Output: A complete `.size-limit.json`, ratcheted `vitest.config.ts` thresholds, `display-trivial.test.ts` removed, and a proven-green pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the size-limit budget set + tree-shaking canary (PERF-01, D-07/D-08/D-09, Pitfall 10)</name>
  <files>.size-limit.json</files>
  <read_first>
    - .size-limit.json (the core-bundle entry seeded by plan 01)
    - vite.config.ts (lines 186-203 — multi-entry build; externals `lit`/`@lit/*`/`@floating-ui/*` so dist/*.js reflect shipped size)
    - package.json (exports 19-47; sideEffects 52-57 — the full barrel `dist/amris.js` is self-registering/side-effectful, so a tree-shaking canary must target a per-component deep entry, NOT the full barrel)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (§Bundle-Size Gate config; Pitfall 4 externals note)
    - .planning/research/PITFALLS.md (Pitfall 10 — measure min+gzip of core/full/representative + tree-shaking assertion)
  </read_first>
  <action>
    Run `npm run build`, then expand `.size-limit.json` (D-08) to four measured path-entries plus a tree-shaking canary: `core bundle` (dist/amris-core.js), `full bundle` (dist/amris.js), `button (light deep import)` (dist/components/button/index.js), `data-grid (heavy deep import)` (dist/components/data-grid/index.js). Set each `limit` to the real min+gzip measured via `npx size-limit` + ~5-10% headroom (D-07 ratchet; exact kB are D-09 discretion). Add a tree-shaking canary entry (Pitfall 10): base it on a per-component deep entry (e.g. `dist/components/button/index.js`) with `"ignore": ["lit","@floating-ui/dom"]` and a small `limit`, so that if a shared module regresses to a barrel/`import *` and drags the whole library into a single component's transitive graph, the canary busts. Do NOT base the canary on the self-registering full barrel `dist/amris.js` (importing anything from it runs every `customElements.define` side effect and would count the whole library by design). Keep `data-grid` as the heavy representative (largest display component, renders all rows per CONCERNS).
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && npx size-limit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `.size-limit.json` has entries for core, full, button (light), data-grid (heavy), and a tree-shaking canary with `ignore` for lit/@floating-ui.
    - Every entry has a numeric `limit`; `npx size-limit` (after build) exits 0 on current code.
    - The tree-shaking canary targets a per-component deep entry, not `dist/amris.js`.
  </acceptance_criteria>
  <done>Bundle budgets cover core/full/light/heavy and a tree-shaking canary, all green at the measured baseline and hard-blocking on regression.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet coverage thresholds to the final floor; retire display-trivial; verify the 1:1 invariant (TEST-01, TEST-07, D-01/D-02/D-03, OQ-3)</name>
  <files>vitest.config.ts, test/components/display-trivial.test.ts</files>
  <read_first>
    - vitest.config.ts (coverage.thresholds seeded by plan 01 — raise them here)
    - test/components/display-trivial.test.ts (confirm all its components now have dedicated files from plans 03/04 before deleting)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (§Coverage Gate per-dir tiers; §Test Gap — the 23-component list to verify against src)
  </read_first>
  <action>
    Delete `test/components/display-trivial.test.ts` (OQ-3 — plans 03/04 redistributed all its components; this is the single race-free deletion point). Verify the 1:1 invariant: every `src/components/*/` directory has a matching dedicated `test/components/<name>.test.ts` and NO grouped multi-component file remains (layout-primitives + misc-display already deleted by plans 02/01). Re-measure coverage with `npx vitest run --project jsdom --coverage` and ratchet `test.coverage.thresholds` UP to the new measured floor (D-01 ratchet — coverage rose as the 23 splits + TEST-04/05 assertions landed), keeping the branch gate (D-02) and raising the per-directory overrides for combobox/select/dialog/date-picker toward the D-03 ceiling (branch>=80 / lines-fns-stmts>=85) only as far as the real measured numbers support — the gate must stay green-on-arrival (D-05). Coverage folds over jsdom only (OQ-1).
  </action>
  <verify>
    <automated>test ! -f test/components/display-trivial.test.ts && test ! -f test/components/layout-primitives.test.ts && test ! -f test/components/misc-display.test.ts && echo "no grouped files remain" && npx vitest run --project jsdom --coverage 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - No grouped test file remains (display-trivial, layout-primitives, misc-display all absent).
    - Every `src/components/*/` directory has a dedicated `test/components/<name>.test.ts` (23-gap closed; 66/66 1:1).
    - `vitest.config.ts` thresholds are numerically >= the plan-01 baseline and the coverage run exits 0 (green-on-arrival).
  </acceptance_criteria>
  <done>The 1:1 invariant holds, the grouped files are gone, and the coverage gate is ratcheted to the final measured floor.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the full four-gate pipeline green + read-only CI (TEST-07, PERF-01, D-05, D-06)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the verify/browser/size jobs wired by plan 01 — confirm they hard-block and stay read-only)
    - package.json (scripts — confirm `test` is jsdom-only per D-06)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (§Architecture Patterns CI data-flow; §Security Domain V14 — no packages:write in PR CI)
  </read_first>
  <action>
    Confirm and, if needed, finalize `.github/workflows/ci.yml` so each gate hard-blocks on regression (D-05): `verify` runs `vitest run --project jsdom --coverage`; `browser` runs `npx playwright install chromium` then `npm run test:browser` (form + focus + dialog + floating + a11y); `size` runs `npm run build` then `npm run size`. Ensure the workflow is PR-triggered and requests NO `packages:write`/publish permissions (that is Phase 6; changesets SHA-pinning is also Phase 6). Confirm `package.json` `test` stays `vitest --project jsdom` so local contributors do not need Playwright (D-06). Run the full pipeline locally to prove green-on-arrival: jsdom coverage, browser project, build+size. Record the final thresholds/budgets and any captured findings (Phase 3/4) in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom --coverage 2>&1 | tail -8 && npx vitest run --project browser 2>&1 | tail -8 && npm run build >/dev/null 2>&1 && npx size-limit 2>&1 | tail -8 && node -e "const s=require('./package.json').scripts; if(!/--project jsdom/.test(s.test)) throw new Error('local test must stay jsdom-only D-06'); console.log('jsdom-only default ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml` has verify (jsdom coverage), browser (Chromium install + browser project), and size (build + size-limit) jobs, each hard-blocking, with no `packages:write` permission.
    - `package.json` `test` script is `vitest --project jsdom` (contributor default, D-06).
    - All four gates (coverage, browser, a11y-in-browser, size) exit 0 locally on current code.
  </acceptance_criteria>
  <done>The complete Phase 1 safety net — coverage, real-browser fidelity + a11y, and bundle budgets — is green on existing code and hard-blocks any regression, with local runs staying jsdom-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> CI runner | The finalized gate jobs run untrusted PR code (browser job) under the read-only workflow token |
| config authoring only | No runtime code, network, or input surface is changed; no new deps added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08a | Tampering | Coverage/size gate config in vitest.config.ts / .size-limit.json | low | mitigate | Thresholds/budgets committed and green-on-arrival; hard-block only on regression (D-05); no new deps |
| T-01-08b | Elevation of Privilege | Finalized `ci.yml` gate jobs | low | mitigate | Keep PR-triggered + read-only GITHUB_TOKEN; no `packages:write`/publish (Phase 6) |
</threat_model>

<verification>
- No grouped test files remain; 66/66 components have dedicated 1:1 files.
- `vitest run --project jsdom --coverage` (ratcheted thresholds), `vitest run --project browser`, and `npm run build && size-limit` all exit 0.
- `ci.yml` gates hard-block and request no publish permissions; local `npm test` is jsdom-only.
</verification>

<success_criteria>
Every component is guarded by a dedicated test; the coverage, bundle-size (incl. tree-shaking), and real-browser a11y gates hard-block regressions in CI while green on existing code; local development stays jsdom-only — the Phase 1 safety net is complete.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- Finalized: `.size-limit.json` (core, full, button, data-grid, tree-shaking canary), `vitest.config.ts` (ratcheted thresholds + per-dir tiers), `.github/workflows/ci.yml` (verify/browser/size gates, read-only).
- Removed: `test/components/display-trivial.test.ts` (last grouped file).
- Established phase-wide: 66/66 dedicated 1:1 test files; four hard-blocking CI gates.
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-08-SUMMARY.md` when done.
</output>
