---
phase: 01-test-coverage-ci-gates-foundation
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/browser/overlay-focus.test.ts
  - test/browser/dialog-top-layer.test.ts
  - test/browser/floating-position.test.ts
autonomous: true
requirements: [TEST-03, TEST-06]
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "In real Chromium, overlay focus trap and restoration are verified for dialog, drawer, command-palette, popover: focus moves into the opened overlay, Tab cycles within it, and focus returns to the opener on close (TEST-03)."
    - "Native `<dialog>` top-layer semantics are verified against the real `showModal()`/`close()` (NOT the test/setup.ts mock) — the dialog reports open and participates in the top layer (TEST-06, Pitfall 2)."
    - "floating-ui positioning is verified in a real browser: an opened positioned overlay computes a non-zero, viewport-anchored rect (TEST-06)."
    - "No browser file imports getMockInternals or relies on any test/setup.ts mock (browser project omits setupFiles)."
    - "Focus-restoration edge cases that reveal the Phase 3 isConnected guard are captured as findings, not fixed here (scope note)."
  artifacts:
    - test/browser/overlay-focus.test.ts
    - test/browser/dialog-top-layer.test.ts
    - test/browser/floating-position.test.ts
  key_links:
    - "deepActiveElement() (from plan 01 helpers) pierces shadow roots to assert the trapped/restored element"
    - "browser project runs these with native focus/top-layer/layout — the jsdom mocks are absent by design"
---

<objective>
Author three of the four real-browser fidelity suites that jsdom cannot prove: overlay focus trap + restoration (TEST-03), native `<dialog>`/top-layer (TEST-06), and floating-ui positioning (TEST-06). These run in the `browser` Vitest project (Chromium) established by the tracer, with native APIs and no mocks.

Purpose: Give the form-heavy freeze real evidence that focus, dialog top-layer, and positioning behave correctly in a browser — the areas where "passes in jsdom, breaks in the browser" hides.
Output: `test/browser/overlay-focus.test.ts`, `dialog-top-layer.test.ts`, `floating-position.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real-browser overlay focus trap + restoration (TEST-03)</name>
  <files>test/browser/overlay-focus.test.ts</files>
  <behavior>
    - Opening an am-dialog moves focus into the dialog; Tab cycling stays within the overlay (focus trap), asserted via deepActiveElement() piercing shadow roots.
    - Closing the overlay restores focus to the opener element that was focused before opening.
    - The same shape is exercised for drawer, command-palette, and popover (at least dialog + one more asserted fully; the rest at least open/restore).
    - No mock is used — this runs against the real focus model in Chromium.
  </behavior>
  <read_first>
    - test/components/dialog.test.ts (lines 223-244 "restores focus to previously focused element on close" — reuse the structure, run against real focus)
    - test/helpers.ts (deepActiveElement added by plan 01; fixture, mount, waitForUpdate, keydown)
    - src/components/dialog/dialog.ts, src/components/drawer/drawer.ts, src/components/command-palette/command-palette.ts, src/components/popover/popover.ts (open/close API + how focus is set; read only what's needed to drive open/close and identify the trap container)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§overlay-focus analog)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (Pattern 3 focus trap + restoration)
  </read_first>
  <action>
    Create `test/browser/overlay-focus.test.ts` (browser project — import from `'../helpers'`, NO getMockInternals). Focus a real opener button, open the overlay, and assert focus lands inside it using `deepActiveElement()`; Tab (via keydown or real focus movement) and assert the active element stays within the overlay (trap). Close and assert `deepActiveElement()`/`document.activeElement` returns to the opener. Cover am-dialog fully; add at least open+restore coverage for drawer, command-palette, popover. Where closing an overlay whose opener was removed would throw (the Phase 3 `isConnected` guard gap, CONCERNS focus-restoration), capture it as a finding in the SUMMARY — do NOT fix the component here (FIX-03 is Phase 3).
  </action>
  <verify>
    <automated>npx vitest run --project browser test/browser/overlay-focus.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/browser/overlay-focus.test.ts` exists, runs under the browser project, and does not import getMockInternals.
    - It asserts focus-trap containment and focus-restoration-to-opener for am-dialog (plus open/restore for drawer, command-palette, popover).
    - It uses `deepActiveElement()` to pierce shadow roots.
    - No component source is modified.
  </acceptance_criteria>
  <done>Focus trap + restoration are proven in real Chromium for the overlay set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Native dialog / top-layer against real showModal (TEST-06, Pitfall 2)</name>
  <files>test/browser/dialog-top-layer.test.ts</files>
  <behavior>
    - Opening an am-dialog invokes the REAL `HTMLDialogElement.showModal()` (not the setup.ts mock); the inner `<dialog>` reports `open === true` and is in the top layer.
    - A native-API guard asserts `HTMLDialogElement.prototype.showModal` is the genuine browser method (not the mock), proving the browser project omits setup.ts.
    - Closing (Escape / close button / `open=false`) fires the native `close` and the dialog reports closed.
  </behavior>
  <read_first>
    - test/components/dialog.test.ts (lines 6-30 open/close event flow; note lines 87-99 use real getBoundingClientRect — carry the real-rect idea into the browser)
    - src/components/dialog/dialog.ts (how `open` drives `showModal()`/`close()` on the inner `<dialog>`; the `.close-btn`)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§dialog-top-layer analog; §form-association native-API guard)
  </read_first>
  <action>
    Create `test/browser/dialog-top-layer.test.ts` (browser project). Mount am-dialog, set `open = true`, await update, and assert the shadow `<dialog>` reports `open === true` via the real `showModal()` (assert a real top-layer effect such as `dialog.matches(':modal')` or a non-zero client rect — pick the check that is reliable in Chromium after reading the source). Add the native-API guard: assert `HTMLDialogElement.prototype.showModal` is present and is the native method (Pitfall 2 — proves no setup.ts mock). Then close via the close button / Escape / `open=false` and assert the native `close` path leaves the dialog closed. Do NOT import getMockInternals.
  </action>
  <verify>
    <automated>npx vitest run --project browser test/browser/dialog-top-layer.test.ts 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - `test/browser/dialog-top-layer.test.ts` exists and passes under the browser project.
    - It asserts a real top-layer property (e.g. `:modal`) and includes a native-`showModal` guard.
    - It does not reference getMockInternals or any setup.ts mock symbol.
  </acceptance_criteria>
  <done>Native dialog/top-layer behavior is proven against the real browser API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: floating-ui positioning in a real browser (TEST-06)</name>
  <files>test/browser/floating-position.test.ts</files>
  <behavior>
    - Opening a floating-ui-positioned overlay (popover/tooltip/dropdown) produces a positioned floating element with a non-zero, viewport-anchored bounding rect computed by real layout.
    - The floating element is placed relative to its trigger (its rect is offset from the trigger's rect, not at 0,0).
  </behavior>
  <read_first>
    - src/components/popover/popover.ts (or tooltip/dropdown) — how floating-ui `computePosition`/`autoUpdate` sets the floating element's top/left and which shadow element is the floating panel
    - test/components/dialog.test.ts (lines 87-99 — real getBoundingClientRect usage as the closest existing reference)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§floating-position — No Analog Found note; assert computed top/left of a positioned overlay in Chromium)
    - .planning/codebase/CONCERNS.md (floating-UI components list, lines 63-67)
  </read_first>
  <action>
    Create `test/browser/floating-position.test.ts` (browser project). Choose ONE representative floating-ui overlay (popover is a good default — read its source to confirm the floating panel selector and open API). Mount it with a visible trigger, open it, await positioning (allow a microtask/rAF for `computePosition`), and assert the floating panel has a non-zero `getBoundingClientRect()` that is anchored to the viewport/trigger (e.g. top/left within viewport bounds and offset from the trigger's rect). This is the only fidelity area with no in-repo analog — keep the assertion about real computed geometry, not exact pixels. Do NOT modify the component; if positioning reveals a bug, capture it as a finding (PERF-04 autoUpdate gating is Phase 4).
  </action>
  <verify>
    <automated>npx vitest run --project browser test/browser/floating-position.test.ts 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - `test/browser/floating-position.test.ts` exists and passes under the browser project.
    - It asserts a non-zero, viewport-anchored rect for an opened floating overlay computed by real layout.
    - No component source is modified.
  </acceptance_criteria>
  <done>floating-ui positioning is proven with real layout in Chromium.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> CI browser runner | These tests execute in the PR-triggered headless-Chromium job (established read-only by plan 01) |
| test authoring only | No runtime code, network, or input surface is changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05a | Tampering | New browser test files executing in CI | low | accept | No runtime code or new deps; reuse existing helpers; runs in the read-only browser CI job from plan 01 |
| T-01-05b | Tampering | Temptation to fix component focus/positioning bugs a browser test reveals | low | mitigate | Capture as findings for Phase 3 (FIX-03) / Phase 4 (PERF-04); do not edit component source |
</threat_model>

<verification>
- All three browser suites pass under `vitest run --project browser`.
- None imports getMockInternals or a setup.ts mock symbol.
- No component source is modified.
</verification>

<success_criteria>
Focus trap+restoration, native dialog/top-layer, and floating-ui positioning are each proven in real Chromium, satisfying TEST-03 and completing TEST-06's four fidelity areas alongside the tracer's form lane.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- New: `test/browser/overlay-focus.test.ts`, `test/browser/dialog-top-layer.test.ts`, `test/browser/floating-position.test.ts`.
- Findings (captured, not fixed): Phase 3 `isConnected` focus-restoration guard (FIX-03), Phase 4 `autoUpdate` gating (PERF-04) if surfaced.
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-05-SUMMARY.md` when done.
</output>
