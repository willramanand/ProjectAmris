---
phase: 01-test-coverage-ci-gates-foundation
plan: 07
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/components/combobox.test.ts
  - test/components/select.test.ts
  - test/components/rich-select.test.ts
  - test/components/dropdown.test.ts
  - test/components/context-menu.test.ts
  - test/components/date-picker.test.ts
  - test/components/popover.test.ts
  - test/components/tooltip.test.ts
autonomous: true
requirements: [TEST-04, TEST-05]
estimate:
  tokens: 52000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Rapid/async replacement of `options` while open keeps the highlighted index clamped within `[0, options.length-1]` for combobox, select, rich-select (TEST-04)."
    - "Teardown spies confirm document-level listeners attach on open and detach on close for combobox, dropdown, context-menu, date-picker, popover, tooltip (TEST-05)."
    - "All new assertions are green-on-arrival on existing code (D-05); any genuine index-out-of-bounds or disconnect-leak is captured as a finding for the owning phase (Phase 3 FIX-02), not authored as a red assertion or fixed here."
    - "These run in the jsdom project (no browser needed — pure logic/lifecycle)."
  artifacts:
    - "clamp assertions in combobox/select/rich-select .test.ts"
    - "listener-teardown-spy assertions in combobox/dropdown/context-menu/date-picker/popover/tooltip .test.ts"
  key_links:
    - "vi.spyOn(document,'addEventListener'/'removeEventListener') asserts attach-on-open / detach-on-close in jsdom"
    - "options replacement + waitForUpdate asserts clamped highlighted index"
---

<objective>
Add the two jsdom lifecycle/logic suites that do NOT need a browser: async/dynamic option-update index clamping (TEST-04) and global-listener teardown spies (TEST-05). These extend existing dedicated test files in the jsdom project.

Purpose: Cover the async-option race and listener-lifecycle risks (CONCERNS fragile areas) with fast jsdom assertions, and surface any real leak/clamp bug for the owning phase without breaking green-on-arrival.
Output: New assertions in eight existing `test/components/*.test.ts` files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async option-update index clamp (TEST-04) — combobox, select, rich-select</name>
  <files>test/components/combobox.test.ts, test/components/select.test.ts, test/components/rich-select.test.ts</files>
  <behavior>
    - With the listbox open and a highlighted index near the end of a large options array, replacing `options` with a much shorter array keeps the highlighted index within `[0, newLength-1]` (no stale out-of-bounds index).
    - Stale filter state from the previous option set is not retained after replacement.
  </behavior>
  <read_first>
    - test/components/combobox.test.ts (existing async test around lines 104-120; makeCombobox helper 18-31; getOptions 29-31)
    - src/components/combobox/combobox.ts, src/components/select/select.ts, src/components/rich-select/rich-select.ts (how the highlighted/active index is stored and whether it is clamped when `options` changes — read the property name and any clamp logic)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (Pattern 4 async index clamp; §Component->Lane map TEST-04)
    - .planning/codebase/CONCERNS.md (Async/dynamic option loading, lines 77-81)
  </read_first>
  <action>
    In each of combobox/select/rich-select test files, add a describe/it for the clamp case: open the listbox, set the highlighted/active index high, replace `options` with a shorter array, `await waitForUpdate(el)`, and assert the highlighted index is `< options.length` (within bounds) — use the real property name found in the source. Add a rapid-replacement variant (replace options twice in quick succession) and assert bounds hold. If the source genuinely fails to clamp (a real bug per CONCERNS), do NOT author a red assertion and do NOT fix the component — capture it as a finding for the owning phase in the SUMMARY and assert the current observable bound instead so the suite stays green-on-arrival (D-05). Keep these in the jsdom project (pure logic — no browser).
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/combobox.test.ts test/components/select.test.ts test/components/rich-select.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files has a new clamp-after-replacement assertion using the real highlighted-index property.
    - `vitest run --project jsdom` over the three files exits 0.
    - Any genuine out-of-bounds bug is recorded as a finding, not fixed in component source.
  </acceptance_criteria>
  <done>combobox, select, rich-select assert the highlighted index stays clamped after rapid option updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Listener teardown spies (TEST-05) — combobox, dropdown, context-menu</name>
  <files>test/components/combobox.test.ts, test/components/dropdown.test.ts, test/components/context-menu.test.ts</files>
  <behavior>
    - Opening the component attaches a document-level listener (spy on `document.addEventListener` records the call); closing it removes that listener (spy on `document.removeEventListener` records the matching removal).
    - The attach/detach counts across an open->close cycle are balanced.
  </behavior>
  <read_first>
    - src/components/combobox/combobox.ts, src/components/dropdown/dropdown.ts, src/components/context-menu/context-menu.ts (where document-level click/keydown listeners are added on open and removed on close — confirm the event names and the open trigger)
    - test/components/dropdown.test.ts, context-menu.test.ts (existing open/close tests — reuse the open/close driving)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (Pattern 4 listener teardown spy; §Component->Lane map TEST-05)
    - .planning/codebase/CONCERNS.md (Global event listener lifecycle, lines 71-75)
  </read_first>
  <action>
    In each file add a describe/it using `vi.spyOn(document,'addEventListener')` and `vi.spyOn(document,'removeEventListener')`: open the component, `await waitForUpdate`, assert the document listener was attached; close it, `await waitForUpdate`, assert the matching listener was removed; assert the open->close cycle is balanced. Focus on the attach-on-open / detach-on-close behavior that holds on current code (green-on-arrival, D-05). Also drive `el.remove()` (disconnect) and assert teardown WHERE the component already tears down on disconnect; if a disconnect leak exists (FIX-02 is Phase 3), capture it as a finding rather than authoring a failing assertion — do NOT fix the component. jsdom project only.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/combobox.test.ts test/components/dropdown.test.ts test/components/context-menu.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each file has a teardown-spy assertion for attach-on-open and detach-on-close.
    - `vitest run --project jsdom` over the three files exits 0.
    - Disconnect leaks (if any) are captured as Phase 3 FIX-02 findings, not fixed here.
  </acceptance_criteria>
  <done>combobox, dropdown, context-menu have teardown-spy coverage for their document listeners.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Listener teardown spies (TEST-05) — date-picker, popover, tooltip</name>
  <files>test/components/date-picker.test.ts, test/components/popover.test.ts, test/components/tooltip.test.ts</files>
  <behavior>
    - Opening each component attaches a document-level listener; closing removes it; the open->close cycle is balanced.
  </behavior>
  <read_first>
    - src/components/date-picker/date-picker.ts, src/components/popover/popover.ts, src/components/tooltip/tooltip.ts (document-level listener attach-on-open / remove-on-close; open trigger)
    - test/components/date-picker.test.ts, popover.test.ts, tooltip.test.ts (existing open/close driving to reuse)
    - test/components/combobox.test.ts (the teardown-spy pattern established in Task 2 — mirror it)
  </read_first>
  <action>
    Mirror the Task 2 teardown-spy pattern for date-picker, popover, tooltip: `vi.spyOn` on document add/removeEventListener, assert attach-on-open and detach-on-close, assert balanced cycle. Capture any disconnect leak as a FIX-02 (Phase 3) finding without fixing the component or authoring a red assertion (green-on-arrival, D-05). jsdom project only.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/date-picker.test.ts test/components/popover.test.ts test/components/tooltip.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each file has a teardown-spy assertion for attach-on-open and detach-on-close.
    - `vitest run --project jsdom` over the three files exits 0.
    - Disconnect leaks (if any) recorded as Phase 3 findings.
  </acceptance_criteria>
  <done>date-picker, popover, tooltip have teardown-spy coverage for their document listeners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test authoring only | No runtime code, network, or input surface is changed; component source stays untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07a | Tampering | New jsdom assertions executing in CI | low | accept | No runtime code or new deps; runs in the read-only CI from plan 01 |
| T-01-07b | Tampering | Temptation to fix a clamp/leak bug to make an assertion pass | low | mitigate | Capture as Phase 3 (FIX-02) findings; keep assertions green-on-arrival; no component edit |
</threat_model>

<verification>
- All eight modified files pass under `vitest run --project jsdom`.
- Clamp assertions use the real highlighted-index property; teardown assertions use document add/removeEventListener spies.
- No component source is modified; leaks/clamp bugs captured as findings.
</verification>

<success_criteria>
Async option-update clamping (TEST-04) and global-listener teardown lifecycle (TEST-05) are covered in the fast jsdom lane, green on existing code, with any real defect surfaced for Phase 3.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- Modified: clamp assertions in `combobox.test.ts`, `select.test.ts`, `rich-select.test.ts`; teardown-spy assertions in `combobox.test.ts`, `dropdown.test.ts`, `context-menu.test.ts`, `date-picker.test.ts`, `popover.test.ts`, `tooltip.test.ts`.
- Findings (captured, not fixed): Phase 3 FIX-02 disconnect-teardown leaks; any highlighted-index-out-of-bounds bug.
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-07-SUMMARY.md` when done.
</output>
