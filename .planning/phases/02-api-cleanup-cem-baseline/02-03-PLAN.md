---
phase: 02-api-cleanup-cem-baseline
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/dropdown/dropdown.ts
  - src/components/popover/popover.ts
  - src/components/context-menu/context-menu.ts
  - test/components/dropdown.test.ts
  - test/components/popover.test.ts
  - test/components/context-menu.test.ts
  - .changeset/normalize-overlay-lifecycle-events.md
  - api/custom-elements.baseline.json
autonomous: false
requirements: [API-02]
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "dropdown, popover, and context-menu emit `am-open` on open and `am-close` on close, matching the native <dialog> and existing-majority vocabulary (dialog/drawer/command-palette/toast) — D-01."
    - "The old overlay lifecycle event strings are removed outright everywhere in src and test — no dual-firing, no alias, no deprecation window (D-04 hard rename)."
    - "The three characterization tests assert the new event names and stay green (renames intentionally rewrite the assertions — distinct from the behavior-preserving refactor rule)."
    - "Exactly one Changeset documents this overlay-lifecycle normalization wave (D-05)."
    - "api/custom-elements.baseline.json is re-committed after the wave so the report-only CI diff flags only UNINTENDED drift beyond these intended renames (D-14)."
  artifacts:
    - .changeset/normalize-overlay-lifecycle-events.md (one Changeset for the wave)
    - api/custom-elements.baseline.json (re-committed post-wave snapshot)
  key_links:
    - "component dispatch string + @fires JSDoc + test assertion renamed together in one wave -> no surviving old name (Pattern 2)"
    - "re-run npm run build:manifest then re-copy dist/custom-elements.json to api/custom-elements.baseline.json -> diff shows only intended renames"
    - "already-correct overlays (dialog/drawer/command-palette/toast/alert) are NOT touched"
  prohibitions:
    - "Do NOT keep any backward-compat alias or dual-fire the old event name (D-04)."
    - "Do NOT touch the already-correct overlays (dialog, drawer, command-palette, toast, alert)."
    - "Do NOT fix any listener-lifecycle/leak seam exposed while editing these files — that is Phase 3 (FIX-02); capture, do not fix."
    - "Do NOT create more than one Changeset for this wave (D-05)."
---

<!-- planner-discipline-allow: am-show -->
<!-- planner-discipline-allow: am-hide -->

<objective>
Normalize the overlay lifecycle event vocabulary: rename the outlier overlays (dropdown, popover, context-menu) so they emit `am-open`/`am-close` like the native `<dialog>` and the existing majority (dialog, drawer, command-palette, toast), landing the break as one reviewable Changeset wave with the baseline re-committed. This is the first rename wave — it exercises the atomic wave discipline (code + JSDoc + tests + one Changeset + baseline) that Plans 04 and 05 repeat.

Purpose: D-01 locks overlay lifecycle on open/close as a one-way published-contract decision; pre-freeze is the only window to make the surface consistent. The Changeset is the durable old→new record Phase 5's MIGRATION.md builds on.

Output: renamed dispatches + `@fires` JSDoc + updated test assertions across three components, one Changeset, and a re-committed baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@api/AUDIT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the overlay lifecycle rename batch (one-way door — D-01)</name>
  <read_first>
    - api/AUDIT.md ("Rename mapping" section — the overlay lifecycle rows marked PENDING-DECISION)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-01, D-04 — reversibility: one-way, event-name contract)
  </read_first>
  <decision>Land the breaking overlay-lifecycle event rename on dropdown, popover, and context-menu — their open event becomes `am-open` and their close event becomes `am-close`, with the old names removed outright (no alias).</decision>
  <context>This is a one-way door: the emitted event name is a published contract consumers bind listeners to; reverting after 1.0 needs a migration (D-01/D-04). The already-correct overlays (dialog, drawer, command-palette, toast, alert) are untouched. Confirm the mapping in api/AUDIT.md matches the three components before the rename lands.</context>
  <options>
    <option id="approve">
      <name>Approve as mapped (D-01)</name>
      <pros>Consistent overlay vocabulary matching native &lt;dialog&gt; and the existing majority; frozen surface is clean before 1.0.</pros>
      <cons>Breaks any current consumer listening on the old open/close event names — recorded in the wave Changeset.</cons>
    </option>
    <option id="adjust">
      <name>Adjust mapping</name>
      <pros>Lets the reviewer correct a target name the audit got wrong before it becomes a published contract.</pros>
      <cons>Requires re-running the audit mapping row; delays the wave.</cons>
    </option>
  </options>
  <resume-signal>Type "approved" to land the mapping as-is, or name the adjustment.</resume-signal>
  <acceptance_criteria>
    - Human has reviewed the overlay lifecycle rows in api/AUDIT.md and typed "approved" (or provided an adjusted mapping) before Task 2 runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename overlay lifecycle events + JSDoc + tests, write one Changeset (API-02, D-01/D-04/D-05)</name>
  <reversibility rating="one-way">The emitted event name is a published contract consumers bind listeners to; reverting after 1.0 needs a consumer migration (D-01).</reversibility>
  <files>src/components/dropdown/dropdown.ts, src/components/popover/popover.ts, src/components/context-menu/context-menu.ts, test/components/dropdown.test.ts, test/components/popover.test.ts, test/components/context-menu.test.ts, .changeset/normalize-overlay-lifecycle-events.md</files>
  <read_first>
    - src/components/dropdown/dropdown.ts (dispatch call sites at :110 and :115; @fires JSDoc at :17-18)
    - src/components/popover/popover.ts (dispatch at :159 and :164; @fires at :16-17)
    - src/components/context-menu/context-menu.ts (dispatch at :115, :122, :129; @fires at :16-17)
    - test/components/dropdown.test.ts, popover.test.ts, context-menu.test.ts (assertions binding the old open/close event names — dropdown :7,17,25,35; popover :7,17,25,35; context-menu :34,38,45,51)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§Event-rename edits — "Wave: overlay lifecycle" table + done-condition grep)
    - .changeset/config.json (Changeset format; commit:false)
  </read_first>
  <action>
    In dropdown, popover, and context-menu: change the open-event dispatch string to `am-open` and the close-event dispatch string to `am-close` (preserving each existing `bubbles`/`composed` option object exactly — relocate the string only). Update each component's `@fires` JSDoc tags to document `am-open`/`am-close`. Update any internal `addEventListener`/`removeEventListener` for the old open/close names in these three files to the new names (grep each file for the old strings to catch every occurrence, including listeners and stories). Update the three characterization tests to assert `am-open`/`am-close`. Do NOT alter runtime behavior (open/close gating, listener attach/detach ordering) — only the event-name strings and the assertions binding them. Do NOT touch dialog, drawer, command-palette, toast, or alert (already canonical). Write exactly one Changeset `.changeset/normalize-overlay-lifecycle-events.md` (minor/major per Changesets convention for a breaking change) describing the overlay lifecycle normalization and listing the three affected components with old→new names. After editing, grep src and test for the old open/close event strings — zero hits is the done condition (D-04 hard rename).
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/dropdown.test.ts test/components/popover.test.ts test/components/context-menu.test.ts 2>&1 | tail -20 && ! grep -rnE "am-show|am-hide" src/components/dropdown src/components/popover src/components/context-menu test/components/dropdown.test.ts test/components/popover.test.ts test/components/context-menu.test.ts && ls .changeset/normalize-overlay-lifecycle-events.md</automated>
  </verify>
  <acceptance_criteria>
    - dropdown, popover, context-menu dispatch `am-open` on open and `am-close` on close; their `@fires` JSDoc documents the new names.
    - `grep -rnE "am-show|am-hide" src/components/dropdown src/components/popover src/components/context-menu test/components/dropdown.test.ts test/components/popover.test.ts test/components/context-menu.test.ts` returns zero hits (D-04).
    - The three characterization tests assert the new names and pass under `npx vitest run --project jsdom`.
    - Exactly one Changeset file `.changeset/normalize-overlay-lifecycle-events.md` exists for the wave (D-05).
    - dialog/drawer/command-palette/toast/alert dispatch strings are unchanged (`git diff --name-only` does not list them).
  </acceptance_criteria>
  <done>The three overlay outliers emit the canonical open/close events, the old strings are gone repo-wide, tests are green, and one Changeset records the break.</done>
</task>

<task type="auto">
  <name>Task 3: Re-commit baseline + report-only diff + full suite green (API-02, D-14)</name>
  <files>api/custom-elements.baseline.json</files>
  <read_first>
    - scripts/cem-diff.mjs (the report-only comparator from Plan 01 — run it to confirm only intended renames drift)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-14 — baseline re-committed after each approved wave)
    - .planning/phases/02-api-cleanup-cem-baseline/02-VALIDATION.md (§Sampling Rate — full suite jsdom+browser per wave merge)
  </read_first>
  <action>
    Re-run `npm run build:manifest`, run `npm run diff:surface` and confirm the printed drift is exactly the three overlay open/close event renames (no unexpected surface change; controllers register no element so the tagName set stays 66). Then re-copy dist/custom-elements.json to api/custom-elements.baseline.json so the baseline absorbs the intended renames (D-14). Run the full characterization suite (`npm run test:run` then `npm run test:browser`) to confirm no regression in jsdom or Chromium.
  </action>
  <verify>
    <automated>npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && npm run test:run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run diff:surface` runs (exit 0, report-only) and its output reflects only the three overlay open/close renames.
    - api/custom-elements.baseline.json is updated to the post-wave manifest (its content differs from the pre-wave snapshot only by the renamed events).
    - `npm run test:run` (jsdom) passes; the browser lane (`npm run test:browser`) passes on the wave-merge run.
    - The CEM tagName count remains 66 (no element added/removed).
  </acceptance_criteria>
  <done>The baseline is re-committed for the overlay wave, the report-only diff shows only the intended renames, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → component events | renamed public event names are the only surface change; consumers rebind listeners (recorded in the Changeset) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | Lit-safe templating invariant | low | accept | Rename touches only event-name strings + assertions; no innerHTML/eval introduced, no template change. |

No new threats: client-side rendering library; the change is a public event-name normalization with no new input sink and no dependency.
</threat_model>

## Artifacts this plan produces
- `.changeset/normalize-overlay-lifecycle-events.md` — one Changeset recording the overlay lifecycle break (D-05)
- `api/custom-elements.baseline.json` — re-committed post-wave snapshot (D-14)
- Renamed dispatches + `@fires` JSDoc in dropdown/popover/context-menu and updated assertions in their tests

<verification>
- The old overlay open/close event strings do not survive anywhere in src or test.
- Exactly one Changeset for the wave; baseline re-committed.
- Full suite (jsdom + browser) green; tagName count 66.
</verification>

<success_criteria>
The overlay lifecycle events are normalized to `am-open`/`am-close` across the three outliers with the old names removed, one Changeset, a re-committed baseline, and green tests (ROADMAP SC2, first wave).
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-03-SUMMARY.md` when done.
</output>
