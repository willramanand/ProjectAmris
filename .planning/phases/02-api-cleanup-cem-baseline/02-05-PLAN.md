---
phase: 02-api-cleanup-cem-baseline
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - src/components/tabs/tabs.ts
  - src/components/combobox/combobox.ts
  - test/components/tabs.test.ts
  - test/components/combobox.test.ts
  - .changeset/normalize-remaining-outliers.md
  - api/custom-elements.baseline.json
autonomous: false
requirements: [API-02]
estimate:
  tokens: 66000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every remaining event/prop/boolean/default outlier the matrices surfaced is normalized to the canonical vocabulary — D-03 full normalization, not just the egregious cases."
    - "The value-changing tabs event is renamed to `am-change` (matching pagination), while expand-state events (accordion/tree-view `am-toggle`) are confirmed canonical and left unchanged."
    - "The combobox boolean prop that collides with the am-select element name, and any mis-named boolean, are renamed to the approved canonical names (public attribute surface change)."
    - "All old names are removed outright (D-04); the CEM surface diff reflects exactly these intended renames."
    - "Exactly one Changeset documents the remaining-outliers normalization wave (D-05); the baseline is re-committed post-wave (D-14)."
  artifacts:
    - .changeset/normalize-remaining-outliers.md (one Changeset for the D-03 cleanup wave)
    - api/custom-elements.baseline.json (re-committed post-wave snapshot)
  key_links:
    - "tabs value-change event + internal listeners + @fires + tests renamed together -> no surviving old name"
    - "combobox @property rename changes the reflected attribute -> CEM attributes/members entry updates; diff:surface shows the rename"
    - "accordion/tree-view am-toggle and pagination am-change confirmed canonical -> not touched"
  prohibitions:
    - "Do NOT collapse expand-state `am-toggle` (accordion/tree-view) into `am-change` — expand/collapse is a distinct semantic (D-03 audit decision)."
    - "Do NOT keep a backward-compat alias for any renamed prop/event (D-04)."
    - "Do NOT refactor combobox internals here — that is Plan 06 (API-03); this plan only renames its public prop(s)."
    - "Do NOT create more than one Changeset for this wave (D-05)."
---

<!-- planner-discipline-allow: am-tab-change -->

<objective>
Complete the D-03 full normalization: rename every remaining event/prop/boolean/default outlier the audit matrices surfaced beyond the overlay (Plan 03) and selection (Plan 04) waves — the value-changing tabs event to `am-change`, and the mis-named combobox boolean prop(s) — landed as one Changeset wave with the baseline re-committed. This is the last rename wave; after it the public vocabulary is consistent and ready to freeze.

Purpose: D-03 mandates fixing every outlier, not just the egregious cases, because pre-freeze is the only window. The change/toggle family is disambiguated (value-change → `am-change`, expand-state → `am-toggle`) and prop/boolean naming is normalized.

Output: renamed events + props + JSDoc + tests, one Changeset for the cleanup wave, and a re-committed baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@api/AUDIT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the remaining-outliers rename set (one-way door — D-03/D-04)</name>
  <read_first>
    - api/AUDIT.md ("Rename mapping" — every remaining PENDING-DECISION row: change/toggle family + prop/boolean/default outliers with proposed targets)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§Open Questions Q2 — change/toggle family resolution; combobox `select`/`async` boolean note)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-03 full normalization, D-04 hard rename)
  </read_first>
  <decision>Land the full remaining-outlier normalization set exactly as mapped in api/AUDIT.md: tabs value-change event → `am-change`; accordion/tree-view keep `am-toggle`; pagination stays `am-change`; and the combobox boolean prop(s) rename to the approved canonical names — old names removed outright.</decision>
  <context>One-way door: renamed event AND prop/attribute names are published contracts. Confirm each remaining row (especially which change/toggle members collapse to `am-change` vs stay `am-toggle`, and the exact combobox prop target names) before the break lands, since after 1.0 every one requires a consumer migration. The Changeset records the full set.</context>
  <options>
    <option id="approve-all">
      <name>Approve the full mapped set (D-03)</name>
      <pros>Fully consistent surface at freeze; no lingering outliers locked forever post-1.0.</pros>
      <cons>Largest single break of the phase; every renamed name is recorded in the Changeset for consumers.</cons>
    </option>
    <option id="approve-subset">
      <name>Approve a subset, defer specific rows</name>
      <pros>Reviewer can hold back a rename whose target is still uncertain.</pros>
      <cons>Deferred outliers stay inconsistent into the freeze unless handled before Plan 09 — surface the risk explicitly.</cons>
    </option>
  </options>
  <resume-signal>Type "approved: all" or list the rows to land / defer.</resume-signal>
  <acceptance_criteria>
    - Human has reviewed every remaining rename row in api/AUDIT.md and approved the exact set (and combobox prop target names) before Task 2 runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename remaining event + prop/boolean outliers + JSDoc + tests, write one Changeset (API-02, D-03/D-04/D-05)</name>
  <reversibility rating="one-way">Renamed event and prop/attribute names are published contracts; reverting after 1.0 needs a consumer migration (D-03/D-04).</reversibility>
  <files>src/components/tabs/tabs.ts, src/components/combobox/combobox.ts, test/components/tabs.test.ts, test/components/combobox.test.ts, .changeset/normalize-remaining-outliers.md</files>
  <read_first>
    - src/components/tabs/tabs.ts (the value-change event dispatch at :376 and any internal listener; @fires JSDoc)
    - src/components/combobox/combobox.ts (boolean props at :63 `async` and :72 `select`; their @property decorators + JSDoc + any internal usage; @fires block :24-26)
    - test/components/tabs.test.ts, test/components/combobox.test.ts (assertions binding the old event/prop names)
    - api/AUDIT.md (the approved target names from Task 1)
    - .planning/codebase/CONVENTIONS.md (boolean prop naming conventions the new names must follow)
  </read_first>
  <action>
    Apply the approved mapping from api/AUDIT.md. In tabs: rename the value-change event dispatch string to `am-change`, update its `@fires` JSDoc and any internal `addEventListener`/`removeEventListener`, and update the tabs test assertions. Confirm accordion/tree-view `am-toggle` and pagination `am-change` are already canonical and leave them untouched.
    In combobox: rename the mis-named boolean prop(s) — the `select` boolean that collides with the am-select element and any boolean flagged by the boolean-naming matrix — to the approved canonical property names, updating the `@property` declaration, all internal references to the renamed property within combobox.ts, the component JSDoc, and the combobox test assertions. Do NOT change combobox runtime behavior or refactor its internals (that is Plan 06); only the public property name and its internal references change.
    Write exactly one Changeset `.changeset/normalize-remaining-outliers.md` describing the D-03 full-normalization cleanup, listing every old→new event and prop name. After editing, grep src and test for the old event name and confirm the combobox CEM entry no longer carries the old attribute — zero surviving old names is the done condition (D-04).
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/tabs.test.ts test/components/combobox.test.ts 2>&1 | tail -20 && ! grep -rnE "am-tab-change" src test && npm run build:manifest && npm run diff:surface 2>&1 | tail -30 && ls .changeset/normalize-remaining-outliers.md</automated>
  </verify>
  <acceptance_criteria>
    - tabs dispatches `am-change`; `grep -rnE "am-tab-change" src test` returns zero hits (D-04); `@fires` JSDoc updated.
    - The combobox renamed boolean prop(s) use the approved canonical name(s); the old `@property` declaration name(s) no longer appear in combobox.ts; `npm run diff:surface` output shows the combobox attribute rename.
    - accordion/tree-view/pagination event dispatch strings are unchanged (`git diff --name-only` does not list them).
    - tabs.test.ts and combobox.test.ts assert the new names and pass under jsdom.
    - Exactly one Changeset `.changeset/normalize-remaining-outliers.md` exists (D-05).
  </acceptance_criteria>
  <done>Every remaining event/prop/boolean outlier is normalized to the approved canonical names with old names removed, one Changeset written, tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-commit baseline + report-only diff + full suite green (API-02, D-14)</name>
  <files>api/custom-elements.baseline.json</files>
  <read_first>
    - scripts/cem-diff.mjs (Plan 01 comparator — confirm only intended renames drift)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-14)
  </read_first>
  <action>
    Re-run `npm run build:manifest`, run `npm run diff:surface`, and confirm the printed drift is exactly the tabs event rename plus the combobox prop rename(s) (the CEM tagName SET is unchanged from the committed baseline — no element added or removed). Re-copy dist/custom-elements.json to api/custom-elements.baseline.json (D-14). Run the full characterization suite (`npm run test:run` then `npm run test:browser`) to confirm no regression. This is the last rename wave — after this the public vocabulary is consistent for the refactor and freeze.
  </action>
  <verify>
    <automated>npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && npm run test:run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run diff:surface` runs report-only (exit 0); its output reflects only the tabs + combobox renames.
    - api/custom-elements.baseline.json is updated to the post-wave manifest.
    - `npm run test:run` passes; browser lane passes on the wave-merge run.
    - The CEM tagName SET is unchanged from the committed baseline (no element added or removed).
  </acceptance_criteria>
  <done>The baseline is re-committed for the final rename wave, the diff shows only intended renames, and the full suite is green — the vocabulary is normalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → component events/props | renamed event + attribute names are the surface change; consumers rebind/rename (Changeset records it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | Lit-safe templating invariant | low | accept | Rename touches event/property names + assertions; no innerHTML/eval, no template change. |

No new threats: public event/prop normalization on a client-side rendering library; no new input sink, no dependency.
</threat_model>

## Artifacts this plan produces
- `.changeset/normalize-remaining-outliers.md` — one Changeset for the D-03 cleanup wave (D-05)
- `api/custom-elements.baseline.json` — re-committed post-wave snapshot (D-14)
- Renamed tabs event + combobox prop(s) + `@fires`/JSDoc + updated tests

<verification>
- Old change/toggle + prop names do not survive in src or test.
- Exactly one Changeset; baseline re-committed.
- Full suite green; CEM tagName SET unchanged from baseline (no element added/removed); expand-state `am-toggle` preserved.
</verification>

<success_criteria>
Every remaining outlier is normalized under D-03 with old names removed, one Changeset, a re-committed baseline, and green tests — the public vocabulary is consistent (ROADMAP SC2, final wave).
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-05-SUMMARY.md` when done.
</output>
