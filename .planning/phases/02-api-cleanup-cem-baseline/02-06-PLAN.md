---
phase: 02-api-cleanup-cem-baseline
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - src/internal/controllers/floating-position.ts
  - src/internal/controllers/listbox-nav.ts
  - src/internal/controllers/option-filter.ts
  - src/components/combobox/combobox.ts
autonomous: true
requirements: [API-03]
estimate:
  tokens: 95000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A non-exported `src/internal/` boundary exists with three Lit Reactive Controllers — FloatingPositionController, ListboxNavController, OptionFilterController — that register no custom element (D-07, D-08, D-09)."
    - "combobox consumes the three controllers via host.addController, delegating positioning, listbox keyboard nav, and option filtering that were previously inline (API-03)."
    - "The refactor is behavior-preserving: the Phase 1 combobox characterization tests (jsdom + Chromium) stay GREEN with ZERO test edits (D-10)."
    - "`autoUpdate` stays UNGATED and the `_highlightedIndex` un-clamp-on-replace is preserved exactly — the seams are captured for Phase 3/4, not fixed here (Pitfalls)."
    - "src/internal/ is absent from src/index.ts, src/index.all.ts, and package.json exports; the CEM tagName count stays 66 (Pitfall 4)."
    - "hostDisconnected mirrors combobox's existing disconnectedCallback teardown exactly — no reordering, no new gating."
  artifacts:
    - src/internal/controllers/floating-position.ts (wraps computePosition + autoUpdate lifecycle, per-host options)
    - src/internal/controllers/listbox-nav.ts (Arrow/Enter/Escape + highlightedIndex movement/clamp, preserving the un-clamp-on-replace)
    - src/internal/controllers/option-filter.ts (client-side option filtering + async gating)
    - src/components/combobox/combobox.ts (delegates to the three controllers, behavior-preserving)
  key_links:
    - "controllers import type { ReactiveController } from 'lit' + register via host.addController(this) in the field initializer -> Lit stays peerDep, no element registered"
    - "combobox.updated() delegates start/stop to the floating controller with the SAME open gating it had inline -> byte-for-byte behavior"
    - "src/internal/ NOT added to any barrel or exports -> off the frozen CEM surface (D-09)"
  prohibitions:
    - "Do NOT gate `autoUpdate` on open transitions — that is PERF-04/Phase 4; keep it exactly as inline today."
    - "Do NOT re-clamp `_highlightedIndex` on option replace — preserve the known un-clamp bug (FIX-02/Phase 3)."
    - "Do NOT add `src/internal/` to src/index.ts, src/index.all.ts, or package.json exports (D-09, Pitfall 4)."
    - "Do NOT edit combobox's characterization test to make the refactor pass (D-10) — the unchanged test is the acceptance signal."
    - "Do NOT introduce innerHTML/eval or a module-level singleton — controllers are per-host instances."
---

<objective>
Establish the non-exported `src/internal/` boundary and the three shared Reactive Controllers as one production-quality vertical slice, proven end-to-end by wiring the heaviest consumer (combobox, 741 lines — the source of all three duplicated patterns) and keeping its Phase 1 characterization tests green with zero edits. This is the refactor tracer: it proves the controller decomposition works against the real green tests before Plans 07/08 expand it to select, date-picker, and time-picker.

Purpose: D-07/D-08 decompose the Big-4 via Lit Reactive Controllers reusing the shared machinery (floating positioning, listbox keyboard nav, option filtering); D-09 keeps that machinery off the frozen public surface in `src/internal/`. Proving the boundary and pattern on combobox first means the expansion is mechanical delegation against a proven seam.

Output: three controllers under `src/internal/controllers/` and a combobox that delegates to them, behavior-preserving.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish src/internal/ + FloatingPositionController, wire combobox positioning (API-03, D-07/D-08/D-09)</name>
  <reversibility rating="costly">Establishing the non-exported src/internal/ controller boundary is a cross-module relocation Phase 3/4 also build on; undo touches many call sites but changes no public contract (D-09).</reversibility>
  <files>src/internal/controllers/floating-position.ts, src/components/combobox/combobox.ts</files>
  <read_first>
    - src/components/combobox/combobox.ts (imports :1-7; floating setup around :393-398; disconnectedCallback teardown :365-370; the computePosition/autoUpdate call sites and `size` middleware import :5)
    - src/components/dropdown/dropdown.ts (:105-138 — the cleanest floating analog: _startAutoUpdate/_updatePosition shape, strategy:'fixed', offset/flip/shift middleware)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (Pattern 1: FloatingPositionController interface + the ReactiveController callbacks; the note that per-host options — reference, floating, placement, strategy, offset, extra middleware — differ per component)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§floating-position.ts controller: existing duplication, teardown pattern to mirror, import conventions, CRITICAL keep autoUpdate ungated)
  </read_first>
  <action>
    Create `src/internal/controllers/floating-position.ts` implementing Lit's `ReactiveController` (import type ReactiveController, ReactiveControllerHost from 'lit'; import computePosition, autoUpdate, flip, shift, offset from '@floating-ui/dom'; relative `.js`-less type import). The controller takes the host plus a per-host options object with `reference()` and `floating()` accessor callbacks, `placement`, optional `strategy`, `offset`, and optional extra middleware (combobox uses a `size` middleware that dropdown does not — the options must accommodate both). It exposes `start()` (begins autoUpdate → computePosition, applying left/top to the floating element) and `stop()` (runs the cleanup), registers via `host.addController(this)` in its constructor, and mirrors the host's existing teardown in `hostDisconnected()`. Keep `autoUpdate` UNGATED (behavior-preserving; gating is PERF-04/Phase 4).
    Refactor combobox to instantiate this controller in a field initializer with combobox's exact reference/floating accessors, placement, offset, and its `size` middleware, and delegate its inline positioning start/stop from `updated()` using the SAME open gating combobox has today. Remove combobox's now-duplicated inline `_startAutoUpdate`/`_updatePosition` and their teardown, ensuring the controller's `hostDisconnected` reproduces the exact cleanup (combobox.ts:365-370) with no reordering and no new `if (open)` guard on autoUpdate. Do NOT add src/internal/ to any barrel or package.json exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --project jsdom test/components/combobox.test.ts 2>&1 | tail -20 && ! grep -rnE "innerHTML|eval\(" src/internal && node -e "const p=require('./package.json'); if(JSON.stringify(p.exports).includes('internal')) throw new Error('src/internal must not be exported'); console.log('internal-unexported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/internal/controllers/floating-position.ts` exists, implements ReactiveController (has hostDisconnected), imports `type ReactiveController` from 'lit', and registers via host.addController.
    - combobox imports the controller from `../../internal/controllers/floating-position.js` and no longer defines its inline positioning methods; `autoUpdate` appears only in the controller (grep combobox.ts shows no direct `autoUpdate(` call).
    - `npx tsc --noEmit` passes (strict) and `npx vitest run --project jsdom test/components/combobox.test.ts` passes with the test file UNCHANGED (`git diff --name-only` does not list test/components/combobox.test.ts).
    - `grep -rnE "innerHTML|eval\(" src/internal` returns zero hits.
    - package.json exports contains no `internal` path.
  </acceptance_criteria>
  <done>The src/internal/ boundary and FloatingPositionController exist, combobox delegates positioning behavior-preservingly, and combobox's characterization test is green unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ListboxNavController + OptionFilterController, wire combobox nav + filtering (API-03, D-07/D-08)</name>
  <files>src/internal/controllers/listbox-nav.ts, src/internal/controllers/option-filter.ts, src/components/combobox/combobox.ts</files>
  <read_first>
    - src/components/combobox/combobox.ts (_handleKeydown :442-489 — Arrow/Enter/Escape/Tab + _highlightedIndex movement; the option filter :443-445; the Enter branch to requestAssociatedFormSubmit :468)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§listbox-nav.ts: core keydown pattern + CRITICAL preserve un-clamp-on-replace + Enter host-specific callback; §option-filter.ts: filter pattern + async gating, may be a pure helper per D-07)
    - src/internal/controllers/floating-position.ts (as written in Task 1 — mirror its controller structure/registration)
  </read_first>
  <action>
    Create `src/internal/controllers/listbox-nav.ts` implementing ReactiveController: it owns Arrow/Enter/Escape/Tab handling and `_highlightedIndex` movement/clamping over a host-provided option count, exposing a keydown handler the host calls. Preserve combobox's current clamp behavior EXACTLY — including the known un-clamp-on-replace (do not re-clamp when options change; that is FIX-02/Phase 3). Host-specific concerns (the Enter → requestAssociatedFormSubmit branch) stay in the host or are passed as a callback, not absorbed into the controller.
    Create `src/internal/controllers/option-filter.ts` for the client-side option filtering (`options.filter(o => o.toLowerCase().includes(value.toLowerCase()))`) with the async-gating branch; per D-07 this may be a plain module rather than a controller if that fits the real call site better (the async branch is the only stateful concern) — choose based on how combobox consumes it, and document the choice in the SUMMARY.
    Wire combobox to delegate its keydown navigation and option filtering to these units, removing the now-duplicated inline logic while keeping the highlighted-index behavior, async gating, and form-submit branch identical. Keep the `am-search`/change/input dispatches in the host (events are public surface). Do NOT add src/internal/ to any barrel or exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --project jsdom test/components/combobox.test.ts 2>&1 | tail -20 && ! grep -rnE "innerHTML|eval\(" src/internal</automated>
  </verify>
  <acceptance_criteria>
    - `src/internal/controllers/listbox-nav.ts` and `src/internal/controllers/option-filter.ts` exist; listbox-nav implements ReactiveController.
    - combobox delegates keydown nav + option filtering to the new units; its inline `_handleKeydown` index-movement and inline filter are removed/relocated.
    - The un-clamp-on-replace behavior is preserved (no new re-clamp on option change) and the Enter → form-submit branch still fires.
    - `npx tsc --noEmit` passes and `npx vitest run --project jsdom test/components/combobox.test.ts` passes with the test UNCHANGED.
    - `grep -rnE "innerHTML|eval\(" src/internal` returns zero hits.
  </acceptance_criteria>
  <done>All three shared controllers exist and combobox delegates positioning, listbox nav, and filtering to them behavior-preservingly, tests green unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Behavior-preservation gate — full suite green, boundary non-exported, surface unchanged (API-03, D-09/D-10)</name>
  <files>src/components/combobox/combobox.ts</files>
  <read_first>
    - src/index.ts, src/index.all.ts (confirm neither imports/exports src/internal/)
    - scripts/cem-diff.mjs (Plan 01 comparator — confirm the refactor added no public surface)
    - .planning/phases/02-api-cleanup-cem-baseline/02-VALIDATION.md (§Sampling Rate — full jsdom+browser per wave)
  </read_first>
  <action>
    Run the full characterization suite (`npm run test:run` then `npm run test:browser`) to prove the combobox refactor is behavior-preserving in both jsdom and Chromium with zero test edits (D-10). Run `npm run build:manifest` and `npm run diff:surface`; confirm the diff shows NO surface change from the refactor (no added/removed elements, events, slots, parts, tokens — controllers register no element) and the tagName count stays 66. Confirm src/internal/ is absent from src/index.ts, src/index.all.ts, and package.json exports. Do NOT re-commit api/custom-elements.baseline.json — the refactor changes no surface, so the baseline is unchanged (any diff here is an unintended regression to investigate, not to absorb).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -15 && npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && ! grep -rn "internal/" src/index.ts src/index.all.ts && node -e "const p=require('./package.json'); if(JSON.stringify(p.exports).includes('internal')) throw new Error('exported'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` (jsdom) and `npm run test:browser` (Chromium) both pass with no test file edits across the combobox refactor.
    - `npm run diff:surface` reports NO surface change from the refactor and the CEM tagName count remains 66.
    - `grep -rn "internal/" src/index.ts src/index.all.ts` returns zero hits; package.json exports has no `internal` path.
    - api/custom-elements.baseline.json is unchanged by this plan (the refactor adds no public surface).
  </acceptance_criteria>
  <done>The combobox controller refactor is proven behavior-preserving in jsdom and Chromium, the src/internal/ boundary is non-exported, and the public surface is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refactor edits → Lit-safe templating invariant | relocating machinery must not introduce a non-Lit-safe render sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | src/internal controllers | low | mitigate | Grep gate `! grep -rnE "innerHTML|eval\(" src/internal` on every task; controllers are per-host instances (no module-level singleton), Lit-safe templating preserved. |

No new threats beyond the templating invariant: no server, no untrusted input, no new dependency (@floating-ui/dom already present).
</threat_model>

## Artifacts this plan produces
- `src/internal/controllers/floating-position.ts` — FloatingPositionController (computePosition + ungated autoUpdate, per-host options)
- `src/internal/controllers/listbox-nav.ts` — ListboxNavController (Arrow/Enter/Escape + highlightedIndex, un-clamp-on-replace preserved)
- `src/internal/controllers/option-filter.ts` — OptionFilter controller or pure module (client-side filter + async gating)
- `src/components/combobox/combobox.ts` — delegates positioning/nav/filtering to the three units, behavior-preserving

<verification>
- combobox characterization tests green in jsdom + Chromium with zero test edits.
- src/internal/ absent from barrels and package.json exports; tagName count 66; no innerHTML/eval in src/internal.
- diff:surface shows no surface change; baseline unchanged.
</verification>

<success_criteria>
The non-exported src/internal/ boundary and the three shared Reactive Controllers exist and combobox delegates to them behavior-preservingly with the Phase 1 tests green unchanged (ROADMAP SC3, refactor tracer proven).
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-06-SUMMARY.md` when done.
</output>