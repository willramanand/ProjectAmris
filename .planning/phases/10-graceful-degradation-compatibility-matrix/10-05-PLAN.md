---
phase: 10-graceful-degradation-compatibility-matrix
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/components/slider/slider.ts
  - src/components/switch/switch.ts
  - src/components/textarea/textarea.ts
  - src/components/time-picker/time-picker.ts
  - test/capabilities-off-constructor.test.ts
  - test/form-fallback-integration.test.ts
autonomous: true
requirements: [COMPAT-02, COMPAT-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With ElementInternals PRESENT, the fallback hidden-input never attaches on slider, switch, textarea, or time-picker — the XOR gate is proven per-component (COMPAT-03 concurrency edge: no double-submit)."
    - "With hasFormAssociation() forced false AND the fallback enabled, each of these 4 components' FormData submission matches its above-floor value (FormData parity)."
    - "Below the floor, all 4 components construct, connect, and render without throwing (COMPAT-02) — this closes out full 16/16 coverage of test/capabilities-off-constructor.test.ts's FORM_TAGS list, started in Plan 01."
  artifacts: []
  key_links:
    - "slider.ts, switch.ts, textarea.ts, time-picker.ts each call attachInternalsSafe(this) in their constructor and syncFormFallback/warnBelowFloorOnce in updated() when this.internals is null"
  prohibitions:
    - "Above-floor behavior must not change on any of these 4 components — the freeze holds outside the new below-floor branch."
    - "No component wires both channels (ElementInternals + fallback) in the same render."
---

<objective>
Combined COMPAT-02 + COMPAT-03 rollout, batch B (the final 4 remaining
components): slider, switch, textarea, time-picker. Runs in parallel with Plan
04 in wave 2 (zero file overlap) — both depend only on Plan 01
(capabilities.ts/attachInternalsSafe) and Plan 02 (form-participation.ts).

This is the smallest of the two rollout batches — slider is the tracer
(proving the pattern on a numeric-value control, distinct from the
string-value controls already proven in Plans 01/04), then switch, textarea,
and time-picker expand it.

Purpose: complete the mechanical COMPAT-02/COMPAT-03 rollout across all 16
form-associated custom elements — by the end of this plan (combined with
Plan 04), every one of the 16 attach sites across 15 files is guarded and,
where value-bearing, XOR-fallback-capable.
Output: slider, switch, textarea, and time-picker are fully
degradation-capable; the shared jsdom test suites reach full 16/16 coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-CONTEXT.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-RESEARCH.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-PATTERNS.md
@src/components/input/input.ts
@src/internal/helpers/capabilities.ts
@src/internal/helpers/attach-internals-safe.ts
@src/internal/helpers/form-participation.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: slider.ts — combined rollout on a numeric-value control (tracer)</name>
  <files>src/components/slider/slider.ts, test/capabilities-off-constructor.test.ts, test/form-fallback-integration.test.ts</files>
  <read_first>
    - src/components/slider/slider.ts:64 (attachInternals site) — grep `internals\|_internals\|setFormValue\|setValidity` in this file for the naming convention, exact call sites, and how the numeric value is currently serialized (per test/browser/form-association.test.ts:264-273, am-slider "submits its numeric value as a string through native setFormValue" — the fallback mirror must serialize the SAME way, i.e. `String(this.value)`).
    - src/components/input/input.ts (Plan 01) and any of Plan 04's completed components as the reference for the exact combined-transform shape (widen field, `attachInternalsSafe(this)`, null-safe call sites, below-floor fallback/warn branch in `updated()`, `disconnectedCallback` teardown).
    - src/internal/helpers/form-participation.ts (`syncFormFallback`'s `value: string` parameter — confirm the numeric value is coerced to a string before passing).
    - test/capabilities-off-constructor.test.ts, test/form-fallback-integration.test.ts (as extended through Plan 04 — the arrays to append to).
  </read_first>
  <action>
    Apply the combined transform to slider.ts: widen the internals field to `ElementInternals | null`, replace the raw `this.attachInternals()` call with `attachInternalsSafe(this)` (import from `'../../internal/helpers/attach-internals-safe.js'`), null-safe every `.setFormValue(...)`/`.setValidity(...)` call site (preserve every reset branch), and add the below-floor fallback/warn branch in `updated()`: when `!this.internals` (or `!this._internals`), call `isFormFallbackEnabled() ? syncFormFallback(this, { name: this.name, value: String(this.value), disabled: this.disabled }) : warnBelowFloorOnce('am-slider')` (import from `'../../internal/helpers/form-participation.js'`) — slider has no `required`/`pattern` constraint properties, so omit those keys. Add a `disconnectedCallback()` override calling `teardownFormFallback(this)`.

    Extend test/capabilities-off-constructor.test.ts's tag array with `'am-slider'`. Extend test/form-fallback-integration.test.ts with a case for am-slider covering fallback-on numeric-value parity (assert `FormData.get(name)` equals the stringified numeric value), fallback-off warn, and above-floor XOR.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/capabilities-off-constructor.test.ts test/form-fallback-integration.test.ts src/components/slider</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - slider.ts: internals field is `ElementInternals | null`, constructor calls `attachInternalsSafe(this)`, all setter call sites `?.`-guarded, below-floor fallback/warn branch + teardown present.
    - Extended tests pass: am-slider constructs without throw below the floor; fallback mirrors its numeric value as a string with FormData parity; XOR holds above the floor.
    - Existing above-floor am-slider specs (including test/browser/form-association.test.ts's numeric-value-as-string assertion) stay conceptually unchanged (not modified by this plan; verified green in Task 2's full-suite pass).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Component-local wiring behind existing null-checks.</reversibility>
  <done>slider.ts is fully degradation-capable, proving the combined pattern generalizes to a numeric-value control.</done>
</task>

<task type="auto">
  <name>Task 2: switch, textarea, time-picker (3 files) — complete the 16/16 rollout</name>
  <files>src/components/switch/switch.ts, src/components/textarea/textarea.ts, src/components/time-picker/time-picker.ts, test/capabilities-off-constructor.test.ts, test/form-fallback-integration.test.ts</files>
  <read_first>
    - src/components/switch/switch.ts:70 (attachInternals site — grep for naming convention/call sites; a switch mirrors boolean `checked` state like checkbox — omit the fallback input from FormData when unchecked, matching Plan 04's checkbox teardown-on-uncheck approach).
    - src/components/textarea/textarea.ts:79 (attachInternals site — grep for call sites; textarea has `required`/`minlength`/`maxlength` constraints similar to input.ts — mirror `required` onto the fallback input; `minlength`/`maxlength` have no direct HTML hidden-input equivalent to project natively, so mirror only `required`, matching D-03's "required/pattern, etc." scope while noting minlength/maxlength constraint blocking degrades below the floor — same documented limit as validation.ts's custom-message UI per RESEARCH.md Q4).
    - src/components/time-picker/time-picker.ts:106 (attachInternals site — grep for call sites; mirrors its serialized time-string value).
    - test/capabilities-off-constructor.test.ts, test/form-fallback-integration.test.ts (arrays extended through Task 1 — append here to reach full 16/16 and full value-bearing-control coverage respectively).
  </read_first>
  <action>
    Apply the identical combined transform to switch.ts (boolean checked-state, teardown-on-off like checkbox), textarea.ts (mirror `value`/`required`), and time-picker.ts (mirror its string-serialized `value`). Each: widen internals field, `attachInternalsSafe(this)`, null-safe call sites (preserve reset branches), below-floor fallback/warn branch in `updated()`, `disconnectedCallback` teardown.

    Extend test/capabilities-off-constructor.test.ts's tag array with `'am-switch'`, `'am-textarea'`, `'am-time-picker'` — this completes ALL 16 form-associated custom elements (started in Plan 01, continued in Plan 04) in the constructor-no-throw suite. Extend test/form-fallback-integration.test.ts with cases for all 3 (fallback-on parity, fallback-off warn, above-floor XOR).

    Finally, run the full jsdom suite and confirm zero regressions across the entire 16-component rollout (Plans 01/04/05 combined).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/capabilities-off-constructor.test.ts test/form-fallback-integration.test.ts src/components/switch src/components/textarea src/components/time-picker</automated>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - switch.ts, textarea.ts, time-picker.ts: internals field widened, guarded attach, null-safe call sites, below-floor fallback/warn branch + teardown present.
    - test/capabilities-off-constructor.test.ts's FORM_TAGS array now has all 16 entries (am-input, am-button, am-checkbox, am-combobox, am-color-picker, am-date-picker, am-input-otp, am-number-field, am-radio, am-radio-group, am-rich-select, am-select, am-slider, am-switch, am-textarea, am-time-picker) and every one passes construct-without-throw.
    - test/form-fallback-integration.test.ts covers every value-bearing control across the whole library.
    - Full jsdom suite green — zero regressions across all 16 components.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Component-local wiring behind existing null-checks.</reversibility>
  <done>All 16 form-associated custom elements are fully degradation-capable: guarded ElementInternals attach (COMPAT-02) and, where value-bearing, an XOR-gated FormData-parity-proven hidden-input fallback with one-time dev warning (COMPAT-03). The mechanical rollout (Plans 01/04/05) is complete.</done>
</task>

</tasks>

<flagged_assumptions>
Specless edge-probe fallback — unresolved edges dispositioned (no silent drops):
- [COMPAT-03 concurrency] AUTHORED as must_haves.truth + Task 1/2 XOR test cases, completing the edge across the final 4 components.
- [COMPAT-02 unclassified] AUTHORED as must_haves.truth: all 4 components construct/connect/render without throwing below the floor, completing full 16/16 coverage.
</flagged_assumptions>

<artifacts_this_phase_produces>
NEW symbols/files this plan introduces (excluded from source-drift verification):
- Below-floor fallback/warn branches + `disconnectedCallback` teardown overrides in slider.ts, switch.ts, textarea.ts, time-picker.ts.
- `attachInternalsSafe(this)` call sites replacing raw `attachInternals()` in all 4 files.
- FORM_TAGS/fallback-tag array extensions in test/capabilities-off-constructor.test.ts and test/form-fallback-integration.test.ts (shared files, first created in Plan 01/04) reaching full 16/16 and full-value-bearing-control coverage respectively.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Light-DOM hidden `<input>` mirrors ↔ 3 components' enclosing `<form>` | slider, textarea, time-picker below the floor mirror their values via the same one-way sync discipline established in Plan 02/04. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Repudiation / double-submit | slider/switch/textarea/time-picker FormData | high | mitigate | Same structural XOR guard as Plan 04 (T-10-08) — gated on the shared `!this.internals` check, re-verified per component in Task 1/2. |
| T-10-11 | Tampering | switch's unchecked-state omission | low | accept | Mirrors Plan 04's checkbox teardown-on-uncheck approach (T-10-09) — an off switch is absent from FormData below the floor, matching native semantics. |

Highest severity present: T-10-10 (high) — mitigated by the shared internals-presence guard; no threat left unmitigated at high severity.
</threat_model>

<verification>
- `npx vitest run --project jsdom` (full suite) green — zero regressions across the completed 16-component rollout.
- `npx tsc --noEmit` clean.
- test/capabilities-off-constructor.test.ts's FORM_TAGS array has all 16 entries, all passing.
</verification>

<success_criteria>
The final 4 form-associated components (slider, switch, textarea, time-picker) are fully degradation-capable. Combined with Plan 04, all 16 form-associated custom elements across 15 files now guard their ElementInternals attach and, where value-bearing, provide an XOR-gated FormData-parity-proven hidden-input fallback with one-time dev warning — COMPAT-02 and COMPAT-03 are fully rolled out.
</success_criteria>

<output>
Create `.planning/phases/10-graceful-degradation-compatibility-matrix/10-05-SUMMARY.md` when done.
</output>
