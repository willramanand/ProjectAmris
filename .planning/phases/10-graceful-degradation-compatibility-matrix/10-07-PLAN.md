---
phase: 10-graceful-degradation-compatibility-matrix
plan: 07
type: execute
wave: 4
depends_on: ["10-06"]
files_modified:
  - BROWSER_SUPPORT.md
  - test/browser-support-doc.test.ts
autonomous: true
requirements: [COMPAT-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "BROWSER_SUPPORT.md states behavior exactly AT the Safari 16.4 floor and one version below (15.4/16.3) for the ElementInternals-dependent capabilities — the boundary edge is explicit, not implied (COMPAT-05 boundary edge)."
    - "BROWSER_SUPPORT.md's floor = max(JS-API floor, CSS-feature floor), matching the composite already documented, and now cross-checked against Plan 06's real WebKit/Firefox empirical results rather than restated from the RESEARCH.md hypothesis table unchanged."
    - "BROWSER_SUPPORT.md documents the NEW degradation matrix: what still works below the floor now that COMPAT-01/02/03/06 exist (capability-probed no-throw upgrade, opt-in hidden-input form fallback via /compat-forms, guarded CSS empty-slot collapse) versus what remains genuinely unavailable (custom validation-message UI, ARIA reflection where absent)."
  artifacts:
    - test/browser-support-doc.test.ts
  key_links:
    - "BROWSER_SUPPORT.md's new 'Graceful Degradation' section references the @willramanand/amris/compat-forms opt-in by exact import path"
  prohibitions: []
---

<objective>
COMPAT-05: document the true per-capability browser floor and the new
degradation matrix in `BROWSER_SUPPORT.md`, cross-checked against Plan 06's
real cross-engine test results rather than restated unverified from
RESEARCH.md's starting hypothesis table. This is the final plan of Phase 10 —
it depends on every other plan's code being complete, since it describes what
the finished degradation story actually does.

Per CONTEXT.md, "the true empirical per-capability floor... resolved by the
research agent on the widened matrix" — Plan 06 produced that empirical
evidence (test/browser/capabilities.test.ts's real Chromium/WebKit/Firefox
results, including any documented Firefox ARIA-reflection gap); this plan
writes it up.

Purpose: give consumers an accurate, currently-true picture of what degrades
and what doesn't below the documented floor — including the NEW opt-in
fallback and guarded CSS this milestone shipped, replacing the pre-Phase-10
"forms just don't work below the floor" framing with the accurate, narrower
"forms don't work below the floor UNLESS you opt into /compat-forms" framing.
Output: updated BROWSER_SUPPORT.md with a Graceful Degradation section, a
revised "what does NOT work below the floor" section, and a doc-structure
assertion test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-CONTEXT.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-RESEARCH.md
@BROWSER_SUPPORT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite the floor table + add the Graceful Degradation section (tracer)</name>
  <files>BROWSER_SUPPORT.md, test/browser-support-doc.test.ts</files>
  <precondition>Plan 06's test/browser/capabilities.test.ts has been run against all 3 engines (chromium/webkit/firefox) and its results — including any Firefox ARIA-reflection exception — are available from that plan's SUMMARY.</precondition>
  <read_first>
    - BROWSER_SUPPORT.md (whole file, already read this session — the "Supported floor" table at lines 6-13, "Why this floor" table at 16-32, "What does NOT work below the floor" at 41-46, "Future work" at 56-64).
    - .planning/phases/10-graceful-degradation-compatibility-matrix/10-06-SUMMARY.md (Plan 06's recorded empirical cross-engine results — read this to get the REAL observed values, not the RESEARCH.md hypothesis table, for the floor table update).
    - 10-RESEARCH.md Q6 "authoritative version table" (the starting hypothesis this task cross-checks against Plan 06's real results) and the System Architecture Diagram (for describing the degradation flow accurately).
  </read_first>
  <action>
    In BROWSER_SUPPORT.md, update the "Why this floor — load-bearing features" table's `ElementInternals + formAssociated` row and add an explicit "ARIA reflection" row distinct from form-association (since COMPAT-01 now probes them independently) — populate both rows' Floor column from Plan 06's actual observed cross-engine results (not the unverified RESEARCH.md hypothesis), citing the source as "empirically confirmed on the widened WebKit/Firefox/Chromium matrix, Phase 10" rather than a web-sourced claim alone.

    Add a new `## Graceful Degradation (v1.1)` section (after "Why this floor", before "Progressive enhancement") documenting, in plain prose: (1) capabilities.ts probes FACE/ARIA-reflection/adoptedStyleSheets/:has() independently, so a partial-support engine degrades per-capability rather than all-or-nothing; (2) below the ElementInternals floor, every form-associated component now upgrades, renders, and emits events instead of throwing (COMPAT-02); (3) form submission below the floor can be restored via the opt-in `import '@willramanand/amris/compat-forms'` at app init — state plainly that it is OFF by default, XOR with ElementInternals (never both), and mirrors value plus native `required`/`pattern` constraint validation (D-03) but NOT the library's custom validation-message UI, which remains ElementInternals-dependent and is a documented limit, not a bug; (4) below the floor with the fallback OFF, a one-time console warning names the missing capability and points to the opt-in; (5) `:has()`-guarded CSS (COMPAT-06) collapses to a plainer reserved-space layout via `@supports selector(:has(*))` rather than silently rendering wrong.

    Create test/browser-support-doc.test.ts (a plain Node-side vitest test, no browser/jsdom needed) that reads `BROWSER_SUPPORT.md` via `fs.readFileSync` and asserts required strings/sections are present: the heading `## Graceful Degradation (v1.1)`, the literal import path `@willramanand/amris/compat-forms`, the words "XOR", "capabilities.ts", and an ARIA reflection row distinct from the ElementInternals form-association row.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/browser-support-doc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - BROWSER_SUPPORT.md's floor table has a distinct ARIA-reflection row from the ElementInternals form-association row, both populated with Plan 06's real observed cross-engine values.
    - A new `## Graceful Degradation (v1.1)` section exists covering all 5 points above, including the exact `@willramanand/amris/compat-forms` import path and the XOR/no-double-submit guarantee.
    - test/browser-support-doc.test.ts passes, asserting the required sections/strings exist.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation-only change.</reversibility>
  <done>BROWSER_SUPPORT.md's floor table reflects Plan 06's real empirical cross-engine results, and a new Graceful Degradation section accurately documents COMPAT-01/02/03/06's behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Revise "what does NOT work below the floor" + Future work — remove now-implemented items</name>
  <files>BROWSER_SUPPORT.md, test/browser-support-doc.test.ts</files>
  <read_first>
    - BROWSER_SUPPORT.md (as modified by Task 1 — the "What does **not** work below the floor" section at the original lines 41-46, and "Future work" at 56-64).
    - 10-CONTEXT.md's boundary section (what this phase explicitly did NOT change — e.g. no hard polyfill, no full below-floor cross-browser CI — so the revised doc doesn't overclaim beyond what shipped).
  </read_first>
  <action>
    Revise the "What does **not** work below the floor" section: change the blanket "Form controls submit no value to their parent `<form>`" line to the accurate, narrower claim — form controls submit no value below the floor UNLESS the consumer opts into `@willramanand/amris/compat-forms`, in which case value + native required/pattern validation is restored (custom validation-message UI remains unavailable). Change the "Empty card/dialog/drawer/panel/side-nav/app-shell slots reserve space instead of collapsing" line to reflect the NEW guarded behavior: below the `:has()` floor, empty slots now reserve space via the documented functional-default fallback (unchanged end-state, but now an intentional, guarded degradation rather than an unguarded selector silently failing — reword to make clear this is now BY DESIGN, not an accident).

    In "Future work", remove the "A custom-hidden-input strategy for form-associated components (drops ElementInternals requirement)" bullet — COMPAT-03 implemented exactly this, so it is no longer future work; keep the "JS-driven empty-slot detection" and "Pre-mixed color tokens" bullets (still genuinely unimplemented) and add a note that lowering the floor further would still require a hard polyfill for ElementInternals, which the project has decided not to pursue (permanently out of scope per REQUIREMENTS.md's Out of Scope table).

    Extend test/browser-support-doc.test.ts to assert the revised sections no longer contain the exact stale phrase "Form controls submit no value to their parent" as an unqualified blanket claim (i.e., assert the section DOES mention `compat-forms` in the same section as the forms discussion, proving the qualifier was added — do not assert a negative on the literal old sentence text alone, since that is fragile; assert the NEW qualifying content is present instead) and that the removed Future-work bullet about a "custom-hidden-input strategy" is gone.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/browser-support-doc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "What does NOT work below the floor" accurately reflects the opt-in fallback's existence and its value+native-validation (not custom-message-UI) scope.
    - "Future work" no longer lists the custom-hidden-input strategy as unimplemented (COMPAT-03 shipped it); the permanent-polyfill-exclusion rationale is stated.
    - test/browser-support-doc.test.ts passes with the extended assertions.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation-only change.</reversibility>
  <done>BROWSER_SUPPORT.md fully and accurately documents Phase 10's shipped graceful-degradation behavior — no stale claims about forms or CSS failing unguarded remain. COMPAT-05 is complete; Phase 10 is done.</done>
</task>

</tasks>

<flagged_assumptions>
Specless edge-probe fallback — unresolved edges dispositioned (no silent drops):
- [COMPAT-05 boundary] AUTHORED as must_haves.truth + Task 1: the floor table explicitly states behavior at Safari 16.4 and one version below.
- [COMPAT-05 precision] N/A: COMPAT-05 documents categorical browser-version floors, not numeric measurements — no rounding/precision contract applies to a version-number table. Flagged, not silently dropped.
- [COMPAT-05 concurrency] N/A: BROWSER_SUPPORT.md is a static, manually-authored doc edited by this plan's tasks sequentially — no concurrent-writer path exists (no CI job auto-regenerates this file, unlike docs/contract.md's CEM-derived generation). Flagged, not silently dropped.
</flagged_assumptions>

<artifacts_this_phase_produces>
NEW symbols/files this plan introduces (excluded from source-drift verification):
- test/browser-support-doc.test.ts — new doc-structure assertion spec.
- BROWSER_SUPPORT.md `## Graceful Degradation (v1.1)` section — new documentation section.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (none new) | Documentation-only change; no code path affected. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Information Disclosure | BROWSER_SUPPORT.md accuracy | low | accept | Documentation that overclaims support could mislead a consumer into shipping to an unsupported engine; mitigated by grounding every claim in Plan 06's actual empirical cross-engine test results rather than restating an unverified hypothesis table. |

No high-severity threats — nothing to block on.
</threat_model>

<verification>
- `npx vitest run --project jsdom test/browser-support-doc.test.ts` green.
- Manual read-through confirms no stale pre-Phase-10 claims remain (forms/CSS "just don't work" framing removed).
</verification>

<success_criteria>
BROWSER_SUPPORT.md documents the true per-capability floor (empirically confirmed on the widened matrix) and the complete degradation matrix — capability probing, guarded ElementInternals attach, the opt-in hidden-input fallback, the below-floor DX warning, and the guarded CSS fallback — closing out COMPAT-05 and Phase 10.
</success_criteria>

<output>
Create `.planning/phases/10-graceful-degradation-compatibility-matrix/10-07-SUMMARY.md` when done.
</output>
