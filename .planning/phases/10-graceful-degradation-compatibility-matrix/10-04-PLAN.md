---
phase: 10-graceful-degradation-compatibility-matrix
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/components/input/input.ts
  - src/components/button/button.ts
  - src/components/checkbox/checkbox.ts
  - src/components/combobox/combobox.ts
  - src/components/color-picker/color-picker.ts
  - src/components/date-picker/date-picker.ts
  - test/capabilities-off-constructor.batch-a1.test.ts
  - test/form-fallback-integration.batch-a1.test.ts
  - test/browser/form-fallback.test.ts
autonomous: true
requirements: [COMPAT-02, COMPAT-03]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With ElementInternals PRESENT (the default/above-floor state), the fallback hidden-input NEVER attaches on any of these 6 components — internals?.setFormValue continues to be the only channel; the XOR gate is proven per-component, not assumed (COMPAT-03 concurrency edge: no double-submit)."
    - "With hasFormAssociation() forced false AND the fallback enabled, each of the 5 value-bearing components (am-input, checkbox, combobox, color-picker, date-picker) submits FormData matching what it submitted above the floor for an equivalent value (FormData parity), and required/pattern constraints (where the component has them) block invalid native submit."
    - "With hasFormAssociation() forced false AND the fallback disabled (the default), each of these 6 components fires warnBelowFloorOnce exactly once (globally deduped across components) instead of throwing or silently submitting nothing new."
    - "Below the floor, all 6 components construct, connect, and render without throwing (COMPAT-02, extending Plan 01's am-input proof to this batch)."
  artifacts:
    - test/browser/form-fallback.test.ts
    - test/capabilities-off-constructor.batch-a1.test.ts
    - test/form-fallback-integration.batch-a1.test.ts
  key_links:
    - "Each component's updated()/willUpdate() calls, when this.internals is null: isFormFallbackEnabled() ? syncFormFallback(this, {...}) : warnBelowFloorOnce(tagName)"
    - "Each value-bearing component's disconnectedCallback calls teardownFormFallback(this)"
  prohibitions:
    - "Above-floor behavior (event cadence, DOM structure, a11y tree, validation timing) must not change on any of these 6 components — the freeze holds except for the below-floor branch, which is entirely new code that only executes when this.internals is null."
    - "No component wires BOTH the ElementInternals setFormValue path and the hidden-input fallback in the same render — exactly one channel executes, gated on this.internals presence."
---

<objective>
Combined COMPAT-02 + COMPAT-03 rollout, batch A1 (6 of 15 remaining files):
complete am-input's degradation story (it already has `attachInternalsSafe`
from Plan 01 — this plan adds its below-floor fallback/warn branch), then apply
the FULL combined pattern (guarded attach + below-floor fallback/warn wiring,
in one edit per file) to button, checkbox, combobox, color-picker, and
date-picker.

This is the first of the two batch-A rollout plans (batch A2 — input-otp,
number-field, radio, rich-select, select — is Plan 05). The original single
batch-A plan was split into 04/05 to keep each plan's context budget in the
smart zone (the user's cross-cutting signal in 10-CONTEXT.md accepts higher
proof cost for maximal reach; two smaller plans raise per-plan confidence over
one 11-component pass). Batch A1 and batch A2 own DISTINCT test files (see
below), so they run genuinely in parallel in wave 2 with zero file overlap.

Doing both concerns together, per file, in one pass — rather than a separate
COMPAT-02-only pass followed by a separate COMPAT-03-only pass — avoids
touching each of these net-new files twice. `input.ts` is the one exception:
it already got `attachInternalsSafe` in Plan 01's tracer, so here it only gains
the fallback/warn branch (a small additive edit).

Purpose: prove the FULL degradation story (no-throw + FormData-parity fallback
+ XOR no-double-submit + one-time warn) on am-input as this plan's tracer, then
mechanically expand the same combined pattern to the next 5 components in one
expansion task.
Output: 6 components fully degradation-capable (or, for button, guard+warn-only
per its no-value nature); this batch's own jsdom capability-off +
fallback-integration test files; a new browser-lane FormData-parity spec for
the tracer component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-CONTEXT.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-RESEARCH.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-PATTERNS.md
@src/components/input/input.ts
@src/internal/helpers/capabilities.ts
@src/internal/helpers/attach-internals-safe.ts
@src/internal/helpers/form-participation.ts
@test/capabilities-off-constructor.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: am-input's below-floor fallback branch — full degradation story (tracer)</name>
  <files>src/components/input/input.ts, test/form-fallback-integration.batch-a1.test.ts, test/browser/form-fallback.test.ts</files>
  <read_first>
    - src/components/input/input.ts (as modified by Plan 01 — the `internals: ElementInternals | null` field, `attachInternalsSafe(this)` constructor call, `updated()` at ~283-292, `_syncValidation()` at ~300-321, `disconnectedCallback` — NOT present today, must be added).
    - src/internal/helpers/form-participation.ts (as created in Plan 02 — `isFormFallbackEnabled`, `syncFormFallback`, `teardownFormFallback`, `warnBelowFloorOnce` signatures).
    - src/internal/helpers/capabilities.ts, attach-internals-safe.ts (Plan 01 — `hasFormAssociation`, `attachInternalsSafe`).
    - test/browser/form-association.test.ts:1-40 (fixture/settle scaffold to mirror for the new browser spec) and :280-286 (the shadow-input-non-association finding that motivates the light-DOM requirement).
    - test/capabilities-off-constructor.test.ts (Plan 01 — the constructor-no-throw scaffold shape this batch's own batch-a1 file mirrors; am-input itself is already covered there, so this batch's constructor file covers only the 5 net-new tags).
    - 10-RESEARCH.md architecture diagram (System Architecture Diagram) and Q4 mechanics.
  </read_first>
  <action>
    In src/components/input/input.ts, import `isFormFallbackEnabled`, `syncFormFallback`, `teardownFormFallback`, `warnBelowFloorOnce` from `'../../internal/helpers/form-participation.js'`. In `updated()` (after the existing `if (changed.has('value')) { this.internals?.setFormValue(this.value); }` block, still inside `updated()`), add: when `!this.internals`, call `isFormFallbackEnabled() ? syncFormFallback(this, { name: this.name, value: this.value, required: this.required, pattern: this.pattern || undefined, disabled: this.disabled }) : warnBelowFloorOnce('am-input')`. This must run on every `updated()` pass (not gated to `changed.has('value')` alone), so the mirror stays in sync with `name`/`required`/`pattern`/`disabled` changes too — but the branch itself is only entered when `this.internals` is null, so above-floor `updated()` behavior (timing, call order) is completely unaffected. Add a `disconnectedCallback()` override (call `super.disconnectedCallback()` first) that calls `teardownFormFallback(this)` unconditionally (a no-op if no fallback was ever attached).

    Create test/form-fallback-integration.batch-a1.test.ts (jsdom) — the below-floor FULL story, proven via native jsdom FormData (not ElementInternals-dependent): force `hasFormAssociation()` false via the capabilities.ts shim + `__resetCapabilitiesForTest()`, `__resetFormParticipationForTest()`, mount `am-input` inside a real `<form>`. Case A (fallback ON): call `enableFormFallback()`, set `name`/`value`/`required`, trigger an update, assert `new FormData(form).get(name)` matches the component's value (parity), assert the DOM contains exactly one hidden `<input>` mirror (idempotency — trigger a second update, re-assert still exactly one). Case B (fallback OFF, the default): with fallback disabled, spy on `console.warn`, trigger an update, assert `warnBelowFloorOnce` fired (spy called) and NO hidden input exists in the DOM. Case C (XOR/no-double-submit): restore `hasFormAssociation()` to true (real capability), reset capabilities, mount fresh, enable the fallback, assert NO hidden input is ever attached and `FormData` contains exactly ONE entry for `name` (the ElementInternals-mirrored one only). Structure around a top-level `const FALLBACK_TAGS = ['am-input'];`-style extensible list (a loop generating the 3 cases per tag) so Task 2 appends this batch's remaining value-bearing tags to the SAME batch-a1 file — never to a file another plan also writes.

    Create test/browser/form-fallback.test.ts (Chromium, existing `browser` project — widened to WebKit/Firefox by Plan 07). Since real Chromium is always above the floor, this spec cannot force capability-off natively; instead it directly exercises `syncFormFallback`/`teardownFormFallback` against a real `am-input` host mounted in a real `<form>` (bypassing the internal `hasFormAssociation()` gate by calling the fallback functions directly, as Plan 02's unit test does but now against a REAL component instance instead of a synthetic host) and asserts real-browser `FormData` parity + native `required` constraint blocking (`form.reportValidity()` returns false when the mirrored input's `required` is set and empty). This is the real-browser fidelity proof for D-03 (value + native validation).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/form-fallback-integration.batch-a1.test.ts test/capabilities-off-constructor.test.ts src/components/input</automated>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser -- test/browser/form-fallback.test.ts test/browser/form-association.test.ts</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - am-input's `updated()` calls `syncFormFallback`/`warnBelowFloorOnce` only when `this.internals` is null; `disconnectedCallback` calls `teardownFormFallback`.
    - test/form-fallback-integration.batch-a1.test.ts passes all 3 cases for am-input (fallback-on parity+idempotency, fallback-off warn, XOR no-double-submit above floor).
    - test/browser/form-fallback.test.ts passes on Chromium: FormData parity + native required-blocking for the light-DOM mirror.
    - Existing test/browser/form-association.test.ts (am-input's above-floor entries) stays green unchanged.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Component-local wiring behind the existing `this.internals` null-check; revert by removing the below-floor branch and disconnectedCallback override.</reversibility>
  <done>am-input's full degradation story (no-throw, XOR-gated fallback, FormData parity, one-time warn, teardown) is proven end-to-end in both jsdom and the real browser lane.</done>
</task>

<task type="auto">
  <name>Task 2: Combined rollout — button, checkbox, combobox, color-picker, date-picker (5 files)</name>
  <files>src/components/button/button.ts, src/components/checkbox/checkbox.ts, src/components/combobox/combobox.ts, src/components/color-picker/color-picker.ts, src/components/date-picker/date-picker.ts, test/capabilities-off-constructor.batch-a1.test.ts, test/form-fallback-integration.batch-a1.test.ts</files>
  <read_first>
    - src/components/button/button.ts:44,46-49 (`private _internals: ElementInternals;` field + constructor — the `_internals` naming convention, distinct from input.ts's `internals`), 52-54 (`get form()` — reads `this._internals.form`), 260-278 (`handleClick` — `internals: this._internals` passed to `requestAssociatedFormSubmit`/`resetAssociatedForm`, already null-safe per src/utilities/form-actions.ts). NOTE: am-button has NO `setFormValue`/`setValidity` calls and is not itself a value-bearing form field (its `value`/`name` participate only via `type="submit"`/`type="reset"` semantics) — it needs `attachInternalsSafe` + the `.form` getter null-safe, but the below-floor fallback branch is N/A for it (a submit/reset button has no value to mirror into a hidden input) — apply ONLY the attachInternalsSafe guarded-attach half of the pattern to button.ts, with the `.form` getter becoming `this._internals?.form ?? null`; still call `warnBelowFloorOnce('am-button')` when `!this._internals` so the DX signal (D-04) still fires, since a submit button below the floor genuinely can't submit via ElementInternals either.
    - src/components/checkbox/checkbox.ts:37-38 (`name`/`value` properties, `value = 'on'` default), 42 (`private internals: ElementInternals;`), 52-56 (ValidationController wiring), 67-73 (constructor), and its `updated()`/validity-sync method (grep `setFormValue`/`setValidity` in this file for exact line numbers) — checkbox mirrors `checked` state via `setFormValue(this.checked ? this.value : null)`-style logic; the fallback mirror should send `value` only when `checked` is true (mirror native `<input type=checkbox>` semantics — omit the input from FormData when unchecked, which `syncFormFallback`'s teardown-on-uncheck can achieve).
    - src/components/combobox/combobox.ts:226 (attachInternals site — grep `setFormValue`/`setValidity`/`internals` in this file for exact call sites), src/components/color-picker/color-picker.ts:92, src/components/date-picker/date-picker.ts:111 — each follows the `_internals` naming convention (confirm via grep `attachInternals` in each file) with its own `setFormValue`/`setValidity` call sites to null-safe.
    - test/capabilities-off-constructor.batch-a1.test.ts and test/form-fallback-integration.batch-a1.test.ts (Task 1 created the fallback-integration file; the constructor file is created here — both owned solely by this plan, appended only within this plan).
    - 10-RESEARCH.md Pattern 2 (the two naming conventions: `this.internals` vs `this._internals`) and the "16 attach sites" table.
  </read_first>
  <action>
    For each of button.ts, checkbox.ts, combobox.ts, color-picker.ts, date-picker.ts: widen the internals field (`ElementInternals` → `ElementInternals | null`, preserving whichever of `internals`/`_internals` naming the file already uses), replace the raw `this.attachInternals()` call with `attachInternalsSafe(this)` (import from `'../../internal/helpers/attach-internals-safe.js'`), and null-safe every `.setFormValue(...)`, `.setValidity(...)`, and `.form` call site in the file with `?.` — do not drop any `setValidity({})` reset branch. For the 4 value-bearing controls (checkbox, combobox, color-picker, date-picker — NOT button, per the read_first note), add the below-floor fallback/warn branch in each component's existing `updated()`/validity-sync method (import `isFormFallbackEnabled`, `syncFormFallback`, `teardownFormFallback`, `warnBelowFloorOnce` from `'../../internal/helpers/form-participation.js'`): when `!this.internals` (or `!this._internals`), call `syncFormFallback`/`warnBelowFloorOnce` exactly as in Task 1's am-input wiring, mirroring each component's own `name`/current-value/`required`/`disabled` (checkbox mirrors `checked ? this.value : ''` and treats an empty/unchecked mirror as a teardown case so unchecked boxes are correctly absent from FormData; combobox/color-picker/date-picker mirror their respective string-serializable `value`). Add `disconnectedCallback()` overrides calling `teardownFormFallback(this)` for the 4 value-bearing controls (button.ts does not need this, since it never calls `syncFormFallback`).

    Create test/capabilities-off-constructor.batch-a1.test.ts (jsdom) mirroring Plan 01's constructor-no-throw scaffold: a top-level `const FORM_TAGS = ['am-button', 'am-checkbox', 'am-combobox', 'am-color-picker', 'am-date-picker'];` array with a loop generating one describe/it per tag, forcing `hasFormAssociation()` false via the capabilities.ts shim and asserting each tag constructs/connects/renders without throwing below the floor (am-input is intentionally excluded — it is already covered by Plan 01's test/capabilities-off-constructor.test.ts; provide minimal valid markup/children only if a given component throws on empty content during your verification pass; otherwise plain `document.createElement(tag)` suffices). Extend test/form-fallback-integration.batch-a1.test.ts's `FALLBACK_TAGS` array with the 4 value-bearing controls (checkbox, combobox, color-picker, date-picker), covering fallback-on parity + fallback-off warn + above-floor XOR for each — button.ts is excluded from this file (no value channel to fall back).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/capabilities-off-constructor.batch-a1.test.ts test/form-fallback-integration.batch-a1.test.ts src/components/button src/components/checkbox src/components/combobox src/components/color-picker src/components/date-picker</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files: internals field is `ElementInternals | null`, constructor calls `attachInternalsSafe(this)`, no raw `this.attachInternals()` remains, every setter/getter call site is `?.`-guarded.
    - checkbox, combobox, color-picker, date-picker each gain the below-floor fallback/warn branch + `disconnectedCallback` teardown; button.ts gains only the guarded-attach half plus a `warnBelowFloorOnce('am-button')` call.
    - test/capabilities-off-constructor.batch-a1.test.ts covers all 5 batch-A1 tags (construct-without-throw below the floor); test/form-fallback-integration.batch-a1.test.ts covers am-input plus the 4 value-bearing controls.
    - Existing above-floor jsdom specs for these 5 components stay green unchanged.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Component-local wiring behind existing null-checks; revert per-file independently.</reversibility>
  <done>button, checkbox, combobox, color-picker, and date-picker are fully degradation-capable (or, for button, guard+warn-only per its no-value nature); this batch's own jsdom coverage spans all 6 batch-A1 components.</done>
</task>

</tasks>

<flagged_assumptions>
Specless edge-probe fallback — unresolved edges dispositioned (no silent drops):
- [COMPAT-03 concurrency] AUTHORED as must_haves.truth + Task 1/2 XOR test cases: with ElementInternals present, the fallback never attaches on any of these 6 components (completing the edge Plan 02 deliberately deferred, since form-participation.ts itself is capabilities.ts-agnostic by design).
- [COMPAT-02 unclassified] AUTHORED as must_haves.truth: all 6 components construct/connect/render without throwing below the floor.
</flagged_assumptions>

<artifacts_this_phase_produces>
NEW symbols/files this plan introduces (excluded from source-drift verification):
- Below-floor fallback/warn branches + `disconnectedCallback` teardown overrides in input.ts, checkbox.ts, combobox.ts, color-picker.ts, date-picker.ts.
- `attachInternalsSafe(this)` call sites replacing raw `attachInternals()` in button.ts, checkbox.ts, combobox.ts, color-picker.ts, date-picker.ts.
- test/browser/form-fallback.test.ts — new browser-lane spec (widened to WebKit+Firefox by Plan 07).
- test/capabilities-off-constructor.batch-a1.test.ts, test/form-fallback-integration.batch-a1.test.ts — new jsdom specs owned solely by this plan (no shared-array append with any other plan).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Light-DOM hidden `<input>` mirrors ↔ 5 value-bearing components' enclosing `<form>` | Each value-bearing component below the floor now appends its own mirror, following the same one-way (component → input) sync discipline established in Plan 02. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Repudiation / double-submit | All value-bearing batch-A1 components' FormData | high | mitigate | Each component's below-floor branch is gated on `!this.internals` (or `!this._internals`) — the exact same guard that already prevents `setFormValue` from being called above the floor — so the two channels are structurally exclusive per component, not just by convention. Task 1/2's XOR test cases re-verify this per component. |
| T-10-09 | Tampering | checkbox's unchecked-state omission | low | accept | An unchecked checkbox below the floor must be ABSENT from FormData (matching native `<input type=checkbox>` semantics) — implemented via teardown-on-uncheck rather than syncing an empty value, avoiding a divergent "checked=false but present with empty value" state that native forms never produce. |

Highest severity present: T-10-08 (high) — mitigated by the shared internals-presence guard; no threat left unmitigated at high severity.
</threat_model>

<verification>
- `npx vitest run --project jsdom test/capabilities-off-constructor.batch-a1.test.ts test/form-fallback-integration.batch-a1.test.ts src/components/button src/components/checkbox src/components/combobox src/components/color-picker src/components/date-picker src/components/input` green — no regression across this batch's touched components.
- `npm run test:browser -- test/browser/form-fallback.test.ts test/browser/form-association.test.ts` green.
- `npx tsc --noEmit` clean.
- This batch's constructor-no-throw file covers 5 tags (button, checkbox, combobox, color-picker, date-picker); combined with Plan 01 (am-input), Plan 05 (batch A2, 6 tags), and Plan 06 (batch B, 4 tags), all 16 form-associated tags are covered across the four partitioned files.
</verification>

<success_criteria>
6 of the 15 remaining form-associated components are fully degradation-capable: guarded ElementInternals attach (COMPAT-02) plus, where value-bearing, an XOR-gated, FormData-parity-proven hidden-input fallback with one-time dev warning (COMPAT-03). Plan 05 (batch A2) and Plan 06 (batch B) complete the remaining 9 components, each on its own distinct test files.
</success_criteria>

<output>
Create `.planning/phases/10-graceful-degradation-compatibility-matrix/10-04-SUMMARY.md` when done.
</output>
