---
phase: 08-bundle-size-deferral
plan: 07
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06"]
files_modified:
  - scripts/deep-import-purity.mjs
  - test/browser/registration-smoke.test.ts
  - api/size.baseline.json
  - api/perf.json
autonomous: true
requirements: [SIZE-03, SIZE-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SIZE-04: a new report-only deep-import-purity script asserts every NON-overlay component's dist static graph contains ZERO @floating-ui/dom, and that @floating-ui/dom appears ONLY behind a dynamic import() — SIZE-04 empty + ordering edges."
    - "SIZE-04 (D-06 outcome): floating-ui resolves to ONE shared async chunk across all overlay entries (two entries importing the same deferred chunk dedupe to one — no per-component duplication) — SIZE-04 adjacency edge, verified via the bundle-attribution report."
    - "SIZE-03 (D-10): the registration-smoke spec asserts a representative set of imported components still call customElements.define at runtime (registration never shaken away), and the no-bundled-Lit assertion reports zero inlined-Lit markers — SIZE-03 edges."
    - "The final post-deferral brotli size baseline and perf baseline are re-written; the size baseline records the full deferral win (report-only); custom-elements.json is UNCHANGED (diff:surface clean)."
  artifacts:
    - scripts/deep-import-purity.mjs
    - "re-written api/size.baseline.json and api/perf.json (post-full-deferral)"
  key_links:
    - "deep-import-purity.mjs reuses collectImportSpecifiers / collectDistJs / stripCommentNoise from scripts/assert-no-bundled-lit.mjs; report-only exit-0 discipline (enforcing flip is Phase 11)."
    - "attribution:check (VISUALIZE=1 build) + size:why confirm the single shared floating chunk (SIZE-04 dedupe)."
  prohibitions:
    - "MUST NOT add manualChunks tuning to resolve dedupe — verify only; manualChunks is PERF-V2-01, deferred."
    - "MUST NOT flip any script/gate from report-only to enforcing (Phase 11 owns the flip)."
    - "MUST NOT edit the vite.config.ts external array."
    - "MUST NOT let custom-elements.json drift (surface-freeze)."
---

<objective>
Close SIZE-03 and SIZE-04 with the tree-shaking/no-bundled-Lit canary and the shared-chunk dedupe + deep-import purity verification, then re-write the committed size and perf baselines against the fully-deferred graph. This is the phase's verification capstone — it can only run once every Wave-2 migration has removed its static imports, so the purity assertion sees a clean graph.

Purpose: prove the deferral introduced NO cross-entry duplication (SIZE-04) — the single-shared-chunk outcome D-06 promised — that registration survives (SIZE-03, D-10), and that the byte win is banked as a committed number — all report-only (the enforcing flip is Phase 11).
Output: `scripts/deep-import-purity.mjs`, a completed registration-smoke spec, and re-written post-deferral baselines with an unchanged CEM.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
@scripts/assert-no-bundled-lit.mjs
</context>

## Artifacts this phase produces (created by this plan)

- `scripts/deep-import-purity.mjs` (NEW report-only node script) — asserts floating-ui absent from non-overlay dist graphs and present only behind `import()`; reuses helpers from `assert-no-bundled-lit.mjs`.
- Completes `test/browser/registration-smoke.test.ts` (started in 08-01) with a broader representative component set.
- Re-written `api/size.baseline.json` (final post-deferral brotli baseline) and `api/perf.json` (post-deferral perf snapshot).

<precondition>All Wave-2 migrations (08-02…08-06) are merged so no component holds a static floating-ui/virtualizer runtime import — the purity assertion assumes a fully-deferred graph.</precondition>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deep-import-purity script — SIZE-04 non-overlay purity + dynamic-only floating-ui</name>
  <files>scripts/deep-import-purity.mjs</files>
  <read_first>
    - scripts/assert-no-bundled-lit.mjs (reuse `collectDistJs` lines 52-57, `stripCommentNoise` 65-69, `collectImportSpecifiers` 78-90; the report-only exit-0 discipline lines 161-172; usage-error exit 2)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Pattern 3 lines 194-200; Validation SIZE-01/04 lines 392-397; the deep-import-purity Wave 0 gap line 410)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (no-bundled-Lit + purity assertion harness lines 52-54)
    - .size-limit.json (which entries are overlay vs non-overlay — button/input are non-overlay; popover/select/etc are overlay)
  </read_first>
  <behavior>
    - Given a built `dist/`, for each NON-overlay component entry (e.g. button, input) the script finds ZERO static `from '@floating-ui/dom'` / bare side-effect import in that entry's static graph → reports clean.
    - Across the whole `dist/`, `@floating-ui/dom` appears only inside a dynamic `import('@floating-ui/dom')` specifier (behind the loader chunk), never a static `from`/side-effect specifier.
    - If a non-overlay entry statically imports floating-ui (a regression), the script PRINTS the offender and its entry but exits 0 this phase (report-only); a usage error exits 2.
  </behavior>
  <action>
    Create `scripts/deep-import-purity.mjs` importing the reusable helpers from `assert-no-bundled-lit.mjs` (`collectDistJs`, `stripCommentNoise`, `collectImportSpecifiers`). Walk `dist/**/*.js`; classify each specifier occurrence as STATIC (`from '...'` / bare side-effect `import '...'`) vs DYNAMIC (`import('...')`). Assert `@floating-ui/dom` (and its subpaths) never appears as a STATIC specifier anywhere, and specifically not in the static graph reachable from the non-overlay entries; it may appear only as a DYNAMIC specifier. Print a clear report (clean / offenders) and `process.exit(0)` this phase (usage error → `process.exit(2)`), mirroring `assert-no-bundled-lit.mjs`'s report-only discipline. Add an `npm` script alias (e.g. `purity:check`) if convenient. Do NOT add `manualChunks`. <!-- planner-discipline-allow: @floating-ui/dom -->
  </action>
  <verify>
    <automated>npm run build && node scripts/deep-import-purity.mjs dist</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/deep-import-purity.mjs dist` exits 0 and reports: zero STATIC `@floating-ui/dom` specifiers in non-overlay entries, and floating-ui present only behind a dynamic `import()`.
    - Running it against a deliberately-reverted static import (manual smoke, not committed) prints the offender — proving the assertion actually detects a leak — then is reverted.
    - The script reuses the `assert-no-bundled-lit.mjs` helpers (no re-implemented import parser) and stays report-only (exit 0; usage error exit 2).
  </acceptance_criteria>
  <done>The purity script exists, reuses the shared helpers, proves floating-ui is absent from non-overlay static graphs and present only behind import(), and is report-only.</done>
</task>

<task type="auto">
  <name>Task 2: SIZE-03 registration canary + SIZE-04 shared-chunk dedupe attribution</name>
  <files>test/browser/registration-smoke.test.ts</files>
  <read_first>
    - test/browser/registration-smoke.test.ts (the spec started in 08-01 — extend to a representative set)
    - scripts/assert-no-bundled-lit.mjs (the no-bundled-Lit report; run it as part of the canary)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (SIZE-03 test map lines 395-396; SIZE-04 attribution lines 397; Pitfall SE1 lines 267-270; Pitfall L1 lines 262-265)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (report-only registration-smoke lines 56-58)
  </read_first>
  <action>
    Broaden `registration-smoke.test.ts` to import a representative set spanning the migrated overlays and a non-overlay control (e.g. select, combobox, popover, tooltip, rich-select, color-picker, data-grid, button) and assert each `customElements.get('am-…')` is defined — proving no `sideEffects`/tree-shake change shook away `@customElement` registration across the refactor (Pitfall SE1). Then verify SIZE-04 shared-chunk dedupe via the existing attribution tooling: a `VISUALIZE=1` build + `npm run attribution:check` and `npm run size:why` show `@floating-ui/dom` resolving to ONE shared async chunk (not duplicated per overlay entry). Do NOT add `manualChunks` — this is verify-only; if duplication were found that purity/dedupe cannot explain, record it as a PERF-V2-01 follow-up rather than tuning here.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --project browser test/browser/registration-smoke.test.ts && node scripts/assert-no-bundled-lit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `registration-smoke.test.ts` passes for the full representative set (every `customElements.get('am-…')` defined).
    - `node scripts/assert-no-bundled-lit.mjs` reports zero inlined-Lit markers and zero relative Lit imports (Lit stays external after the chunk-graph shift).
    - `VISUALIZE=1 npm run build && npm run attribution:check` (plus `npm run size:why`) shows floating-ui in a single shared chunk with no per-overlay duplication; any anomaly is recorded as a PERF-V2-01 note, not tuned.
  </acceptance_criteria>
  <done>Registration survives across the refactor for a broad component set; Lit stays external; the bundle-attribution report confirms one shared floating-ui chunk (SIZE-04 dedupe) with no manualChunks added.</done>
</task>

<task type="auto">
  <name>Task 3: Re-write post-deferral size + perf baselines; confirm CEM unchanged</name>
  <files>api/size.baseline.json, api/perf.json</files>
  <read_first>
    - scripts/size-baseline.mjs (the `--write`/`--check` modes; brotli unit; report-only)
    - scripts/perf-diff.mjs (perf before/after delta comparison)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Runtime State Inventory build-artifacts row lines 240-241 — re-write size + perf baselines; custom-elements.json MUST NOT change; Wave 0 Gaps line 412)
    - api/perf.baseline.json (the pre-deferral overlay/data scenario baseline for the before/after delta)
  </read_first>
  <action>
    Rebuild the fully-deferred library. Re-`--write` `api/size.baseline.json` so the committed brotli baseline reflects the full deferral (floating-ui + virtualizer now in async `chunks/*`); confirm via `--check` that the win is real and stable versus the prior committed value. Re-run the perf lane and write `api/perf.json`, then run `perf:diff` against the pre-deferral `api/perf.baseline.json` to record the overlay/data before/after delta (report-only — do NOT fail on wall-clock). Run `npm run diff:surface` to confirm `custom-elements.json` is byte-unchanged (the enforcing freeze gate). Record the measured brotli reduction in the SUMMARY. <!-- planner-discipline-allow: @floating-ui/dom -->
  </action>
  <verify>
    <automated>npm run build && node scripts/size-baseline.mjs --write && node scripts/size-baseline.mjs --check && npm run diff:surface</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/size-baseline.mjs --check` exits 0 and shows the committed post-deferral baseline with the overlay/full initial brotli sizes reduced versus pre-deferral — the banked win is a committed number (report-only).
    - `npm run test:perf && npm run perf:diff` records an overlay/data before/after delta (report-only; wall-clock never red-builds).
    - `npm run diff:surface` exits 0 — `custom-elements.json` is unchanged (surface-freeze holds across the whole phase).
  </acceptance_criteria>
  <done>The post-deferral size and perf baselines are committed; the brotli win is recorded as a number; the CEM is provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → shipped artifact | Final integrity check: Lit externalized, registration intact, floating-ui only behind import(), one shared chunk, CEM frozen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Tampering (integrity) | Lit bundled/duplicated after chunk-graph shift | high | mitigate | no-bundled-Lit assertion (zero markers) + frozen external snapshot; block on this (ASVS L1 threshold = high). |
| T-08-19 | Tampering (integrity) | registration shaken away by a sideEffects change | high | mitigate | Broadened registration-smoke spec asserts customElements.define survives; block on this. |
| T-08-20 | Tampering (integrity) | cross-entry duplication / floating-ui leaks into non-overlay entry | medium | mitigate | deep-import-purity assertion + attribution report; recorded, report-only this phase. |

No package-manager installs occur in this phase — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `node scripts/deep-import-purity.mjs dist` — non-overlay purity + dynamic-only floating-ui, exit 0.
- `npx vitest run --project browser test/browser/registration-smoke.test.ts` + `node scripts/assert-no-bundled-lit.mjs` — registration survives; Lit external.
- `node scripts/size-baseline.mjs --check` — banked brotli win; `npm run perf:diff` — recorded delta.
- `npm run diff:surface` — CEM unchanged.
</verification>

<success_criteria>
- Non-overlay entries carry zero floating-ui; floating-ui appears only behind import() in one shared chunk (SIZE-04).
- Registration survives across the refactor; Lit never bundled (SIZE-03).
- Post-deferral size + perf baselines committed; brotli win banked (report-only); CEM frozen.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-07-SUMMARY.md` when done.
</output>
