---
phase: 08-bundle-size-deferral
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/rich-select/rich-select.ts
  - src/components/dropdown/dropdown.ts
  - test/components/rich-select.test.ts
  - test/components/dropdown.test.ts
  - test/browser/overlay-no-zero-frame.test.ts
autonomous: true
requirements: [SIZE-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "rich-select positions via FloatingPositionController (which already runs autoUpdate) with its `size`/minWidth middleware built from the loaded module — behavior identical to its former inline autoUpdate+computePosition."
    - "rich-select's former inline `_startAutoUpdate` / `_updatePosition` are deleted; the controller is the single positioning path (mirrors select)."
    - "dropdown stays type-only for `@floating-ui/dom` (imports only `type Placement`, erased at build) — verified to remain type-only after the refactor (D-07)."
  artifacts:
    - "rich-select using a FloatingPositionController field; no static floating-ui runtime import"
  key_links:
    - "rich-select controller middleware getter: `(mod) => [mod.size({ apply({rects,elements}) { elements.floating.style.minWidth = ... } })]`, matching the controller's default offset(4)→flip()→shift({padding:8}) base stack."
    - "rich-select calls `controller.start()` on open (mirroring select.ts:594)."
  prohibitions:
    - "MUST NOT change rich-select's observable positioning behavior — its inline stack (offset(4)→flip()→shift({padding:8})→size) already matches the controller defaults, so the migration is behavior-neutral; verify in real browser."
    - "MUST NOT introduce a runtime floating-ui import in dropdown (keep it type-only)."
    - "MUST NOT change any CEM/public surface (surface-freeze)."
    - "MUST NOT paint the rich-select listbox at 0,0 before first position."
---

<objective>
Complete the remaining component-level floating-ui import sites: migrate rich-select from its inline `computePosition`+`autoUpdate` onto the shared `FloatingPositionController` (the correct target because rich-select ALREADY runs autoUpdate with the controller's exact default middleware stack), and verify dropdown stays type-only (D-07). After this plan, no component holds a static floating-ui runtime import except via the controller/loader.

Purpose: rich-select is the second D-06 inline migration (rated costly). Unlike color-picker, its behavior contract MATCHES the controller, so the controller is the right home — the opposite call from color-picker, which is why they are distinct tasks.
Output: rich-select on the controller; dropdown confirmed type-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
@src/internal/controllers/floating-position.ts
@src/components/select/select.ts
</context>

## Artifacts this phase produces (touched by this plan)

- Consumes the deferred `FloatingPositionController` (from 08-01) and its module-receiving middleware getter for rich-select.
- Consumes `prefetchFloating()` from `src/internal/helpers/lazy-load.ts`.

<tasks>

<task type="auto">
  <name>Task 1: rich-select — migrate inline positioning onto FloatingPositionController</name>
  <files>src/components/rich-select/rich-select.ts, test/components/rich-select.test.ts, test/browser/overlay-no-zero-frame.test.ts</files>
  <read_first>
    - src/components/rich-select/rich-select.ts (static import at line 3; inline `_startAutoUpdate` at 370-374; inline `_updatePosition` at 452-463 with the `offset(4)→flip()→shift({padding:8})→sizeMiddleware(minWidth)` stack; the open/close path and `_cleanupAutoUpdate` teardown)
    - src/components/select/select.ts (the migration target analog — FloatingPositionController field with a `size` middleware getter; `controller.start()` on open at ~line 594)
    - src/internal/controllers/floating-position.ts (the controller's default base stack at 121-127 which matches rich-select's inline stack; the module-receiving middleware getter option)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (rich-select assignment lines 162-166)
  </read_first>
  <action>
    Remove rich-select's static `@floating-ui/dom` import (line 3). Add a `FloatingPositionController` field configured like select/combobox: `reference` → the trigger, `floating` → the listbox, `placement:'bottom-start'`, `strategy:'fixed'`, `offset:4`, and `middleware: (mod) => [mod.size({ apply({ rects, elements }) { elements.floating.style.minWidth = ` + '`${rects.reference.width}px`' + `; } })]`. Delete the inline `_startAutoUpdate` (370-374) and `_updatePosition` (452-463); replace their call sites with `this._floatingController.start()` on open and `.stop()` on close/teardown (mirroring select and the existing `_cleanupAutoUpdate` lifecycle). Keep the reveal gated on first position (D-02). Hang `prefetchFloating()` off the trigger intent (D-03). The base middleware stack the controller applies (offset→flip→shift) is byte-identical to rich-select's former inline stack, so positioning is behavior-neutral. <!-- planner-discipline-allow: @floating-ui/dom -->
    Add a rich-select case to `overlay-no-zero-frame.test.ts`.
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/rich-select.test.ts && npx vitest run --project browser test/browser/overlay-no-zero-frame.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rich-select.test.ts` passes unchanged (open/close, selection, min-width matching behavior-preserving).
    - After build, `rich-select.ts` has no static runtime `@floating-ui/dom` import and no inline `_startAutoUpdate`/`_updatePosition`; positioning is delegated to `FloatingPositionController`.
    - Real-browser: rich-select listbox positions at a non-zero viewport-anchored rect, min-width matches the trigger, no 0,0 frame.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">The second D-06 inline migration (rated costly in CONTEXT). Undo means re-inlining. Guard: the middleware stack provably matches the controller defaults + component test + real-browser positioning + surface-diff.</reversibility>
  <done>rich-select positions solely through the controller with module-sourced `size` middleware; inline positioning deleted; behavior, min-width, and surface unchanged; no 0,0 frame.</done>
</task>

<task type="auto">
  <name>Task 2: dropdown — verify type-only floating-ui import survives the refactor (D-07)</name>
  <files>src/components/dropdown/dropdown.ts, test/components/dropdown.test.ts</files>
  <read_first>
    - src/components/dropdown/dropdown.ts (line 3 — imports only `type Placement` from `@floating-ui/dom`; the controller usage that supplies positioning)
    - .planning/phases/08-bundle-size-deferral/08-CONTEXT.md (D-07 — dropdown is type-only, erased at build; leave it, verify it stays type-only)
  </read_first>
  <action>
    Verify (no functional change) that `dropdown.ts` imports ONLY `type Placement` from `@floating-ui/dom` and that this import is erased at build (TypeScript `import type` / type-only specifier). Confirm dropdown's positioning is supplied entirely by the deferred `FloatingPositionController` (from 08-01) with no static runtime floating-ui symbol. If any runtime floating-ui symbol is found, convert it to route through the controller/loader; otherwise leave the file untouched and record the verification. <!-- planner-discipline-allow: @floating-ui/dom -->
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/dropdown.test.ts && node scripts/assert-no-bundled-lit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The built `dist/components/dropdown/index.js` static import graph contains NO `@floating-ui/dom` runtime import (the type-only import is erased) — confirmed via the dist grep.
    - `dropdown.test.ts` passes; dropdown positioning still works via the deferred controller.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>dropdown confirmed type-only for floating-ui (no runtime import in dist); positioning inherited from the deferred controller; no change needed or a minimal route-through applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → deferred chunk fetch | rich-select's controller triggers the shared floating-ui `import()` at open time. |
| build → shipped artifact | dropdown's type-only import must remain erased (no floating-ui bytes in a non-migrated entry). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Tampering (integrity) | rich-select migration alters positioning behavior | medium | mitigate | Controller default stack proven byte-identical to former inline stack; component test + real-browser positioning + surface-diff guard. |
| T-08-08 | Tampering (integrity) | dropdown accidentally gains a runtime floating-ui import | low | mitigate | dist-graph grep asserts floating-ui absent from the dropdown entry. |

No package-manager installs occur in this phase — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `npm run build` green; `rich-select.test.ts` + `dropdown.test.ts` unchanged and passing.
- Real-browser rich-select positioning + no-0,0 case pass.
- dropdown dist graph free of runtime floating-ui; `npm run diff:surface` clean.
</verification>

<success_criteria>
- rich-select positions solely via the deferred controller with module-sourced `size` middleware; behavior/min-width preserved.
- dropdown confirmed type-only; no runtime floating-ui in its entry.
- No 0,0 frame; surface frozen.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-03-SUMMARY.md` when done.
</output>
