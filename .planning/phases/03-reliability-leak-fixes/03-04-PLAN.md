---
phase: 03-reliability-leak-fixes
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
gap_closure: true
files_modified:
  - src/components/command-palette/command-palette.ts
  - src/components/dialog/dialog.ts
  - src/components/drawer/drawer.ts
  - src/components/toast/toast.ts
  - test/components/command-palette.test.ts
  - test/components/dialog.test.ts
  - test/components/drawer.test.ts
  - test/components/toast.test.ts
autonomous: true
requirements: [FIX-01, FIX-03]
source: 03-REVIEW.md (CR-01, WR-01, WR-02)
estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "CR-01 fixed: command-palette keyboard navigation and Enter-selection index into the SAME flattened, grouped order that render() emits, so the visually highlighted item is always the item Enter executes — even when consumer command groups interleave (e.g. group A, group B, group A). Proven by a failing-first test using non-contiguous groups."
    - "WR-01 fixed: dialog, drawer, and command-palette do NOT dispatch a spurious am-close on initial mount when never opened, while <am-*  open> on mount STILL fires am-open and shows. Only a genuine open→closed transition fires am-close."
    - "WR-02 fixed: toast _dismiss() completes (removes dismissing, sets open=false, fires am-close) ONLY on the host's own toast-out animationend, not on composed animationend events bubbling from shadow descendants (countdown ring, toast-in)."
  artifacts:
    - src/components/command-palette/command-palette.ts
    - src/components/dialog/dialog.ts
    - src/components/drawer/drawer.ts
    - src/components/toast/toast.ts
  key_links:
    - "command-palette.ts: single flattened grouped order (_ordered) drives BOTH render() itemIndex and _handleKeydown length/index"
    - "dialog.ts/drawer.ts/command-palette.ts updated(): show when open truthy; only _hide/am-close when a prior open state existed"
    - "toast.ts _dismiss(): onEnd gates on e.animationName === 'toast-out'; timeout fallback calls onEnd() with no event"
  prohibitions:
    - statement: "No user-visible regression — <am-dialog open> / <am-drawer open> / <am-command-palette open> on mount still open and fire am-open; open/close/select still fire am-open/am-close/am-select on real transitions; command-palette Cmd/Ctrl+K toggle unchanged; toast auto-dismiss and click-close still fire am-close and complete the fade."
      status: must_hold
      verification: "npx vitest run test/components/command-palette.test.ts test/components/dialog.test.ts test/components/drawer.test.ts test/components/toast.test.ts --project jsdom exits 0; full jsdom + browser suites still green"
    - statement: "Lit-safe only — no innerHTML/eval, no global CSS, no hardcoded colors; property→event + no-global-state model preserved; frozen public surface unchanged (no new @property/@fires/slots/parts; TeardownScope not re-exported)."
      status: must_hold
      verification: "grep confirms no innerHTML/eval added; npx tsc --noEmit exits 0; npm run build:manifest produces no new public members on the four components"
---

<objective>
Close the three code-review defects (03-REVIEW.md) that ship in files this phase modified and land on the frozen v1.0 public API. These are outside the four leak-fix criteria (already verified) but must not ship in v1.0.

- CR-01 (Critical): command-palette can execute the WRONG command when consumer groups interleave, because keyboard nav/Enter index into `_filtered` (original order) while render() emits a grouped order.
- WR-01 (Warning): dialog/drawer/command-palette emit a spurious public `am-close` on initial mount (first Lit update runs the closed/else branch).
- WR-02 (Warning): toast dismiss `animationend` host listener can fire early on composed child animations, cutting the fade and firing `am-close` prematurely.

Each task is test-first (RED → GREEN), atomic-committed.
Output: corrected command-palette ordering, mount-safe overlay lifecycle, and animation-name-gated toast dismiss — all with new tests that fail before the fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reliability-leak-fixes/03-REVIEW.md

@src/components/command-palette/command-palette.ts
@src/components/dialog/dialog.ts
@src/components/drawer/drawer.ts
@src/components/toast/toast.ts
@test/components/command-palette.test.ts
@test/components/dialog.test.ts
@test/components/drawer.test.ts
@test/components/toast.test.ts
@test/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix command-palette wrong-command on interleaved groups (CR-01)</name>
  <files>src/components/command-palette/command-palette.ts, test/components/command-palette.test.ts</files>
  <read_first>
    - command-palette.ts: `_handleKeydown` (ArrowDown/ArrowUp/Enter index into `this._filtered`, ~262/269-271, select at ~349), and `render()` grouping loop that builds a Map from `_filtered` and increments a flat `itemIndex` group-by-group (~306-317), applying `.highlighted` when `idx === this._highlightedIndex`.
    - test/components/command-palette.test.ts (existing nav/select cases + mount/shadowQuery conventions).
  </read_first>
  <steps>
    1. RED: add a test with NON-contiguous groups, e.g. commands `[{id:a,group:'A'},{id:b,group:'B'},{id:c,group:'A'}]`. Open palette, ArrowDown to the 2nd rendered item, press Enter; assert the executed `cmd.action` / `am-select` detail matches the item that carries `.highlighted` in the rendered DOM. This fails today (highlight vs selection diverge).
    2. GREEN: introduce a single flattened grouped order used by BOTH render and keyboard handling — e.g. a `private get _ordered(): CommandItem[]` that groups `_filtered` the same way render() does and returns `Array.from(groups.values()).flat()`. Drive render()'s item emission and `itemIndex`, and `_handleKeydown` (length bound, `_ordered[_highlightedIndex]`), and `_selectCommand` from `_ordered`. Keep grouping/rendering output identical for contiguous data.
    3. Confirm existing contiguous-group nav/select tests still pass.
  </steps>
  <verify>npx vitest run test/components/command-palette.test.ts --project jsdom</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suppress spurious am-close on mount for dialog/drawer/command-palette (WR-01)</name>
  <files>src/components/dialog/dialog.ts, src/components/drawer/drawer.ts, src/components/command-palette/command-palette.ts, test/components/dialog.test.ts, test/components/drawer.test.ts, test/components/command-palette.test.ts</files>
  <read_first>
    - dialog.ts updated() (~192-211), drawer.ts updated() (~196-215), command-palette.ts updated() (~216-232). Each runs the close/else branch on the first update because `changed.has('open')` is true on initial render.
  </read_first>
  <steps>
    1. RED (per component): mount the element WITHOUT `open`, attach an `am-close` listener via `oneEvent`/spy BEFORE the first update completes (or assert zero am-close over the first update using a counting listener added synchronously after createElement but before append), and assert NO am-close fires on mount. Fails today.
    2. RED guard against regression: also assert that mounting WITH `open` (e.g. `<am-dialog open>` / setting `.open = true` before append) STILL fires `am-open` and shows.
    3. GREEN: in each updated(), react only to a genuine transition. Show whenever `open` is truthy (covers mount-with-open → am-open), and run the hide/am-close branch ONLY when there was a prior open state. Concretely:
       ```ts
       if (changed.has('open')) {
         const prev = changed.get('open');           // undefined on first update
         if (this.open) this._show();
         else if (prev) this._hide();                // skip when never opened
       }
       ```
       Do NOT use the bare `changed.get('open') !== undefined` guard — it would also suppress the mount-with-open _show and break `am-open`. Apply the equivalent shape to command-palette's close branch.
    4. Confirm real open→close transitions still fire am-close, and Cmd/Ctrl+K toggle is unchanged.
  </steps>
  <verify>npx vitest run test/components/dialog.test.ts test/components/drawer.test.ts test/components/command-palette.test.ts --project jsdom</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate toast dismiss on animationName === 'toast-out' (WR-02)</name>
  <files>src/components/toast/toast.ts, test/components/toast.test.ts</files>
  <read_first>
    - toast.ts `_dismiss()` (~263-271): `animationend` on `this` runs onEnd which removes `dismissing`, sets `open=false`, dispatches `am-close`; a `done` guard prevents double dispatch; 300ms `_teardown.timeout` fallback.
    - the `toast-out`, `toast-in`, and countdown-ring keyframe names in the component styles (confirm the exit animation name is `toast-out`).
  </read_first>
  <steps>
    1. RED: start a dismiss, then dispatch a composed `AnimationEvent('animationend', { animationName: 'toast-in', bubbles: true, composed: true })` (or the countdown-ring animation name) from a shadow descendant / on the host with a non-`toast-out` name; assert the toast is NOT yet dismissed (still `dismissing`, no premature `am-close`). Fails today because any animationend completes it.
    2. GREEN: change onEnd to `(e?: AnimationEvent) => { if (e && e.animationName !== 'toast-out') return; if (done) return; done = true; ... }`. Keep the `{ signal: this._teardown.signal }` binding; the 300ms fallback calls `onEnd()` with no event (so it still completes). Drop `once: true` if present (the name check replaces it; a one-shot listener could be consumed by an unrelated bubbling event).
    3. Assert a real `toast-out` animationend (or the 300ms fallback) still completes the dismiss and fires exactly one `am-close`.
  </steps>
  <verify>npx vitest run test/components/toast.test.ts --project jsdom</verify>
</task>

</tasks>

<verification>
- All three tasks green: `npx vitest run test/components/command-palette.test.ts test/components/dialog.test.ts test/components/drawer.test.ts test/components/toast.test.ts --project jsdom` exits 0.
- Full regression: `npx vitest run --project jsdom` and `npx vitest run --project browser` both exit 0.
- `npx tsc --noEmit` exits 0.
- `git grep -nE "innerHTML|eval\(" src/components/command-palette src/components/dialog src/components/drawer src/components/toast` shows no new occurrences.
- Frozen surface intact: no new public @property/@fires/slots/parts on the four components; TeardownScope still not re-exported from src/index.ts / src/index.all.ts.
</verification>
