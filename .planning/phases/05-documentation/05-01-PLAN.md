---
phase: 05-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-contract-doc.mjs
  - docs/contract.md
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [DOCS-02]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Running `node scripts/build-contract-doc.mjs` writes docs/contract.md listing the frozen slots, ::part() names, and --am-* tokens sourced from dist/custom-elements.json (per D-05)."
    - "docs/contract.md opens with a hand-written freeze-guarantee intro explaining what 'frozen' means and that CI blocks unversioned surface changes at freeze (per D-06), above the generated exhaustive tables."
    - "CI regenerates the contract doc and fails if the committed docs/contract.md is stale — it can never drift from the enforced CEM surface (per D-05)."
  artifacts:
    - scripts/build-contract-doc.mjs
    - docs/contract.md
  key_links:
    - "build-contract-doc.mjs reads dist/custom-elements.json (the same manifest api/custom-elements.baseline.json + scripts/cem-diff.mjs guard) so the doc and the enforced surface share one source."
    - "package.json build script chains build:contract-doc after build:manifest; ci.yml surface-diff job runs a git-diff drift check on docs/contract.md."
---

<objective>
Build the one code artifact of this phase: a zero-dependency Node generator that emits the consumer-facing frozen slot / `::part()` / `--am-*` token contract doc from `dist/custom-elements.json`, wrapped in a hand-written freeze-guarantee intro, and wire it into the build + CI so it regenerates like `api/AUDIT.md` and can never drift from the enforced CEM surface.

Purpose: The frozen public surface (212 global tokens + 53 per-component tokens + 21 slots + 76 parts, Phase 2) must be readable by a consumer as a single authoritative page that is provably in sync with the surface Phase 6's enforcing diff gate guards. Generation-over-hand-authoring is the house style (AUDIT.md, tokens.css, baseline are all generated) — the contract doc must anchor to the same CEM, not a parallel hand-maintained list (D-05).
Output: `scripts/build-contract-doc.mjs`, the committed `docs/contract.md`, a `build:contract-doc` npm script chained into `build`, and a CI drift check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md

# The generator pattern to clone (reads dist/custom-elements.json + src/tokens/*.css.ts):
@scripts/build-audit.mjs
# The frozen surface this doc describes (numbers to match):
@api/AUDIT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — generate docs/contract.md from the CEM end-to-end</name>
  <files>scripts/build-contract-doc.mjs, docs/contract.md</files>
  <read_first>
    - scripts/build-audit.mjs — clone its structure: CEM indexing keyed by tagName (never array position), the `css`` global-token extraction from src/tokens/{primitives,semantic,dark}.css.ts, and the `frozenSurface()` / slot / part / token table builders. This is THE pattern named by D-05.
    - api/AUDIT.md — the "Frozen public surface" section; the contract doc reproduces the same counts (global tokens, per-component tokens, slots, parts) for a consumer audience.
    - dist/custom-elements.json exists (653 KB); confirm before reading — it is the generation source.
  </read_first>
  <action>
    Create `scripts/build-contract-doc.mjs` as a zero-dependency ESM Node script (no new package installs — mirror build-audit.mjs's imports from node:fs/node:path/node:url) implementing D-05. It reads `dist/custom-elements.json` for slots (`@slot`), `::part()` names (`@csspart`), and per-component `--am-*` tokens (`@cssprop`), keyed by tagName and sorted, exactly as build-audit.mjs indexes; and reads `src/tokens/{primitives,semantic,dark}.css.ts` for the global `--am-*` token scale using the same `css`` block extraction as build-audit.mjs. It writes `docs/contract.md` containing, in order: (1) an H1 title and a "generated — do not hand-edit, re-run the generator" provenance banner naming the generation command; (2) the hand-written freeze-guarantee intro required by D-06, embedded as a constant string in the generator so the whole file stays deterministically regenerable — the intro explains what "frozen" means, that a consumer can rely on these slot/part/token names not churning after 1.0, and that CI blocks any unversioned public-surface change at the Phase 6 freeze; (3) a counts summary line matching AUDIT.md (global tokens, per-component tokens, unique slots, unique parts); (4) generated exhaustive tables — global `--am-*` tokens, per-component `--am-*` tokens per tagName, slots per tagName, and `::part()` names per tagName. Do NOT read or enumerate anything under `src/internal/` — that boundary is non-exported and absent from the CEM by design (threat T-05-01). Use plain text/markdown table cells only (escape `|`), no code execution of manifest data.
  </action>
  <verify>
    <automated>node scripts/build-contract-doc.mjs && test -f docs/contract.md && grep -q "frozen" docs/contract.md && grep -Eq "am-button|Slots|::part" docs/contract.md && grep -Eiq "freeze|frozen" docs/contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-contract-doc.mjs` exits 0 and writes docs/contract.md.
    - docs/contract.md contains the hand-written freeze-guarantee intro (D-06) above the generated tables.
    - docs/contract.md lists slots, `::part()` names, and `--am-*` tokens with counts consistent with api/AUDIT.md's frozen-surface section (D-05).
    - No `src/internal/` symbol appears in docs/contract.md (only public CEM surface).
  </acceptance_criteria>
  <done>The generator emits a consumer-facing frozen-contract doc from the CEM in one run, and the doc is committed.</done>
  <reversibility rating="reversible">Generated doc + generator script; re-runnable and revertible via git.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire regeneration into build + CI drift check (can never drift)</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - package.json `scripts` — the existing `build` chain (`tsc && vite build && tsc -p tsconfig.types.json && npm run build:manifest && npm run build:tokens-css`) and the `build:manifest` / `build:tokens-css` / `diff:surface` script naming convention to mirror.
    - .github/workflows/ci.yml — the `surface-diff` job (builds the manifest via `npm run build:manifest`, then runs `npm run diff:surface`); this is where the contract-doc drift check belongs since the manifest is already built there.
  </read_first>
  <action>
    Implement the D-05 "wire into build/CI so it regenerates like AUDIT.md and can never drift" requirement. In package.json add a `build:contract-doc` script running `node scripts/build-contract-doc.mjs`, and chain it into the `build` script immediately after `build:manifest` (the manifest must exist before the doc is generated). In .github/workflows/ci.yml, extend the existing `surface-diff` job: after the `Build manifest` step, add a step that runs `npm run build:contract-doc` then `git diff --exit-code docs/contract.md`, so CI fails when the committed contract doc is stale relative to the freshly generated CEM output (threat T-05-02, drift). Keep the job read-only (permissions unchanged) and do not touch the report-only `diff:surface` step — Phase 6 owns the enforcing flip.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.scripts['build:contract-doc'])process.exit(1);if(!p.scripts.build.includes('build:contract-doc'))process.exit(1)" && grep -q "build:contract-doc" .github/workflows/ci.yml && grep -q "git diff --exit-code docs/contract.md" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - package.json has a `build:contract-doc` script and the `build` chain invokes it after `build:manifest`.
    - ci.yml regenerates docs/contract.md and fails on drift via `git diff --exit-code docs/contract.md`.
    - The report-only `diff:surface` step is unchanged (Phase 6 flips it, not this phase).
  </acceptance_criteria>
  <done>The contract doc regenerates on every build and CI blocks a stale committed copy — driftless by construction (D-05).</done>
  <reversibility rating="reversible">package.json + workflow edits, revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CEM manifest → published doc | Only the public surface in dist/custom-elements.json + global tokens in src/tokens may reach the consumer-facing doc; internal machinery must not. |
| committed doc ↔ enforced surface | The committed docs/contract.md must equal the freshly generated CEM output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information disclosure | build-contract-doc.mjs | low | mitigate | Generator reads only dist/custom-elements.json (public CEM) + src/tokens global tokens; never src/internal — verified by acceptance check that no internal symbol appears. |
| T-05-02 | Tampering | docs/contract.md | medium | mitigate | CI regenerates and runs `git diff --exit-code docs/contract.md`; a hand-edited/stale doc fails CI. |
| T-05-SC | Tampering | dependencies | low | accept | No package installs this phase; generator is zero-dependency Node. |
</threat_model>

<verification>
- `npm run build:contract-doc` regenerates docs/contract.md identically (idempotent): running twice leaves `git status` clean.
- Counts in docs/contract.md align with api/AUDIT.md's frozen-surface section.
- `npx tsc --noEmit` unaffected (no TS touched).
</verification>

<success_criteria>
A committed docs/contract.md, generated from the CEM with a hand-written freeze-guarantee intro, exists and is enforced driftless in CI.
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-01-SUMMARY.md` when done.
</output>
