---
phase: 05-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stories/patterns/validation.stories.ts
  - src/stories/patterns/virtualization.stories.ts
autonomous: false
requirements: [DOCS-03]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "src/stories/patterns/validation.stories.ts exists as a dedicated pattern-level story (title under a Patterns group) with interactive controls that surface ElementInternals.validationMessage live as the user blurs/submits (per D-07, D-08)."
    - "src/stories/patterns/virtualization.stories.ts exists as a dedicated pattern-level story with interactive controls (e.g. row-count slider crossing the 100-row auto-threshold) that show am-data-grid virtualizing 1000+ rows live (per D-07, D-08)."
    - "Both stories are runnable/interactive (live Storybook args/controls), not static snapshots, and are auto-discovered by the existing .storybook glob."
  artifacts:
    - src/stories/patterns/validation.stories.ts
    - src/stories/patterns/virtualization.stories.ts
  key_links:
    - "Stories import the real shipped components (am-field/am-input/am-data-grid/am-combobox) so the examples exercise as-built Phase 4 behavior; .storybook/preview.ts already registers src/index.all.js and feeds the CEM."
---

<objective>
Ship the two DOCS-03 Storybook examples as dedicated pattern story files — one for validation-message display (FEAT-01/02) and one for list virtualization (PERF-02/03) — with live interactive controls, so a consumer can find "the validation example" and "the virtualization example" as cross-cutting patterns rather than buried inside a single component's stories.

Purpose: DOCS-03 requires runnable examples for the virtualization and validation-message patterns. These span multiple components, so they need a pattern-level home (D-07) and must be interactive, not static snapshots (D-08).
Output: `src/stories/patterns/validation.stories.ts` and `src/stories/patterns/virtualization.stories.ts`, plus a human check that the controls are genuinely live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-documentation/05-CONTEXT.md

# As-built feature behavior to demonstrate (source of truth for the examples):
@.planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md
@.planning/phases/04-performance-feature-capabilities/04-08-SUMMARY.md
# Storybook wiring + an existing story pattern to mirror:
@.storybook/preview.ts
@src/stories/input.stories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: patterns/validation.stories.ts — interactive validation-message example (DOCS-03)</name>
  <files>src/stories/patterns/validation.stories.ts</files>
  <read_first>
    - src/stories/input.stories.ts — the Meta/StoryObj shape, `title`, `argTypes` controls, and `render(args)` convention to mirror.
    - .storybook/preview.ts — confirms `src/index.all.js` is registered globally and the CEM feeds arg tables; stories need only import components they render (or rely on the global registration) and set a `title`.
    - .planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md — as-built validation: `am-field` composes label/hint/error via slots and owns aria-describedby/aria-invalid; controls expose `setCustomError(message)`; native `validationMessage` is touch-gated (surfaces on blur / failed submit, D-01), error replaces hint (D-02), submit announces assertively (D-04).
  </read_first>
  <action>
    Create `src/stories/patterns/validation.stories.ts` with a pattern-level `title` (group it as "Patterns/Validation") demonstrating FEAT-01/FEAT-02 per D-07/D-08. Build a runnable `<form>` example using `am-field` + a real form-associated control (e.g. `am-input required`, type=email) that surfaces the native `validationMessage` live on blur/submit, plus a story that drives the public `setCustomError` API from a Storybook control so a consumer sees a server/consumer-supplied error win over the native message and clear back on `setCustomError('')` (D-03 precedence). Expose interactive args (Storybook controls) — at minimum: required toggle, a custom-error text arg wired through `setCustomError`, and a submit action — so the example is interactive, not a static snapshot (D-08). Render live message text via Lit template bindings only (no raw-HTML sink; threat T-05-03). Do not restate component internals — demonstrate the shipped public behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Patterns/Validation" src/stories/patterns/validation.stories.ts && grep -q "setCustomError" src/stories/patterns/validation.stories.ts && grep -q "argTypes" src/stories/patterns/validation.stories.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/stories/patterns/validation.stories.ts with a "Patterns/Validation" title.
    - It renders am-field + a form-associated control that surfaces validationMessage and drives setCustomError from a live control (D-08).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A dedicated, interactive validation-pattern story exists and typechecks.</done>
  <reversibility rating="reversible">New story file, revertible via git.</reversibility>
</task>

<task type="auto">
  <name>Task 2: patterns/virtualization.stories.ts — interactive virtualization example (DOCS-03)</name>
  <files>src/stories/patterns/virtualization.stories.ts</files>
  <read_first>
    - .planning/phases/04-performance-feature-capabilities/04-08-SUMMARY.md — as-built: `am-data-grid` auto-virtualizes above VIRTUALIZE_ROW_THRESHOLD = 100 rows (D-05, no new public attribute); below it the `<table>`/`repeat()` path stands; ARIA is truthful from state (aria-rowcount = full total). No public attribute toggles it — the example drives it purely by row count.
    - src/stories/data-grid.stories.ts (reference) and src/stories/input.stories.ts (Meta/render shape).
  </read_first>
  <action>
    Create `src/stories/patterns/virtualization.stories.ts` with a pattern-level `title` ("Patterns/Virtualization") demonstrating PERF-02/PERF-03 per D-07/D-08. Provide a runnable `am-data-grid` example whose row dataset size is driven by an interactive Storybook control (e.g. a numeric/range `rowCount` arg spanning below and above the 100-row auto-threshold, up to 1000+), so a consumer can watch the grid switch to the virtualized windowed render path live and confirm smooth scrolling of 1000+ rows (D-08). Note in the story description that virtualization auto-activates at ~100 rows with no public attribute (freeze-neutral, D-05) and acknowledge the documented mobile screen-reader recycled-row limitation. Optionally include an option-popup virtualization example (combobox/select with a large option list) to cover PERF-03. Generate row data in the render function; bind cell text via Lit bindings only (no raw-HTML sink; threat T-05-03).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Patterns/Virtualization" src/stories/patterns/virtualization.stories.ts && grep -q "am-data-grid" src/stories/patterns/virtualization.stories.ts && grep -q "argTypes" src/stories/patterns/virtualization.stories.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/stories/patterns/virtualization.stories.ts with a "Patterns/Virtualization" title.
    - An interactive rowCount control drives am-data-grid across the ~100-row auto-threshold up to 1000+ rows (D-08).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A dedicated, interactive virtualization-pattern story exists and typechecks.</done>
  <reversibility rating="reversible">New story file, revertible via git.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the pattern stories are live/interactive (DOCS-03, D-08)</name>
  <what-built>Two dedicated pattern stories (Patterns/Validation, Patterns/Virtualization) with live Storybook controls, satisfying DOCS-03's "runnable examples" requirement (D-08).</what-built>
  <how-to-verify>
    Run `npm run storybook` and open http://localhost:6006. Confirm:
    1. A "Patterns/Validation" story appears; typing an invalid email then blurring surfaces the native validationMessage, and editing the custom-error control live shows/clears the message (setCustomError precedence).
    2. A "Patterns/Virtualization" story appears; dragging/editing the rowCount control above ~100 (up to 1000+) switches the grid to the virtualized path and scrolls smoothly.
    Both must respond to the controls live (not static).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is static/broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story data → rendered DOM | Example row/message content is developer-authored but must render through Lit bindings, never a raw-HTML sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | pattern stories | low | mitigate | Cell/message content rendered via Lit `${}` text bindings only — no innerHTML/raw-HTML sink in either story. |
| T-05-04 | Information disclosure | pattern stories | low | mitigate | Stories demonstrate only shipped public component APIs (am-field/setCustomError/am-data-grid), never src/internal controllers. |
| T-05-SC | Tampering | dependencies | low | accept | No package installs; Storybook + components already present. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with both new stories.
- The existing `.storybook/main.ts` glob `../src/**/*.stories.@(ts|js)` auto-discovers `src/stories/patterns/*.stories.ts` — no config change required (confirm no main.ts edit needed).
- Human checkpoint confirms live interactivity (D-08).
</verification>

<success_criteria>
Storybook exposes runnable, interactive validation and virtualization pattern examples under a Patterns group (DOCS-03).
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-04-SUMMARY.md` when done.
</output>
