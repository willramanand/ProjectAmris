---
phase: 09-runtime-performance-tuning
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/internal/controllers/floating-position.ts
  - src/internal/helpers/lazy-load.ts
  - test/perf/overlay.perf.test.ts
  - test/browser/lazy-load-retry.test.ts
  - test/browser/popover-a11y-snapshot.test.ts
autonomous: true
requirements: [RPERF-03, RPERF-04]
estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "computePosition output (x, y, placement, middlewareData) is BYTE-IDENTICAL before/after the middleware-slice cache — proven on the real-browser lane (jsdom cannot; it mocks positioning/ResizeObserver). (RPERF-03)"
    - "Getter-backed options (popover/tooltip/dropdown live getters for placement/offset/arrow) are RE-RESOLVED every autoUpdate tick — never cached (caching would freeze a stale placement/arrow = surface-observable). Only fixed-value inputs (combobox/select/date-picker) are hoisted/cached. (RPERF-03, D-03)"
    - "The middleware array order [offset, flip, shift, ...hostMiddleware] is preserved exactly; only the host tail is rebuilt each tick, the cached base slice is prepended. (RPERF-03)"
    - "autoUpdate open-gating (start()/stop()), the _startToken close-during-load guard, and hostDisconnected→stop() are unchanged — no positioning loop runs while closed, none dangles after disconnect. (RPERF-03)"
    - "middlewareBuilds count drops from once-per-tick to once-per-config-change; computePosition and repositions counts stay UNCHANGED by design (F-2 — per-tick churn reduction does not reduce autoUpdate tick count). middlewareBuilds is the count evidence; wall-clock is the primary win (report-only). (RPERF-03)"
    - "CR-01 fixed: a rejected import() no longer poisons the loader cache — loadFloating and loadVirtualizer null their slot on reject so the next call retries and succeeds; behavior-preserving, no new public API. (D-05)"
    - "popover accessible name/role tree + trigger/panel roles + focusability are byte-identical before/after the churn edit, snapshotted on the BROWSER lane per D-04 (am-popover as the representative overlay). (RPERF-04, D-04)"
  artifacts:
    - "src/internal/controllers/floating-position.ts — new private _buildMiddleware(mod) + cached static base slice + fixed-resolve hoist"
    - "src/internal/helpers/lazy-load.ts — .catch slot-reset on both loadFloating and loadVirtualizer (CR-01)"
    - "test/perf/overlay.perf.test.ts — middlewareBuilds count wiring"
    - "test/browser/lazy-load-retry.test.ts — NEW cold-load rejection→retry spec"
    - "test/browser/popover-a11y-snapshot.test.ts — NEW report-only hybrid a11y snapshot spec"
  key_links:
    - "harness.countMethod (from Plan 01) → wraps FloatingPositionController.prototype._buildMiddleware → counts.middlewareBuilds"
    - "_updatePosition → _buildMiddleware(mod) → cached base slice + re-resolved getter tail → computePosition (output identical)"
    - "combobox._ensureVirtualizer retry (798-809) → loadVirtualizer .catch reset → genuine retry succeeds"
  prohibitions:
    - "Do NOT change computePosition output or the perceived reposition frequency (D-03)."
    - "Do NOT cache getter-backed resolve() results (popover/tooltip/dropdown live getters) — freezes stale placement/arrow (surface-observable)."
    - "Do NOT weaken autoUpdate open-gating or the _startToken close-during-load guard."
    - "Do NOT coalesce/debounce repositions this phase (changes WHEN the panel moves)."
    - "Do NOT touch the frozen vite `external` array / .size-limit.json (byte-snapshot-guarded); no bytes move this phase."
    - "Do NOT build a computed/origin-qualified dynamic-import path — keep static bare specifiers (CR-01 adds only a .catch reset)."
    - "Do NOT strip any aria-*/role/focusability from the overlay."
---

<objective>
RPERF-03 + RPERF-04 (overlay) + the D-05 fold-in of the open Phase-8 Critical
CR-01. COSTLY change: the shared FloatingPositionController is the chokepoint for
all 6 overlays (combobox, select, dropdown, popover, tooltip, date-picker), so a
churn-reduction bug regresses all of them — the browser regression lane is the
HARD GATE for this plan (per the Phase-8 lesson: jsdom mocks ResizeObserver and
positioning and cannot prove real behavior).

`_updatePosition` (floating-position.ts:146-166) rebuilds the
[offset, flip, shift, ...hostMiddleware] array and re-runs the resolve() getters
on EVERY autoUpdate tick (scroll/resize/layout). Per D-03 + RESEARCH Pattern 3:
cache the static base slice (flip(), shift({padding:8}), and offset(n) when offset
is fixed) and hoist resolve() for hosts passing fixed values, while RE-RESOLVING
getter-backed options every tick and keeping computePosition output byte-identical.
Per F-2, computePosition/reposition counts will NOT drop (autoUpdate ticks are
unchanged) — the count evidence is a new middlewareBuilds probe; wall-clock is the
primary win (report-only).

CR-01 lives in lazy-load.ts (the loader these overlays await): `??=` caches a
rejected import() → one transient chunk-load failure permanently bricks
positioning/virtualization page-wide. D-05 folds the fix here because Phase 9
already re-touches this loader's callers and runs the browser gate.

Purpose: bank the overlay reposition-churn win across all 6 overlays at once and
retire a pre-ship Critical. Output: cached-base-slice controller, CR-01 loader
fix + cold-load retry spec, middlewareBuilds count, popover a11y snapshot guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-runtime-performance-tuning/09-CONTEXT.md
@.planning/phases/09-runtime-performance-tuning/09-RESEARCH.md
@.planning/phases/09-runtime-performance-tuning/09-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/08-bundle-size-deferral/08-REVIEW.md
@src/internal/controllers/floating-position.ts
@src/internal/helpers/lazy-load.ts
@src/components/combobox/combobox.ts
@test/perf/overlay.perf.test.ts
@test/browser/a11y.browser.test.ts
@test/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache static middleware slice + hoist fixed resolve() in the shared controller (browser-lane gated)</name>
  <files>src/internal/controllers/floating-position.ts, test/perf/overlay.perf.test.ts</files>
  <read_first>
    - src/internal/controllers/floating-position.ts:146-166 (`_updatePosition` — the exact array-assembly to extract), 19-21 (`resolve()`), 44-61 (options JSDoc — "getters are resolved on every reposition"), 99/121-125/137 (`_startToken`), 118-144 (start/stop/hostDisconnected open-gating).
    - 09-RESEARCH.md Pattern 3 (the SAFE-vs-REQUIRED-to-re-resolve table — the load-bearing correctness map) + Assumption A1 (middleware object reuse — resolve empirically on the browser lane) + Pitfall 2/F-2 (computePosition count will NOT drop).
    - 09-PATTERNS.md "Pattern Assignments → floating-position.ts" + "Preserve the controller lifecycle invariants".
    - test/perf/harness.ts (`countMethod` from Plan 01, `countComputePosition`, `countRepositions`); test/perf/overlay.perf.test.ts (whole file — am-popover scenario, cpos/repos cross-check).
  </read_first>
  <behavior>
    - `_buildMiddleware(mod)` returns a middleware array whose ORDER is [offset, flip, shift, ...hostMiddleware] identical to today.
    - For a FIXED-value host (fixed offset/placement/strategy, e.g. combobox/select/date-picker), repeated autoUpdate ticks with unchanged options reuse the cached base slice → _buildMiddleware's expensive assembly runs once per config, not once per tick.
    - For a GETTER-backed host (popover/tooltip/dropdown live getters), placement/offset/arrow are re-resolved every tick — a getter that returns a new value between ticks is reflected (no stale freeze).
    - computePosition output (x,y,placement,middlewareData) for am-popover is byte-identical to pre-edit for the same geometry (proven on the browser lane).
    - _startToken, start()/stop() gating, and hostDisconnected→stop() behave exactly as before.
  </behavior>
  <action>
    In src/internal/controllers/floating-position.ts extract the middleware-array assembly from `_updatePosition` into a nameable private `_buildMiddleware(mod)`. Detect at construction which of placement/offset/strategy/middleware are FIXED values vs getters (typeof !== 'function'); for fixed inputs, build and cache the static base slice [mod.offset(fixedOffset), mod.flip(), mod.shift({padding:8})] once and hoist the fixed resolve() results; for getter-backed inputs, re-resolve every tick and rebuild only the host tail, prepending the cached base slice so the final array order [offset, flip, shift, ...host] is preserved exactly. Keep `_updatePosition` calling `_buildMiddleware(mod)` and passing the resolved placement/strategy to computePosition unchanged. Preserve the `_startToken` guard, start()/stop() open-gating, and hostDisconnected→stop() untouched. Do NOT cache getter-backed resolve() results. Do NOT coalesce/debounce repositions. Verify Assumption A1 (flip()/shift()/offset(n) object reuse across computePosition calls) empirically via the browser floating-position specs before considering the plan done; fallback per RESEARCH A1 is to cache only flip()/shift() and keep rebuilding offset().
    In test/perf/overlay.perf.test.ts import `countMethod` from ./harness, construct `const mwb = countMethod(FloatingPositionController, '_buildMiddleware')`, reset per repeat, merge `middlewareBuilds: mwb.count` into `counts`, `mwb.restore()` in the `finally`. Assert middlewareBuilds is deterministic (assertStableCounts) and that computePosition/repositions counts are UNCHANGED vs the pre-edit baseline (F-2). Record untuned→tuned middlewareBuilds in the SUMMARY.
    RED first: write the middlewareBuilds assertion (per-tick vs per-config) and the computePosition-output-identity browser assertion, then implement the cache.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser -- test/browser/floating-position.test.ts</automated>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project perf test/perf/overlay.perf.test.ts</automated>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - floating-position.ts contains a private `_buildMiddleware(` and a cached static base slice; getter-backed options are re-resolved each tick (source assertion + browser proof).
    - The full browser lane (`npm run test:browser`) is GREEN — floating-position.test.ts, overlay-focus.test.ts, data-grid-virtual/combobox-virtual, and the Phase-8 no-0,0-frame specs — proving all 6 overlays behave identically (this is the hard gate).
    - overlay.perf.test.ts asserts middlewareBuilds deterministic AND computePosition/repositions counts unchanged (F-2).
    - _startToken / start()/stop() / hostDisconnected logic is textually unchanged except the extracted _buildMiddleware call.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="costly">Shared chokepoint for all 6 overlays — a churn bug regresses every overlay; NOT one-way (revert = inline _buildMiddleware back into _updatePosition), but behavior-preservation MUST be proven on the browser lane before merge (D-03). Flagged costly; no decision checkpoint required.</reversibility>
  <done>The shared controller caches the static middleware slice and hoists fixed resolve() while re-resolving live getters every tick; computePosition output is byte-identical (browser-proven); middlewareBuilds drops; guards intact; full browser lane green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-01 fold-in — null the loader cache on reject + cold-load retry spec (D-05)</name>
  <files>src/internal/helpers/lazy-load.ts, test/browser/lazy-load-retry.test.ts</files>
  <read_first>
    - src/internal/helpers/lazy-load.ts:38-40 (`loadFloating` `??=`), 59-61 (`loadVirtualizer` `??=`), 82-85 (`__resetLazyLoadCachesForTest`).
    - .planning/phases/08-bundle-size-deferral/08-REVIEW.md CR-01 (73-120, the verbatim bug + fix) and WR-02/WR-03 (opportunistic-only).
    - src/components/combobox/combobox.ts:798-809 (`_ensureVirtualizer` retry that today re-awaits the poisoned promise).
    - 09-RESEARCH.md §"CR-01 fix" + Pitfall 4; 09-PATTERNS.md "Pattern Assignments → lazy-load.ts".
  </read_first>
  <behavior>
    - loadFloating(): a rejected import() nulls floatingPromise before rethrowing, so the NEXT loadFloating() starts a fresh import() and can succeed.
    - loadVirtualizer(): identical .catch slot-reset on virtualizerPromise.
    - A successful import() still memoizes exactly once (no double fetch on the happy path).
    - After a first (forced) rejection and cache reset, a subsequent call resolves — proving positioning/virtualization recovers instead of permanently bricking.
  </behavior>
  <action>
    In src/internal/helpers/lazy-load.ts change both memoized loaders so the cached promise resets to null on rejection: wrap the `import()` with `.catch((err) => { floatingPromise = null; throw err; })` (and the virtualizer equivalent `virtualizerPromise = null`) before assigning via `??=`. Behavior-preserving on the happy path; no new public API; keep the static bare package specifiers unchanged (no computed path). Do NOT touch the prefetch* helpers or `__resetLazyLoadCachesForTest`. WR-02 (no-0,0 reveal gate) and WR-03 (fire-and-forget rejection handling) are OPPORTUNISTIC-only per D-05 — apply them only if trivially cheap while in-file; otherwise carry them forward (note the decision in the SUMMARY).
    Create test/browser/lazy-load-retry.test.ts (browser project). Using `__resetLazyLoadCachesForTest()` for isolation, force the first import to reject (e.g. temporarily stub the dynamic-import path / simulate a rejected first call), assert the loader rethrows AND that a subsequent call returns a fresh promise that resolves to the real module (retry succeeds). Include an assertion that the happy path memoizes to the same promise instance twice (no double fetch).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser -- test/browser/lazy-load-retry.test.ts</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - lazy-load.ts nulls floatingPromise AND virtualizerPromise on reject (source assertion: `.catch(` resetting the slot on both loaders).
    - test/browser/lazy-load-retry.test.ts proves: first rejection rethrows; next call retries and resolves; happy path memoizes once.
    - Static bare specifiers unchanged; no computed import path.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <precondition>The browser lane runs on real Chromium via Playwright (`npm run test:browser`) — Task 1 must have left it green before this retry spec is added to it.</precondition>
  <reversibility rating="reversible">Localized helper fix; revert = drop the .catch resets (D-05).</reversibility>
  <done>Both loaders retry after a rejected import(); the cold-load retry spec proves recovery; the Phase-8 Critical CR-01 is retired; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: popover accessible-name/role snapshot guard (report-only)</name>
  <files>test/browser/popover-a11y-snapshot.test.ts</files>
  <read_first>
    - test/browser/a11y.browser.test.ts:1-53 (scaffold); test/helpers.ts:15-43, 127-135 (`deepActiveElement`).
    - test/perf/overlay.perf.test.ts:55-60 (the am-popover markup / representative overlay to reuse for apples-to-apples).
    - 09-RESEARCH.md Pattern 4 (hybrid, A3); 09-CONTEXT.md Claude's Discretion ("reuse the Phase-7 perf-scenario overlay am-popover").
  </read_first>
  <action>
    Create test/browser/popover-a11y-snapshot.test.ts (report-only, `browser` project), representing "overlay" with `am-popover` (reuse the perf-scenario overlay). Mount + open it, then: (1) `await expect.element(page.getByRole(...)).toMatchAriaSnapshot()` for the trigger/panel role + accessible-name tree (advisory); (2) explicit role/attribute snapshots via `shadowQuery` for the trigger and panel roles and focusability (tabindex / `deepActiveElement`) — asserted at exact values (load-bearing); assert byte-identical shape before/after the churn edit. Report-only. This is the RPERF-04 proof the overlay churn edit strips no a11y DOM. Node document-order is captured verbatim (the edit reorders no nodes).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser -- test/browser/popover-a11y-snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/browser/popover-a11y-snapshot.test.ts exists and runs on the `browser` project (real Chromium).
    - Asserts trigger/panel roles + focusability via shadowQuery (load-bearing) plus a toMatchAriaSnapshot role/name tree (advisory).
    - Spec passes green; report-only.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only file; delete to revert.</reversibility>
  <done>A browser-lane hybrid a11y snapshot guard exists for am-popover and passes, proving the tuned overlay's name/role/focusability DOM is intact.</done>
</task>

</tasks>

<flagged_assumptions>
Specless edge-probe fallback — unresolved edges dispositioned (no silent drops):
- [RPERF-03 unclassified] AUTHORED as must_haves.truth: computePosition output byte-identical; getter-backed options re-resolve every tick; _startToken/open-gating preserved; middlewareBuilds is the count evidence (computePosition/repositions flat by design, F-2).
- [RPERF-04 ordering] N/A: the a11y snapshot compares DOM in document order verbatim; the churn edit reorders no nodes (only the middleware assembly is cached) — captured as-is via Task 3.
- Assumption A1 (RESEARCH): floating-ui flip()/shift()/offset(n) middleware objects are safely reusable across computePosition calls — RESOLVE EMPIRICALLY on the browser lane during Task 1; fallback = cache only flip()/shift(), keep rebuilding offset().
</flagged_assumptions>

<artifacts_this_phase_produces>
NEW symbols/files this plan introduces (excluded from source-drift verification):
- `FloatingPositionController._buildMiddleware(mod)` — new private nameable method + cached static base slice + fixed-resolve hoist fields.
- lazy-load.ts `.catch` slot-reset on `loadFloating` and `loadVirtualizer` (CR-01 fix).
- `counts.middlewareBuilds` — new perf count key in api/perf.json (committed to baseline in Plan 04).
- test/browser/lazy-load-retry.test.ts — new cold-load retry spec.
- test/browser/popover-a11y-snapshot.test.ts — new report-only a11y snapshot spec.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dynamic import() specifier | lazy-load.ts fetches @floating-ui/dom / @lit-labs/virtualizer via static bare specifiers resolved by the consumer bundler (standing boundary; unchanged this phase). |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering / Elevation | lazy-load dynamic import target | low | mitigate | CR-01 fix adds ONLY a `.catch` slot-reset; import specifiers stay static bare package strings — never a computed/origin-qualified module path (supply-chain seam avoided; V-supply-chain standing control). |
| T-09-05 | Denial of Service | poisoned loader cache (CR-01) | medium | mitigate | Null-on-reject retry removes a self-inflicted permanent-brick failure mode (one transient chunk 404 no longer disables all overlays page-wide); proven by the cold-load retry spec. |
| T-09-06 | Tampering | overlay middleware cache | low | accept | Cached middleware are stateless floating-ui config objects; computePosition output asserted byte-identical on the browser lane (A1). |

No packages installed/upgraded — Package Legitimacy Gate N/A. No high-severity threats — nothing to block on.
</threat_model>

<verification>
- `npm run test:browser` FULL lane green — the hard gate for the shared-controller blast radius (all 6 overlays behave identically; no-0,0 frame specs pass).
- `npx vitest run --project perf test/perf/overlay.perf.test.ts` green; middlewareBuilds deterministic; computePosition/repositions unchanged (F-2).
- `npm run test:browser -- test/browser/lazy-load-retry.test.ts` green (CR-01 recovery proven).
- `npm run test:browser -- test/browser/popover-a11y-snapshot.test.ts` green (report-only).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Overlay reposition churn is reduced across all 6 overlays via a cached static
middleware slice + fixed-resolve hoist in the shared controller, with computePosition
output byte-identical (browser-proven) and all lifecycle guards intact; middlewareBuilds
drops (wall-clock primary win, report-only); the Phase-8 Critical CR-01 is retired with a
cold-load retry proof; the am-popover a11y name/role/focusability DOM is byte-identical.
RPERF-03 tuned + RPERF-04 (overlay) guarded + D-05 closed.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-performance-tuning/09-03-SUMMARY.md` when done.
</output>
