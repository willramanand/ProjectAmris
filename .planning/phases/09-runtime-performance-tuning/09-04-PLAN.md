---
phase: 09-runtime-performance-tuning
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - api/perf.baseline.json
autonomous: true
requirements: [RPERF-01, RPERF-02, RPERF-03, RPERF-04]
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The committed api/perf.baseline.json is regenerated on the FULLY-TUNED code and now contains all three new count keys (sortComputes, filterCalls, middlewareBuilds) plus the unchanged update/updated/render/nodes(+computePosition/repositions) keys — the trustworthy forward reference Phase 11 sets enforcing thresholds against. (RPERF-01/02/03)"
    - "A phase perf summary tabulates the untuned to tuned count drop for all three targets (sortComputes N to 1 per render-seq; filterCalls 4-5 to 1 per keystroke; middlewareBuilds per-tick to per-config) sourced from the per-plan SUMMARYs, plus report-only wall-clock (median + mean+3sigma band). (RPERF-01/02/03)"
    - "Lit lifecycle (update/updated/render) and DOM node counts are IDENTICAL to the pre-Phase-9 baseline for every scenario — proof the three memos changed no observable render structure. (RPERF-01/02/03)"
    - "All three a11y snapshot specs (data-grid, combobox, popover) are green and unchanged — the tuning stripped no aria-*/role/focusability. (RPERF-04)"
    - "The full suite is green: test:run (jsdom+browser+perf), test:browser (regression gate incl. no-0,0 frame + CR-01 retry), and no size regression (size) — no bytes moved this phase. (all)"
    - "The frozen v1.0 CEM public surface and the byte-snapshot-guarded vite external array / .size-limit.json are untouched. (all)"
  artifacts:
    - "api/perf.baseline.json — regenerated to the final tuned state with the three new count keys"
  key_links:
    - "test:perf -> api/perf.json (all-tuned) -> perf-diff.mjs --write -> api/perf.baseline.json (committed)"
    - "per-plan SUMMARY untuned numbers -> aggregated phase perf summary (untuned to tuned deltas)"
  prohibitions:
    - "Do NOT re-run any tuning — this is verification + baseline finalization only."
    - "Do NOT touch the frozen vite external array / .size-limit.json / CEM baseline."
    - "Do NOT flip any budget from report-only to enforcing — that is Phase 11 (GATE-*)."
---

<objective>
Verification capstone (Wave 3) — the single owner of the committed
api/perf.baseline.json. Runs only after all three tunings (Plans 01-03) land.
Because api/perf.json is gitignored and each tuning plan demonstrated its own
untuned-to-tuned drop ephemerally, this plan finalizes the trustworthy forward
baseline on the fully-tuned code and proves the phase-level acceptance bar in one
place: three count improvements demonstrated, lifecycle/node counts and a11y DOM
byte-identical, full suite green, and no size regression.

Baseline decision (planner discretion, per CONTEXT.md): the committed baseline is
regenerated to the FINAL TUNED state (not the untuned "before") so it is the
forward reference Phase 11 gates against; the untuned "before" numbers live in the
per-plan SUMMARYs and are aggregated here. This satisfies the requirement's
"demonstrated count + wall-clock improvement" via the per-plan ephemeral diffs +
durable memo-effectiveness assertions in Plans 01-03.

Purpose: lock in a trustworthy post-Phase-9 measurement artifact and gate the
whole phase. Output: regenerated api/perf.baseline.json + a phase perf summary in
the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-runtime-performance-tuning/09-CONTEXT.md
@.planning/phases/09-runtime-performance-tuning/09-RESEARCH.md
@scripts/perf-diff.mjs
@api/perf.baseline.json
@test/perf/harness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: regenerate the committed perf baseline on fully-tuned code + aggregate the phase perf summary</name>
  <files>api/perf.baseline.json</files>
  <read_first>
    - scripts/perf-diff.mjs:24-33 (usage/modes), 54-67 (reduceToBaseline), 184-203 (--write mode + defaults), 205-223 (diff mode, report-only exit 0).
    - api/perf.baseline.json (current committed shape — per-scenario counts + wallClock{median,band} + throttle.profile + repeats).
    - 09-RESEARCH.md section "Perf Re-measurement Methodology" (steps 1-5) + section "Runtime State Inventory" (baseline must be regenerated to contain the new keys).
    - The three per-plan SUMMARYs (09-01/02/03-SUMMARY.md) for the recorded untuned "before" counts.
  </read_first>
  <action>
    On the merged, fully-tuned working tree run `npm run test:perf` to produce a fresh api/perf.json containing all four scenarios with the new count keys at their TUNED values (data-grid.sortComputes, combobox.filterCalls, overlay.middlewareBuilds) plus the unchanged update/updated/render/nodes (and computePosition/repositions for overlay) keys. Then regenerate the committed baseline with `node scripts/perf-diff.mjs --write api/perf.json api/perf.baseline.json`. Confirm api/perf.baseline.json now contains the three new keys. Run `npm run perf:diff` as a sanity check (it reports "No count drift" against the just-written tuned baseline — expected, report-only exit 0). In the plan SUMMARY, tabulate the untuned-to-tuned deltas for all three targets (pulled from the per-plan SUMMARYs) and the report-only wall-clock (median + mean+3sigma band) per scenario, and explicitly confirm update/updated/render + nodes (and computePosition/repositions for overlay) are IDENTICAL to the pre-Phase-9 baseline values, with the button control scenario staying flat as the noise control.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:perf && node scripts/perf-diff.mjs --write api/perf.json api/perf.baseline.json && npm run perf:diff</automated>
    <automated>cd C:/repos/ProjectAmris && grep -q sortComputes api/perf.baseline.json && grep -q filterCalls api/perf.baseline.json && grep -q middlewareBuilds api/perf.baseline.json && echo "baseline OK: all three new count keys present"</automated>
  </verify>
  <acceptance_criteria>
    - api/perf.baseline.json contains sortComputes (data-grid), filterCalls (combobox), and middlewareBuilds (overlay) at their tuned values (grep gate passes).
    - `npm run perf:diff` exits 0 with "No count drift" against the newly written baseline.
    - The SUMMARY tabulates untuned-to-tuned deltas for all three targets + report-only wall-clock per scenario.
    - update/updated/render + nodes (and computePosition/repositions for overlay) equal the pre-Phase-9 baseline values (recorded in SUMMARY); button scenario flat.
  </acceptance_criteria>
  <reversibility rating="reversible">Baseline is a measurement artifact; revert = restore the prior api/perf.baseline.json.</reversibility>
  <done>The committed baseline reflects the final tuned state with all three new count keys; the phase perf summary tabulates the demonstrated improvements; lifecycle/node counts unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: full-suite regression gate + no-size-regression + frozen-surface confirmation</name>
  <files>api/perf.baseline.json</files>
  <read_first>
    - package.json:71-84 (scripts: test:run, test:browser, test:perf, size, perf:diff, assert:no-lit).
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (the frozen vite external array is byte-snapshot-guarded; do not touch .size-limit.json).
    - 09-CONTEXT.md code_context ("no .size-limit.json / vite.config.ts external changes; the frozen array is byte-snapshot-guarded").
  </read_first>
  <action>
    Run the full regression gate on the merged tree: `npm run test:run` (jsdom + browser + perf all green), then `npm run test:browser` explicitly (the shared-controller blast-radius gate — floating-position, overlay-focus, data-grid-virtual, combobox-virtual, the Phase-8 no-0,0-frame specs, the CR-01 cold-load retry spec, and the three new a11y snapshot specs all green). Confirm no size regression with `npm run size` (no bytes move this phase) and that the no-bundled-Lit assertion still holds via `npm run assert:no-lit`. Do a source check that vite.config.ts `external` array and .size-limit.json are unmodified this phase (git diff shows no change to them). Record the gate results in the SUMMARY. Do NOT flip any budget to enforcing (Phase 11).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:run</automated>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser</automated>
    <automated>cd C:/repos/ProjectAmris && npm run size && npm run assert:no-lit</automated>
    <automated>cd C:/repos/ProjectAmris && git diff --quiet -- .size-limit.json vite.config.ts && echo "frozen size/external surface untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` green (all three lanes).
    - `npm run test:browser` green — the regression gate incl. no-0,0 frame, CR-01 retry, and the three a11y snapshot specs.
    - `npm run size` shows no regression; `npm run assert:no-lit` passes.
    - `git diff --quiet -- .size-limit.json vite.config.ts` passes (frozen surface untouched).
  </acceptance_criteria>
  <reversibility rating="reversible">Verification only — no source change beyond the baseline artifact from Task 1.</reversibility>
  <done>The full suite is green, no size regression, the frozen external/size surface and CEM are untouched, and no budget was flipped to enforcing.</done>
</task>

</tasks>

<flagged_assumptions>
No new unresolved edges are introduced by this verification-only plan. All 11
specless edge-probe rows are dispositioned in Plans 01-03 (authored as
must_haves.truths or flagged N/A); this capstone confirms the aggregate result.
</flagged_assumptions>

<artifacts_this_phase_produces>
This plan introduces NO new source symbols. It only regenerates the committed
measurement artifact:
- api/perf.baseline.json — regenerated to the final tuned state, now containing
  sortComputes / filterCalls / middlewareBuilds (created in Plans 01-03).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + baseline-artifact regeneration only; no source behavior change, no input path, no network, no new deps. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Tampering | committed perf baseline | low | accept | Baseline is a report-only measurement artifact under api/ (outside package.files — never ships); regenerated only via the explicit `--write` single-writer path (perf-diff.mjs T-07-07). |

No packages installed/upgraded — Package Legitimacy Gate N/A. No high-severity threats — nothing to block on.
</threat_model>

<verification>
- `npm run test:perf` green; committed baseline regenerated with the three new count keys (grep gate).
- `npm run perf:diff` exits 0 ("No count drift" vs the tuned baseline).
- `npm run test:run` + `npm run test:browser` green (full regression + a11y snapshots + CR-01 retry).
- `npm run size` + `npm run assert:no-lit` pass; `.size-limit.json`/`vite.config.ts` untouched.
- SUMMARY tabulates untuned-to-tuned deltas for sortComputes/filterCalls/middlewareBuilds + report-only wall-clock; confirms lifecycle/node counts unchanged.
</verification>

<success_criteria>
The committed post-Phase-9 perf baseline is trustworthy and forward-useful (contains
the new count keys at tuned values); all three count improvements are demonstrated
and aggregated; lifecycle/node counts and the a11y DOM are byte-identical across the
phase; the full suite is green with no size regression and the frozen surface intact.
RPERF-01/02/03/04 verified at the phase level; nothing flipped to enforcing (Phase 11).
</success_criteria>

<output>
Create `.planning/phases/09-runtime-performance-tuning/09-04-SUMMARY.md` when done.
</output>
