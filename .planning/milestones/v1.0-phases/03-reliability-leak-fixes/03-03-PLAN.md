---
phase: 03-reliability-leak-fixes
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/drawer/drawer.ts
  - src/components/command-palette/command-palette.ts
  - test/components/drawer.test.ts
  - test/components/command-palette.test.ts
  - test/components/popover.test.ts
autonomous: true
requirements: [FIX-03, FIX-02]
estimate:
  tokens: 54000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Focus restoration guards against removed/disconnected _previouslyFocused nodes via isConnected (drawer, command-palette) — closing an overlay whose opener was removed does not throw and does not focus the disconnected node (FIX-03)."
    - "am-popover performs no focus restoration (no _previouslyFocused), so closing it after its trigger is removed trivially cannot throw — documented and asserted, completing the FIX-03 overlay set (dialog covered in 03-02)."
    - "Global click/keydown listeners are gated on open state and torn down on disconnect across combobox, dropdown, context-menu, date-picker, popover, and tooltip, asserted with teardown spies (FIX-02) — verified green as the pre-freeze regression lock."
  artifacts:
    - src/components/drawer/drawer.ts
    - src/components/command-palette/command-palette.ts
  key_links:
    - "drawer.ts _hide() → isConnected guard gates _previouslyFocused.focus()"
    - "command-palette.ts updated() close-branch → isConnected guard gates _previouslyFocused.focus()"
    - "the 6 existing TEST-05 teardown suites are the FIX-02 proof; this plan runs them as the regression lock"
  prohibitions:
    - statement: "No user-visible regression — am-drawer and am-command-palette still open/close, emit am-open/am-close/am-select, restore focus to a still-connected opener, and (command-palette) toggle on Cmd/Ctrl+K exactly as before; the 6 FIX-02 components' open/close behavior is unchanged (no source edits)."
      status: must_hold
      verification: "npx vitest run test/components/drawer.test.ts test/components/command-palette.test.ts --project jsdom exits 0 with pre-existing cases green; the 6 FIX-02 TEST-05 suites still exit 0"
    - statement: "Lit-safe only — no innerHTML/eval, no global CSS, no hardcoded colors; property→event + no-global-state model preserved; frozen public surface unchanged."
      status: must_hold
      verification: "grep confirms no innerHTML/eval added; diffs touch only the isConnected guards + tests"
---

<objective>
Complete FIX-03 across the remaining focus-restoring overlays and verify FIX-02.

FIX-03: am-drawer (`_hide()` ~209-215) and am-command-palette (`updated()` close-branch ~224-230) restore focus guarded only by `instanceof HTMLElement`, missing `isConnected` — identical to the dialog gap fixed in 03-02. Add the isConnected guard to both. am-popover is listed in the FIX-03 overlay set but performs NO focus restoration (no `_previouslyFocused`, never moves focus into itself); document that finding and assert the trivial no-throw so the overlay set is completely accounted for.

FIX-02: the six components (combobox, dropdown, context-menu, date-picker, popover, tooltip) already gate document click/keydown listeners on open state and tear them down on disconnect, and Phase 1 (TEST-05) already ships attach-on-open / detach-on-close / detach-on-disconnect spies for all six (tooltip asserts the no-document-listener case). This plan verifies that proof is green as the pre-freeze regression lock — it does NOT rewrite the six working, tested components (a 6-component refactor would add regression risk against this phase's no-regression directive).

Purpose: focus restoration is safe when the opener is gone across every overlay (FIX-03); the global-listener teardown discipline is confirmed uniform and proven (FIX-02).
Output: isConnected guards in drawer + command-palette, a popover finding test, and a verified-green FIX-02 teardown-spy set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/drawer/drawer.ts
@src/components/command-palette/command-palette.ts
@src/components/popover/popover.ts
@test/components/drawer.test.ts
@test/components/command-palette.test.ts
@test/components/popover.test.ts
@test/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard drawer focus restoration with isConnected (FIX-03)</name>
  <files>src/components/drawer/drawer.ts, test/components/drawer.test.ts</files>
  <read_first>
    - src/components/drawer/drawer.ts (`_hide()` at ~209-215: `if (this._previouslyFocused instanceof HTMLElement) { this._previouslyFocused.focus(); }`; `_show()` captures `document.activeElement`).
    - src/components/dialog/dialog.ts (the analog fixed in 03-02 — replicate the exact isConnected guard shape and test approach).
    - test/components/drawer.test.ts (existing focus-restoration case, if any, plus mount/shadowQuery/waitForUpdate conventions).
  </read_first>
  <action>
    In drawer `_hide()`, extend the focus-restoration condition so `focus()` is only called when `this._previouslyFocused instanceof HTMLElement && this._previouslyFocused.isConnected`. No other behavior changes.

    Add a jsdom test to test/components/drawer.test.ts: create + append an opener button, focus it, mount + open the drawer, spy on the opener's `focus`, remove the opener from the DOM, then close the drawer; assert the close does NOT throw and the opener's `focus` spy was NOT called. If a connected-opener restoration case does not already exist, add one too so both guard branches are covered.
  </action>
  <verify>
    <automated>npx vitest run test/components/drawer.test.ts --project jsdom</automated>
  </verify>
  <acceptance_criteria>
    - drawer.ts `_hide()` condition includes `this._previouslyFocused.isConnected`.
    - test/components/drawer.test.ts contains a removed-opener case asserting no throw AND opener.focus not called, plus a connected-opener restoration case.
    - `npx vitest run test/components/drawer.test.ts --project jsdom` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">One-condition guard; behavior-preserving for connected openers.</reversibility>
  <done>Closing a drawer whose opener was removed does not throw and does not focus the disconnected node; connected-opener restoration unchanged; drawer suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard command-palette focus restoration with isConnected (FIX-03)</name>
  <files>src/components/command-palette/command-palette.ts, test/components/command-palette.test.ts</files>
  <read_first>
    - src/components/command-palette/command-palette.ts (the close-branch in `updated()` at ~224-230: `if (this._previouslyFocused instanceof HTMLElement) { this._previouslyFocused.focus(); }`; `_previouslyFocused` captured at ~219).
    - src/components/dialog/dialog.ts (the analog fixed in 03-02 — same guard shape).
    - test/components/command-palette.test.ts (existing open/close + focus conventions to mirror).
  </read_first>
  <action>
    In command-palette's `updated()` close-branch, extend the focus-restoration condition so `focus()` is only called when `this._previouslyFocused instanceof HTMLElement && this._previouslyFocused.isConnected`. Leave the Cmd/Ctrl+K global-keydown handling, the am-close dispatch, and input-focus-on-open behavior untouched.

    Add a jsdom test to test/components/command-palette.test.ts: create + append an opener button, focus it, mount the palette, open it (set `open = true`), spy on the opener's `focus`, remove the opener from the DOM, then close the palette (`open = false`); assert no throw and the opener's `focus` spy was NOT called. Add/keep a connected-opener restoration case so both branches are covered.
  </action>
  <verify>
    <automated>npx vitest run test/components/command-palette.test.ts --project jsdom</automated>
  </verify>
  <acceptance_criteria>
    - command-palette.ts close-branch condition includes `this._previouslyFocused.isConnected`.
    - test/components/command-palette.test.ts contains a removed-opener case asserting no throw AND opener.focus not called, plus a connected-opener restoration case.
    - `npx vitest run test/components/command-palette.test.ts --project jsdom` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">One-condition guard; behavior-preserving for connected openers.</reversibility>
  <done>Closing the command palette whose opener was removed does not throw and does not focus the disconnected node; Cmd/Ctrl+K toggle and am-close unchanged; palette suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Popover FIX-03 finding + FIX-02 teardown-spy verification (regression lock)</name>
  <files>test/components/popover.test.ts</files>
  <read_first>
    - src/components/popover/popover.ts (confirm there is NO `_previouslyFocused` / no focus restoration — popover is a non-modal click/hover panel that never moves focus into itself; document listeners are already gated on open at ~154-167 and torn down in disconnectedCallback at ~98-105).
    - test/components/popover.test.ts (existing "document listener teardown (TEST-05)" describe block at ~72-127 — the FIX-02 proof for popover; mirror its harness for the new finding case).
    - test/components/dropdown.test.ts / test/components/combobox.test.ts / test/components/context-menu.test.ts / test/components/date-picker.test.ts / test/components/tooltip.test.ts (the other five FIX-02 teardown suites to be run as the regression lock — read the describe titles so the verification enumerates the real suites).
  </read_first>
  <action>
    Add a popover FIX-03 finding case to test/components/popover.test.ts (its own `describe`, e.g. "am-popover — focus restoration (FIX-03 finding)") that: mounts a manual-trigger popover, focuses its trigger, opens it, removes the trigger from the DOM, then closes the popover and asserts the close does NOT throw. Add a comment stating the finding: am-popover has no `_previouslyFocused` and performs no focus restoration, so there is no disconnected-node focus() call to guard — the isConnected discipline is N/A here and the no-throw predicate holds trivially. This completes the FIX-03 overlay set (dialog/drawer/command-palette guarded; popover documented).

    Do NOT modify any of the six FIX-02 component source files. Instead, verify FIX-02 as the regression lock: run the existing TEST-05 teardown-spy suites for all six components (combobox, dropdown, context-menu, date-picker, popover, tooltip) and confirm they exit 0 — these already assert attach-on-open / detach-on-close / detach-on-disconnect (and, for tooltip, the no-document-listener case). Record in the SUMMARY that FIX-02 source is already gated-on-open + torn-down-on-disconnect and proven by these suites, and that no refactor was undertaken because it would add regression risk against the phase's no-regression directive.
  </action>
  <verify>
    <automated>npx vitest run test/components/popover.test.ts test/components/combobox.test.ts test/components/dropdown.test.ts test/components/context-menu.test.ts test/components/date-picker.test.ts test/components/tooltip.test.ts --project jsdom</automated>
  </verify>
  <acceptance_criteria>
    - test/components/popover.test.ts contains a FIX-03 finding case asserting close-after-trigger-removed does not throw, with a comment documenting popover has no focus-restoration path.
    - The six FIX-02 TEST-05 teardown-spy suites (combobox, dropdown, context-menu, date-picker, popover, tooltip) all exit 0 in the jsdom project.
    - No source edits were made to the six FIX-02 component files (git diff shows only test/components/popover.test.ts changed by this task).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Test + verification only; no source change.</reversibility>
  <done>Popover's absence of focus restoration is documented and no-throw is asserted; all six FIX-02 teardown-spy suites are green, locking the gated-on-open + torn-down-on-disconnect invariant before the freeze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal overlay lifecycle only. No user input parsing, no network, no new DOM sinks. Focus-guard hardening + test verification only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | am-drawer / am-command-palette focus restoration | low | mitigate | isConnected guard prevents focus() against disconnected nodes on close. |
| T-03-05 | Denial of Service | 6 overlay components document listeners | low | accept | Already gated-on-open + torn-down-on-disconnect and proven by TEST-05 spies; verified green, no change needed. |
| T-03-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs in this plan — no supply-chain surface introduced. |
</threat_model>

<verification>
- `npx vitest run test/components/drawer.test.ts test/components/command-palette.test.ts --project jsdom` exits 0.
- `npx vitest run test/components/popover.test.ts test/components/combobox.test.ts test/components/dropdown.test.ts test/components/context-menu.test.ts test/components/date-picker.test.ts test/components/tooltip.test.ts --project jsdom` exits 0.
- `npx tsc --noEmit` exits 0.
- git diff shows no source edits to the six FIX-02 component files.
</verification>

<success_criteria>
FIX-03 completed across the overlay set: drawer + command-palette guard focus restoration via isConnected (dialog covered in 03-02); popover's no-focus-restoration finding is documented and no-throw asserted. FIX-02 verified: the six components' gated-on-open + torn-down-on-disconnect global listeners are proven green by their TEST-05 teardown spies as the pre-freeze regression lock.
</success_criteria>

## Artifacts this plan produces

- `src/components/drawer/drawer.ts` — `_hide()` focus guard extended with `.isConnected`.
- `src/components/command-palette/command-palette.ts` — `updated()` close-branch focus guard extended with `.isConnected`.
- `test/components/drawer.test.ts` — removed-opener + connected-opener focus-guard cases (FIX-03).
- `test/components/command-palette.test.ts` — removed-opener + connected-opener focus-guard cases (FIX-03).
- `test/components/popover.test.ts` — FIX-03 finding case (no focus-restoration path; close-after-trigger-removed no-throw).
- (verification only) the six FIX-02 TEST-05 teardown-spy suites confirmed green — no new symbols.

<output>
Create `.planning/phases/03-reliability-leak-fixes/03-03-SUMMARY.md` when done.
</output>
