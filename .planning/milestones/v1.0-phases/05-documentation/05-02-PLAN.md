---
phase: 05-documentation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/theming.md
  - docs/validation.md
  - docs/usage.md
autonomous: true
requirements: [DOCS-02]
estimate:
  tokens: 55000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "docs/theming.md documents the --am-* token contract: the global semantic token system, light/dark theming via am-theme-provider / data-theme, and how a consumer overrides tokens — linking to the generated docs/contract.md for the exhaustive frozen list (D-01, DOCS-02)."
    - "docs/validation.md documents the as-built validation-message behavior: am-field wiring, touch-gated native validationMessage (blur/submit, D-01 timing), error-replaces-hint (D-02), and the public setCustomError precedence (D-03), for the 15 form-associated controls (DOCS-02)."
    - "docs/usage.md documents the consumer usage model including per-component import, the keyboard-shortcut registry + am-shortcuts provider usage (FEAT-03/04), and the virtualization auto-threshold behavior (DOCS-02)."
  artifacts:
    - docs/theming.md
    - docs/validation.md
    - docs/usage.md
  key_links:
    - "All three docs live under docs/ as GitHub-native Markdown (D-01) and will be linked from the rebuilt README (plan 05-03); theming.md links to the generated docs/contract.md (plan 05-01)."
---

<objective>
Author the three DOCS-02 prose pages as GitHub-native Markdown under `docs/`: a theming/token-contract guide, a validation-message usage guide, and a general usage guide (import model + keyboard shortcuts + virtualization behavior). These are the human-readable companions to the generated `docs/contract.md`; together they satisfy "validation, theming/token-contract, and usage docs exist — including the frozen slot/part/token contract."

Purpose: Prose docs live as Markdown in-repo linked from the README because GitHub-native rendering is where consumers of the GitHub Packages install actually land (D-01). Storybook stays the home for runnable examples (plan 05-04); these pages are the reference prose.
Output: `docs/theming.md`, `docs/validation.md`, `docs/usage.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-documentation/05-CONTEXT.md

# As-built feature behavior (source of truth — do not re-derive or re-litigate):
@.planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md
@.planning/phases/04-performance-feature-capabilities/04-05-SUMMARY.md
@.planning/phases/04-performance-feature-capabilities/04-06-SUMMARY.md
@.planning/phases/04-performance-feature-capabilities/04-08-SUMMARY.md
# Token + validation component sources:
@src/components/field/field.ts
# Prose to migrate out of the retiring static site (D-02):
@docs/theming.html
@docs/getting-started.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/theming.md — token contract + theming guide (DOCS-02)</name>
  <files>docs/theming.md</files>
  <read_first>
    - src/tokens/semantic.css.ts + dist/styles/tokens.css (already read via context) — the semantic `--am-*` token names and the light/dark structure a consumer overrides.
    - docs/theming.html — the retiring static page; migrate any still-accurate theming prose (D-02) into this Markdown page, corrected against the current token names.
    - api/AUDIT.md frozen-surface counts (global vs per-component tokens) so the prose matches the generated docs/contract.md.
  </read_first>
  <action>
    Write `docs/theming.md` documenting the `--am-*` token contract per DOCS-02 and D-01. Cover: the two token levels (primitive vs semantic) and that consumers theme via semantic `--am-*` tokens (no hardcoded colors); light/dark theming via `<am-theme-provider theme="light|dark|system">` and the `data-theme="dark"` on `:root` alternative; consuming the global stylesheet via `@willramanand/amris/styles/tokens.css` for apps not using the provider; and how `::part()` + slots + tokens together form the public styling API. Link to the generated `docs/contract.md` (plan 05-01) as the exhaustive, CI-enforced frozen token/slot/part list — this page is the guide, contract.md is the enumeration. Migrate accurate prose from docs/theming.html (D-02); drop anything that drifted from the current token names. Use `--am-*` token names only — no invented tokens.
  </action>
  <verify>
    <automated>test -f docs/theming.md && grep -q "am-theme-provider" docs/theming.md && grep -q "tokens.css" docs/theming.md && grep -q "contract.md" docs/theming.md && grep -Eq "\-\-am-" docs/theming.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/theming.md documents semantic `--am-*` theming, light/dark via am-theme-provider/data-theme, and the tokens.css stylesheet path.
    - It links to docs/contract.md for the frozen exhaustive surface (D-05/D-06 handoff).
    - Token names referenced exist in src/tokens / tokens.css (no invented tokens).
  </acceptance_criteria>
  <done>A consumer can theme Amris and find the frozen token/slot/part contract from this page.</done>
  <reversibility rating="reversible">New Markdown doc, revertible via git.</reversibility>
</task>

<task type="auto">
  <name>Task 2: docs/validation.md — validation-message usage guide (DOCS-02)</name>
  <files>docs/validation.md</files>
  <read_first>
    - .planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md — the authoritative as-built validation behavior: same-shadow-root aria-live message region wired by aria-describedby/aria-invalid; native validationMessage touch-gated on blur/failed submit (D-01, never on first paint); `setCustomError(message: string): void` public API with custom-wins precedence and `''`-clears-to-native (D-03); `:host([invalid])` reflection; polite per-field / assertive-on-submit announcement (D-04).
    - src/components/field/field.ts — how am-field composes label/hint/error slots and owns id generation.
  </read_first>
  <action>
    Write `docs/validation.md` documenting the shipped validation-message display (FEAT-01/FEAT-02) per DOCS-02. Cover: how `am-field` auto-surfaces a form control's native `ElementInternals.validationMessage` through the error region with same-shadow-root `aria-describedby`/`aria-invalid`; the `:user-invalid` timing model (errors appear on blur or failed submit and then track live as the user fixes them — never on first paint, D-01); that the error message replaces the hint text and the hint returns when the error clears (D-02); the public `setCustomError(message)` API for consumer/server-supplied errors with its precedence (custom wins over native; `setCustomError('')` falls back to the native constraint message, D-03); and the list of the 15 form-associated controls this applies to, noting am-search-field and am-file-upload are NOT form-associated and are excluded. Cross-link to the Patterns/Validation Storybook example (plan 05-04) as the runnable demonstration (this is the discretion item — validation prose and stories cross-link). Describe only shipped public behavior; do not document src/internal ValidationController as consumer API.
  </action>
  <verify>
    <automated>test -f docs/validation.md && grep -q "setCustomError" docs/validation.md && grep -q "validationMessage" docs/validation.md && grep -q "am-field" docs/validation.md && grep -Eiq "blur|user-invalid|submit" docs/validation.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/validation.md documents am-field auto-surfacing validationMessage, the touch-gated timing (D-01), error-replaces-hint (D-02), and setCustomError precedence (D-03).
    - It states which controls participate (15 form-associated; search-field/file-upload excluded).
    - It does not present src/internal ValidationController as public API.
  </acceptance_criteria>
  <done>A consumer can wire validation messages and use setCustomError from this page.</done>
  <reversibility rating="reversible">New Markdown doc, revertible via git.</reversibility>
</task>

<task type="auto">
  <name>Task 3: docs/usage.md — import model + shortcuts + virtualization (DOCS-02)</name>
  <files>docs/usage.md</files>
  <read_first>
    - package.json `exports` — the deep entry points (`.`, `./core`, `./components/*`, `./styles/tokens.css`) a consumer imports; the per-component tree-shakeable import model.
    - .planning/phases/04-performance-feature-capabilities/04-05-SUMMARY.md + 04-06-SUMMARY.md — the shortcut registry public shape: `<am-shortcuts>` provider distributing an explicit ShortcutRegistry via @lit/context; `registry.register({ id, keys, scope, handler })` returning a no-throw result union; reserved-combo blocklist + single-key opt-in (WCAG 2.1.4); am-command-palette rebindable mod+k with graceful fallback when no provider is present.
    - .planning/phases/04-performance-feature-capabilities/04-08-SUMMARY.md — virtualization auto-activates above ~100 rows with no public attribute (D-05 freeze-neutral); the documented mobile screen-reader recycled-row limitation.
  </read_first>
  <action>
    Write `docs/usage.md` as the general consumer usage guide per DOCS-02. Cover: the import model (full bundle `@willramanand/amris`, `./core`, and per-component `@willramanand/amris/components/<name>` deep imports for tree-shaking; the `./styles/tokens.css` stylesheet); the keyboard-shortcut registry usage — wrapping a subtree in `<am-shortcuts>` to provide a registry, calling `registry.register(...)` with the no-throw result, the reserved-combo blocklist and single-key opt-in behavior, and that am-command-palette works with or without a provider (mod+k default, rebindable when a provider is present) — this is the discretion decision to fold shortcuts into the general usage docs rather than a standalone page; and a short note on list virtualization: am-data-grid and combobox/select popups auto-virtualize above ~100 rows with no public attribute (freeze-neutral), plus the documented mobile SR limitation. Link out to docs/theming.md, docs/validation.md, and docs/contract.md. Reference only shipped public API (am-shortcuts, registry.register, deep import paths) — not src/internal machinery.
  </action>
  <verify>
    <automated>test -f docs/usage.md && grep -q "am-shortcuts" docs/usage.md && grep -q "@willramanand/amris" docs/usage.md && grep -Eiq "virtualiz" docs/usage.md && grep -q "register" docs/usage.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/usage.md documents deep/per-component imports, the am-shortcuts + registry.register usage (FEAT-03/04), and the virtualization auto-threshold note.
    - It links to theming.md, validation.md, and contract.md.
    - It references only shipped public API.
  </acceptance_criteria>
  <done>A consumer can import components, wire shortcuts, and understand virtualization from this page.</done>
  <reversibility rating="reversible">New Markdown doc, revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| as-built behavior → prose | Docs must describe only shipped public surface, not internal controllers or unshipped/deferred features. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information disclosure | prose docs | medium | mitigate | Content sourced strictly from Phase 4 SUMMARYs + public exports; src/internal (ValidationController/ShortcutRegistry/virtualize-support) is documented only as behavior, never as a consumer import. |
| T-05-06 | Repudiation | prose docs | low | mitigate | Token/API names verified against src/tokens, package.json exports, and CEM before publishing (grep acceptance checks). |
| T-05-SC | Tampering | dependencies | low | accept | No package installs this phase. |
</threat_model>

<verification>
- Each doc exists, references only real token/API names, and links resolve to sibling docs (theming↔contract, validation↔stories, usage↔all).
- No src/internal symbol is presented as a consumer import.
</verification>

<success_criteria>
Validation, theming/token-contract, and usage docs exist as GitHub-native Markdown under docs/, completing the prose half of DOCS-02.
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-02-SUMMARY.md` when done.
</output>
