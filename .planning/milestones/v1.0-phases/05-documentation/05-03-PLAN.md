---
phase: 05-documentation
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - docs/vision.md
  - README.md
  - docs/getting-started.html
  - docs/components.html
  - docs/theming.html
  - docs/docs.ts
  - docs/styles.css
autonomous: true
requirements: [DOCS-01]
estimate:
  tokens: 50000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "README.md is a consumer-first quick-start: what-it-is → install (GitHub Packages registry + Lit peer-dependency) → browser floor (Safari 16.4 / ElementInternals, including that form controls silently fail to submit below the floor = DOCS-01) → a ~30s usage example → links out to docs/*.md, Storybook, and BROWSER_SUPPORT.md (D-03)."
    - "The vision / roadmap / non-goals prose is relocated to docs/vision.md and is no longer the first thing a consumer reads in the README (D-04)."
    - "The hand-authored docs/*.html static site (getting-started.html, components.html, theming.html) + docs.ts + styles.css is retired after confirming no build wiring references it (D-02)."
  artifacts:
    - README.md
    - docs/vision.md
  key_links:
    - "README quick-start links to docs/contract.md (05-01), docs/theming.md + docs/validation.md + docs/usage.md (05-02), local Storybook, and BROWSER_SUPPORT.md; DOCS-01 browser-floor content summarizes BROWSER_SUPPORT.md rather than duplicating it."
---

<objective>
Turn the repository's front door into a consumer-first package entry point: relocate the existing 702-line vision/roadmap prose to `docs/vision.md`, rebuild `README.md` as a quick-start that includes the DOCS-01 peer-dependency + browser-floor statement, and retire the drifted hand-authored `docs/*.html` static site.

Purpose: The current README is a "Project Amris Library Roadmap / Vision" doc, not a package entry point (D-03). A consumer landing on the GitHub Packages repo needs what-it-is → install → browser floor → a 30-second example → links, fast — with the internal vision moved out of the way (D-04) and the duplicated, drifting HTML site retired (D-02).
Output: rebuilt `README.md`, new `docs/vision.md`, and removal of `docs/*.html` + `docs/docs.ts` + `docs/styles.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-documentation/05-CONTEXT.md

# DOCS-01 source (summarize + link, do NOT duplicate):
@BROWSER_SUPPORT.md
# Install/peer-dep facts:
@package.json
# The doc pages the README links to (created in 05-01 / 05-02):
@docs/contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate vision/roadmap/non-goals prose to docs/vision.md (D-04)</name>
  <files>docs/vision.md</files>
  <read_first>
    - README.md (current 702-line roadmap doc) — the vision, core goals, non-goals, technical/product requirements, and design-token narrative that must be preserved, not lost (D-04).
  </read_first>
  <action>
    Create `docs/vision.md` and move the current README's vision / core-goals / non-goals / roadmap / requirements prose into it verbatim (lightly re-headed as a "Project Vision & Background" document) per D-04. Preserve the content faithfully — this is a relocation, not a rewrite — so nothing is lost; it simply stops being the first thing a consumer reads. Do not edit README.md in this task (Task 2 rebuilds it); this task only extracts the prose into its new home so Task 2 can safely overwrite README.md.
  </action>
  <verify>
    <automated>test -f docs/vision.md && grep -Eiq "vision|non-goal|roadmap" docs/vision.md && test $(wc -l < docs/vision.md) -gt 40</automated>
  </verify>
  <acceptance_criteria>
    - docs/vision.md exists and contains the relocated vision/roadmap/non-goals prose (D-04).
    - The prose is preserved (substantial content, not a stub).
  </acceptance_criteria>
  <done>The vision prose has a preserved home outside the README.</done>
  <reversibility rating="reversible">New Markdown doc; original prose remains in git history regardless.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Rebuild README.md as a consumer-first quick-start + DOCS-01 (D-03)</name>
  <files>README.md</files>
  <read_first>
    - BROWSER_SUPPORT.md — the Safari 16.4 floor, ElementInternals requirement, and the "form controls submit no value below the floor" statement; the README summarizes and links this (DOCS-01) rather than duplicating the full table.
    - package.json — name `@willramanand/amris`, `publishConfig.registry` (GitHub Packages `npm.pkg.github.com`, restricted), `peerDependencies.lit ^3.3.2`, and the `exports`/import entry points for the usage example.
    - docs/contract.md, docs/theming.md, docs/validation.md, docs/usage.md (from 05-01/05-02) — the link targets.
  </read_first>
  <action>
    Rebuild `README.md` from scratch as a consumer-first quick-start in the D-03 order: (1) what-it-is — one-paragraph description of the framework-agnostic Lit 3 / Web Components library; (2) install — configuring the GitHub Packages registry (`npm.pkg.github.com`, scope `@willramanand`) and installing `@willramanand/amris`, plus the Lit `^3.3.2` peer-dependency the consumer must provide (do not bundle Lit); (3) browser floor — the DOCS-01 statement: supported floor is Safari 16.4 (and Chrome/Edge 111, Firefox 121), the ElementInternals requirement, and explicitly that **form-associated controls silently fail to submit their value below the floor** — summarize and link BROWSER_SUPPORT.md, do not duplicate its full tables; (4) a ~30-second usage example — import a component (or tokens.css) and drop an `<am-*>` element into HTML; (5) a Documentation links section pointing to docs/usage.md, docs/theming.md, docs/validation.md, docs/contract.md, the local Storybook (`npm run storybook`), and BROWSER_SUPPORT.md, plus a link to docs/vision.md for project background placed so it is NOT the first thing a consumer reads (D-04). Keep it concise; the reference detail lives in the linked docs. Every stated fact (registry URL, peer-dep range, floor version) must match package.json / BROWSER_SUPPORT.md.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "npm.pkg.github.com" README.md && grep -q "@willramanand/amris" README.md && grep -Eiq "peer" README.md && grep -q "Safari 16.4" README.md && grep -Eiq "silently|silent" README.md && grep -q "BROWSER_SUPPORT.md" README.md && grep -q "docs/usage.md" README.md && grep -q "docs/vision.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md follows the D-03 order (what-it-is → install → browser floor → usage example → links).
    - It states the GitHub Packages registry, the Lit peer-dependency, the Safari 16.4 floor, and the silent form-submit failure below the floor (DOCS-01).
    - It links docs/*.md, Storybook, BROWSER_SUPPORT.md, and docs/vision.md (vision not the lead).
    - Facts match package.json + BROWSER_SUPPORT.md.
  </acceptance_criteria>
  <done>README.md is a package entry point a consumer can install and use from in under a minute.</done>
  <reversibility rating="reversible">README rewrite; prior content preserved in docs/vision.md and git history.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Retire the docs/*.html static site + docs.ts (D-02)</name>
  <files>docs/getting-started.html, docs/components.html, docs/theming.html, docs/docs.ts, docs/styles.css</files>
  <read_first>
    - docs/docs.ts + docs/*.html — confirm the static site's only inbound references are its own intra-site cross-links (already verified during planning: no vite.config.ts / package.json script / CI reference targets docs/*.html or docs/docs.ts). The still-useful prose from theming.html/getting-started.html was migrated in plan 05-02 (docs/theming.md, docs/usage.md).
  </read_first>
  <action>
    Retire the hand-authored static site per D-02. First re-verify there is no build wiring: grep the repo (vite.config.ts, package.json scripts, .github/workflows, index.html) for references to `docs/`, `docs.ts`, or the three html filenames — expect only intra-`docs/` cross-links. Having confirmed the useful prose already lives in the new Markdown docs (05-02), delete `docs/getting-started.html`, `docs/components.html`, `docs/theming.html`, `docs/docs.ts`, and `docs/styles.css`. This is git-reversible (D-02, not a one-way door). Leave the new `docs/*.md` files intact.
  </action>
  <verify>
    <automated>test ! -f docs/getting-started.html && test ! -f docs/components.html && test ! -f docs/theming.html && test ! -f docs/docs.ts && test -f docs/usage.md && test -f docs/theming.md</automated>
  </verify>
  <acceptance_criteria>
    - The three docs/*.html files, docs/docs.ts, and docs/styles.css are removed.
    - No build/config file referenced them (re-verified before deletion).
    - The new docs/*.md pages remain.
  </acceptance_criteria>
  <done>The drifted static site is retired; docs/ contains only the maintained Markdown set (D-02).</done>
  <reversibility rating="reversible">Deletion is recoverable via git history (D-02 explicitly not a one-way door).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented facts → consumer trust | Install/registry/peer-dep/browser-floor claims in the README must match the actual package.json + BROWSER_SUPPORT.md. |
| retired site → build | Deleting docs/*.html must not break any build/dev/CI path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Repudiation | README.md | medium | mitigate | Registry URL, peer-dep range, and Safari 16.4 floor asserted in README are grep-verified against package.json + BROWSER_SUPPORT.md; DOCS-01 silent-form-fail statement is present. |
| T-05-08 | Denial of service | build/dev pipeline | low | mitigate | Task 3 re-greps for docs/*.html build wiring before deletion; planning confirmed only intra-docs cross-links exist. |
| T-05-SC | Tampering | dependencies | low | accept | No package installs this phase. |
</threat_model>

<verification>
- README asserts the GitHub Packages registry, Lit peer-dep, Safari 16.4 floor, and silent form-submit failure, and links docs/*.md + Storybook + BROWSER_SUPPORT.md.
- `git status` shows docs/*.html + docs.ts + styles.css removed and docs/vision.md added.
- No remaining repo reference to the deleted files (grep clean).
</verification>

<success_criteria>
README is a consumer-first quick-start documenting the Lit peer-dependency and Safari 16.4 / ElementInternals floor with the silent form-submit failure (DOCS-01); vision is relocated (D-04); the static HTML site is retired (D-02).
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-03-SUMMARY.md` when done.
</output>
