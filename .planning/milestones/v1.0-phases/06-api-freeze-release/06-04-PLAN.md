---
phase: 06-api-freeze-release
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified: []
autonomous: false
requirements: [SHIP-04]
user_setup:
  - service: github-packages
    why: "Publishing @willramanand/amris@1.0.0 to GitHub Packages and creating the v1.0 tag requires a maintainer with packages:write on the repository."
    dashboard_config:
      - task: "Confirm the repository's Actions secrets / default GITHUB_TOKEN grant packages:write for the release workflow"
        location: "GitHub → repo Settings → Actions → General → Workflow permissions"
estimate:
  tokens: 20000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The one-way publish is gated by an explicit human decision before it fires — no autonomous run publishes v1.0."
    - statement: "SHIP-04: `@willramanand/amris@1.0.0` is published to GitHub Packages and a v1.0 tag exists on the release commit (ROADMAP Phase 6 SC4)."
      verification: backstop
    - statement: "After publish, package.json `version` is `1.0.0` (applied by `changeset version` on the release commit)."
      verification: backstop
  artifacts:
    - "git tag v1.0.0 (release tag)"
    - "@willramanand/amris@1.0.0 on GitHub Packages"
  key_links:
    - "The green-gated, SHA-pinned pipeline from plan 06-03 performs the mechanical publish; this plan is the human authorization gate on the irreversible door."
---

<objective>
The one-way door of the milestone: authorize and perform the v1.0 publish. A published package version is immutable and a release tag is effectively permanent, so this plan gates the publish behind an explicit human decision (per the reversibility policy) and then records the human-performed release. All reversible preparation — enforcing gate, package hardening, smoke, SHA-pinned green-gated pipeline, major Changeset, frozen baseline — landed in plans 06-01..06-03; nothing here is autonomous.

Purpose: SHIP-04 completes only when v1.0 actually ships on the green pipeline and is tagged. Publishing is a live, irreversible action against an external registry that the agent must not perform on its own; the human confirms the door, then triggers the release.
Output: `@willramanand/amris@1.0.0` on GitHub Packages and a v1.0 release tag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The staged, SHA-pinned, green-gated release path this plan fires:
@.github/workflows/release.yml
# The version-carrying Changeset staged in plan 06-03:
@.changeset/release-v1.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Publish @willramanand/amris@1.0.0 to GitHub Packages and tag v1.0 now?</decision>
  <context>
    This is a one-way door. A published package version is immutable (it cannot be overwritten or unpublished cleanly), and the v1.0 tag is a permanent public marker of the frozen API. Everything reversible is already in place and verified: the enforcing surface-diff gate (06-01), the hardened exports/sideEffects/peer and passing tarball smoke (06-02), and the SHA-pinned, green-gated pipeline + major Changeset + zero-drift frozen baseline (06-03). Before confirming, the maintainer should be satisfied that CI is fully green on main and that `changeset version` will land exactly 1.0.0. Also confirm the resolved decision carried from planning: the Lit peer range is `^3.3.2` (chosen and recorded in 06-02 to match the CLAUDE.md/PROJECT.md-documented ElementInternals/Safari-16.4 floor, overriding SHIP-02's literal `^3.3.0` wording). The decision is already made — this checkpoint just confirms `^3.3.2` before the immutable publish; it is the last moment to override.
  </context>
  <options>
    <option id="publish-v1">
      <name>Publish v1.0 now</name>
      <pros>Ships the frozen, dependable v1.0 consumers can depend on; completes SHIP-04 and the milestone.</pros>
      <cons>Immutable — a mistake in the published surface cannot be recalled, only superseded by 1.0.1+.</cons>
    </option>
    <option id="hold">
      <name>Hold for more changes</name>
      <pros>Keeps the door open to land additional fixes or reconsider the Lit peer range before freezing.</pros>
      <cons>v1.0 does not ship; the milestone stays open.</cons>
    </option>
  </options>
  <resume-signal>Select: publish-v1 or hold</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task: Trigger the v1.0 release and confirm publish + tag</name>
  <reversibility rating="one-way">Publishing an immutable package version and creating the v1.0 tag cannot be undone — a wrong surface can only be superseded by a new version, never retracted.</reversibility>
  <action>Fire the staged, green-gated release so the pipeline publishes @willramanand/amris@1.0.0 to GitHub Packages and tags v1.0 — a live, credentialed, irreversible action the agent must not perform autonomously.</action>
  <instructions>
    Plans 06-01..06-03 already staged everything automatable: the enforcing surface-diff gate, the hardened package + passing tarball smoke, and a SHA-pinned, green-gated `changesets/action` pipeline plus a `major` Changeset that carries the version to 1.0.0. The ONE human step:
    1. Confirm CI is green on `main` (verify / browser / surface-diff / size all passing).
    2. Merge the changesets-generated "Version Packages" PR (this applies `changeset version` → package.json 1.0.0 and triggers `changeset publish` on the green-gated path), OR run the documented gated `workflow_dispatch` fallback from plan 06-03.
  </instructions>
  <verification>
    - `npm view @willramanand/amris version --registry=https://npm.pkg.github.com` reports `1.0.0` (or the repo Packages tab shows 1.0.0).
    - `git fetch --tags && git tag --list "v1.0*"` shows the release tag on the release commit.
    - Optional: install the published `@willramanand/amris@1.0.0` in a scratch project and run the plan 06-02 resolution smoke against it.
  </verification>
  <resume-signal>Type "published" once 1.0.0 is live on GitHub Packages and the v1.0 tag exists, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human authorization → irreversible publish | The immutable publish + permanent tag must not fire without an explicit human decision. |
| release credential → registry | The publish token is exercised only by the green-gated pipeline, never by the agent or a PR trigger. |
| published tarball → consumers | What ships is exactly the `files` allowlist proven by the smoke; no secrets or source leak in the tarball. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Elevation of privilege | v1.0 publish action | high | mitigate | Publish gated by a blocking human decision (never auto-approved) on the SHA-pinned, least-privilege, green-gated pipeline from 06-03. |
| T-06-11 | Repudiation | release tag + published version | medium | mitigate | Publish flows through changesets on a green pipeline producing a tagged, attributable release commit; human witnesses version + tag. |
| T-06-12 | Information disclosure | published tarball | medium | mitigate | `files` stays `["dist","README.md"]`; the plan 06-02 smoke proves tarball contents; token passed only as workflow env, never logged. |
| T-06-SC | Tampering | supply chain at publish | high | mitigate | SHA-pinned actions (06-03) + human authorization here close the publish-path supply-chain surface; no `[ASSUMED]`/`[SLOP]` package installs in this phase. |
</threat_model>

<verification>
- `npm view @willramanand/amris version --registry=https://npm.pkg.github.com` reports `1.0.0` (backstop — observable only after the live publish).
- A `v1.0*` tag exists on the release commit (`git tag --list "v1.0*"`).
- Installing the published `@willramanand/amris@1.0.0` and running the plan 06-02 smoke resolves full + per-component + tokens.css entries.
</verification>

<success_criteria>
v1.0 is published to GitHub Packages and tagged, authorized by an explicit human decision on a green, SHA-pinned pipeline — SHIP-04 and the milestone complete.
</success_criteria>

<artifacts_produced>
This plan produces (observed on external systems, no local file edits):
- `@willramanand/amris@1.0.0` published to GitHub Packages (npm.pkg.github.com).
- A `v1.0` release tag on the release commit.
- package.json `version` applied to `1.0.0` by `changeset version` on the release commit.
</artifacts_produced>

<output>
Create `.planning/phases/06-api-freeze-release/06-04-SUMMARY.md` when done.
</output>
