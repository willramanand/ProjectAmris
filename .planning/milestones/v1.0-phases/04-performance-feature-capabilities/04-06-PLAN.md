---
phase: 04-performance-feature-capabilities
plan: 06
type: execute
wave: 3
depends_on: ["04-05"]
files_modified:
  - package.json
  - src/components/shortcuts/shortcuts.ts
  - src/components/shortcuts/index.ts
  - src/index.ts
  - src/index.all.ts
  - src/components/command-palette/command-palette.ts
  - test/components/command-palette.test.ts
  - test/browser/shortcuts-context.test.ts
autonomous: true
requirements: [FEAT-03, FEAT-04]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "am-shortcuts is a public, exported provider element that owns a ShortcutRegistry instance and distributes it per-subtree via @lit/context (D-08)."
    - "am-shortcuts attaches a single document keydown listener via TeardownScope and torn down on disconnect; it resolves the true focused element via event.composedPath()[0] across shadow roots (Pitfall 4)."
    - "Single-key shortcuts are suppressed while the true focused element is editable (input/textarea/select/contentEditable/form-associated custom element) and while event.isComposing is true (WCAG 2.1.4, IME)."
    - "am-command-palette registers mod+k through the provider when an am-shortcuts provider is present in its subtree (rebindable), and falls back to its existing hardcoded mod/ctrl+k document listener when no provider is present (D-09 graceful fallback)."
    - "The default mod+k binding preserves today's Cmd/Ctrl+K open UX with no provider present."
    - "EDGE: typing the letter k inside a focused text field does NOT open the command palette (single-key/typing suppression via composedPath)."
  artifacts:
    - src/components/shortcuts/shortcuts.ts
    - src/components/shortcuts/index.ts
    - test/browser/shortcuts-context.test.ts
  key_links:
    - "am-shortcuts ContextProvider(shortcutRegistryContext) -> am-command-palette ContextConsumer"
    - "am-shortcuts document keydown -> composedPath()[0] editable/isComposing check -> registry dispatch (TeardownScope signal)"
    - "package.json dependencies += @lit/context ^1.1.6 (already matched by vite external /^@lit\\//)"
  prohibitions:
    - "NO module-level singleton registry — am-shortcuts owns an explicit instance (D-08)."
    - "NO new registered tagName beyond am-shortcuts this phase."
    - "NO firing single-key shortcuts while typing (WCAG 2.1.4) — composedPath editable suppression."
    - "NO preventDefault on reserved browser/OS combos (D-10)."
    - "NO bundling @lit/context — it stays external (D-12)."
---

<objective>
Deliver the FEAT-04 distribution layer: a new public `am-shortcuts` provider element that owns a `ShortcutRegistry` instance and distributes it per-subtree via `@lit/context`, plus a single document keydown listener that dispatches via `composedPath()` with typing/IME suppression. Refactor `am-command-palette` off its hardcoded Cmd+K onto the registry with graceful fallback (D-09). Prove the cross-shadow-root composedPath behavior and typing suppression in the Chromium browser lane.

Purpose: FEAT-04 — the one net-new registered element this phase adds; command-palette's open combo becomes rebindable while today's drop-in Cmd/Ctrl+K keeps working when no provider is present.
Output: am-shortcuts (exported + JSDoc'd for CEM), the refactored command-palette, and the browser shortcuts-context test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/phases/04-performance-feature-capabilities/04-05-SUMMARY.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `am-shortcuts` public element + `AmShortcuts` class + `shortcutRegistryContext` — `src/components/shortcuts/shortcuts.ts` + `index.ts` (NEW registered tagName; the one net-new element this phase adds). Exported from `src/index.ts` + `src/index.all.ts`. Captured in the CEM baseline + Changeset by plan 04-10.
- `package.json` `dependencies` += `@lit/context` `^1.1.6` (D-12/D-13, kept external — already matched by vite `/^@lit\//`).
- am-command-palette ContextConsumer refactor (D-09).
- `test/browser/shortcuts-context.test.ts`.

<tasks>

<task type="auto">
  <name>Create the am-shortcuts provider element with @lit/context distribution + document keydown dispatch</name>
  <reversibility rating="costly">Adds @lit/context to package.json dependencies (D-12) — changes dependency classification + interacts with SHIP-02 exports/sideEffects; moving peer↔dep after publish is consumer-visible.</reversibility>
  <files>package.json, src/components/shortcuts/shortcuts.ts, src/components/shortcuts/index.ts, src/index.ts, src/index.all.ts</files>
  <read_first>
    - src/components/theme-provider/theme-provider.ts (thin exported provider element analog: @customElement, :host { display: contents }, render() returns <slot></slot>, declare global HTMLElementTagNameMap block, JSDoc header)
    - src/internal/controllers/shortcut-registry.ts (the ShortcutRegistry instance am-shortcuts owns — from 04-05)
    - src/internal/helpers/teardown-scope.ts (attach the document keydown with { signal: scope.signal }; scope.clear() on disconnect — Phase 3 discipline)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Code Examples: Providing the registry via @lit/context" (lines 369-383) + §"Pattern 2" composedPath/isComposing notes + §"Pitfall 4"
    - src/index.ts:14-15 (where AmThemeProvider is exported — add AmShortcuts next to it) and src/index.all.ts:10-12
  </read_first>
  <action>
    Add `@lit/context` at `^1.1.6` (caret, D-13) to package.json `dependencies` (kept external — it is already matched by the vite `/^@lit\//` external pattern, so no vite.config.ts edit is needed; do NOT add it to devDependencies or bundle it). Run `npm install` to update the lockfile.

    Create `src/components/shortcuts/shortcuts.ts`: export `const shortcutRegistryContext = createContext<ShortcutRegistry>('amris-shortcut-registry')` and `@customElement('am-shortcuts') class AmShortcuts extends LitElement`. The element owns `private _registry = new ShortcutRegistry()` (explicit instance — not a module singleton) and a `new ContextProvider(this, { context: shortcutRegistryContext, initialValue: this._registry })`. `static styles` sets `:host { display: contents; }`; `render()` returns `<slot></slot>`. Use a `TeardownScope`; in `connectedCallback` attach `document.addEventListener('keydown', this._onKeydown, { signal: this._teardown.signal })`; in `disconnectedCallback` call `this._teardown.clear()`. `_onKeydown` resolves `const target = e.composedPath()[0] as HTMLElement` (pierces shadow roots — a document listener otherwise sees the retargeted host, Pitfall 4); computes whether the target is editable (tagName input/textarea/select, isContentEditable, or a form-associated custom element) and whether `e.isComposing`; asks the registry to resolve the combo among the active scopes; and invokes the handler UNLESS it is a single-key shortcut while the target is editable or composing (suppress). Add the `declare global { interface HTMLElementTagNameMap { 'am-shortcuts': AmShortcuts } }` block. Add a JSDoc header with `@slot` (default), and document that the element distributes the registry via context and drives shortcuts — follow the theme-provider.ts JSDoc convention so CEM captures the new public surface. Optionally expose the registry (e.g. a `registry` getter) so consumers can register imperatively.

    Create `src/components/shortcuts/index.ts` as the barrel (`export * from './shortcuts.js';`). Export `AmShortcuts` (and `shortcutRegistryContext` if consumers need it) from both `src/index.ts` (next to AmThemeProvider at :14-15) and `src/index.all.ts`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies['@lit/context']==='^1.1.6'?0:1)"` exits 0; `@lit/context` NOT in devDependencies.
    - `node -e "const s=require('fs').readFileSync('src/index.ts','utf8'); process.exit(/AmShortcuts/.test(s)?0:1)"` exits 0 (exported public surface).
    - `npm run build` succeeds and `@lit/context` does not appear bundled in dist (stays external via /^@lit\//): `node -e "const fs=require('fs');const g=require('glob');process.exit(0)"` (build green is the gate; external is enforced by the existing vite pattern).
    - `npm run build:manifest` regenerates dist/custom-elements.json and it contains an `am-shortcuts` entry.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run build:manifest</automated>
  </verify>
  <done>am-shortcuts is a public provider element owning an explicit ShortcutRegistry instance, distributing it via @lit/context, with a single composedPath/isComposing-aware document keydown torn down via TeardownScope; @lit/context is an external dependency; the element is exported and appears in the CEM manifest.</done>
</task>

<task type="auto">
  <name>Refactor am-command-palette off hardcoded Cmd+K onto the registry with graceful fallback (D-09)</name>
  <files>src/components/command-palette/command-palette.ts, test/components/command-palette.test.ts</files>
  <read_first>
    - src/components/command-palette/command-palette.ts:206-214 (current connectedCallback/disconnectedCallback document keydown add/remove) and :240-245 (_handleGlobalKeydown: (metaKey||ctrlKey)&&key==='k' → preventDefault + toggle open — the fallback to PRESERVE verbatim)
    - src/components/shortcuts/shortcuts.ts (shortcutRegistryContext to consume)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Command-palette graceful fallback (D-09)" (lines 386-399) — ContextConsumer wiring
    - .planning/phases/04-performance-feature-capabilities/04-PATTERNS.md §"command-palette.ts (MODIFIED — D-09 refactor)"
  </read_first>
  <action>
    Add a `new ContextConsumer(this, { context: shortcutRegistryContext, subscribe: true, callback: (registry) => {…} })` to am-command-palette. When a registry becomes available in the subtree, register `{ id: 'command-palette.open', keys: 'mod+k', handler: () => { this.open = !this.open; }, description: 'Open command palette' }` through it (rebindable) and remove the hardcoded document listener; when no registry is present (consumer resolves to undefined), keep the EXISTING `_handleGlobalKeydown` document-listener fallback verbatim (today's mod/ctrl+k behavior). Handle the subscribe timing: register/unregister as the registry appears/disappears; on disconnect, unregister the binding and remove any document listener. `mod+k` normalizes to metaKey||ctrlKey — matching today's UX. Do not change the palette's open/close animation or focus behavior.

    Extend `test/components/command-palette.test.ts` (jsdom): with NO provider, mod+k / ctrl+k still toggles open (fallback path unchanged). With a provider present (mount inside am-shortcuts), the palette registers command-palette.open and toggling is driven through the registry; assert the hardcoded document listener is not the active path when a provider exists.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run command-palette` passes: fallback path (no provider) toggles on mod/ctrl+k; provider path registers command-palette.open and toggles through the registry.
    - The no-provider fallback preserves the existing Cmd/Ctrl+K open behavior (regression-locked by the extended test).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run command-palette</automated>
  </verify>
  <done>am-command-palette registers a rebindable mod+k through an am-shortcuts provider when present and falls back to its hardcoded Cmd/Ctrl+K when absent (D-09), with no UX regression.</done>
</task>

<task type="auto">
  <name>Prove composedPath dispatch + typing/IME suppression in the Chromium browser lane</name>
  <files>test/browser/shortcuts-context.test.ts</files>
  <read_first>
    - test/browser/overlay-focus.test.ts (browser-lane interaction/keydown analog: real focus, real events across shadow roots)
    - src/components/shortcuts/shortcuts.ts (the composedPath/isComposing dispatch to exercise)
    - .planning/phases/04-performance-feature-capabilities/04-VALIDATION.md §"Per-Requirement Verification Map" (FEAT-03 composedPath/typing-suppression row → test/browser/shortcuts-context.test.ts) and §"Critical lane boundary"
  </read_first>
  <action>
    Create `test/browser/shortcuts-context.test.ts` (Chromium — composedPath across real shadow roots cannot be proven in jsdom). Mount an am-shortcuts wrapping a subtree that includes a focusable inside a component shadow root and a command-palette. Register a single-key shortcut (allowSingleKey) and a mod+k shortcut. Assert: a real keydown for mod+k dispatches the handler regardless of focus; a single-key keydown while focus is inside a text input is SUPPRESSED (does not fire); the same single-key fires when focus is not in an editable; a keydown with isComposing=true is ignored; event.composedPath()[0] correctly identifies the focused element inside a nested shadow root (the handler sees the true target, not the retargeted host). Assert a reserved combo registration was refused upstream (no handler fires for it).
  </action>
  <acceptance_criteria>
    - `npm run test:browser -- --run shortcuts-context` passes: mod+k dispatch; single-key suppressed while typing; single-key fires when not editable; isComposing ignored; composedPath resolves the nested-shadow focused element.
    - The file lives under test/browser/ (browser project).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- --run shortcuts-context</automated>
  </verify>
  <done>The am-shortcuts document dispatch resolves the true focused element via composedPath and suppresses single-key shortcuts while typing/composing — proven in Chromium.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard events → am-shortcuts document listener | Attacker-influenced key events dispatched to consumer handlers |
| consumer subtree → context distribution | Registry instance shared per-subtree, not globally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Denial-of-control / a11y | single-key shortcuts while typing | high | mitigate | composedPath()[0] editable check + isComposing guard suppress single keys while typing (WCAG 2.1.4); proven in browser lane. |
| T-04-14 | Tampering | retargeted host hides true focused element | medium | mitigate | Dispatch uses event.composedPath()[0], not event.target, so shadow-DOM retargeting cannot spoof the focus check (Pitfall 4). |
| T-04-15 | Denial-of-control | document keydown listener leak | low | mitigate | Listener attached with TeardownScope signal; cleared on disconnect (Phase 3 discipline). |
</threat_model>

<verification>
- jsdom: `npm test -- --run command-palette` green (D-09 both paths).
- browser: `npm run test:browser -- --run shortcuts-context` green (composedPath + suppression).
- `npx tsc --noEmit && npm run build && npm run build:manifest` green; am-shortcuts in the manifest; @lit/context external.
</verification>

<success_criteria>
am-shortcuts distributes an explicit ShortcutRegistry per-subtree via @lit/context with a composedPath/isComposing-aware document dispatch, and am-command-palette is rebindable through the provider with a graceful hardcoded Cmd/Ctrl+K fallback — the only net-new registered element this phase, kept external and exported for the CEM baseline.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-06-SUMMARY.md` when done.
</output>
