---
phase: 04-performance-feature-capabilities
plan: 10
type: execute
wave: 6
depends_on: ["04-04", "04-06", "04-07", "04-08", "04-09"]
files_modified:
  - api/custom-elements.baseline.json
  - .changeset/add-setcustomerror-validation.md
  - .changeset/add-am-shortcuts-registry.md
  - vitest.config.ts
autonomous: true
requirements: [FEAT-02, FEAT-04]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The regenerated CEM manifest captures the new public surface: setCustomError on the 14 form-associated controls and the am-shortcuts element; api/custom-elements.baseline.json is re-committed to include them (SHIP-01 seam — gate stays report-only until Phase 6)."
    - "A .changeset entry exists for the setCustomError validation-message API and a .changeset entry exists for the am-shortcuts provider + shortcut registry (each describing the added public surface)."
    - "The report-only surface diff (npm run diff:surface) shows ONLY the intended additions (setCustomError methods + am-shortcuts) and no unexpected public-surface change; virtualization added ZERO public surface (D-05/D-06)."
    - "jsdom coverage thresholds are re-baselined to the new measured floor after the phase's new controllers/code landed, and the full suite (jsdom + browser + a11y) is green."
    - "EDGE: the surface diff confirms no accidental public-surface drift from the validation/registry/virtualization edits beyond the two intended additions."
  artifacts:
    - api/custom-elements.baseline.json
    - .changeset/add-setcustomerror-validation.md
    - .changeset/add-am-shortcuts-registry.md
  key_links:
    - "npm run build:manifest -> dist/custom-elements.json -> re-committed api/custom-elements.baseline.json"
    - "new public surface (setCustomError + am-shortcuts) -> two .changeset entries"
  prohibitions:
    - "NO flipping the surface-diff gate to enforcing (Phase 6 / SHIP-01) — it stays report-only this phase."
    - "NO public-surface additions beyond setCustomError + am-shortcuts (virtualization is freeze-neutral, D-05/D-06)."
    - "NO lowering coverage below the measured floor (ratchet-to-floor discipline from Phase 1)."
---

<objective>
Close Phase 4 by capturing its new public surface into the CEM baseline and Changesets, and by re-baselining coverage so the full green-gated suite reflects the phase's added code. Validation added `setCustomError` to 14 controls; the registry added the `am-shortcuts` element — both must be baselined + Changeset'd before the Phase 6 freeze (the gate stays report-only until then). Virtualization added zero public surface, which the surface diff confirms.

Purpose: SHIP-01 seam — the new public surface is captured and diffable; the phase ends on a green pipeline.
Output: Re-committed CEM baseline, two Changesets, re-baselined coverage thresholds, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.changeset/normalize-overlay-lifecycle-events.md
</context>

## Artifacts this phase produces (finalized by this plan)

- Re-committed `api/custom-elements.baseline.json` capturing `setCustomError` (14 controls) + `am-shortcuts`.
- `.changeset/add-setcustomerror-validation.md` and `.changeset/add-am-shortcuts-registry.md` (minor bumps).
- Re-baselined jsdom coverage thresholds in `vitest.config.ts`.

<tasks>

<task type="auto">
  <name>Regenerate the CEM manifest, re-baseline it, and write the two Changesets</name>
  <files>api/custom-elements.baseline.json, .changeset/add-setcustomerror-validation.md, .changeset/add-am-shortcuts-registry.md</files>
  <read_first>
    - package.json scripts (build:manifest = cem analyze; diff:surface = node scripts/cem-diff.mjs api/custom-elements.baseline.json dist/custom-elements.json)
    - .changeset/normalize-overlay-lifecycle-events.md (the existing Changeset format to mirror: front-matter with "@willramanand/amris": minor + a prose body + a migration/summary table)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Runtime State Inventory" (rebuild manifest, re-commit baseline, add Changeset; gate report-only until Phase 6) and §"New public API surface"
  </read_first>
  <action>
    Run `npm run build` (or at least `npm run build:manifest`) to regenerate `dist/custom-elements.json` with the phase's new surface (setCustomError JSDoc on the 14 controls + the am-shortcuts element). Run `npm run diff:surface` and review its report: it MUST show the added `setCustomError` methods and the added `am-shortcuts` element, and NOTHING else unexpected (virtualization is freeze-neutral — no new attributes/props/events/slots/parts/cssprops). If the diff shows any unintended public-surface change, stop and reconcile it in the owning plan before re-baselining. Then re-commit the baseline: overwrite `api/custom-elements.baseline.json` with the freshly generated `dist/custom-elements.json` (the Phase 2 re-baseline pattern) so the baseline captures the intended additions.

    Write `.changeset/add-setcustomerror-validation.md` (minor): describe the new `setCustomError(message)` API + auto-surfaced validationMessage across the 14 form-associated controls, the D-03 precedence, the D-01/D-04 timing/politeness, and the am-field D-02 swap. Write `.changeset/add-am-shortcuts-registry.md` (minor): describe the new `am-shortcuts` provider element + the shortcut registry `register(...)` contract (scopes, mod/opt normalization, conflict detection, reserved-combo blocklist) and the command-palette rebindable-mod+k refactor. Mirror the front-matter + prose format of the existing changeset.
  </action>
  <acceptance_criteria>
    - `npm run build:manifest` regenerates dist/custom-elements.json containing `am-shortcuts` and `setCustomError` (on the 14 controls).
    - `npm run diff:surface` reports the setCustomError + am-shortcuts additions and no unexpected public-surface change (report-only — exit status is not a gate this phase).
    - `git diff api/custom-elements.baseline.json` shows the baseline now includes am-shortcuts + setCustomError.
    - Both `.changeset/add-setcustomerror-validation.md` and `.changeset/add-am-shortcuts-registry.md` exist with `"@willramanand/amris": minor` front-matter and a descriptive body.
    - No new public surface beyond setCustomError + am-shortcuts appears in the diff.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:manifest && npm run diff:surface</automated>
  </verify>
  <done>The CEM baseline captures setCustomError (14 controls) + am-shortcuts, two Changesets describe the additions, and the surface diff confirms no unintended drift — the freeze surface is recorded (report-only until Phase 6).</done>
</task>

<task type="auto">
  <name>Re-baseline jsdom coverage and prove the full green-gated suite</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts:40-66 (current thresholds: branches 67 / functions 82 / lines 84 / statements 83, plus per-directory tiers for combobox/date-picker/select/dialog — the ratchet-to-floor discipline to preserve)
    - .planning/phases/04-performance-feature-capabilities/04-VALIDATION.md §"Wave 0 Requirements" (re-baseline coverage after new controllers land) and §"Sampling Rate"
  </read_first>
  <action>
    Run `npm run test:coverage` (jsdom project) to measure the new floor after the phase's new controllers (validation.ts, shortcut-registry.ts, virtualize-support.ts) and control edits landed. Update the global and per-directory thresholds in vitest.config.ts to sit just under the newly measured coverage (ratchet-to-final-floor discipline from Phase 1 — floors trip on any real regression, never above measured). Keep the existing per-directory tiers (combobox/date-picker/select/dialog) and re-measure them. Then run the full green-gated suite: `npm run test:run` (jsdom + browser), `npm run test:browser`, and `npm run test:a11y` — all must pass with the new validation/virtualization/shortcut behavior covered. Do NOT lower any floor below what is measured.
  </action>
  <acceptance_criteria>
    - `npm run test:coverage` passes at the updated thresholds (no threshold sits above the measured value; none lowered below the prior floor without justification).
    - `npm run test:run` passes (jsdom + browser projects green).
    - `npm run test:browser` passes (validation-timing, validation-aria, shortcuts-context, virtualize-smoke, data-grid-virtual, combobox-virtual all green).
    - `npm run test:a11y` passes (axe scans green, including validated/virtualized states).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:coverage && npm run test:run && npm run test:a11y</automated>
  </verify>
  <done>Coverage is re-baselined to the new measured floor and the full jsdom + browser + a11y suite is green — Phase 4 ends on a green-gated pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated manifest → committed baseline | Public-surface record must reflect only intended additions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Tampering | unintended public-surface drift baselined | medium | mitigate | diff:surface reviewed before re-baseline; only setCustomError + am-shortcuts permitted; any other drift stops the re-baseline. |
| T-04-24 | Repudiation | coverage floor lowered silently | low | mitigate | Ratchet-to-floor: floors sit just under measured; no floor lowered below the prior measured value. |
</threat_model>

<verification>
- `npm run build:manifest && npm run diff:surface` shows only setCustomError + am-shortcuts additions.
- `npm run test:coverage && npm run test:run && npm run test:browser && npm run test:a11y` all green.
- Two Changesets present; baseline re-committed.
</verification>

<success_criteria>
The new public surface (setCustomError on 14 controls + am-shortcuts) is captured in the CEM baseline with two Changesets and confirmed by a report-only surface diff showing no unintended drift; coverage is re-baselined and the full green-gated suite passes.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-10-SUMMARY.md` when done.
</output>
