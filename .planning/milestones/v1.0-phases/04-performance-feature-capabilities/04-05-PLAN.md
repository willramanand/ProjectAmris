---
phase: 04-performance-feature-capabilities
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/internal/controllers/shortcut-registry.ts
  - test/internal/shortcut-registry.test.ts
autonomous: false
requirements: [FEAT-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "registry.register({id, keys, scope, handler}) detects a same-scope collision, keeps the first binding, refuses the second, and returns an inspectable {ok:false, reason:'conflict', existingId} — it NEVER throws (D-11)."
    - "The same key in two DIFFERENT scopes is legal and both register (D-11)."
    - "register refuses a reserved browser/OS combo by default and returns {ok:false, reason:'reserved'}; single-character shortcuts require explicit allowSingleKey opt-in (D-10, WCAG 2.1.4)."
    - "mod normalizes to metaKey on macOS else ctrlKey; opt normalizes to altKey; platform is detected once, not per-event (D-08)."
    - "The topmost active scope with a binding for a combo wins; global is the base scope (scope stacking)."
    - "registry.list()/serialize() returns a serializable registration list for a help sheet — the FEAT-V2-01 persistence seam (storage stays consumer-owned; the registry ships NO persistence store)."
    - "EDGE: registering a single-key shortcut without allowSingleKey is refused (returns an inspectable non-ok result), never silently accepted."
  artifacts:
    - src/internal/controllers/shortcut-registry.ts
    - test/internal/shortcut-registry.test.ts
  key_links:
    - "ShortcutRegistry.register -> conflict/reserved detection -> RegisterResult (no throw)"
    - "ShortcutRegistry is an explicit instance (owned later by am-shortcuts) — NOT a module-level singleton"
  prohibitions:
    - "NO module-level singleton / global registry — the registry is an explicit instance (D-08 no-global-state)."
    - "NO shortcut-persistence store inside the registry (FEAT-V2-01 deferred / locked anti-feature) — serialize() only."
    - "NO eval / new Function on combo strings — structured {keys,scope,handler} objects only (ASVS V5 code-injection)."
    - "NO keybindings that shadow browser/OS combos by default (D-10 reserved blocklist)."
    - "ShortcutRegistry registers NO custom element and is NEVER re-exported from src/index*.ts (src/internal boundary)."
---

<objective>
Build the FEAT-03 keyboard-shortcut registry as a plain, non-exported `src/internal/` class: registration with same-scope conflict detection (refuse + report, never throw — D-11), `mod`/`opt` platform normalization, scope stacking, a reserved-combo blocklist that default-refuses browser/OS combos (D-10, WCAG 2.1.4), and a serializable registration list for a help sheet (FEAT-V2-01 seam). This is the registry engine; the `am-shortcuts` provider element and document keydown dispatch land in 04-06.

Purpose: Deliver the conflict-aware, reserved-combo-safe registry core, proven in the fast jsdom lane (pure logic). The public `register(...)` contract is confirmed at the checkpoint because consumers bind to it through the provider and it freezes at v1.0.
Output: The `ShortcutRegistry` class + types and its jsdom test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `ShortcutRegistry` class + `Shortcut`, `RegisterResult`, `ShortcutScope` types — `src/internal/controllers/shortcut-registry.ts` (NEW, non-exported `src/internal/`).
- The public `register(...)` contract (called by consumers through the am-shortcuts-provided instance) — its shape binds at the Phase 6 freeze; captured in the CEM baseline scope by plan 04-10 via the am-shortcuts element.
- `test/internal/shortcut-registry.test.ts` (jsdom).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the public register() / RegisterResult contract (D-08) before it binds at freeze</name>
  <reversibility rating="one-way">register(...) is the published contract consumers bind to through am-shortcuts; changing its argument shape or result shape after v1.0 breaks every consumer registration.</reversibility>
  <decision>The exact shape of register(shortcut) and its RegisterResult (D-08). CONTEXT locks the imperative-registry-via-provider approach and the no-throw conflict policy; the open sub-decision (RESEARCH A6) is the exact argument object and result union.</decision>
  <context>Consumers and components call register through the am-shortcuts-provided instance; this binds into the CEM baseline at freeze (via am-shortcuts, plan 04-10). Confirm once here before the engine cements it.</context>
  <options>
    <option id="option-a">
      <name>register(s: {id; keys; scope?; handler; allowSingleKey?; description?}): RegisterResult, where RegisterResult = {ok:true} | {ok:false; reason:'conflict'|'reserved'; existingId?}</name>
      <pros>Single structured argument (extensible without signature breaks); inspectable non-throwing result carries the reason + the winning existingId (D-11); description feeds list()/serialize() help sheet; allowSingleKey gates WCAG 2.1.4 single keys (D-10).</pros>
      <cons>Consumers must read the result rather than catch — intended (no-throw).</cons>
    </option>
    <option id="option-b">
      <name>register(id, keys, handler, opts?) positional + throws on conflict</name>
      <pros>Terser call site.</pros>
      <cons>Throwing violates D-11 (a conflict during a component connect must not break rendering); positional args are harder to extend at a frozen API. Rejected.</cons>
    </option>
    <option id="option-c">
      <name>Declarative <am-shortcut> child elements instead of imperative register</name>
      <pros>Markup-first.</pros>
      <cons>D-08 explicitly chose imperative-via-provider and set the declarative wrapper aside as a possible v1.x addition. Out of scope.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended), option-b, or option-c — and confirm the exact field names on the Shortcut argument and the RegisterResult union.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>ShortcutRegistry core: register/conflict/scope/normalization + list/serialize</name>
  <reversibility rating="one-way">Implements the confirmed register()/RegisterResult public contract (D-08); binds at freeze.</reversibility>
  <files>src/internal/controllers/shortcut-registry.ts, test/internal/shortcut-registry.test.ts</files>
  <read_first>
    - src/internal/controllers/listbox-nav.ts (plain callback-driven class shape + the FIX-02 no-re-clamp/no-throw discipline framing at :33-43 — copy the class style and the src/internal boundary doc-comment)
    - src/internal/helpers/teardown-scope.ts:1-19 (src/internal boundary doc-comment: registers no custom element, never re-exported)
    - test/internal/teardown-scope.test.ts (jsdom-lane pure-logic test analog to copy for the registry tests)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Pattern 2: ShortcutRegistry" + §"Open Questions" Q-4 (reserved blocklist)
  </read_first>
  <action>
    Create `src/internal/controllers/shortcut-registry.ts` exporting a `ShortcutRegistry` class plus `type Shortcut`, `type RegisterResult`, `type ShortcutScope` per the confirmed contract (option A). Lead with the src/internal boundary doc-comment (copy the framing from listbox-nav.ts/teardown-scope.ts: registers no custom element, never re-exported from src/index*.ts, never on the frozen CEM surface). The registry is an explicit instance — no module-level mutable state.

    Implement: a normalized combo key so `mod` maps to metaKey on macOS else ctrlKey and `opt` maps to altKey, detecting platform ONCE at construction (navigator.userAgentData?.platform ?? navigator.platform) and caching it — no per-event branching. `register(s)` computes the normalized combo, checks the target scope's bindings: on same-scope collision keep the first and return `{ok:false, reason:'conflict', existingId}` (never throw); otherwise store and return `{ok:true}`. The same combo in a different scope registers independently (legal). A `resolve(combo, activeScopes)` (or equivalent dispatch lookup) returns the handler from the topmost active scope that binds the combo, with `global` as the base. `list()` returns a ReadonlyArray of `{id, keys, scope, description}`; `serialize()` returns the JSON-serializable equivalent (the help-sheet / FEAT-V2-01 seam — no persistence, just the shape). `unregister(id)` removes a binding. Use structured objects throughout; never eval/new Function on any combo string.

    TDD (write tests first in test/internal/shortcut-registry.test.ts, jsdom): same-scope conflict returns {ok:false, reason:'conflict', existingId} and keeps the first; same key in different scopes both register; mod/opt normalization resolves correctly on a mac-like and a win-like platform (stub the platform); scope stacking picks the topmost active scope; list()/serialize() return the expected shape; register never throws on any input.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (strict; `type` not `interface` for exported types; explicit return types).
    - `src/internal/controllers/shortcut-registry.ts` opens with the src/internal boundary doc-comment and exports ShortcutRegistry + Shortcut + RegisterResult + ShortcutScope.
    - `node -e "const s=require('fs').readFileSync('src/index.ts','utf8')+require('fs').readFileSync('src/index.all.ts','utf8'); process.exit(/shortcut-registry|ShortcutRegistry/.test(s)?1:0)"` exits 0 (NOT on the public surface).
    - `npm test -- --run shortcut-registry` passes: conflict no-throw refuse+report; cross-scope legality; mod/opt normalization; scope stacking; list/serialize shape.
    - No module-level mutable state in shortcut-registry.ts (state lives on the instance).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run shortcut-registry</automated>
  </verify>
  <done>ShortcutRegistry registers with no-throw conflict detection, platform-normalized combos, scope stacking, and a serializable list — an explicit instance on the src/internal boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Reserved-combo blocklist + single-key opt-in policy (D-10, WCAG 2.1.4)</name>
  <files>src/internal/controllers/shortcut-registry.ts, test/internal/shortcut-registry.test.ts</files>
  <read_first>
    - src/internal/controllers/shortcut-registry.ts (the core built in the prior task — extend it)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Open Questions" Q-4 (proposed starting blocklist: mod+t, mod+w, mod+n, mod+shift+n, mod+l, mod+q, mod+r, mod+shift+t, mod+tab, mod+`, mod+-, mod+0, F1–F12, mod+shift+i) + §"Pitfall 4"
    - .planning/REQUIREMENTS.md Out-of-Scope ("Keybindings that shadow browser/OS shortcuts" anti-feature)
  </read_first>
  <action>
    Extend ShortcutRegistry with a reserved-combo blocklist that register() consults BEFORE storing: a reserved combo returns `{ok:false, reason:'reserved'}` and is not stored (D-10 — enforces the locked no-shadowing anti-feature). Finalize the blocklist contents from the RESEARCH Q-4 starting set (document the chosen list in a doc-comment). Add single-key policy: a shortcut whose normalized combo is a bare single character (no modifier) is refused unless `allowSingleKey === true` (WCAG 2.1.4 — single keys must be opt-in, remappable, disablable). Expose the mechanism for a consumer to remap/disable a registration (unregister + re-register, or a disable flag). Reserved refusal and single-key gating never throw.

    TDD: assert a reserved combo (e.g. mod+w) is refused with reason:'reserved'; a bare single key without allowSingleKey is refused; the same single key with allowSingleKey:true registers; a disabled/unregistered single key stops resolving.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run shortcut-registry` passes including reserved-combo refusal + single-key opt-in/disable.
    - The chosen reserved blocklist is documented in a doc-comment in shortcut-registry.ts.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run shortcut-registry</automated>
  </verify>
  <done>The registry default-refuses reserved browser/OS combos and gates single-key shortcuts behind explicit opt-in, remappable and disablable per WCAG 2.1.4 — all no-throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer code → register({keys,handler}) | Attacker-influenced combo config strings enter the registry |
| combo string → key matching | Combo notation parsed into structured modifier flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Elevation of privilege | combo config parsing | high | mitigate | Combos parsed into structured modifier flags; never eval/new Function on any config string (ASVS V5). Registry stores structured objects only. |
| T-04-11 | Denial-of-control / a11y | reserved browser/OS combos | high | mitigate | Blocklist default-refuses reserved combos at registration (D-10); no preventDefault on reserved combos; single keys opt-in only (WCAG 2.1.4). |
| T-04-12 | Tampering | same-scope conflict overwrite | medium | mitigate | register keeps the first binding and refuses the second (D-11), returning existingId; never silently overwrites. |
</threat_model>

<verification>
- `npm test -- --run shortcut-registry` green (conflict, cross-scope, normalization, scope stacking, reserved blocklist, single-key opt-in).
- `npx tsc --noEmit` exits 0.
- ShortcutRegistry not on the public surface; no module-level singleton.
</verification>

<success_criteria>
The ShortcutRegistry engine registers with no-throw conflict detection (D-11), platform-normalized combos, scope stacking, a reserved-combo blocklist (D-10), and a serializable help-sheet list (FEAT-V2-01 seam) — an explicit instance on the src/internal boundary, proven in the jsdom lane.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-05-SUMMARY.md` when done.
</output>
