---
phase: 04-performance-feature-capabilities
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/checkbox/checkbox.ts
  - test/components/checkbox.test.ts
  - src/components/switch/switch.ts
  - test/components/switch.test.ts
  - src/components/radio/radio.ts
  - test/components/radio.test.ts
  - src/components/slider/slider.ts
  - test/components/slider.test.ts
  - src/components/color-picker/color-picker.ts
  - test/components/color-picker.test.ts
autonomous: true
requirements: [FEAT-01, FEAT-02]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "am-checkbox, am-switch, am-radio (radio-group), am-slider, and am-color-picker each surface the native validationMessage in a same-shadow-root aria-live region with aria-describedby/aria-invalid on D-01 blur/submit timing (FEAT-01)."
    - "Each of the five controls exposes setCustomError(message) with D-03 custom-wins precedence, reusing the tracer ValidationController (FEAT-02)."
    - "The radio validation message and aria-describedby/aria-invalid attach to the form-associated radio GROUP (the element holding the value), not an individual radio."
    - "EDGE: a required, unchecked am-checkbox shows no error on first paint and surfaces its error only after blur/submit (D-01)."
  artifacts:
    - src/components/checkbox/checkbox.ts
    - src/components/switch/switch.ts
    - src/components/radio/radio.ts
    - src/components/slider/slider.ts
    - src/components/color-picker/color-picker.ts
  key_links:
    - "each choice control -> ValidationController (src/internal/controllers/validation.ts) reuse"
    - "radio-group internals + primary focusable -> same-root validation message"
  prohibitions:
    - "NO auto-show on first paint (D-01)."
    - "NO raw-HTML sink for messages — text binding only."
    - "NO hardcoded colors — reuse --am-* tokens."
    - "NO reliance on :state()/CustomStateSet as the sole validity CSS hook (Safari 16.4 floor)."
---

<objective>
Expand validation-message display to the choice/range controls: am-checkbox, am-switch, am-radio (radio-group), am-slider, and am-color-picker. These have non-text focusables (native checkbox/radio/range inputs, or composite pickers), so each adopts the `ValidationController` with a control-specific anchor for aria-describedby/aria-invalid.

Purpose: Deliver FEAT-01/FEAT-02 for the remaining boolean and range controls.
Output: Five validated controls reusing the tracer ValidationController.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/phases/04-performance-feature-capabilities/04-01-SUMMARY.md
</context>

## Artifacts this phase produces (added by this plan)

- `setCustomError(message: string)` public method on am-checkbox, am-switch, am-radio (radio-group), am-slider, am-color-picker (NEW public surface — captured by plan 04-10).
- Same-shadow-root validation message region + aria-describedby/aria-invalid wiring on each (internal DOM).

<tasks>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-checkbox and am-switch</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to two more controls; binds at freeze.</reversibility>
  <files>src/components/checkbox/checkbox.ts, test/components/checkbox.test.ts, src/components/switch/switch.ts, test/components/switch.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog: instantiation, same-root message region, markTouched, setCustomError facade)
    - src/components/checkbox/checkbox.ts and src/components/switch/switch.ts (locate formAssociated/internals, the focusable that receives aria-describedby/aria-invalid, the blur handler, and invalid reflection)
  </read_first>
  <action>
    Adopt the ValidationController in am-checkbox and am-switch: instantiate `{ internals: () => this.internals, anchor: () => <inner focusable>, describedById: uniqueId(...) }`; render a same-shadow-root aria-live message region (polite / role=alert on submit — D-04) shown only when `controller.invalid`; set the focusable's aria-describedby to the message id when invalid and aria-invalid from `this.invalid`; call setValidity from the host mirroring the native checked/required state; hook markTouched into blur + submit; add the public setCustomError(message) facade with JSDoc. Reuse existing --am-danger styling; do not depend on :state() as the only hook (reflect :host([invalid])). Text binding only.

    TDD: write checkbox.test.ts and switch.test.ts assertions first — required-unchecked shows no first-paint error; error appears after blur; setCustomError override + '' fallback; markTouched gating.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run checkbox switch` passes: no first-paint error; setCustomError D-03 precedence; markTouched timing.
    - Both setCustomError methods carry JSDoc; ValidationController imported from src/internal (not re-exported).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run checkbox switch</automated>
  </verify>
  <done>am-checkbox and am-switch surface native validationMessage + setCustomError with D-03 precedence and D-01/D-04 timing.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-radio (radio-group) and am-slider</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to the radio-group and slider; binds at freeze.</reversibility>
  <files>src/components/radio/radio.ts, test/components/radio.test.ts, src/components/slider/slider.ts, test/components/slider.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog)
    - src/components/radio/radio.ts (AmRadioGroup is the form-associated element holding the value; AmRadio children do not — wire the GROUP)
    - src/components/slider/slider.ts (range control; identify its focusable thumb/input and internals)
  </read_first>
  <action>
    Wire the ValidationController onto AmRadioGroup (NOT the individual AmRadio) — the group owns internals + value; attach the same-root aria-live message and aria-describedby/aria-invalid to the group's primary focusable (the radiogroup container or the checked radio's input). Wire am-slider onto its range focusable. For each: setValidity from the host's value/required state, markTouched on group/slider blur + submit, same-root message region (D-04 politeness), public setCustomError facade with JSDoc, --am-danger styling, :host([invalid]) reflection, text binding only.

    TDD: radio.test.ts asserts the message attaches to the group and setCustomError/precedence works on the group; slider.test.ts asserts no first-paint error + setCustomError precedence + markTouched.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run radio slider` passes: radio message on the group; no first-paint error; setCustomError D-03 precedence; markTouched timing.
    - setCustomError methods carry JSDoc; ValidationController imported from src/internal.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run radio slider</automated>
  </verify>
  <done>am-radio (group) and am-slider surface native validationMessage + setCustomError with D-03 precedence; radio message attaches to the form-associated group.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire ValidationController + setCustomError into am-color-picker</name>
  <reversibility rating="costly">Applies the setCustomError API shape confirmed in 04-01 (D-03) to am-color-picker; binds at freeze.</reversibility>
  <files>src/components/color-picker/color-picker.ts, test/components/color-picker.test.ts</files>
  <read_first>
    - src/internal/controllers/validation.ts (reuse target)
    - src/components/input/input.ts (proven analog)
    - src/components/color-picker/color-picker.ts (composite picker; identify the form-associated internals, the value, and the primary focusable that should carry aria-describedby/aria-invalid)
  </read_first>
  <action>
    Adopt the ValidationController in am-color-picker following the am-input pattern, attaching the same-root aria-live message and aria-describedby/aria-invalid to its primary focusable (the trigger/text input). Wire setValidity from the value/required state, markTouched on blur + submit, D-04 politeness, public setCustomError facade with JSDoc, --am-danger styling, :host([invalid]) reflection, text binding only.

    TDD: color-picker.test.ts asserts no first-paint error, setCustomError override + '' fallback, markTouched gating.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run color-picker` passes: no first-paint error; setCustomError D-03 precedence; markTouched timing.
    - setCustomError carries JSDoc; ValidationController imported from src/internal.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run color-picker</automated>
  </verify>
  <done>am-color-picker surfaces native validationMessage + setCustomError with D-03 precedence and D-01/D-04 timing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer/server → setCustomError on 5 choice controls | Server error strings rendered into control DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering / Info-disclosure | setCustomError on checkbox/switch/radio/slider/color-picker | high | mitigate | Message rendered via Lit text binding only; no raw-HTML sink. jsdom tests assert text content. |
| T-04-07 | Spoofing | radio message anchored to wrong element | low | mitigate | Message + aria attach to the form-associated group (holds value/internals), asserted by radio.test.ts. |
</threat_model>

<verification>
- `npm test -- --run checkbox switch radio slider color-picker` green.
- `npx tsc --noEmit` exits 0.
- No ValidationController re-export on the public surface.
</verification>

<success_criteria>
The five choice/range controls surface native validation messages and setCustomError with D-03 precedence on D-01/D-04 timing, reusing the tracer ValidationController; the radio message anchors to the form-associated group.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-03-SUMMARY.md` when done.
</output>
