---
phase: 04-performance-feature-capabilities
plan: 07
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - src/components/popover/popover.ts
  - src/components/tooltip/tooltip.ts
  - src/components/dropdown/dropdown.ts
  - src/components/context-menu/context-menu.ts
  - src/components/select/select.ts
  - src/components/date-picker/date-picker.ts
  - test/components/autoupdate-gating.test.ts
autonomous: true
requirements: [PERF-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Across every floating-ui overlay (combobox, select, dropdown, popover, tooltip, date-picker, context-menu), autoUpdate starts only on the false→true open transition and stops on the true→false transition AND on disconnect (PERF-04)."
    - "am-popover's previously-inline autoUpdate is migrated onto FloatingPositionController for consistency, behavior-preserving."
    - "The gating is behavior-preserving — no user-facing change; the full existing jsdom + Chromium suites stay green with zero test edits to unrelated files."
    - "EDGE: opening then disconnecting an overlay while open leaves no live autoUpdate loop (stop() called on hostDisconnected)."
  artifacts:
    - test/components/autoupdate-gating.test.ts
  key_links:
    - "each overlay open-state transition -> FloatingPositionController.start()/stop()"
    - "FloatingPositionController.hostDisconnected() -> stop() (leak guard)"
  prohibitions:
    - "NO user-facing behavior change — positioning must remain identical while open."
    - "NO autoUpdate running while an overlay is closed."
    - "NO per-component ad-hoc autoUpdate lifecycle where the shared FloatingPositionController already applies."
---

<objective>
Complete PERF-04: gate floating-ui `autoUpdate` to open transitions across all overlay components — the seam Phase 2 intentionally left ungated. Migrate am-popover's inline `autoUpdate` onto the shared `FloatingPositionController`, audit every floating-ui consumer to confirm start-on-open / stop-on-close+disconnect, and lock the invariant with jsdom teardown-spy tests (TEST-05 pattern). Behavior-preserving — no user-facing change.

Purpose: PERF-04 — no `autoUpdate` loop runs while an overlay is closed, and none dangles after disconnect.
Output: Consistent open-transition gating via FloatingPositionController + a spy test that proves it for all seven overlays.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `test/components/autoupdate-gating.test.ts` — jsdom teardown-spy test proving open-transition gating across all overlays.
- am-popover migrated onto FloatingPositionController (no new public surface; freeze-neutral).

<tasks>

<task type="auto">
  <name>Migrate/gate inline-autoUpdate overlays onto FloatingPositionController open-transition gating</name>
  <files>src/components/popover/popover.ts, src/components/tooltip/tooltip.ts, src/components/dropdown/dropdown.ts, src/components/context-menu/context-menu.ts</files>
  <read_first>
    - src/internal/controllers/floating-position.ts (start() self-clears before restart at :61-70; stop() at :72-76; hostDisconnected()→stop() at :78-80 — the migration target and the leak guard)
    - src/components/combobox/combobox.ts:429-437 (the GOOD analog: start()/stop() inside if (changed.has('_open')))
    - src/components/popover/popover.ts:154-174 (INLINE autoUpdate inside if (changed.has('open')) with manual _cleanupAutoUpdate — the migrate candidate)
    - .planning/phases/04-performance-feature-capabilities/04-PATTERNS.md §"Overlay components (MODIFIED — PERF-04)" + §"Pattern 4: Open-transition-gated autoUpdate"
    - .planning/codebase/INTEGRATIONS.md (floating-ui usage across the overlays)
  </read_first>
  <action>
    Audit popover, tooltip, dropdown, and context-menu for their floating-ui lifecycle. For am-popover, replace the inline `autoUpdate` + manual `_cleanupAutoUpdate` (:154-174) with a `FloatingPositionController`, calling `start()` on the open transition (`false→true` inside the `if (changed.has('open'))` gate) and `stop()` on close; hostDisconnected() on the controller already stops on disconnect. For tooltip, dropdown, and context-menu, confirm each starts autoUpdate only on its open transition and stops on close + disconnect; migrate any ungated inline autoUpdate onto FloatingPositionController the same way (behavior-preserving — mirror the combobox:429-437 pattern). Preserve each overlay's existing placement/middleware (e.g. any width-matcher) by passing it through the controller's `middleware` option. Do NOT change any user-facing positioning or open/close behavior.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run popover tooltip dropdown context-menu` passes with the existing suites green (behavior-preserving).
    - am-popover no longer contains an inline autoUpdate call — positioning routes through FloatingPositionController: `node -e "const s=require('fs').readFileSync('src/components/popover/popover.ts','utf8'); process.exit(/FloatingPositionController/.test(s)?0:1)"` exits 0.
    - Each of the four overlays starts autoUpdate only inside an open-transition gate (reviewed against the combobox:429-437 analog).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run popover tooltip dropdown context-menu</automated>
  </verify>
  <done>popover, tooltip, dropdown, and context-menu gate autoUpdate on their open transition via FloatingPositionController (or an equivalent open-gated call), behavior-preserving.</done>
</task>

<task type="auto">
  <name>Confirm form-overlay gating + lock the invariant with a teardown-spy test</name>
  <files>src/components/select/select.ts, src/components/date-picker/date-picker.ts, test/components/autoupdate-gating.test.ts</files>
  <read_first>
    - src/components/select/select.ts and src/components/date-picker/date-picker.ts (both delegate positioning to FloatingPositionController per Phase 2 02-07 — confirm start()/stop() are called on the open transition, not eagerly)
    - src/components/combobox/combobox.ts:429-437 (already-gated analog — no change expected)
    - test/components/*.test.ts Phase 3 TEST-05 teardown-spy tests (the spy pattern to copy: spy on controller start/stop or listener attach/detach)
    - .planning/phases/04-performance-feature-capabilities/04-VALIDATION.md (PERF-04 row: jsdom spy, TEST-05 pattern)
  </read_first>
  <action>
    Confirm am-select and am-date-picker call `FloatingPositionController.start()` only on the open transition and `stop()` on close; gate them if the audit finds an eager/ungated start. Leave am-combobox unchanged (already gated). Create `test/components/autoupdate-gating.test.ts` (jsdom) that, for each of the seven overlays (combobox, select, dropdown, popover, tooltip, date-picker, context-menu), spies on the positioning start/stop path and asserts: start fires on open (false→true) and NOT while closed; stop fires on close (true→false); and stop fires on disconnect-while-open (no live loop remains). Use the Phase 3 TEST-05 spy approach.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run autoupdate-gating select date-picker combobox` passes: for all seven overlays, start-on-open-only, stop-on-close, stop-on-disconnect-while-open.
    - Full jsdom suite stays green: `npm test -- --run` passes (no regressions to unrelated overlays).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run autoupdate-gating select date-picker combobox</automated>
  </verify>
  <done>All seven floating-ui overlays gate autoUpdate on their open transition and stop on close + disconnect, locked by a teardown-spy test — PERF-04 complete and behavior-preserving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overlay open-state → autoUpdate lifecycle | Positioning loop lifecycle must not outlive the open overlay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Denial of service (resource leak) | autoUpdate loop after close/disconnect | medium | mitigate | start()/stop() gated on open transition; hostDisconnected()→stop(); spy test asserts no loop remains after disconnect-while-open. |
| T-04-17 | Tampering | behavior regression from migration | low | mitigate | Behavior-preserving migration; existing per-overlay suites must stay green with no test edits to unrelated files. |
</threat_model>

<verification>
- `npm test -- --run autoupdate-gating popover tooltip dropdown context-menu select date-picker combobox` green.
- `npm test -- --run` full jsdom suite green (no regressions).
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
autoUpdate is gated to open transitions across all seven floating-ui overlays and stops on close + disconnect, popover is migrated onto FloatingPositionController, and the invariant is locked by a jsdom teardown-spy test — behavior-preserving.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-07-SUMMARY.md` when done.
</output>
