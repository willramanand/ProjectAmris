---
phase: 04-performance-feature-capabilities
plan: 09
type: execute
wave: 5
depends_on: ["04-08", "04-07", "04-04"]
files_modified:
  - src/components/combobox/combobox.ts
  - test/components/combobox.test.ts
  - src/components/select/select.ts
  - test/components/select.test.ts
  - test/browser/combobox-virtual.test.ts
autonomous: true
requirements: [PERF-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "combobox/select option popups virtualize automatically once the option count exceeds the threshold (same activation model as the grid, D-06); small lists stay on repeat()."
    - "Each rendered option carries a stable id and aria-setsize (FULL filtered total from state) + aria-posinset (1-based absolute), and the input/listbox sets aria-activedescendant to the active option's id (PERF-03 — combobox has NEITHER today)."
    - "On Arrow/Home/End/PageUp/PageDown/type-ahead, scrollToIndex(target) brings the target option into the DOM BEFORE aria-activedescendant is set to its id (the id must exist)."
    - "Form value integrity is preserved: setFormValue and aria-selected are driven by this.value (state), never by the presence of the option node (Pitfall 2)."
    - "Virtualization does NOT reintroduce the FIX-02 stale-index re-clamp (ListboxNavController still does not re-clamp on option replace) nor a FIX-03 focus regression (never unmount the focused/active element without moving focus first)."
    - "EDGE: navigating to an option far outside the current window (e.g. End on a 5000-option list) scrolls it into the window and announces via aria-activedescendant."
  artifacts:
    - test/browser/combobox-virtual.test.ts
  key_links:
    - "virtualize() directive over filtered options -> renderItem(opt, absIndex): option id + aria-setsize/aria-posinset"
    - "scrollToIndex(target) -> then set aria-activedescendant to option id (ordering is load-bearing)"
    - "this.value (state) -> setFormValue + aria-selected (never option-node presence)"
  prohibitions:
    - "NO aria-activedescendant pointing at an id that is not rendered — scroll into window first."
    - "NO counting options from the DOM — count from filtered state (Pitfall 2)."
    - "NO re-clamping _highlightedIndex on option replace (FIX-02) and NO focus-restoration regression (FIX-03)."
    - "NO new public attribute for virtualization (D-06 auto-threshold — freeze-neutral)."
    - "NO <lit-virtualizer> element (virtualize() directive only)."
    - "NO raw-HTML sink in renderItem — text bindings only."
---

<objective>
Deliver PERF-03: `am-combobox` and `am-select` option popups virtualize automatically above a threshold (D-06), a11y-correct — introducing the stable option `id`s and `aria-activedescendant` the combobox lacks today, with `aria-setsize`/`aria-posinset` from full filtered state and `scrollToIndex` before activedescendant so keyboard nav to off-screen options works. Form-value integrity and the Phase 3 FIX-02/FIX-03 invariants are preserved.

Purpose: PERF-03 — the hardest a11y-heavy slice, scheduled last with the most buffer; the credibility gate is truthful windowed ARIA + working keyboard nav to unmounted options.
Output: Virtualized combobox/select popups with a Chromium browser proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/phases/04-performance-feature-capabilities/04-VALIDATION.md
</context>

## Artifacts this phase produces (introduced by this plan)

- Stable per-option `id`s + `aria-activedescendant` on am-combobox and am-select popups (internal DOM — NEW a11y wiring, not CEM public surface).
- Threshold-gated `virtualize()` render path for both popup modes (no new public attribute — D-06 freeze-neutral).
- `test/browser/combobox-virtual.test.ts`.

<tasks>

<task type="auto">
  <name>Virtualize the am-combobox option popup with stable ids + aria-activedescendant</name>
  <files>src/components/combobox/combobox.ts, test/components/combobox.test.ts</files>
  <read_first>
    - src/components/combobox/combobox.ts:611-625 (combobox-mode repeat() listbox: role="listbox" tabindex, options role="option" with aria-selected but NO id and NO aria-activedescendant — the gap to close) and :678-694 (select-mode dropdown listbox, same shape)
    - src/internal/controllers/listbox-nav.ts:33-43 (FIX-02: does NOT re-clamp _highlightedIndex on option replace — must remain untouched; combobox delegates nav here)
    - src/internal/helpers/virtualize-support.ts (threshold + ARIA helpers from 04-08) and RESEARCH §"Pattern 3" (virtualize()/virtualizerRef/scrollToIndex, keyFunction identity) + §"Pitfall 2"
    - .planning/phases/04-performance-feature-capabilities/04-PATTERNS.md §"combobox.ts / select.ts (MODIFIED — PERF-03)"
  </read_first>
  <action>
    In both combobox render modes, gate on `filteredOptions.length > VIRTUALIZE_ROW_THRESHOLD` (D-06). At/below threshold keep the existing repeat() path (with the ARIA additions below applied to it too, so behavior is consistent). Above threshold render the listbox body via the `virtualize()` directive over the filtered options with `keyFunction: (opt) => opt` (identity). For BOTH paths: assign each option a stable id via a deterministic per-option id function (e.g. based on the option and its absolute index), set `aria-setsize` = the full filtered total from state and `aria-posinset` = absolute index + 1 on each option, keep `aria-selected=${this.value === option}` driven by state, and set `aria-activedescendant` on the input (combobox mode) / listbox (select mode) to the ACTIVE option's id. On Arrow/Home/End/PageUp/PageDown/type-ahead, call the virtualizer `scrollToIndex(targetIndex)` to bring the target into the DOM BEFORE setting `aria-activedescendant` to its id (the id must exist in the DOM). Never move focus/unmount the active option without scrolling it in first (preserve FIX-03). Drive `setFormValue`/selection from `this.value` (state), never from option-node presence (Pitfall 2). Do NOT modify src/internal/controllers/listbox-nav.ts (FIX-02 no-re-clamp preserved). Render option text via text bindings only. No new public attribute (D-06).

    Extend test/components/combobox.test.ts (jsdom logic where provable): options get stable ids + aria-setsize/aria-posinset from full filtered total; aria-activedescendant tracks the highlighted index; existing FIX-02 index-clamp assertions stay green; setFormValue driven by value not node presence.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run combobox` passes: option ids + aria-setsize/aria-posinset present; aria-activedescendant tracks the active option; existing option-nav/index-clamp assertions green.
    - `git diff --stat src/internal/controllers/listbox-nav.ts` shows no change (FIX-02 preserved).
    - No new @property added to am-combobox for virtualization; no <lit-virtualizer> element.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run combobox</automated>
  </verify>
  <done>am-combobox popups virtualize above the threshold with stable option ids, truthful aria-setsize/aria-posinset from state, and aria-activedescendant + scroll-into-window nav — FIX-02/FIX-03 preserved, no new public surface.</done>
</task>

<task type="auto">
  <name>Virtualize the am-select popup and prove PERF-03 a11y in the Chromium browser lane</name>
  <files>src/components/select/select.ts, test/components/select.test.ts, test/browser/combobox-virtual.test.ts</files>
  <read_first>
    - src/components/select/select.ts (its option listbox render + keyboard nav model — mirror the combobox PERF-03 treatment from the prior task, respecting select's element-based wraparound nav)
    - src/components/combobox/combobox.ts (the just-virtualized combobox as the analog for option ids/activedescendant/scrollToIndex)
    - test/browser/form-association.test.ts and test/browser/overlay-focus.test.ts (browser-lane harness + focus/scroll assertion analogs)
    - .planning/phases/04-performance-feature-capabilities/04-VALIDATION.md (PERF-03 row → test/browser/combobox-virtual.test.ts: setsize/posinset/activedescendant-scroll/form value)
  </read_first>
  <action>
    Apply the same PERF-03 treatment to am-select: threshold-gated virtualize() popup, stable option ids, aria-setsize/aria-posinset from full state, aria-activedescendant on the listbox, scrollToIndex before activedescendant, selection/form-value from state. Preserve select's existing element-based wraparound keyboard nav (do not swap it for combobox's string-clamp model). Text bindings only; no new public attribute.

    Create `test/browser/combobox-virtual.test.ts` (Chromium — scroll/focus/activedescendant windowing cannot be proven in jsdom): for BOTH am-combobox and am-select with 1000+ options, assert aria-setsize equals the full filtered total on rendered options; assert pressing End (or navigating to an option outside the window) scrolls it into the DOM and sets aria-activedescendant to a rendered id (the referenced id resolves to a live element); assert selecting a virtualized option preserves the form value (submit the form, check FormData) even after the option scrolled out of the window; assert only a windowed subset of options is in the DOM.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run select` passes: option ids + setsize/posinset; activedescendant tracking; existing suite green.
    - `npm run test:browser -- --run combobox-virtual` passes: aria-setsize full total; End-navigation scrolls target in + activedescendant resolves to a live id; form value preserved across recycling; windowed DOM subset.
    - The browser test file lives under test/browser/.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run select && npm run test:browser -- --run combobox-virtual</automated>
  </verify>
  <done>am-select and am-combobox popups virtualize with truthful windowed ARIA and working keyboard nav to off-screen options, form-value integrity preserved — proven in Chromium.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer option data → renderItem | Untrusted option content rendered into recycled option nodes |
| keyboard nav → aria-activedescendant target | Active id must always reference a live rendered node |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-20 | Tampering | XSS via virtualized option content | high | mitigate | renderItem uses Lit text bindings only; no raw-HTML sink. |
| T-04-21 | Info-disclosure (a11y correctness) | aria-activedescendant → unmounted id | medium | mitigate | scrollToIndex brings the target into the DOM before activedescendant is set; browser test asserts the referenced id resolves to a live node. |
| T-04-22 | Tampering | form-value desync under recycling | medium | mitigate | setFormValue/aria-selected driven by this.value (state), not node presence; browser test submits and checks FormData after recycling. |
</threat_model>

<verification>
- `npm run test:browser -- --run combobox-virtual` green (setsize/posinset/activedescendant-scroll/form value).
- `npm test -- --run combobox select` green (FIX-02 preserved; listbox-nav.ts unchanged).
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
combobox/select popups virtualize above the threshold with stable option ids, truthful aria-setsize/aria-posinset from state, aria-activedescendant with scroll-into-window nav to off-screen options, and preserved form-value integrity — FIX-02/FIX-03 intact, freeze-neutral.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-09-SUMMARY.md` when done.
</output>
