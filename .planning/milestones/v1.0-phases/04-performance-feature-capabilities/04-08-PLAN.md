---
phase: 04-performance-feature-capabilities
plan: 08
type: execute
wave: 4
depends_on: ["04-06"]
files_modified:
  - package.json
  - vite.config.ts
  - src/internal/helpers/virtualize-support.ts
  - test/browser/virtualize-smoke.test.ts
  - src/components/data-grid/data-grid.ts
  - test/browser/data-grid-virtual.test.ts
autonomous: true
requirements: [PERF-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "am-data-grid virtualizes automatically once row count exceeds the threshold; at/below it, today's repeat() table rendering stands unchanged (D-05 — no new public attribute)."
    - "Above the threshold the grid renders a DIV-based grid (role=grid/row/gridcell/columnheader), NOT native <table>/<tbody>, because the virtualizer's positioning wrappers break table layout (Pitfall 1); the existing ARIA roles transfer."
    - "aria-rowcount reflects the FULL total from state (this.rows.length), and each rendered row carries aria-rowindex (1-based incl. header offset) computed from its absolute index (PERF-02)."
    - "Selection, sort, and focus are identity-keyed via getRowId and driven by state (_selectionSet/_focusedRowIndex), surviving row recycling — a selected row stays selected after scrolling it out and back (PERF-02)."
    - "@lit-labs/virtualizer is exact-pinned 2.1.1 in dependencies, kept external via the vite /^@lit-labs\\// pattern — not bundled, no second copy of Lit (D-12/D-13)."
    - "EDGE: the mobile screen-reader aria-activedescendant limitation is acknowledged in a JSDoc/doc note — documented, not worked around."
    - "EDGE: 1000+ rows render without collapsing/overlapping (div-grid layout) and scrolling keeps column alignment."
  artifacts:
    - src/internal/helpers/virtualize-support.ts
    - test/browser/virtualize-smoke.test.ts
    - test/browser/data-grid-virtual.test.ts
  key_links:
    - "virtualize() directive (@lit-labs/virtualizer/virtualize.js) -> data-grid div-row renderItem(row, absIndex)"
    - "renderItem absolute index -> aria-rowindex; state (rows.length) -> aria-rowcount"
    - "getRowId identity keyFunction -> selection/sort/focus survive recycling"
  prohibitions:
    - "NO new public attribute for virtualization (D-05 auto-threshold — freeze-neutral)."
    - "NO <lit-virtualizer> element (use the virtualize() directive — no new registered tag)."
    - "NO virtualizing native <table>/<tbody> rows (Pitfall 1)."
    - "NO counting rows from the DOM — count from state (Pitfall 2)."
    - "NO editable/sortable full-spreadsheet grid features (FEAT-V2-02 deferred anti-feature) — display virtualizer only."
    - "NO bundling @lit-labs/virtualizer (stays external, D-12)."
    - "NO raw-HTML sink in renderItem — text bindings only."
---

<objective>
Deliver PERF-02: `am-data-grid` virtualizes 1000+ rows via the `@lit-labs/virtualizer` `virtualize()` directive, auto-activating above a row threshold (D-05, no new public attribute) with a div-based grid render path (Pitfall 1), truthful ARIA (`aria-rowcount` from state, per-row `aria-rowindex`), and identity-keyed selection/sort/focus that survive recycling. First de-risk the labs/pre-1.0 runtime API (A1–A4) in a browser smoke test, then wire the grid.

Purpose: PERF-02 — the documented 1000+ row bottleneck, internal-only and freeze-neutral.
Output: The virtualize-support helper, a verified virtualizer API, and a virtualized am-data-grid with a browser proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-feature-capabilities/04-CONTEXT.md
@.planning/phases/04-performance-feature-capabilities/04-RESEARCH.md
@.planning/phases/04-performance-feature-capabilities/04-PATTERNS.md
@.planning/phases/04-performance-feature-capabilities/04-VALIDATION.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `package.json` `dependencies` += `@lit-labs/virtualizer` `2.1.1` (exact-pin, D-13, kept external).
- `vite.config.ts` external list += `/^@lit-labs\//` (D-12).
- `src/internal/helpers/virtualize-support.ts` (NEW, non-exported) — threshold constant (D-07) + ARIA position helpers.
- `test/browser/virtualize-smoke.test.ts`, `test/browser/data-grid-virtual.test.ts`.
- am-data-grid threshold-gated div-grid virtualized render path (no new public surface — D-05 freeze-neutral).

<tasks>

<task type="auto">
  <name>Add @lit-labs/virtualizer (external), verify its v2.1.1 runtime API, and create virtualize-support helper</name>
  <reversibility rating="costly">Adds @lit-labs/virtualizer to package.json dependencies + a vite external pattern (D-12) — changes dependency classification and interacts with SHIP-02; moving peer↔dep after publish is consumer-visible.</reversibility>
  <files>package.json, vite.config.ts, src/internal/helpers/virtualize-support.ts, test/browser/virtualize-smoke.test.ts</files>
  <read_first>
    - vite.config.ts:195 (external list: ['lit', /^lit\//, /^@lit\//, '@floating-ui/dom', /^@floating-ui\//] — add /^@lit-labs\// here; @lit/context is already covered by /^@lit\//)
    - src/internal/helpers/teardown-scope.ts:1-19 (src/internal boundary doc-comment framing to copy)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Standard Stack" (exact-pin 2.1.1, peerDeps lit ^3.2.0) + §"Pattern 3" + §"Assumptions Log" A1–A4 (renderItem absolute index, virtualizerRef+scrollToIndex, virtualize.js import path, flow layout) + §"Deprecated/outdated"
  </read_first>
  <action>
    Add `@lit-labs/virtualizer` at exactly `2.1.1` (exact-pin, no caret — D-13; it is pre-1.0/labs and can break on a minor bump) to package.json `dependencies`. Edit vite.config.ts:195 external list to add `/^@lit-labs\//` so the virtualizer stays external/unbundled (D-12). Run `npm install`.

    Verify the v2.1.1 runtime API against its installed README/types (resolve A1–A4 before wiring): the `virtualize()` directive import path (`@lit-labs/virtualizer/virtualize.js`), whether `renderItem(item, index)` receives the ABSOLUTE index in the items array (A1 — critical for aria-rowindex/posinset), the `virtualizerRef` + `.scrollToIndex(index, position)` scroll-to API (A2), and the default `flow` layout for auto-measured heights (A4). Record the confirmed API in a doc-comment in virtualize-support.ts.

    Create `src/internal/helpers/virtualize-support.ts` (NEW, non-exported; lead with the src/internal boundary doc-comment): export a `VIRTUALIZE_ROW_THRESHOLD` constant set from benchmarked render cost on ~100 rows of representative content (D-07 — document the chosen number and the measurement basis in the doc-comment; a round ~100 is expected) and small pure ARIA helpers (e.g. `ariaPosinset(index)`, `ariaSetsize(total)`, `ariaRowindex(absIndex, headerOffset)`). No custom element; never re-exported from src/index*.ts.

    Create `test/browser/virtualize-smoke.test.ts` (Chromium — ResizeObserver is mocked in jsdom, so windowing is only provable in the browser lane): render a large list (e.g. 2000 items) with the virtualize() directive and assert only a windowed subset of item nodes is in the DOM while the full logical count is preserved in state, and that scrollToIndex brings a target index's node into the DOM. This de-risks the labs API before the grid depends on it.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies['@lit-labs/virtualizer']==='2.1.1'?0:1)"` exits 0 (exact pin).
    - `node -e "const s=require('fs').readFileSync('vite.config.ts','utf8'); process.exit(/@lit-labs\\\\\//.test(s)?0:1)"` exits 0 (external pattern present).
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds (virtualizer stays external).
    - `src/internal/helpers/virtualize-support.ts` exists, exports VIRTUALIZE_ROW_THRESHOLD (documented number), and is NOT re-exported from src/index*.ts.
    - `npm run test:browser -- --run virtualize-smoke` passes: windowed DOM subset + full logical count in state + scrollToIndex brings target into DOM.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run test:browser -- --run virtualize-smoke</automated>
  </verify>
  <done>@lit-labs/virtualizer 2.1.1 is an external dependency, its runtime API is confirmed, and the virtualize-support helper (threshold + ARIA helpers) exists on the src/internal boundary with a browser smoke proof.</done>
</task>

<task type="auto">
  <name>Virtualize am-data-grid above the threshold with a div-grid path and truthful ARIA</name>
  <files>src/components/data-grid/data-grid.ts, test/browser/data-grid-virtual.test.ts</files>
  <read_first>
    - src/components/data-grid/data-grid.ts:315-383 (current <table role="grid"> path: aria-rowcount=${this.rows.length} at :324; repeat() <tr role="row"> at :350-379 with NO aria-rowindex today; getRowId identity at :352,:355; _selectionSet/_focusedRowIndex state)
    - src/internal/helpers/virtualize-support.ts (threshold + ARIA helpers from the prior task)
    - .planning/phases/04-performance-feature-capabilities/04-RESEARCH.md §"Pattern 3" (state-driven ARIA, keyFunction identity, scrollToIndex-before-activedescendant) + §"Pitfall 1" (table vs div-grid) + §"Pitfall 2" (count from state)
    - .planning/phases/04-performance-feature-capabilities/04-PATTERNS.md §"data-grid.ts (MODIFIED — PERF-02)"
  </read_first>
  <action>
    Gate the render on `this.rows.length > VIRTUALIZE_ROW_THRESHOLD`. At/below threshold keep the existing `<table>`/`repeat()` path unchanged. Above threshold render a DIV-based grid (Pitfall 1): a scroll container with `role="grid"` + `aria-rowcount=${this.rows.length}` (full total from state) + `aria-colcount`/`aria-multiselectable` as today; a header row of `role="columnheader"` divs (carry the existing sort handlers/aria-sort); and the row body via the `virtualize()` directive over `this._sortedRows` with `keyFunction` = `getRowId(row, originalIndex)` (identity — selection/sort/focus survive recycling) and `renderItem(row, absIndex)` producing `role="row"` divs with `aria-rowindex` = 1-based including the header offset (via the helper), `role="gridcell"` children, `aria-selected` from `_selectionSet.has(id)` (state, never row presence), and `tabindex` from `_focusedRowIndex` (state). Keyboard row nav must call `scrollToIndex(targetIndex)` to bring a row into the DOM BEFORE moving focus/tabindex to it (never move focus to an unmounted row — preserves FIX-03). Selection/sort/`setFormValue`-adjacent state stays driven by state, not DOM. Render cell content via text bindings only (no raw-HTML sink). Add a JSDoc/doc note acknowledging the mobile screen-reader aria-activedescendant limitation (document, don't work around). Do NOT add any editable/sortable-cell features (display virtualizer only — FEAT-V2-02 deferred). No new public attribute (D-05).

    Create `test/browser/data-grid-virtual.test.ts` (Chromium): render a grid with 1000+ rows; assert aria-rowcount equals the full total; assert rendered rows carry aria-rowindex and only a windowed subset is in the DOM; select a row, scroll it out of view and back, and assert it is still selected (identity-keyed, state-driven); assert div-grid rows do not collapse/overlap (non-zero height, column alignment preserved).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --run data-grid` passes: the below-threshold table path is unchanged (existing suite green).
    - `npm run test:browser -- --run data-grid-virtual` passes: aria-rowcount full total; per-row aria-rowindex; windowed DOM; selection survives scroll-out-and-back; rows render with non-zero height/aligned columns.
    - No new public attribute added to am-data-grid (grep confirms no new @property for virtualization); no <lit-virtualizer> element used.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run data-grid && npm run test:browser -- --run data-grid-virtual</automated>
  </verify>
  <done>am-data-grid virtualizes 1000+ rows above the threshold via a div-grid path with truthful aria-rowcount/aria-rowindex and identity-keyed selection/sort/focus that survive recycling — freeze-neutral, no new public surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer row data → renderItem | Untrusted cell content rendered into recycled rows |
| npm install → @lit-labs/virtualizer | Third-party runtime dependency added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18 | Tampering | XSS via virtualized cell content | high | mitigate | renderItem uses Lit text bindings only; no raw-HTML injection sink. |
| T-04-SC | Tampering (supply chain) | @lit-labs/virtualizer install | high | mitigate | First-party lit/lit package, legitimacy verdict OK, postinstall null (RESEARCH Package Legitimacy Audit); exact-pinned 2.1.1 and kept external. No [ASSUMED]/[SUS] package — no human-verify gate required. |
| T-04-19 | Info-disclosure (a11y correctness) | recycled-row ARIA counts | medium | mitigate | aria-rowcount/aria-rowindex computed from state/absolute index, not DOM (Pitfall 2); browser test asserts truthful counts. |
</threat_model>

<verification>
- `npm run test:browser -- --run virtualize-smoke data-grid-virtual` green.
- `npm test -- --run data-grid` green (below-threshold path unchanged).
- `npx tsc --noEmit && npm run build` green; virtualizer external + exact-pinned.
</verification>

<success_criteria>
am-data-grid virtualizes 1000+ rows above a documented threshold via the virtualize() directive on a div-grid, with aria-rowcount/aria-rowindex truthful from state and identity-keyed selection/sort/focus surviving recycling — internal-only, freeze-neutral, virtualizer kept external and exact-pinned.
</success_criteria>

<output>
Create `.planning/phases/04-performance-feature-capabilities/04-08-SUMMARY.md` when done.
</output>
