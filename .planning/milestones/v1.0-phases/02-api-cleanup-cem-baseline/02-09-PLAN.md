---
phase: 02-api-cleanup-cem-baseline
plan: 09
type: execute
wave: 8
depends_on: [02-07, 02-08, 02-05, 02-01]
files_modified:
  - api/AUDIT.md
  - api/custom-elements.baseline.json
  - .changeset/freeze-slot-part-token-surface.md
autonomous: false
requirements: [API-04, API-05]
estimate:
  tokens: 72000
  raw_tokens: 37000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every slot name, ::part() name, and --am-* token (global semantic + per-component @cssprop) is enumerated and declared FROZEN public surface in api/AUDIT.md (API-04, D-11)."
    - "Used-but-undocumented tokens/parts/slots flagged by the audit are documented with @cssprop/@csspart/@slot JSDoc (a behavior-neutral change) so the frozen surface is complete, OR explicitly recorded as intentionally internal — the human decides at the freeze checkpoint."
    - "The final api/custom-elements.baseline.json captures the fully-normalized, fully-documented surface, and `npm run diff:surface` against it is clean (API-05)."
    - "The report-only surface-diff CI job stays report-only this phase — it is NOT flipped to enforcing (that is Phase 6/SHIP-01)."
    - "The full characterization suite (jsdom + Chromium) is green, src/internal/ is non-exported, and the CEM tagName SET is unchanged from the committed baseline (no element added or removed)."
  artifacts:
    - api/AUDIT.md (frozen-surface section marked FROZEN with final post-normalization names)
    - api/custom-elements.baseline.json (final phase baseline capturing normalized + documented surface)
    - .changeset/freeze-slot-part-token-surface.md (records the frozen slot/part/token contract + any newly-documented tokens)
  key_links:
    - "@cssprop/@csspart/@slot JSDoc added for used-but-undocumented surface -> CEM enumerates it -> it becomes part of the frozen baseline"
    - "final baseline re-commit after refactor+renames -> diff:surface clean against HEAD"
    - "CI surface-diff job unchanged (still report-only) -> enforcing flip deferred to Phase 6"
  prohibitions:
    - "Do NOT flip the surface-diff CI job to enforcing (exit non-zero) — that is Phase 6/SHIP-01; it stays report-only (D-13)."
    - "Do NOT change any component RUNTIME behavior while adding JSDoc tags — tags are comments only."
    - "Do NOT add src/internal/ to barrels or package.json exports; the frozen surface must not include internal machinery (D-09)."
    - "Do NOT freeze a token/part/slot the human marked intentionally-internal at the checkpoint."
---

<objective>
Freeze the public slot / `::part()` / `--am-*` token surface and capture the final phase baseline: enumerate the complete surface, document any used-but-undocumented tokens/parts/slots the audit flagged (so the frozen contract is complete), mark it FROZEN in api/AUDIT.md, and re-commit api/custom-elements.baseline.json against the fully-normalized, fully-documented surface with a clean report-only diff. This is the phase's closing gate; after it the surface is a consistent, diffable contract ready for the Phase 6 enforcing flip.

Purpose: D-11 freezes all documented tokens (global + per-component), every part, and every slot as published contract — a one-way door. API-05's baseline must reflect the final normalized surface so the Phase 6 gate snapshots a consistent contract. The gate stays report-only this phase (D-13).

Output: a FROZEN surface section in api/AUDIT.md, added JSDoc for approved surface, a final re-committed baseline, and one Changeset recording the frozen contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md
@api/AUDIT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the frozen slot/part/token surface (one-way door — D-11)</name>
  <read_first>
    - api/AUDIT.md ("Frozen public surface" enumeration + "Undocumented-but-used tokens" gap list from Plan 02, re-generated against the post-normalization manifest)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-11 — frozen surface = all documented --am-* tokens + every @csspart + @slot; reversibility one-way)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§Frozen Surface Enumeration; A2 — undocumented tokens invisible to CEM)
  </read_first>
  <decision>Freeze the enumerated slot / ::part() / --am-* token surface as published contract, and for each used-but-undocumented token/part/slot the audit flagged, decide: document-and-freeze it (add @cssprop/@csspart/@slot JSDoc) OR record it as intentionally internal (leave undocumented, excluded from the frozen contract).</decision>
  <context>One-way door: a frozen token/part/slot becomes a published contract consumers rely on and cannot churn after 1.0 (D-11). Undocumented-but-used tokens are invisible to the CEM freeze unless tagged now — so the choice to tag-and-freeze vs keep-internal must be made deliberately before the baseline is finalized. Review the enumeration and the gap list in api/AUDIT.md.</context>
  <options>
    <option id="freeze-all-documented">
      <name>Freeze all documented surface + tag-and-freeze the flagged used tokens</name>
      <pros>Maximum consumer trust; the frozen contract covers every token a consumer might already be relying on (D-11).</pros>
      <cons>Constrains internal token churn at freeze; each newly-tagged token is locked.</cons>
    </option>
    <option id="freeze-documented-only">
      <name>Freeze only the already-documented surface; keep flagged tokens internal</name>
      <pros>Smaller frozen surface; more internal churn room post-1.0.</pros>
      <cons>Consumers relying on an untagged token get no stability guarantee — surface the risk explicitly.</cons>
    </option>
  </options>
  <resume-signal>Type "approved: freeze-all-documented" / "approved: freeze-documented-only", or list per-token exceptions.</resume-signal>
  <acceptance_criteria>
    - Human has reviewed the frozen-surface enumeration + gap list and chosen which flagged tokens/parts/slots to document-and-freeze vs keep internal before Task 2 runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document approved surface + mark AUDIT.md FROZEN (API-04, D-11)</name>
  <reversibility rating="one-way">Freezing a slot/part/token makes it a published contract consumers rely on; churning it after 1.0 breaks that contract (D-11).</reversibility>
  <files>api/AUDIT.md, .changeset/freeze-slot-part-token-surface.md</files>
  <read_first>
    - api/AUDIT.md (frozen-surface enumeration + gap list + the Task-1 decision on which flagged tokens to tag)
    - .planning/codebase/CONVENTIONS.md (JSDoc @slot/@csspart/@cssprop tag conventions to match)
    - src/tokens/primitives.css.ts, src/tokens/semantic.css.ts (global token definitions the frozen list references)
  </read_first>
  <action>
    For each used-but-undocumented token/part/slot the human approved to freeze in Task 1, add the corresponding `@cssprop`/`@csspart`/`@slot` JSDoc tag to the owning component's JSDoc block (comment-only; no runtime change) so CEM enumerates it. Leave the ones marked intentionally-internal undocumented. Re-run `npm run build:manifest` and `node scripts/build-audit.mjs` so the frozen-surface enumeration reflects the newly-documented surface, then mark that section in api/AUDIT.md as FROZEN, listing the final post-normalization slot names, ::part() names, and --am-* tokens (global + per-component) as the published contract. Record the intentionally-internal exclusions explicitly. Write one Changeset `.changeset/freeze-slot-part-token-surface.md` recording the frozen slot/part/token contract and any newly-documented tokens (a minor, additive change — documenting existing surface, not a break).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build:manifest && node scripts/build-audit.mjs && grep -qi 'FROZEN' api/AUDIT.md && npm run test:run 2>&1 | tail -12 && ls .changeset/freeze-slot-part-token-surface.md</automated>
  </verify>
  <acceptance_criteria>
    - Approved used-but-undocumented tokens/parts/slots now carry `@cssprop`/`@csspart`/`@slot` JSDoc and appear in dist/custom-elements.json; intentionally-internal ones are recorded as excluded in api/AUDIT.md.
    - api/AUDIT.md frozen-surface section is marked FROZEN with the final post-normalization slot/part/token names.
    - `npx tsc --noEmit` passes and `npm run test:run` stays green (JSDoc tags are comment-only — no behavior change).
    - One Changeset `.changeset/freeze-slot-part-token-surface.md` exists recording the frozen contract.
  </acceptance_criteria>
  <done>The complete slot/part/token surface is documented and marked FROZEN in api/AUDIT.md with the freeze recorded in a Changeset.</done>
</task>

<task type="auto">
  <name>Task 3: Final baseline re-commit + clean report-only diff + phase gate (API-05, D-13/D-14)</name>
  <files>api/custom-elements.baseline.json</files>
  <read_first>
    - scripts/cem-diff.mjs (Plan 01 comparator — the report-only diff, still exit 0)
    - .github/workflows/ci.yml (the surface-diff job — confirm it stays report-only; NOT flipped to enforcing)
    - .planning/phases/02-api-cleanup-cem-baseline/02-VALIDATION.md (§Sampling Rate — phase gate: full suite green + surface-diff job present + report-only)
  </read_first>
  <action>
    Re-run `npm run build:manifest` and re-copy dist/custom-elements.json to api/custom-elements.baseline.json so the FINAL baseline captures the fully-normalized (Plans 03-05) and fully-documented (Task 2) surface after the refactor (D-14). Run `npm run diff:surface` and confirm it is CLEAN against the fresh build (baseline == current) and still exits 0 (report-only — do NOT flip to enforcing; that is Phase 6/SHIP-01, D-13). Confirm the CI surface-diff job in ci.yml is unchanged and still report-only. Run the full characterization suite (`npm run test:run` then `npm run test:browser`), confirm src/internal/ is absent from barrels + package.json exports, and confirm the CEM tagName SET is unchanged from the committed baseline (no element added or removed). This is the phase closing gate.
  </action>
  <verify>
    <automated>npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && npm run test:run 2>&1 | tail -15 && ! grep -rn "internal/" src/index.ts src/index.all.ts && node -e "const p=require('./package.json'); if(JSON.stringify(p.exports).includes('internal')) throw new Error('exported'); if(p.files.includes('api')||p.files.includes('api/')) throw new Error('api published'); console.log('gate OK')"</automated>
  </verify>
  <acceptance_criteria>
    - api/custom-elements.baseline.json is re-committed as the final phase baseline and `npm run diff:surface` is CLEAN (no drift) against a fresh build, exit 0 (report-only).
    - The ci.yml surface-diff job is still report-only (not flipped to enforcing).
    - `npm run test:run` (jsdom) and `npm run test:browser` (Chromium) pass; CEM tagName SET unchanged from baseline (no element added/removed).
    - src/internal/ is absent from src/index.ts, src/index.all.ts, and package.json exports; api/ is not in package.json files.
  </acceptance_criteria>
  <done>The final baseline captures the normalized + frozen surface, the report-only diff is clean and still non-enforcing, and the full suite is green — the phase is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → frozen surface | frozen slot/part/token names become a published contract; api/ (audit + baseline) stays unpublished |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Information Disclosure | api/ publish leak | low | mitigate | api/ stays out of package.json `files`; verified in the Task 3 gate (`p.files` excludes api). |
| T-02-10 | Tampering | premature enforcing flip | low | accept | The surface-diff job stays report-only this phase; the enforcing flip (exit non-zero) is a deliberate Phase 6/SHIP-01 change, not made here. |

No new threats: JSDoc documentation + baseline snapshot on a client-side rendering library; no server, no untrusted input, no dependency added.
</threat_model>

## Artifacts this plan produces
- `api/AUDIT.md` — frozen-surface section marked FROZEN with final post-normalization slot/part/token names + intentionally-internal exclusions
- `api/custom-elements.baseline.json` — final phase baseline capturing the normalized + documented surface
- `.changeset/freeze-slot-part-token-surface.md` — Changeset recording the frozen contract + newly-documented tokens
- Added `@cssprop`/`@csspart`/`@slot` JSDoc on the components whose used surface was approved to freeze

<verification>
- Frozen surface enumerated + marked FROZEN in api/AUDIT.md; final baseline clean under diff:surface.
- Surface-diff CI job still report-only; full suite green; CEM tagName SET unchanged from baseline (no element added/removed); src/internal/ non-exported; api/ unpublished.
</verification>

<success_criteria>
The slot/part/token surface is enumerated and frozen (ROADMAP SC4) and the final api/custom-elements.baseline.json captures a consistent, diffable contract with a clean report-only diff (ROADMAP SC5) — the phase goal is met.
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-09-SUMMARY.md` when done.
</output>
