---
phase: 02-api-cleanup-cem-baseline
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - src/components/select/select.ts
  - src/components/data-grid/data-grid.ts
  - test/components/select.test.ts
  - test/components/data-grid.test.ts
  - .changeset/normalize-selection-events.md
  - api/custom-elements.baseline.json
autonomous: false
requirements: [API-02]
estimate:
  tokens: 62000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The value-changing select emits `am-change` like native <select> (its old `am-select-option` name is removed), and data-grid selection is reconciled to a single `am-change` value-change event with a decided detail shape (D-02)."
    - "The old selection event strings are removed outright across src and test — no alias (D-04)."
    - "The discrete-pick `am-select` on menu/list/tree-view/command-palette is left unchanged (it is already canonical, D-02)."
    - "select's internal `addEventListener`/`removeEventListener` for the old option-select name is renamed too, so the component still self-wires after the rename."
    - "Exactly one Changeset documents the selection-event normalization wave and its data-grid detail shape (D-05)."
    - "api/custom-elements.baseline.json is re-committed post-wave (D-14)."
  artifacts:
    - .changeset/normalize-selection-events.md (one Changeset for the wave, records the data-grid detail shape)
    - api/custom-elements.baseline.json (re-committed post-wave snapshot)
  key_links:
    - "select dispatch string + internal listener (select.ts:433/438) + @fires JSDoc + tests renamed together -> component still self-wires"
    - "data-grid single value-change event with the chosen detail shape replaces the per-row + aggregate pair"
    - "am-select on menu/list/tree-view/command-palette untouched (already canonical)"
  prohibitions:
    - "Do NOT rename the discrete-pick `am-select` on menu/list/tree-view/command-palette (D-02 keeps it)."
    - "Do NOT keep a backward-compat alias for the old selection names (D-04)."
    - "Do NOT alter data-grid selection RUNTIME behavior (which rows select) — only the event name/shape (behavior of selection logic preserved)."
    - "Do NOT create more than one Changeset for this wave (D-05)."
---

<!-- planner-discipline-allow: am-select-option -->
<!-- planner-discipline-allow: am-row-select -->
<!-- planner-discipline-allow: am-selection-change -->

<objective>
Normalize the selection event vocabulary per D-02: value-changing controls emit `am-change` (like native `<select>`), while discrete pick actions keep `am-select`. Concretely, rename select's `am-select-option` → `am-change` and reconcile data-grid's `am-row-select` + `am-selection-change` pair into a single `am-change` value-change event with a decided `detail` shape — landed as one Changeset wave with the baseline re-committed.

Purpose: D-02 locks the change-vs-select split as a one-way published-contract decision. The data-grid `detail` shape (single vs multi payload) is Claude's Discretion resolved here from the live component API and recorded in the Changeset.

Output: renamed/reconciled dispatches + `@fires` JSDoc + updated internal listener + updated tests across select and data-grid, one Changeset, and a re-committed baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@api/AUDIT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the selection rename + decide the data-grid detail shape (one-way door — D-02)</name>
  <read_first>
    - api/AUDIT.md ("Rename mapping" — the selection rows marked PENDING-DECISION; data-grid single-vs-multi note)
    - src/components/data-grid/data-grid.ts (:245 per-row `am-row-select` with detail { row, index, id, selected, keys }; :249 aggregate `am-selection-change` with detail { keys } — the two payloads to reconcile)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-02, D-04 — one-way; A1/Open Q1 on the payload)
  </read_first>
  <decision>Land select `am-select-option` → `am-change`, and reconcile data-grid's per-row + aggregate selection events into ONE `am-change` value-change event carrying the aggregate selection set (recommended `detail: { keys }` as the natural value-change payload) — old names removed outright.</decision>
  <context>One-way door: these are published event-name contracts. data-grid currently fires both a per-row event and an aggregate event; the reconciled `am-change` should carry the selection value (the aggregate keys set is the native value-change analogue). Confirm the detail shape so it is frozen consistently; it is recorded in the wave Changeset.</context>
  <options>
    <option id="aggregate">
      <name>am-change carries the aggregate selection set (detail { keys })</name>
      <pros>Matches native value-change semantics (the event reports the new value); single event, clean frozen surface.</pros>
      <cons>Consumers that relied on per-row detail must read the aggregate set and diff — recorded in the Changeset.</cons>
    </option>
    <option id="aggregate-plus-delta">
      <name>am-change carries aggregate + changed-row delta (detail { keys, row, selected })</name>
      <pros>Preserves per-row context some consumers used, in one event.</pros>
      <cons>Larger frozen payload; slightly less like native change.</cons>
    </option>
  </options>
  <resume-signal>Type "approved: aggregate" / "approved: aggregate-plus-delta", or specify a different detail shape.</resume-signal>
  <acceptance_criteria>
    - Human has chosen and typed the data-grid `am-change` detail shape and approved the select rename before Task 2 runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename selection events + internal listener + JSDoc + tests, write one Changeset (API-02, D-02/D-04/D-05)</name>
  <reversibility rating="one-way">Selection event names are published contracts; reverting after 1.0 needs a consumer migration (D-02).</reversibility>
  <files>src/components/select/select.ts, src/components/data-grid/data-grid.ts, test/components/select.test.ts, test/components/data-grid.test.ts, .changeset/normalize-selection-events.md</files>
  <read_first>
    - src/components/select/select.ts (dispatch at :111 with detail { value }, composed:false — preserve; internal listener add/remove at :433 and :438; @fires JSDoc at :146-147)
    - src/components/data-grid/data-grid.ts (dispatch at :245 and :249; @fires JSDoc at :42; selection prop JSDoc around :74)
    - test/components/select.test.ts (:7,14,36 assert the old option-select name); test/components/data-grid.test.ts (:91,95,97,164 assert both old names)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§Event-rename edits — "Wave: selection events" table; keep `composed:false`; done-condition grep)
  </read_first>
  <action>
    In select: change the option-select dispatch string to `am-change`, preserving its existing detail `{ value }` and `composed: false` option object; rename the internal `addEventListener`/`removeEventListener` (select.ts:433/438) to the new name so the component still self-wires; update the `@fires` JSDoc (select.ts:146-147) to document `am-change`; update test assertions (select.test.ts:7,14,36) to the new name.
    In data-grid: replace the per-row and aggregate selection dispatches with a single `am-change` value-change event carrying the approved detail shape (from Task 1); update the `@fires` JSDoc (data-grid.ts:42) and the selection-prop JSDoc; update test assertions (data-grid.test.ts:91,95,97,164). Preserve the selection RUNTIME logic (which rows become selected) exactly — change only the emitted event name and its detail shape.
    Leave the discrete-pick `am-select` on menu, list, tree-view, and command-palette untouched. Write exactly one Changeset `.changeset/normalize-selection-events.md` describing the selection normalization, listing select + data-grid old→new names and the chosen data-grid detail shape. After editing, grep src and test for the old selection strings — zero hits is the done condition (D-04).
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/select.test.ts test/components/data-grid.test.ts 2>&1 | tail -20 && ! grep -rnE "am-select-option|am-row-select|am-selection-change" src test && ls .changeset/normalize-selection-events.md</automated>
  </verify>
  <acceptance_criteria>
    - select dispatches `am-change` (detail { value }, composed:false preserved) and its internal listener binds `am-change`; `@fires` JSDoc updated.
    - data-grid dispatches a single `am-change` value-change event with the approved detail shape; the two old data-grid selection events are gone.
    - `grep -rnE "am-select-option|am-row-select|am-selection-change" src test` returns zero hits (D-04).
    - `am-select` on menu/list/tree-view/command-palette is unchanged (`git diff --name-only` does not list them).
    - select.test.ts and data-grid.test.ts assert the new names and pass under jsdom.
    - Exactly one Changeset file `.changeset/normalize-selection-events.md` exists (D-05).
  </acceptance_criteria>
  <done>Selection events are normalized to `am-change` on the value-changing controls with the old names removed, discrete-pick `am-select` preserved, one Changeset written, tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-commit baseline + report-only diff + full suite green (API-02, D-14)</name>
  <files>api/custom-elements.baseline.json</files>
  <read_first>
    - scripts/cem-diff.mjs (Plan 01 comparator — confirm only the intended selection renames drift)
    - .planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md (D-14)
  </read_first>
  <action>
    Re-run `npm run build:manifest`, run `npm run diff:surface`, and confirm the printed drift is exactly the select `am-change` rename plus the data-grid selection-event reconciliation (the CEM tagName SET is unchanged from the committed baseline — no element added or removed). Re-copy dist/custom-elements.json to api/custom-elements.baseline.json (D-14). Run the full characterization suite (`npm run test:run` then `npm run test:browser`) to confirm no regression.
  </action>
  <verify>
    <automated>npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && npm run test:run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run diff:surface` runs report-only (exit 0); its output reflects only the select + data-grid selection renames.
    - api/custom-elements.baseline.json is updated to the post-wave manifest.
    - `npm run test:run` passes; browser lane passes on the wave-merge run.
    - The CEM tagName SET is unchanged from the committed baseline (no element added or removed).
  </acceptance_criteria>
  <done>The baseline is re-committed for the selection wave, the diff shows only intended renames, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → component events | renamed selection event names + data-grid detail shape are the surface change; consumers rebind (Changeset records it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | Lit-safe templating invariant | low | accept | Rename touches event-name strings, one listener, and a detail object; no innerHTML/eval, no template change. |

No new threats: public event-name normalization on a client-side rendering library; no new input sink, no dependency.
</threat_model>

## Artifacts this plan produces
- `.changeset/normalize-selection-events.md` — one Changeset recording the selection break + data-grid detail shape (D-05)
- `api/custom-elements.baseline.json` — re-committed post-wave snapshot (D-14)
- Renamed/reconciled dispatches, internal listener, `@fires` JSDoc in select/data-grid and updated tests

<verification>
- Old selection event strings do not survive in src or test.
- Exactly one Changeset; baseline re-committed.
- Full suite green; CEM tagName SET unchanged from baseline (no element added/removed); `am-select` discrete-pick preserved.
</verification>

<success_criteria>
Selection events are normalized under the D-02 change-vs-select split with the old names removed, one Changeset, a re-committed baseline, and green tests (ROADMAP SC2, second wave).
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-04-SUMMARY.md` when done.
</output>
