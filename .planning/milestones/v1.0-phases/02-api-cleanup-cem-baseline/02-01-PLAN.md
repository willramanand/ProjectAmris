---
phase: 02-api-cleanup-cem-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/custom-elements.baseline.json
  - scripts/cem-diff.mjs
  - test/cem-diff.test.ts
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements: [API-05]
estimate:
  tokens: 70000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One end-to-end path is proven: `npm run build:manifest` emits dist/custom-elements.json, a committed api/custom-elements.baseline.json snapshots it, scripts/cem-diff.mjs diffs the two normalized surfaces, and a new CI surface-diff job runs that diff report-only (always exit 0) — all green on current code (API-05, D-13, D-14)."
    - "The comparator keys declarations by tagName, sorts every compared array by name, and strips volatile `source`/description fields, so identical builds produce zero drift (Pitfall 3)."
    - "The comparator ALWAYS exits 0 this phase; it does not fail CI on a surface change (report-only, D-13 — Phase 6 flips it)."
    - "api/ is absent from package.json `files`, so the baseline and future AUDIT.md are never published (D-12)."
    - "test/cem-diff.test.ts proves normalization on hand-built fixtures: identical → no diff, added event/removed part/renamed cssProperty → reported (de-risks the new load-bearing logic)."
  artifacts:
    - api/custom-elements.baseline.json (initial verbatim snapshot of dist/custom-elements.json)
    - scripts/cem-diff.mjs (normalized report-only CEM comparator, zero-dep Node script)
    - test/cem-diff.test.ts (comparator normalization unit test)
    - .github/workflows/ci.yml (new report-only surface-diff job)
    - package.json (new diff:surface script)
  key_links:
    - "scripts/cem-diff.mjs indexes modules[].declarations[] by `tagName` (never array index) -> stable diff across builds"
    - "cem-diff.mjs process.exit(0) unconditionally this phase -> CI surface-diff job is report-only (D-13)"
    - "ci.yml surface-diff job runs `npm run build:manifest` before the diff -> dist/custom-elements.json exists"
    - "package.json files=[dist,README.md] excludes api/ -> baseline stays unpublished (D-12)"
  prohibitions:
    - "Do NOT add any new npm dependency — D-13 mandates a zero-dep Node comparator."
    - "Do NOT make the comparator exit non-zero on drift this phase — report-only (D-13); the enforcing flip is Phase 6 (SHIP-01)."
    - "Do NOT add api/ to package.json `files` or `exports` — it must stay unpublished (D-12)."
---

<objective>
Establish the CEM baseline + report-only surface-diff spine end-to-end as the phase tracer: the load-bearing surface everything else in this phase diffs against. One production-quality vertical slice — build the manifest, snapshot it as the committed baseline, write the normalized zero-dep JSON comparator, unit-test the comparator's normalization, and wire a report-only CI job — proven green on current (pre-rename) code before any rename wave or refactor expands from it.

Purpose: The rename waves (Plans 03-05) re-commit this baseline after each approved wave (D-14) and rely on the report-only diff to surface only UNINTENDED drift; the freeze (Plan 09) captures the final baseline through the same comparator. Proving the whole diff pipeline works on one commit means every later wave expands from a proven spine.

Output: A committed `api/custom-elements.baseline.json`, `scripts/cem-diff.mjs` (normalized report-only comparator), `test/cem-diff.test.ts`, a new report-only `surface-diff` CI job, and a `diff:surface` npm script.

## Phase-wide prohibitions (apply to every Phase 2 plan)
- Behavior-preserving hard line: the refactor (Plans 06-08) relocates code without changing runtime behavior; the Phase 1 characterization suite (jsdom + Chromium, 66 × 1:1 tests) stays green and is the acceptance signal. No test deletions to make a refactor pass.
- Do NOT introduce `innerHTML`/`eval` or any non-Lit-safe template sink; keep the property→event, no-global-state model (no module-level singletons — controllers are per-host instances).
- Lit stays a `peerDependency` (`^3.3.2`); never bundle it. No global CSS, no CommonJS (ESM-only).
- No new registered custom elements — the feature freeze covers the full set of registered custom elements: **79 unique tagNames across 66 component files** (13 are compound/sub-elements, e.g. am-accordion-item, am-tab, am-tab-panel, am-option, am-menu-item). Reconciliation note: the ROADMAP/REQUIREMENTS "~67 components" and any earlier "feature freeze at 66/67" phrasing were approximate top-level counts; the authoritative freeze scope is the registered-element SET (79 tagNames), which must stay unchanged from the committed baseline — verified by `npm run diff:surface` (a set comparison, not a magic number). Extracted machinery is non-registered and non-exported.
- `--am-*` tokens only — no hardcoded colors introduced during any edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@custom-elements-manifest.config.js
@scripts/build-tokens-css.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Surface-diff spine — capture baseline, write the normalized comparator, wire the report-only CI job end-to-end (API-05, D-13, D-14)</name>
  <files>api/custom-elements.baseline.json, scripts/cem-diff.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - custom-elements-manifest.config.js (all 6 lines — globs src/**/*.ts, outdir dist, litelement true; confirms build:manifest emits dist/custom-elements.json)
    - scripts/build-tokens-css.mjs (the only existing .mjs Node script — mirror its node:fs / node:path / fileURLToPath header conventions and trailing console.log status line)
    - package.json (build:manifest script line 61; files line 48-51; exports 19-47 — confirm no api/ path)
    - .github/workflows/ci.yml (verify job lines 14-33 — mirror its checkout + setup-node@v4 Node 20 cache:npm + npm ci shape; keep workflow-level permissions: contents: read at lines 10-11)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§CEM Surface-Diff Comparator: fields to compare keyed by tagName, normalization rules, report-only exit 0; §CI Wiring for the job shape)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§scripts/cem-diff.mjs analog; §.github/workflows/ci.yml job-to-add)
  </read_first>
  <precondition>`npm run build:manifest` (cem analyze, wired at package.json:61) must succeed and emit dist/custom-elements.json — the task runs it before snapshotting; halt if it errors.</precondition>
  <action>
    Run `npm run build:manifest` to produce dist/custom-elements.json, then create the `api/` directory and copy that file verbatim to `api/custom-elements.baseline.json` (the initial pre-normalization snapshot, D-14). Do NOT add `api/` to package.json `files` (it is `["dist","README.md"]` and must stay that way so api/ is unpublished, D-12) and do NOT add it to `exports`.
    Write `scripts/cem-diff.mjs` as a zero-dep ESM Node script (mirror scripts/build-tokens-css.mjs header: node:fs readFileSync, node:path, fileURLToPath). It reads two manifest paths from process.argv (baseline, current), indexes each by iterating `modules[].declarations[]` and keying every declaration that has a `tagName` into a map keyed by that tagName (never by array position — CEM ordering is not stable across builds). For each element it extracts a normalized surface object: attributes (name+default), public fields (members filtered to kind==='field' and privacy!=='private', names only), events (names), slots (names), cssParts (names), and cssProperties (names — the --am-* freeze surface). Every extracted array is sorted by name. Volatile fields (`source` module/line references, and description/summary prose) are stripped and never compared. It then diffs: the set of tagNames (added/removed elements), and per-element each surface array (added/removed/changed entries), printing a human-readable per-element report to stdout. It ALWAYS calls process.exit(0) at the end this phase (report-only, D-13 — Phase 6 flips to non-zero). Take the comparator field-picking and normalization approach from RESEARCH §CEM Surface-Diff Comparator.
    Add a new `surface-diff` job to `.github/workflows/ci.yml` mirroring the verify job's runner setup (ubuntu-latest, actions/checkout@v4, actions/setup-node@v4 with node-version 20 + cache npm, npm ci). Its steps: build the manifest (`npm run build:manifest`), then run the comparator against the committed baseline and the freshly built manifest. The job must NOT use `continue-on-error` as its only guard — the script's own exit 0 is the report-only mechanism (so the Phase 6 flip is a one-line script change, not a workflow edit). Keep the workflow-level `permissions: contents: read`.
  </action>
  <verify>
    <automated>npm run build:manifest && node scripts/cem-diff.mjs api/custom-elements.baseline.json dist/custom-elements.json; echo "EXIT:$?" && test -f api/custom-elements.baseline.json && node -e "const p=require('./package.json'); if(p.files.includes('api')||p.files.includes('api/')) throw new Error('api/ must not be published'); if(JSON.stringify(p.exports).includes('api/')) throw new Error('api/ must not be exported'); console.log('unpublished OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `api/custom-elements.baseline.json` exists and parses as JSON with a top-level `modules` array (`node -e "JSON.parse(require('fs').readFileSync('api/custom-elements.baseline.json'))"` succeeds).
    - `node scripts/cem-diff.mjs api/custom-elements.baseline.json dist/custom-elements.json` prints a report and the command's exit status is 0 (EXIT:0 in the verify output) — identical inputs report zero drift.
    - scripts/cem-diff.mjs indexes by `tagName` and sorts compared arrays by `name` (grep the script: it references `tagName` and a sort on `name`); it contains a `process.exit(0)`.
    - `.github/workflows/ci.yml` contains a `surface-diff:` job that runs `npm run build:manifest` then invokes the comparator; workflow-level `permissions: contents: read` is retained.
    - package.json `files` does not include `api`/`api/` and `exports` has no `api/` path.
  </acceptance_criteria>
  <done>The baseline is committed, the normalized comparator runs report-only (exit 0) against a fresh build, the CI surface-diff job is wired, and api/ stays unpublished.</done>
  <reversibility rating="reversible">The comparator is a local swappable script behind a stable CLI (baseline path, current path); Phase 6 may replace it with @wc-toolkit/changelog (D-13). No published contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comparator normalization unit test — prove identical/added/removed/renamed diff behavior on fixtures (API-05)</name>
  <files>test/cem-diff.test.ts</files>
  <behavior>
    - Given two identical hand-built manifest fixtures (same tagName, same events/slots/cssParts/cssProperties), the comparator reports NO surface drift.
    - Given a fixture whose element gains one event vs the baseline, the diff reports that event as added.
    - Given a fixture whose element loses one cssPart, the diff reports it as removed.
    - Given a fixture whose element renames a cssProperty (`--am-x-old` → `--am-x-new`), the diff reports one removed + one added token (proving cssProperties are compared by name).
    - Given two fixtures that differ ONLY in each declaration's `source` line references and array ordering, the comparator reports NO drift (normalization strips source and sorts by name — Pitfall 3).
  </behavior>
  <read_first>
    - scripts/cem-diff.mjs (as written in Task 1 — the exported/invocable diff surface to test; if it is a top-level script, refactor its diff+normalize logic into a named export the test imports, keeping the CLI entry intact)
    - test/components/table.test.ts (a Phase 1 test file — mirror its vitest import style, describe/it structure, and expect usage)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§CEM Surface-Diff Comparator "Recommended: a unit test for the comparator itself")
  </read_first>
  <action>
    Ensure scripts/cem-diff.mjs exposes its normalize+diff logic as an importable function (e.g. a named `diffManifests(baseline, current)` returning a structured result of added/removed/changed per tagName), keeping the `process.argv` CLI wrapper that prints and exits 0 when run directly. Write `test/cem-diff.test.ts` in the jsdom project style (Phase 1 vitest conventions) that builds small in-memory manifest objects (a couple of fake elements with attributes/events/slots/cssParts/cssProperties) and asserts the five behaviors in the behavior block by calling the imported diff function directly — no file I/O. Include the source-churn-only case (same surface, different `source` and array order) asserting zero drift to lock the normalization contract.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/cem-diff.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/cem-diff.test.ts` exists and imports the diff function from scripts/cem-diff.mjs.
    - `npx vitest run --project jsdom test/cem-diff.test.ts` exits 0 with at least 5 passing assertions covering identical, added-event, removed-part, renamed-cssProperty, and source-churn-only cases.
    - The source-churn-only case asserts an empty/no-drift result, proving `source` stripping + name sorting.
  </acceptance_criteria>
  <done>The comparator's normalization and diff logic is unit-tested green on hand-built fixtures, de-risking the one piece of new load-bearing logic.</done>
</task>

<task type="auto">
  <name>Task 3: Local diff script + CI wiring finish — add diff:surface npm script and route the CI job through it (API-05)</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - package.json (scripts block lines 58-75 — add alongside build:manifest)
    - .github/workflows/ci.yml (the surface-diff job added in Task 1 — route its diff step through the npm script)
  </read_first>
  <action>
    Add a `diff:surface` script to package.json that runs `node scripts/cem-diff.mjs api/custom-elements.baseline.json dist/custom-elements.json` for local use. Update the CI surface-diff job's diff step to call `npm run diff:surface` (keeping the preceding `npm run build:manifest` step) so local and CI invoke the identical command. Do not change any other script and do not add dependencies.
  </action>
  <verify>
    <automated>npm run build:manifest && npm run diff:surface; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts contains `diff:surface` invoking scripts/cem-diff.mjs with the baseline and dist manifest paths.
    - `npm run diff:surface` (after build:manifest) exits 0.
    - The ci.yml surface-diff job's diff step runs `npm run diff:surface`.
  </acceptance_criteria>
  <done>A single `diff:surface` command runs the report-only diff locally and in CI, identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → repo files | scripts/cem-diff.mjs reads repo-local JSON manifests only; no network, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | npm dependency surface | low | accept | No new packages added (D-13 zero-dep); comparator is a local Node script — no supply-chain surface introduced this plan. |
| T-02-SC | Tampering | package installs | low | accept | Zero installs this phase; package-legitimacy gate N/A (RESEARCH §Package Legitimacy Audit — nothing to verify). |

No new threats: client-side rendering library plus a local build-tooling script; no server, no untrusted input sink, no new dependency.
</threat_model>

## Artifacts this plan produces
- `api/custom-elements.baseline.json` — initial verbatim snapshot of dist/custom-elements.json (D-14 baseline; re-committed by later waves)
- `scripts/cem-diff.mjs` — normalized, report-only CEM surface comparator with an importable `diffManifests`-style function (D-13)
- `test/cem-diff.test.ts` — comparator normalization unit test
- `.github/workflows/ci.yml` — new report-only `surface-diff` job
- `package.json` — new `diff:surface` script (no dependency changes)

<verification>
- `npm run build:manifest` succeeds and emits dist/custom-elements.json.
- `npm run diff:surface` exits 0 and prints a readable per-element report.
- `npx vitest run --project jsdom test/cem-diff.test.ts` passes.
- `git grep -n "api" package.json` shows no `api/` entry in `files` or `exports`.
</verification>

<success_criteria>
The CEM baseline is committed, a normalized zero-dep comparator diffs it report-only (exit 0) locally and in a new CI job, and the comparator's normalization is unit-tested — the surface-diff spine (ROADMAP SC5) is proven end-to-end on pre-normalization code.
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-01-SUMMARY.md` when done.
</output>
