---
phase: 02-api-cleanup-cem-baseline
plan: 07
type: execute
wave: 6
depends_on: [02-06, 02-04]
files_modified:
  - src/components/select/select.ts
  - src/components/date-picker/date-picker.ts
  - src/internal/helpers/date-utils.ts
  - src/internal/controllers/floating-position.ts
  - src/internal/controllers/listbox-nav.ts
  - src/internal/controllers/option-filter.ts
autonomous: true
requirements: [API-03]
estimate:
  tokens: 90000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "select delegates positioning, listbox keyboard nav, and option filtering to the shared src/internal/ controllers created in Plan 06 (API-03, D-08)."
    - "date-picker delegates positioning to FloatingPositionController and its calendar-grid generation + date math is extracted to a pure src/internal/helpers/ module (D-07/D-08)."
    - "Both refactors are behavior-preserving: the Phase 1 select and date-picker characterization tests (jsdom + Chromium) stay GREEN with ZERO test edits (D-10)."
    - "`autoUpdate` stays ungated for both; no listener-lifecycle or focus seam is fixed here (Phase 3/4)."
    - "src/internal/ stays absent from barrels + package.json exports; CEM tagName SET stays unchanged from the committed baseline (no element added or removed); no public surface change from the refactor."
  artifacts:
    - src/components/select/select.ts (delegates to the three controllers)
    - src/components/date-picker/date-picker.ts (delegates positioning to FloatingPositionController, date math to the helper)
    - src/internal/helpers/date-utils.ts (pure calendar-grid + date-math helper)
  key_links:
    - "select reuses FloatingPositionController/ListboxNavController/OptionFilter from Plan 06 -> no NEW controller files; select may extend their options additively, so the three controller files are declared in files_modified and this plan is serialized before 02-08 (which reuses ListboxNavController) to avoid a parallel-edit hazard on listbox-nav.ts"
    - "date-picker consumes FloatingPositionController + a pure date-utils module -> positioning shared, date math testable in isolation"
    - "src/internal/ NOT added to barrels/exports -> surface unchanged (Pitfall 4)"
  prohibitions:
    - "Do NOT gate autoUpdate (PERF-04/Phase 4) or fix any listener/focus seam (Phase 3) while relocating."
    - "Do NOT add src/internal/ to src/index.ts, src/index.all.ts, or package.json exports (D-09)."
    - "Do NOT edit the select or date-picker characterization tests to make the refactor pass (D-10)."
    - "Do NOT modify the controllers created in Plan 06 in a way that breaks combobox — consume them, extend options additively if needed."
---

<objective>
Expand the proven controller decomposition (Plan 06) to the next two floating consumers: wire select onto the three shared controllers, and refactor date-picker to consume FloatingPositionController for positioning while extracting its calendar-grid/date-math into a pure `src/internal/helpers/` module — both behavior-preserving with the Phase 1 tests green unchanged.

Purpose: D-08 targets shared-machinery extraction across the Big-4; select duplicates combobox's floating/listbox/filter machinery, and date-picker duplicates the floating machinery plus carries pure date logic that belongs in a helper (D-07). This lands two of the four refactors from the proven seam.

Output: select + date-picker delegating to the shared units, plus a pure date-utils helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@src/internal/controllers/floating-position.ts
@src/internal/controllers/listbox-nav.ts
@src/internal/controllers/option-filter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire select onto the three shared controllers (API-03, D-08, behavior-preserving)</name>
  <files>src/components/select/select.ts, src/internal/controllers/floating-position.ts, src/internal/controllers/listbox-nav.ts, src/internal/controllers/option-filter.ts</files>
  <read_first>
    - src/components/select/select.ts (its floating setup around :463/:476-485; its listbox keydown + highlighted-index logic; its option filtering; disconnectedCallback teardown)
    - src/internal/controllers/floating-position.ts, listbox-nav.ts, option-filter.ts (the Plan 06 units — the consumption contract combobox already uses)
    - src/components/combobox/combobox.ts (the Plan 06 reference consumer — mirror how it instantiates and delegates to the controllers)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§floating consumers: consume via host.addController in the field initializer, delegate in updated(), keep dispatches in the host)
  </read_first>
  <action>
    Refactor select to consume FloatingPositionController, ListboxNavController, and the OptionFilter unit, mirroring combobox's consumption pattern from Plan 06. Instantiate each in a field initializer with select's exact reference/floating accessors, placement, offset, and middleware set (preserve select's specific options — do not unify them with combobox's). Delegate positioning start/stop from select's `updated()` using the SAME open gating select has today, and delegate keydown navigation + option filtering to the shared units, removing select's now-duplicated inline machinery. Keep select's `am-change` dispatch and its internal listener (renamed in Plan 04) in the host. Mirror select's existing disconnectedCallback teardown in the controllers' hostDisconnected. Keep autoUpdate ungated. If select needs a controller option combobox did not, add it additively to the controller so combobox still works. Do NOT add src/internal/ to any barrel or exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --project jsdom test/components/select.test.ts test/components/combobox.test.ts 2>&1 | tail -20 && ! grep -rnE "innerHTML|eval\(" src/internal</automated>
  </verify>
  <acceptance_criteria>
    - select imports the three units from `../../internal/...js` and delegates positioning/nav/filtering to them; its inline duplicated machinery is removed.
    - `npx vitest run --project jsdom test/components/select.test.ts` AND `test/components/combobox.test.ts` pass, both test files UNCHANGED (`git diff --name-only` lists neither test).
    - `npx tsc --noEmit` passes; `grep -rnE "innerHTML|eval\(" src/internal` returns zero hits.
    - `autoUpdate` appears only in the controller, not directly in select.ts.
  </acceptance_criteria>
  <done>select delegates to the three shared controllers behavior-preservingly with its and combobox's tests green unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor date-picker — FloatingPositionController + pure date-utils helper (API-03, D-07/D-08)</name>
  <files>src/components/date-picker/date-picker.ts, src/internal/helpers/date-utils.ts</files>
  <read_first>
    - src/components/date-picker/date-picker.ts (floating setup around :292/:541-544; the calendar-grid generation + date-math functions that are pure; disconnectedCallback teardown)
    - src/internal/controllers/floating-position.ts (the positioning contract to consume)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§Per-component pure helpers: date-picker calendar-grid + date math → pure module, positioning via controller #1)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§date-picker: floating consumer + pure date helper)
  </read_first>
  <action>
    Create `src/internal/helpers/date-utils.ts` as a pure ESM module (no ReactiveController, no DOM state — per D-07 pure logic is a plain module) holding date-picker's calendar-grid generation and date-math functions, relocated verbatim (same inputs/outputs). Refactor date-picker to import those functions and to consume FloatingPositionController for its dropdown positioning, mirroring the Plan 06 pattern, with date-picker's exact reference/floating accessors and options and the SAME open gating it has today. Remove the now-relocated inline date math and inline positioning, mirroring date-picker's disconnectedCallback teardown in the controller's hostDisconnected. Keep autoUpdate ungated and do not touch any listener/focus seam (Phase 3). Do NOT add src/internal/ to any barrel or exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --project jsdom test/components/date-picker.test.ts 2>&1 | tail -20 && ! grep -rnE "innerHTML|eval\(" src/internal</automated>
  </verify>
  <acceptance_criteria>
    - `src/internal/helpers/date-utils.ts` exists as a pure module (no `addController`, no DOM/instance state) exporting date-picker's calendar-grid + date-math functions.
    - date-picker imports date-utils and consumes FloatingPositionController; its inline date math and inline positioning are removed.
    - `npx vitest run --project jsdom test/components/date-picker.test.ts` passes with the test file UNCHANGED.
    - `npx tsc --noEmit` passes; `grep -rnE "innerHTML|eval\(" src/internal` returns zero hits.
  </acceptance_criteria>
  <done>date-picker delegates positioning to the shared controller and its date logic lives in a pure testable helper, behavior-preserving, test green unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Behavior-preservation gate — full suite green, surface unchanged, boundary non-exported (API-03, D-09/D-10)</name>
  <files>src/components/select/select.ts, src/components/date-picker/date-picker.ts</files>
  <read_first>
    - src/index.ts, src/index.all.ts (confirm neither references src/internal/)
    - scripts/cem-diff.mjs (Plan 01 comparator — confirm no surface change)
    - .planning/phases/02-api-cleanup-cem-baseline/02-VALIDATION.md (§Sampling Rate — full jsdom+browser per wave)
  </read_first>
  <action>
    Run the full characterization suite (`npm run test:run` then `npm run test:browser`) to prove select + date-picker (and the still-green combobox) refactors are behavior-preserving in jsdom and Chromium with zero test edits. Run `npm run build:manifest` and `npm run diff:surface`; confirm NO surface change from the refactor and the CEM tagName SET unchanged from the committed baseline (no element added or removed). Confirm src/internal/ is absent from barrels and package.json exports. Do NOT re-commit the baseline (refactor changes no surface; any diff is an unintended regression to investigate).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -15 && npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && ! grep -rn "internal/" src/index.ts src/index.all.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` and `npm run test:browser` pass with no test edits.
    - `npm run diff:surface` reports no surface change; CEM tagName SET unchanged from baseline (no element added/removed).
    - `grep -rn "internal/" src/index.ts src/index.all.ts` returns zero hits; package.json exports has no `internal` path.
    - api/custom-elements.baseline.json is unchanged by this plan.
  </acceptance_criteria>
  <done>select and date-picker refactors are proven behavior-preserving in jsdom + Chromium, the boundary stays non-exported, and the surface is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refactor edits → Lit-safe templating invariant | relocating machinery must not introduce a non-Lit-safe render sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | src/internal controllers + helpers | low | mitigate | Grep gate `! grep -rnE "innerHTML|eval\(" src/internal` on every task; date-utils is a pure module (no DOM sink); controllers are per-host instances. |

No new threats: no server, no untrusted input, no new dependency (@floating-ui/dom already present).
</threat_model>

## Artifacts this plan produces
- `src/components/select/select.ts` — delegates positioning/nav/filtering to the shared controllers
- `src/components/date-picker/date-picker.ts` — delegates positioning to FloatingPositionController, date math to the helper
- `src/internal/helpers/date-utils.ts` — pure calendar-grid + date-math module

<verification>
- select + date-picker + combobox characterization tests green in jsdom + Chromium, zero test edits.
- src/internal/ non-exported; CEM tagName SET unchanged from baseline (no element added/removed); no innerHTML/eval in src/internal.
- diff:surface shows no surface change; baseline unchanged.
</verification>

<success_criteria>
select and date-picker are refactored onto the shared src/internal/ machinery behavior-preservingly with Phase 1 tests green unchanged (ROADMAP SC3, expansion).
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-07-SUMMARY.md` when done.
</output>
