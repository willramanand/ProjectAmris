---
phase: 02-api-cleanup-cem-baseline
plan: 08
type: execute
wave: 7
depends_on: [02-06, 02-07]
files_modified:
  - src/components/time-picker/time-picker.ts
  - src/internal/helpers/time-utils.ts
autonomous: true
requirements: [API-03]
estimate:
  tokens: 70000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "time-picker's time-list generation and value parsing are extracted into a pure src/internal/helpers/ module (D-07 pure logic → plain module)."
    - "If time-picker has a listbox, it reuses the Plan 06 ListboxNavController; the floating-position controller is NOT forced onto it (time-picker uses no @floating-ui/dom — Pitfall 5)."
    - "The refactor is behavior-preserving: the Phase 1 time-picker characterization tests stay GREEN with ZERO test edits (D-10)."
    - "src/internal/ stays absent from barrels + package.json exports; CEM tagName SET stays unchanged from the committed baseline (no element added or removed); no public surface change."
  artifacts:
    - src/internal/helpers/time-utils.ts (pure time-list generation + value parsing)
    - src/components/time-picker/time-picker.ts (delegates time logic to the helper; optional listbox-nav reuse)
  key_links:
    - "time-picker imports pure time-utils -> time logic testable in isolation, no floating controller"
    - "src/internal/ NOT added to barrels/exports -> surface unchanged (Pitfall 4)"
    - "time-picker's optional ListboxNavController reuse is consumption-only; this plan is serialized AFTER 02-07 (depends_on: 02-07, Wave 7) so any additive controller edit 02-07 makes lands first -> no parallel-edit hazard on listbox-nav.ts"
  prohibitions:
    - "Do NOT force FloatingPositionController onto time-picker — it uses no @floating-ui/dom (Pitfall 5)."
    - "Do NOT add src/internal/ to src/index.ts, src/index.all.ts, or package.json exports (D-09)."
    - "Do NOT edit the time-picker characterization test to make the refactor pass (D-10)."
    - "Do NOT fix any leak/lifecycle seam exposed while relocating (Phase 3)."
---

<objective>
Refactor the fourth Big-4 component, time-picker (627 lines), by extracting its time-list generation and value parsing into a pure `src/internal/helpers/` module and reusing the ListboxNavController where it has a listbox — behavior-preserving, with the Phase 1 test green unchanged. Unlike the other three, time-picker uses no floating-ui, so it gets a pure-helper split, not the positioning controller (Pitfall 5).

Purpose: D-08 covers all four 600+ line components; time-picker's payload is time math and keyboard nav, not positioning. Extracting pure logic to a module (D-07) makes it testable in isolation without registering an element or touching the frozen surface.

Output: a pure time-utils helper and a time-picker that delegates to it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@src/internal/controllers/listbox-nav.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pure time-utils helper + optional listbox-nav reuse (API-03, D-07/D-08)</name>
  <files>src/components/time-picker/time-picker.ts, src/internal/helpers/time-utils.ts</files>
  <read_first>
    - src/components/time-picker/time-picker.ts (its time-list generation, value parsing/formatting, and keyboard-nav functions — identify the pure ones vs the DOM-bound ones)
    - src/internal/controllers/listbox-nav.ts (the Plan 06 nav controller — reuse only if time-picker exposes a listbox with highlighted-index movement)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§Per-component pure helpers: time-picker does NOT use @floating-ui/dom — extract a pure time-list/parse helper; §Pitfall 5)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§time-picker is different: pure helper split, do NOT force floating-position.ts)
  </read_first>
  <action>
    Create `src/internal/helpers/time-utils.ts` as a pure ESM module (no ReactiveController, no DOM/instance state) holding time-picker's time-list generation and value parsing/formatting functions, relocated verbatim (same inputs/outputs). Refactor time-picker to import those functions, removing the now-relocated inline logic. If time-picker has a listbox with highlighted-index keyboard movement matching the ListboxNavController contract, delegate that nav to the controller (reuse, do not duplicate); otherwise leave time-picker's nav inline and note in the SUMMARY that reuse did not apply. Do NOT introduce any floating-ui positioning. Preserve runtime behavior exactly, mirror any disconnectedCallback teardown. Do NOT add src/internal/ to any barrel or exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --project jsdom test/components/time-picker.test.ts 2>&1 | tail -20 && ! grep -rnE "innerHTML|eval\(" src/internal && ! grep -rn "floating-ui" src/components/time-picker/time-picker.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/internal/helpers/time-utils.ts` exists as a pure module (no `addController`, no DOM/instance state) exporting time-picker's time-list + parse/format functions.
    - time-picker imports time-utils and its inline time logic is removed; it introduces no `@floating-ui/dom` import.
    - `npx vitest run --project jsdom test/components/time-picker.test.ts` passes with the test file UNCHANGED (`git diff --name-only` does not list it).
    - `npx tsc --noEmit` passes; `grep -rnE "innerHTML|eval\(" src/internal` returns zero hits.
  </acceptance_criteria>
  <done>time-picker's time logic lives in a pure testable helper (no floating controller), behavior-preserving, test green unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Behavior-preservation gate — full suite green, surface unchanged, boundary non-exported (API-03, D-09/D-10)</name>
  <files>src/components/time-picker/time-picker.ts</files>
  <read_first>
    - src/index.ts, src/index.all.ts (confirm neither references src/internal/)
    - scripts/cem-diff.mjs (Plan 01 comparator — confirm no surface change)
    - .planning/phases/02-api-cleanup-cem-baseline/02-VALIDATION.md (§Sampling Rate — full jsdom+browser per wave)
  </read_first>
  <action>
    Run the full characterization suite (`npm run test:run` then `npm run test:browser`) to prove the time-picker refactor is behavior-preserving in jsdom and Chromium with zero test edits. Run `npm run build:manifest` and `npm run diff:surface`; confirm NO surface change and the CEM tagName SET unchanged from the committed baseline (no element added or removed). Confirm src/internal/ is absent from barrels and package.json exports. Do NOT re-commit the baseline (refactor changes no surface).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -15 && npm run build:manifest && npm run diff:surface; echo "DIFF_EXIT:$?" && ! grep -rn "internal/" src/index.ts src/index.all.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` and `npm run test:browser` pass with no test edits.
    - `npm run diff:surface` reports no surface change; CEM tagName SET unchanged from baseline (no element added/removed).
    - `grep -rn "internal/" src/index.ts src/index.all.ts` returns zero hits; package.json exports has no `internal` path.
    - api/custom-elements.baseline.json is unchanged by this plan.
  </acceptance_criteria>
  <done>The time-picker refactor is proven behavior-preserving in jsdom + Chromium, the boundary stays non-exported, and the surface is unchanged — all four Big-4 components are refactored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refactor edits → Lit-safe templating invariant | relocating time logic must not introduce a non-Lit-safe render sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | src/internal helpers | low | mitigate | Grep gate `! grep -rnE "innerHTML|eval\(" src/internal` on every task; time-utils is a pure module with no DOM sink. |

No new threats: no server, no untrusted input, no new dependency.
</threat_model>

## Artifacts this plan produces
- `src/internal/helpers/time-utils.ts` — pure time-list generation + value parsing/formatting
- `src/components/time-picker/time-picker.ts` — delegates time logic to the helper (optional listbox-nav reuse; no floating-ui)

<verification>
- time-picker characterization test green in jsdom + Chromium, zero test edits.
- src/internal/ non-exported; CEM tagName SET unchanged from baseline (no element added/removed); no innerHTML/eval in src/internal; no floating-ui in time-picker.
- diff:surface shows no surface change; baseline unchanged.
</verification>

<success_criteria>
time-picker is refactored via a pure time-utils helper behavior-preservingly with the Phase 1 test green unchanged, completing the Big-4 refactor (ROADMAP SC3).
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-08-SUMMARY.md` when done.
</output>
