---
phase: 02-api-cleanup-cem-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-audit.mjs
  - api/AUDIT.md
autonomous: true
requirements: [API-01, API-04]
estimate:
  tokens: 78000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "api/AUDIT.md exists with seven dimension matrices — one each for event, prop, boolean-naming, default, slot, part, and --am-* token — tabulated across ALL 79 registered custom elements (the 66 component files register 79 unique tagNames, including 13 compound/sub-elements like am-accordion-item, am-tab, am-tab-panel, am-option, am-menu-item), not just the 66 top-level component files (API-01, ROADMAP SC1)."
    - "The event matrix reconciles CEM `events` (JSDoc @fires-derived) against a live `new CustomEvent` grep of src/components, so events CEM never saw are not missed (RESEARCH provenance caution)."
    - "Each dimension matrix marks the outliers and, for events, the canonical target name per D-01 (overlay open/close), D-02 (change vs select), and D-03 (full normalization) — producing the concrete old→new rename mapping Plans 03-05 consume."
    - "A frozen-surface enumeration section lists every documented --am-* token (global semantic + per-component @cssprop), every @csspart, and every @slot in the CEM (API-04, D-11)."
    - "The enumeration flags undocumented-but-used tokens (referenced in css`` but never @cssprop-tagged) as a gap class, since CEM cannot see them (RESEARCH A2)."
    - "api/AUDIT.md is under api/ and unpublished (D-12) — api/ stays out of package.json files."
  artifacts:
    - scripts/build-audit.mjs (reads dist/custom-elements.json + greps src/components, emits the AUDIT.md matrices)
    - api/AUDIT.md (D-12 dimension matrices + frozen-surface enumeration + concrete rename mapping)
  key_links:
    - "build-audit.mjs reads the SAME dist/custom-elements.json the comparator reads -> audit and baseline share one source of truth"
    - "event matrix = CEM events UNION new CustomEvent grep -> undercounted @fires JSDoc cannot hide a dispatched event"
    - "outlier target names follow D-01/D-02/D-03 vocabulary -> Plans 03-05 rename tasks read the mapping straight from AUDIT.md"
  prohibitions:
    - "Do NOT rename anything in this plan — it only measures and tabulates; renames are Plans 03-05 (API-02)."
    - "Do NOT add api/ to package.json `files` or `exports` (D-12)."
    - "Do NOT add a new dependency — build-audit.mjs is a zero-dep Node script (D-13)."
---

<objective>
Produce the cross-component consistency audit as committed, human-reviewable dimension matrices in `api/AUDIT.md`, and enumerate the frozen slot/part/token surface — the analytical foundation the rename waves (Plans 03-05) and the freeze (Plan 09) both read from. The audit measures the documented public surface off all 79 registered custom elements (the 66 component files register 79 tagNames, including 13 compound/sub-elements), reconciles CEM against a live event grep, and produces the concrete old→new rename mapping under the D-01/D-02/D-03 vocabulary.

Purpose: D-03 mandates full normalization of every outlier the matrices surface; the matrices are that enumeration. Without them the rename waves have no authoritative outlier list, and the freeze (API-04) has no surface to declare frozen.

Output: `scripts/build-audit.mjs` (reproducible matrix generator) and `api/AUDIT.md` (seven dimension matrices + the rename mapping + the frozen-surface enumeration + the undocumented-token gap list).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-cleanup-cem-baseline/02-CONTEXT.md
@.planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md
@.planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@scripts/build-tokens-css.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event/prop/boolean/default matrices + rename mapping — reconcile CEM against live grep, tabulate outliers (API-01, D-01/D-02/D-03)</name>
  <files>scripts/build-audit.mjs, api/AUDIT.md</files>
  <read_first>
    - scripts/build-tokens-css.mjs (mirror its .mjs header: node:fs, node:path, fileURLToPath, top-level script, trailing console.log)
    - .planning/codebase/CONVENTIONS.md (current naming patterns: event am-* prefix, boolean prop names, ButtonVariant/InputSize type conventions — the dimensions to measure against)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§Rename Inventory with verbatim current event names + targets; §"Audit-Matrix Mechanism"; the provenance caution about @fires undercounting dispatched events; §Open Questions on data-grid payload + change/toggle family)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§Event-rename edits tables with file:line for overlay + selection waves)
    - src/components/combobox/combobox.ts (lines 60-80 — the boolean props `async` (63) and `select` (72) that collides with the am-select element name; a concrete boolean/prop outlier example)
  </read_first>
  <precondition>dist/custom-elements.json must exist — run `npm run build:manifest` first if absent (it is regenerated, not committed).</precondition>
  <action>
    Write `scripts/build-audit.mjs` (zero-dep ESM Node script). It reads dist/custom-elements.json and, for every declaration with a `tagName` (ALL 79 registered elements — the 66 component files register 79 tagNames including 13 compound/sub-elements, so sub-elements like am-accordion-item / am-tab / am-option are tabulated too, never only the top-level 66), extracts per-dimension data: events (from CEM `events`), attributes/props (from CEM `attributes` + `members` kind field), boolean props (fields typed boolean / reflected), and defaults (attribute `default` values). It ALSO scans src/components for `new CustomEvent('...')` occurrences and reconciles that set against the CEM `events` set per element, so an event dispatched but not documented in `@fires` still appears in the matrix (RESEARCH provenance caution). It emits markdown tables into `api/AUDIT.md`: one matrix per dimension (event, prop, boolean-naming, default) with a row per component and a column marking whether the value is canonical or an OUTLIER.
    In api/AUDIT.md, add an explicit "Rename mapping" section that lists every event outlier with its canonical target: overlay lifecycle `am-show`/`am-hide` (dropdown, popover, context-menu) → `am-open`/`am-close` per D-01; value-change selection `am-select-option` (select) and data-grid `am-row-select`/`am-selection-change` → `am-change` per D-02 (note the data-grid single-vs-multi `detail` shape as a decision to resolve from the live component API — Discretion under D-02/A1); discrete-pick `am-select` (menu, list, tree-view, command-palette) stays unchanged; and the change/toggle family (tabs `am-tab-change`, pagination `am-change`, accordion/tree-view `am-toggle`) resolved under D-03 (recommend value-changing → `am-change`, expand-state → `am-toggle`; confirm against native analogues). Add the prop/boolean outliers the matrices surface (e.g. combobox `select` boolean colliding with the element name; audit `async` boolean-naming) with proposed target names for Plan 05 to approve. Mark every mapping row as PENDING-DECISION so the rename plans' checkpoints confirm before landing.
  </action>
  <verify>
    <automated>npm run build:manifest && node scripts/build-audit.mjs && test -f api/AUDIT.md && grep -c '| Component' api/AUDIT.md && grep -qi 'Rename mapping' api/AUDIT.md && echo "matrices+mapping OK"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build-audit.mjs` exists and, run after build:manifest, writes api/AUDIT.md without error.
    - api/AUDIT.md contains at least four dimension matrices (event, prop, boolean-naming, default) each with a `| Component` header row (`grep -c '| Component' api/AUDIT.md` ≥ 4).
    - api/AUDIT.md has a "Rename mapping" section naming the overlay (open/close), selection (change/select), and change/toggle targets, each row marked PENDING-DECISION.
    - The event matrix reflects reconciliation with `new CustomEvent` grep — an event dispatched in a component appears even if its `@fires` JSDoc omits it (spot-check: combobox dispatched events count ≥ its `@fires` count).
  </acceptance_criteria>
  <done>The four behavioral dimension matrices and the concrete old→new rename mapping are committed in api/AUDIT.md, reconciled against live event dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Slot/part/token matrices + frozen-surface enumeration + undocumented-token gap (API-04, D-11)</name>
  <files>scripts/build-audit.mjs, api/AUDIT.md</files>
  <read_first>
    - scripts/build-audit.mjs (as written in Task 1 — extend it to emit the slot/part/token matrices)
    - src/tokens/primitives.css.ts, src/tokens/semantic.css.ts, src/tokens/dark.css.ts (the global --am-* token source of truth to aggregate)
    - .planning/phases/02-api-cleanup-cem-baseline/02-RESEARCH.md (§Frozen Surface Enumeration: aggregate cssProperties/cssParts/slots from CEM + global tokens; the undocumented-but-used-token gap; A2)
    - .planning/phases/02-api-cleanup-cem-baseline/02-PATTERNS.md (§Frozen token surface pattern)
  </read_first>
  <action>
    Extend scripts/build-audit.mjs to also aggregate, across all `modules[].declarations[]` in dist/custom-elements.json, the `slots`, `cssParts`, and `cssProperties` per element, and to collect the global `--am-*` token names by extracting them from the three src/tokens css`` modules (reuse the css-extraction approach in scripts/build-tokens-css.mjs). Emit three matrices into api/AUDIT.md — slot names per component, `::part()` names per component, and `--am-*` tokens (global semantic tokens plus every per-component `--am-{component}-*` @cssprop). Add a "Frozen public surface (API-04, D-11)" section enumerating the union of all documented slots, parts, and tokens as the surface to be frozen at Plan 09.
    Add an "Undocumented-but-used tokens (gap)" subsection: grep src/components for `--am-` token references inside css`` templates and list any token used but absent from the CEM cssProperties enumeration (invisible to the freeze because it lacks a @cssprop tag). Flag these for @cssprop tagging before the freeze; do NOT add the tags in this plan (that is a freeze-prep decision surfaced here, resolved at Plan 09).
  </action>
  <verify>
    <automated>npm run build:manifest && node scripts/build-audit.mjs && grep -qi 'Frozen public surface' api/AUDIT.md && grep -qi 'Undocumented' api/AUDIT.md && grep -c -- '--am-' api/AUDIT.md && echo "frozen-surface OK"</automated>
  </verify>
  <acceptance_criteria>
    - api/AUDIT.md contains slot, part, and `--am-*` token matrices (grep finds `--am-` token names in the file, count > 0).
    - api/AUDIT.md has a "Frozen public surface" section enumerating slots + parts + tokens as the freeze target.
    - api/AUDIT.md has an "Undocumented-but-used tokens" subsection (possibly empty, but present) flagging tokens used in css`` but not in CEM cssProperties.
    - No src/ or package.json changes: `git status --porcelain src package.json` shows no modifications from this task.
  </acceptance_criteria>
  <done>The slot/part/token matrices, the frozen-surface enumeration, and the undocumented-token gap list are committed in api/AUDIT.md.</done>
</task>

<task type="auto">
  <name>Task 3: Audit review pass — confirm all 79 registered elements are covered (66 component files register 79 tagNames) and dimensions are complete (API-01)</name>
  <files>api/AUDIT.md</files>
  <read_first>
    - api/AUDIT.md (as generated in Tasks 1-2 — the completeness check target)
    - .planning/phases/02-api-cleanup-cem-baseline/02-VALIDATION.md (§Manual-Only Verifications: audit-matrix completeness is reviewer-assessed)
  </read_first>
  <action>
    Verify api/AUDIT.md tabulates EVERY registered custom element — all 79 unique tagNames the 66 component files register, including the 13 compound/sub-elements (am-accordion-item, am-tab, am-tab-panel, am-option, am-menu-item, am-menu-divider, am-breadcrumb-item, am-list-item, am-radio-group, am-side-nav-item, am-timeline-item, am-tree-item, am-toast-region, am-button-group) — in every dimension matrix AND in the slot/part/token enumeration, not just the top-level component files. There are TWO distinct real numbers, do not conflate them: the count of component FILES with an @customElement decorator (`grep -rl "@customElement" src/components/ | wc -l` = 66) and the count of unique REGISTERED tagNames (`grep -rhoE "@customElement\('[^']*'\)" src | sort -u | wc -l` = 79); the matrix row count must match the latter (79), since scripts/cem-diff.mjs and the audit generator both key declarations by tagName. Add a short header to api/AUDIT.md recording: the generation command (`node scripts/build-audit.mjs`), the source manifest (dist/custom-elements.json), BOTH counts (66 component files / 79 registered tagNames), and the seven dimensions covered. Add a one-line note that matrix correctness (which outlier maps to which target) is reviewer-assessed per the Phase 2 validation strategy and is confirmed at each rename wave's checkpoint (Plans 03-05). Fix any registered element missing from a matrix by re-running the generator (which already iterates every CEM declaration with a tagName); do not hand-edit rows.
  </action>
  <verify>
    <automated>FILES=$(grep -rl "@customElement" src/components/ | wc -l); ELEMENTS=$(grep -rhoE "@customElement\('[^']*'\)" src | sort -u | wc -l); echo "files=$FILES elements=$ELEMENTS"; grep -qi 'dimensions' api/AUDIT.md && grep -qi 'reviewer-assessed' api/AUDIT.md && echo "header OK"</automated>
  </verify>
  <acceptance_criteria>
    - api/AUDIT.md has a header recording the generation command, source manifest, BOTH the component-file count (66) and the registered-tagName count (79), and the seven dimensions.
    - Every one of the 79 registered tagNames appears as a row in each behavioral dimension matrix and in the slot/part/token enumeration (the 13 compound/sub-elements are NOT dropped); the header's registered-element count equals the unique `@customElement` tagName count (`grep -rhoE "@customElement\('[^']*'\)" src | sort -u | wc -l` = 79), and the component-file count equals `grep -rl "@customElement" src/components/ | wc -l` = 66.
    - api/AUDIT.md notes that outlier-target correctness is confirmed at each rename wave's checkpoint.
  </acceptance_criteria>
  <done>api/AUDIT.md is confirmed complete across all 79 registered custom elements (66 component files) and seven dimensions, with generation provenance recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → repo files | scripts/build-audit.mjs reads repo-local manifest + source only; no network, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | api/AUDIT.md publish leak | low | mitigate | api/ stays out of package.json `files` (D-12) — the internal audit is never published. Verified in Plan 01 and Plan 09. |

No new threats: read-only analysis of the repo's own manifest and token modules; no server, no untrusted input, no dependency added.
</threat_model>

## Artifacts this plan produces
- `scripts/build-audit.mjs` — zero-dep matrix generator reading dist/custom-elements.json + src/components + src/tokens
- `api/AUDIT.md` — seven dimension matrices (event, prop, boolean-naming, default, slot, part, --am-* token), the concrete old→new rename mapping (PENDING-DECISION), the frozen-surface enumeration (API-04), and the undocumented-token gap list

<verification>
- `node scripts/build-audit.mjs` regenerates api/AUDIT.md deterministically.
- api/AUDIT.md has ≥4 behavioral matrices + slot/part/token matrices + frozen-surface + rename mapping + gap list.
- Coverage equals all 79 registered tagNames (across 66 component files).
</verification>

<success_criteria>
The cross-component consistency audit exists as committed dimension matrices across all 79 registered custom elements (66 component files) with a concrete rename mapping and a frozen-surface enumeration (ROADMAP SC1 + SC4 enumeration), reproducible via scripts/build-audit.mjs.
</success_criteria>

<output>
Create `.planning/phases/02-api-cleanup-cem-baseline/02-02-SUMMARY.md` when done.
</output>
