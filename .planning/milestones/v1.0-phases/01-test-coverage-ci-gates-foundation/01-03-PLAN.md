---
phase: 01-test-coverage-ci-gates-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/components/app-shell.test.ts
  - test/components/button-group.test.ts
  - test/components/empty-state.test.ts
  - test/components/error-text.test.ts
  - test/components/field.test.ts
  - test/components/hint-text.test.ts
  - test/components/label.test.ts
  - test/components/nav-bar.test.ts
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 42000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "app-shell, button-group, empty-state, error-text, field, hint-text, label, nav-bar each have a dedicated 1:1 test file (TEST-01)."
    - "Each new file lifts its assertions verbatim from display-trivial.test.ts with no coverage loss (Pitfall 1)."
    - "All eight stay smoke-tier per D-04 (render / named-slot / role / attribute reflection)."
    - "display-trivial.test.ts is NOT deleted here (plans 03 and 04 both redistribute it; deletion is deferred to the finalization plan 08 to avoid a cross-plan race)."
  artifacts:
    - test/components/app-shell.test.ts
    - test/components/button-group.test.ts
    - test/components/empty-state.test.ts
    - test/components/error-text.test.ts
    - test/components/field.test.ts
    - test/components/hint-text.test.ts
    - test/components/label.test.ts
    - test/components/nav-bar.test.ts
  key_links:
    - "each file imports one component barrel and only used helpers (noUnusedLocals enforced)"
    - "grouped display-trivial.test.ts stays present (read-only) so plan 04 can also redistribute from it; deletion is plan 08"
---

<objective>
Split the first eight components out of `test/components/display-trivial.test.ts` into dedicated 1:1 files: app-shell, button-group, empty-state, error-text, field, hint-text, label, nav-bar (TEST-01, all D-04 smoke tier). This plan does NOT delete the grouped file — plan 04 redistributes the remaining components and the finalization plan (08) performs the single, race-free deletion.

Purpose: Close 8 more of the 23 dedicated-file gaps in parallel with the other split plans, without touching the grouped file's lifecycle (so plans 03/04 never race on it).
Output: Eight new dedicated jsdom test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split app-shell, button-group, empty-state, error-text into dedicated files (D-04 smoke)</name>
  <files>test/components/app-shell.test.ts, test/components/button-group.test.ts, test/components/empty-state.test.ts, test/components/error-text.test.ts</files>
  <read_first>
    - test/components/display-trivial.test.ts (am-app-shell block lines 204-221, am-button-group 36-50, am-empty-state 52-69, am-error-text 71-77 — lift verbatim)
    - test/helpers.ts (fixture signature)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§Smoke test files import/header + named-slot + role-reflection patterns)
  </read_first>
  <action>
    Create `test/components/app-shell.test.ts`, `button-group.test.ts`, `empty-state.test.ts`, `error-text.test.ts`. Each imports only `'../../src/components/<component>/<component>'` and only the helpers its block uses (`noUnusedLocals` enforced). Lift the matching `describe` block verbatim (app-shell/empty-state assert named slots; button-group asserts orientation reflection + `role="group"`; error-text asserts `role="alert"` + slot). Do NOT delete display-trivial.test.ts (deferred to plan 08). Smoke tier per D-04.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/app-shell.test.ts test/components/button-group.test.ts test/components/empty-state.test.ts test/components/error-text.test.ts 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - Four files exist, each importing exactly one component barrel.
    - `vitest run --project jsdom` over the four files exits 0.
    - Assertions match the lifted blocks (button-group `role=group`, error-text `role=alert`, app-shell/empty-state named slots).
  </acceptance_criteria>
  <done>app-shell, button-group, empty-state, error-text have passing dedicated files.</done>
</task>

<task type="auto">
  <name>Task 2: Split field, hint-text, label, nav-bar into dedicated files (D-04 smoke)</name>
  <files>test/components/field.test.ts, test/components/hint-text.test.ts, test/components/label.test.ts, test/components/nav-bar.test.ts</files>
  <read_first>
    - test/components/display-trivial.test.ts (am-field block lines 79-86, am-hint-text 88-93, am-label 95-103, am-nav-bar 105-110 — lift verbatim)
    - test/components/app-shell.test.ts (created in Task 1 — match the file-header convention)
  </read_first>
  <action>
    Create `test/components/field.test.ts`, `hint-text.test.ts`, `label.test.ts`, `nav-bar.test.ts`, lifting each matching block verbatim (field/hint-text assert default slot; label asserts `for` association + `required` reflection; nav-bar asserts `role="navigation"`). Import only the one component barrel + used helpers per file. Leave display-trivial.test.ts present (plan 04 still needs to read it; plan 08 deletes it). Smoke tier per D-04.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/field.test.ts test/components/hint-text.test.ts test/components/label.test.ts test/components/nav-bar.test.ts 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - Four files exist, each importing exactly one component barrel.
    - `vitest run --project jsdom` over the four files exits 0.
    - `test/components/display-trivial.test.ts` still exists (not deleted here).
  </acceptance_criteria>
  <done>field, hint-text, label, nav-bar have passing dedicated files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test authoring only | No runtime code, network, or input surface is changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03a | Tampering | New jsdom test files executing in CI | low | accept | Test files add no runtime code and no new deps; run in the read-only CI established by plan 01 |
</threat_model>

<verification>
- Eight dedicated files exist and pass under `vitest run --project jsdom`.
- `display-trivial.test.ts` remains present for plan 04.
</verification>

<success_criteria>
Eight display components each gain a dedicated 1:1 test file with no coverage loss and no change to the grouped file's lifecycle.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- New: `test/components/app-shell.test.ts`, `button-group.test.ts`, `empty-state.test.ts`, `error-text.test.ts`, `field.test.ts`, `hint-text.test.ts`, `label.test.ts`, `nav-bar.test.ts`.
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-03-SUMMARY.md` when done.
</output>
