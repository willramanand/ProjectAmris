---
phase: 01-test-coverage-ci-gates-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - .github/workflows/ci.yml
  - .size-limit.json
  - test/helpers.ts
  - test/a11y-helper.ts
  - test/components/table.test.ts
  - test/components/link-button.test.ts
  - test/components/icon.test.ts
  - test/components/misc-display.test.ts
  - test/browser/form-association.test.ts
  - test/browser/a11y.browser.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-06, TEST-07, TEST-08, PERF-01]
estimate:
  tokens: 80000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One end-to-end path is proven: a real Chromium browser test, a jsdom coverage gate, a size-limit budget, and an in-browser axe scan all run green in CI on existing code (D-05 green-on-arrival)."
    - "`npm test` runs the jsdom project only and does NOT require Playwright/Chromium to be installed (D-06 contributor default)."
    - "`npm run test:browser` boots headless Chromium via @vitest/browser-playwright and runs test/browser/ only."
    - "The browser project omits test/setup.ts, so a browser test asserts native ElementInternals/FormData rather than the jsdom mock (Pitfall 2)."
    - "misc-display.test.ts is split into dedicated table/link-button/icon files with no loss of assertions, and the grouped file is removed (TEST-01, OQ-3)."
    - "The coverage gate fails when branch or per-directory coverage drops below the measured baseline floor (TEST-07, D-01/D-02)."
    - "size-limit fails CI when the core bundle exceeds its measured budget (PERF-01, D-07)."
  artifacts:
    - vitest.config.ts (projects: jsdom + browser; root coverage.thresholds)
    - .size-limit.json (core-bundle entry)
    - test/browser/form-association.test.ts
    - test/browser/a11y.browser.test.ts
    - test/components/table.test.ts
    - test/components/link-button.test.ts
    - test/components/icon.test.ts
    - test/helpers.ts (deepActiveElement)
    - test/a11y-helper.ts (browser variant)
    - .github/workflows/ci.yml (verify/browser/size jobs)
  key_links:
    - "vitest.config.ts projects.browser.setupFiles OMITTED -> Chromium native APIs (not test/setup.ts mocks)"
    - "@vitest/browser-playwright minor version == installed vitest minor (4.1.9) -> provider loads"
    - "ci.yml browser job runs `npx playwright install chromium` before `npm run test:browser`"
    - "size-limit core entry points at dist/amris-core.js; requires `npm run build` first"
---

<objective>
Stand up the complete test-lane + CI-gate machinery for Phase 1 as a single production-quality vertical slice, and prove it green end-to-end on existing code before any horizontal expansion. This is the tracer: one path through every layer the phase touches — a jsdom split, the jsdom+browser Vitest `projects` config, one real browser fidelity test, the jsdom coverage gate, one size-limit budget, an in-browser axe scan, and the CI jobs that hard-block on all of them.

Purpose: Prove the architecture (hybrid jsdom+Chromium Vitest, coverage/size/a11y gates, contributor-friendly local default) works end-to-end after one commit, so the remaining 22 file splits, 3 fidelity areas, TEST-04/05 additions, and full budget set expand from a proven spine rather than an unproven one.

Output: A rewired `vitest.config.ts` (two projects + coverage), new `package.json` scripts + pinned devDeps, `.size-limit.json` (core entry), the first `test/browser/` suite (form + a11y), the misc-display split (3 dedicated files, grouped file removed), a `deepActiveElement()` helper, and CI jobs (verify/browser/size) that all pass on current code.

## Locked research decisions (recorded here for the whole phase)
- **OQ-1 — Coverage folds over the jsdom project only.** `coverage.provider` is a single global Vitest setting, so "istanbul for the browser project only" is not achievable. Measure and gate coverage via `vitest run --project jsdom --coverage` (v8, no new dep). The browser lane is fidelity-only and is NOT counted toward coverage.
- **OQ-2 — a11y uses in-browser `axe-core` (already installed), NOT `@axe-core/playwright`.** AxeBuilder needs a Node-side Playwright Page that Vitest browser mode does not hand you. Reuse `test/a11y-helper.ts`'s `checkA11y` in the browser project with `color-contrast`/`region` re-enabled. Do NOT add `@axe-core/playwright`.
- **OQ-3 — Delete the 3 grouped files after their content is redistributed.** This plan deletes `misc-display.test.ts`; `layout-primitives.test.ts` is deleted by plan 02; `display-trivial.test.ts` is deleted by the finalization plan (08) once plans 03+04 redistribute it.

## Phase-wide prohibitions (apply to every plan)
- Do NOT change component APIs or fix component bugs beyond what a test legitimately requires — capture any bug/finding for the owning phase (Phase 3 leaks, Phase 4 features).
- Do NOT carry `getMockInternals()`/jsdom mock assertions into any `test/browser/` file.
- Do NOT introduce global CSS or CommonJS; do NOT bundle Lit (peerDependency stays `^3.3.2`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-CONTEXT.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Config spine — install pinned devDeps, split vitest into jsdom+browser projects, add scripts (D-06)</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - package.json (current scripts block lines 58-73, devDependencies 77-93, peerDependencies 94-96 — Lit stays peer)
    - vitest.config.ts (current single-env config, all 10 lines — the 4 fields move verbatim into the jsdom project)
    - .planning/research/STACK.md (Installation block; Version Compatibility — provider minor MUST match vitest)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (§Standard Stack Additions table with pinned versions; §Package Legitimacy Audit — all 5 Approved, none ASSUMED/SUS; Pattern 1 projects sketch)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§vitest.config.ts MODIFIED; §package.json MODIFIED)
  </read_first>
  <precondition>Chromium for Playwright is not yet installed locally; this task runs `npx playwright install chromium` (no human action required — executor runs it via Bash).</precondition>
  <action>
    Install exact pinned devDeps (RESEARCH Package Legitimacy Audit clears all 5 — no [ASSUMED]/[SUS], no checkpoint needed): `@vitest/browser-playwright@4.1.9` (MUST equal installed `vitest@4.1.9`), `playwright@1.62.1`, `size-limit@13`, `@size-limit/preset-small-lib@13`. Then run `npx playwright install chromium` (Chromium only for v1 — WebKit deferred to TEST-V2-01).
    Rewrite `vitest.config.ts` into a `test.projects` array per RESEARCH Pattern 1: (a) a `jsdom` project moving the current `environment:'jsdom'`, `include:['test/**/*.test.ts']`, `setupFiles:['./test/setup.ts']`, `restoreMocks:true` verbatim, PLUS `exclude:['test/browser/**']` and `name:'jsdom'`; (b) a `browser` project with `name:'browser'`, `include:['test/browser/**/*.test.ts']`, `browser:{ enabled:true, provider:'playwright', headless:true, instances:[{ browser:'chromium' }] }`, and `setupFiles` INTENTIONALLY OMITTED (Pitfall 2 — mocks stay jsdom-only). Add a root-level `test.coverage` block with `provider:'v8'`, `reporter:['text','json-summary','html']`, `include:['src/components/**','src/utilities/**']`, `exclude:['**/*.stories.ts','test/**','dist/**','**/index.ts']`, and an empty/placeholder `thresholds` object (Task 2 fills numeric floors from the measured baseline per D-01/D-02). Do NOT set coverage.provider per-project (OQ-1 — coverage is global, folds over jsdom).
    Update `package.json` scripts per D-06: `"test"` -> `"vitest --project jsdom"` (contributor default stays jsdom, no Playwright needed); `"test:coverage"` -> `"vitest run --project jsdom --coverage"`; add `"test:browser": "vitest run --project browser"` and `"size": "size-limit"`; keep `"test:run"` and `"test:a11y"`. Do NOT move Lit out of peerDependencies.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -20 && echo "PROVIDER:" && node -e "const v=require('./package.json').devDependencies; if(v['@vitest/browser-playwright']!=='4.1.9') throw new Error('provider must pin 4.1.9'); if(v.lit) throw new Error('lit must not be a devDependency'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies contains `@vitest/browser-playwright@4.1.9`, `playwright@1.62.1`, `size-limit@13`(or `^13`/`13.0.3`), `@size-limit/preset-small-lib` at matching major; `lit` remains only in peerDependencies.
    - `package.json` scripts: `test` = `vitest --project jsdom`, `test:coverage` = `vitest run --project jsdom --coverage`, and `test:browser`/`size` exist.
    - `vitest.config.ts` exports a `test.projects` array with a `jsdom` project (setupFiles present, excludes test/browser/**) and a `browser` project (playwright/chromium, setupFiles absent), plus a root `test.coverage` block with provider v8.
    - `npm run test:run` (all projects) exits 0 on existing code.
  </acceptance_criteria>
  <done>Two Vitest projects exist; jsdom is the local default; Chromium is installed; provider is pinned to the vitest minor; existing tests pass.</done>
  <reversibility rating="costly">The Vitest+Playwright runner choice is pre-decided-upstream (CONTEXT locked constraint) — recorded, not re-litigated. Config/threshold numbers within it are reversible.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: jsdom slice + coverage gate — split misc-display into 3 dedicated files, add deepActiveElement, set baseline thresholds (TEST-01, TEST-07, D-01/D-02/D-04, OQ-1/OQ-3)</name>
  <files>test/components/table.test.ts, test/components/link-button.test.ts, test/components/icon.test.ts, test/components/misc-display.test.ts, test/helpers.ts, vitest.config.ts</files>
  <behavior>
    - New `test/components/table.test.ts` reproduces the three am-table assertions from misc-display (part="table" wrapper, striped/hoverable/bordered/compact reflection, scoped light-DOM style) and passes.
    - New `test/components/link-button.test.ts` reproduces all five am-link-button assertions incl. the conditional `disabled` -> omits href + aria-disabled + tabindex="-1" (D-04 keeps it smoke but carries its 2-3 conditional assertions).
    - New `test/components/icon.test.ts` reproduces the am-icon slotted-SVG assertion.
    - `deepActiveElement()` returns the innermost focused element, piercing shadow roots (for later focus tests); given no active element it returns document.body or null deterministically.
    - After deleting misc-display.test.ts, `vitest run --project jsdom` still passes and covers table/link-button/icon.
  </behavior>
  <read_first>
    - test/components/misc-display.test.ts (all 99 lines — the source blocks to lift verbatim: am-table 8-38, am-link-button 40-89, am-icon 91-99)
    - test/helpers.ts (existing helper signatures; add deepActiveElement alongside them)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§Smoke test files import/header pattern; §Don't Hand-Roll deepActiveElement)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (§Coverage Gate — baseline measurement + per-dir tiers; OQ-1)
    - vitest.config.ts (as rewritten in Task 1 — fill thresholds here)
  </read_first>
  <action>
    Create `test/components/table.test.ts`, `link-button.test.ts`, `icon.test.ts`. Each imports only its one component barrel (`'../../src/components/<component>/<component>'`) and only the helpers its block actually uses (`noUnusedLocals` is enforced), then lifts the matching `describe` block verbatim from misc-display.test.ts. Do NOT invent new assertions beyond the lifted ones (Pitfall 1 — this is split + light hardening, not write-from-scratch); link-button keeps its conditional-disabled assertions per D-04.
    Add `deepActiveElement()` to `test/helpers.ts`: walk `document.activeElement`, following `.shadowRoot?.activeElement` until no deeper active element, and return the innermost node. Export it. Used by later focus tests (plan 05).
    Delete `test/components/misc-display.test.ts` (OQ-3 — the 1:1 invariant; its 3 components now have dedicated files).
    Measure the coverage baseline with `npx vitest run --project jsdom --coverage` and read the branch/lines/functions/statements summary. Fill `test.coverage.thresholds` in `vitest.config.ts` at (or 1 point below) the measured floor so the gate is green-on-arrival (D-01 ratchet-from-baseline; D-05 green-on-arrival), gating on `branches` explicitly (D-02). Add per-directory glob overrides under `thresholds` for the complex interactive dirs (`src/components/combobox/**`, `src/components/select/**`, `src/components/dialog/**`, `src/components/date-picker/**`) set at their own measured branch floor (D-02 tiers; exact numbers are D-03 discretion — treat branch>=80 / lines-fns-stmts>=85 as the ratchet ceiling per D-03, NOT a day-one gate). Coverage folds over jsdom only (OQ-1); do not attempt browser coverage.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom --coverage test/components/table.test.ts test/components/link-button.test.ts test/components/icon.test.ts 2>&1 | tail -25 && test ! -f test/components/misc-display.test.ts && echo "grouped-removed OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test/components/table.test.ts`, `link-button.test.ts`, `icon.test.ts` each exist and import `'../../src/components/<component>/<component>'`.
    - `test/components/misc-display.test.ts` no longer exists.
    - `test/helpers.ts` exports `deepActiveElement`.
    - `vitest.config.ts` `test.coverage.thresholds` has a numeric `branches` floor plus per-directory overrides for combobox/select/dialog/date-picker.
    - `vitest run --project jsdom --coverage` exits 0 (gate green-on-arrival, D-05); artificially lowering any file's coverage would make it exit non-zero.
  </acceptance_criteria>
  <done>Three dedicated jsdom files replace the misc-display block, the grouped file is gone, and the branch+per-dir coverage gate passes at the measured baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: browser slice + budget + CI — first real Chromium form test, in-browser axe, core size budget, hard-blocking CI jobs (TEST-02, TEST-06, TEST-08, PERF-01, D-05/D-07/D-08, OQ-2)</name>
  <files>test/browser/form-association.test.ts, test/browser/a11y.browser.test.ts, test/a11y-helper.ts, .size-limit.json, .github/workflows/ci.yml</files>
  <behavior>
    - `test/browser/form-association.test.ts` puts an `am-checkbox name="terms" checked` inside a real `<form>` in Chromium and asserts `new FormData(form).get('terms')` equals the real submitted value via native ElementInternals (NOT getMockInternals), plus a native-API guard proving `HTMLDialogElement.prototype.showModal` is the real method (Pitfall 2).
    - `test/browser/a11y.browser.test.ts` runs in-browser axe over a representative set of components (spanning a form control, an overlay, and a colored display component) with `color-contrast` and `region` ENABLED, and passes on current styles (TEST-08, OQ-2).
    - CI `verify` job runs the jsdom coverage gate and hard-blocks below threshold; `browser` job installs Chromium then runs the browser project (form + a11y); `size` job builds then runs size-limit and hard-blocks over budget.
  </behavior>
  <read_first>
    - test/components/checkbox.test.ts (lines 1-42 — fixture/click shape to mirror; but REPLACE the getMockInternals assertion with real FormData)
    - test/a11y-helper.ts (all 35 lines — the two disables at 11-14 to make optional via a parameter)
    - test/a11y.test.ts (lines 1-43 — the expectNoViolations wrapper and fixture style to reuse for the browser variant)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md (Pattern 2 real ElementInternals; §a11y Browser Lane + OQ-2; §Bundle-Size Gate config; §Architecture Patterns CI data-flow diagram)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§form-association analog; §a11y.browser analog; §.size-limit.json NEW; §ci.yml MODIFIED)
    - .github/workflows/ci.yml (current verify job 1-29 — copy the checkout/setup-node/npm ci step scaffold)
    - vite.config.ts (lines 194-196 — lit/@floating-ui are `external`, so size-limit over dist/*.js reflects shipped size)
  </read_first>
  <action>
    Create `test/browser/form-association.test.ts` (browser project): import `'../../src/components/checkbox/checkbox'` and `fixture` from `'../helpers'` — do NOT import `getMockInternals`. Mount `am-checkbox` inside a real `<form>`, assert `new FormData(form).get('terms')` reflects the checked value through native `ElementInternals.setFormValue`, flip `required`+unchecked and assert `form.checkValidity()` is false, and add one native-API guard asserting the real `HTMLDialogElement.prototype.showModal` exists (proves the browser project is NOT loading the setup.ts mock, Pitfall 2). This is the TEST-02 seed for one control; plan 06 expands it to the remaining form-associated controls.
    Add a browser-lane path to `test/a11y-helper.ts`: give `checkA11y` a way to run WITHOUT the two default disables (e.g. an options/param so the browser lane passes an empty default-disabled set) while the existing jsdom `a11y.test.ts` keeps them; do not change the jsdom caller's behavior. Create `test/browser/a11y.browser.test.ts` reusing the `expectNoViolations` wrapper shape over a representative component set (at minimum one form control, one overlay, one colored display element) with contrast/region ENABLED (OQ-2 — in-browser axe-core; do NOT add `@axe-core/playwright`).
    Create `.size-limit.json` with the core-bundle entry only for the tracer: `{ "name":"core bundle", "path":"dist/amris-core.js", "limit":"<measured min+gzip + ~5-10% headroom>" }`. Measure the real min+gzip by running `npm run build` then `npx size-limit`, and set `limit` from that baseline + headroom (D-07 ratchet). Plan 08 adds the full/deep-import/tree-shaking entries (D-08).
    Rewire `.github/workflows/ci.yml` keeping it PR-triggered and read-only (no `packages:write`; publishing is Phase 6): (1) `verify` job — replace `npm run test:run` with `npx vitest run --project jsdom --coverage` so the coverage gate hard-blocks (D-05); keep tsc + build. (2) NEW `browser` job — checkout/setup-node(20)/npm ci, then `npx playwright install chromium`, then `npm run test:browser`. (3) NEW `size` job — checkout/setup-node/npm ci, then `npm run build`, then `npm run size`. Each gate hard-blocks (D-05 enforce-as-it-lands, green-on-arrival for existing code).
  </action>
  <verify>
    <automated>npx vitest run --project browser 2>&1 | tail -25 && npm run build >/dev/null 2>&1 && npx size-limit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test/browser/form-association.test.ts` exists, does NOT reference `getMockInternals`, and asserts against real `FormData`.
    - `test/browser/a11y.browser.test.ts` exists and runs axe with `color-contrast`/`region` enabled (the two rules are NOT in its disabled set).
    - `.size-limit.json` exists with a `core bundle` entry pointing at `dist/amris-core.js` and a numeric `limit`.
    - `.github/workflows/ci.yml` has a `browser` job running `npx playwright install chromium` before the browser tests and a `size` job running the build then size-limit; the `verify` job runs the jsdom coverage gate; no job requests `packages:write`.
    - `npx vitest run --project browser` exits 0 and `npx size-limit` (after build) exits 0 on current code.
  </acceptance_criteria>
  <done>A real Chromium test, an in-browser axe scan (contrast/region on), and a size budget all pass, and CI hard-blocks on each — the full Phase 1 pipeline is proven green end-to-end on existing code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev/CI machine | New dev-only dependencies are fetched and executed at test time |
| PR author -> CI runner | The PR-triggered browser job executes untrusted PR code in headless Chromium |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm devDep installs (`@vitest/browser-playwright`, `playwright`, `size-limit`, `@size-limit/preset-small-lib`) | medium | mitigate | Pin exact versions per RESEARCH §Package Legitimacy Audit (all 5 Approved, none [ASSUMED]/[SUS]/[SLOP]); install against the committed lockfile via `npm ci` in CI |
| T-01-01 | Elevation of Privilege | PR-triggered `ci.yml` browser job with `GITHUB_TOKEN` | medium | mitigate | Keep the workflow read-only; do NOT grant `packages:write`/publish here (publishing + `changesets/action` SHA-pinning are Phase 6 concerns) |
| T-01-02 | Tampering / Supply chain | `npx playwright install chromium` fetching binaries in CI | low | mitigate | Official `playwright@1.62.1`; Chromium fetched at Playwright's pinned revision |
| T-01-03 | Information Disclosure | Test/CI-only phase ships no runtime code | low | accept | No runtime code paths, network surface, auth, or data handling added; Lit-safe templating unchanged (no `innerHTML`/`eval`) |
</threat_model>

<verification>
- `npm run test:run` (both projects) exits 0.
- `npx vitest run --project jsdom --coverage` exits 0 and the gate would fail if any covered file regressed below threshold.
- `npx vitest run --project browser` exits 0 (real Chromium; native ElementInternals; axe contrast/region on).
- `npm run build && npx size-limit` exits 0.
- `test/components/misc-display.test.ts` is absent; table/link-button/icon dedicated files exist.
</verification>

<success_criteria>
The hybrid jsdom+Chromium lane, the branch+per-dir coverage gate, the size budget, and the in-browser a11y scan all run and hard-block in CI, green on existing code — the phase spine is proven before expansion. `npm test` remains jsdom-only for contributors.
</success_criteria>

<artifacts>
## Artifacts this phase produces (established by this plan)
- **Config:** `vitest.config.ts` (`test.projects` jsdom + browser; root `test.coverage.thresholds` branch + per-dir), `.size-limit.json` (core-bundle entry).
- **package.json:** devDeps `@vitest/browser-playwright@4.1.9`, `playwright@1.62.1`, `size-limit@13`, `@size-limit/preset-small-lib@13`; scripts `test` (jsdom), `test:coverage` (jsdom+coverage), `test:browser`, `size`.
- **CI jobs:** `.github/workflows/ci.yml` `verify` (jsdom coverage gate), `browser` (Chromium install + browser project), `size` (build + size-limit).
- **Test files:** `test/components/table.test.ts`, `link-button.test.ts`, `icon.test.ts`; `test/browser/form-association.test.ts`, `test/browser/a11y.browser.test.ts`. Removed: `test/components/misc-display.test.ts`.
- **Helpers:** `deepActiveElement()` in `test/helpers.ts`; browser-lane variant path in `test/a11y-helper.ts`.
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-01-SUMMARY.md` when done.
</output>
