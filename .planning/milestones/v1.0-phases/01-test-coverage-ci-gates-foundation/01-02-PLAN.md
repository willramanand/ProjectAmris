---
phase: 01-test-coverage-ci-gates-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/components/stack.test.ts
  - test/components/grid.test.ts
  - test/components/surface.test.ts
  - test/components/panel.test.ts
  - test/components/card.test.ts
  - test/components/layout-primitives.test.ts
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 36000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "stack, grid, surface, panel, and card each have a dedicated 1:1 `test/components/<name>.test.ts` file (TEST-01)."
    - "No assertion from layout-primitives.test.ts is lost in the split (Pitfall 1 — split + light hardening, not rewrite)."
    - "layout-primitives.test.ts is removed once its 5 blocks are redistributed (OQ-3, 1:1 invariant)."
    - "All five new files pass under the jsdom project and stay smoke-tier per D-04 (render / attribute-ARIA reflection / slot / part)."
  artifacts:
    - test/components/stack.test.ts
    - test/components/grid.test.ts
    - test/components/surface.test.ts
    - test/components/panel.test.ts
    - test/components/card.test.ts
  key_links:
    - "each new file imports exactly one component barrel `../../src/components/<component>/<component>` -> registers that element"
    - "grouped layout-primitives.test.ts deleted only after all 5 dedicated files pass (single-owner, no cross-plan race)"
---

<objective>
Split `test/components/layout-primitives.test.ts` into five dedicated 1:1 files (stack, grid, surface, panel, card) and retire the grouped file — expanding the tracer's proven split mechanic to the layout primitives (TEST-01, D-04 smoke tier).

Purpose: Close 5 of the 23 dedicated-file gaps with zero coverage loss, holding the 1:1 test-per-component invariant.
Output: Five new dedicated jsdom test files; `layout-primitives.test.ts` removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md
@.planning/phases/01-test-coverage-ci-gates-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split stack, grid, surface into dedicated files (D-04 smoke)</name>
  <files>test/components/stack.test.ts, test/components/grid.test.ts, test/components/surface.test.ts</files>
  <read_first>
    - test/components/layout-primitives.test.ts (am-stack block lines 10-36, am-grid 38-54, am-surface 56-70 — lift verbatim)
    - test/helpers.ts (fixture, waitForUpdate signatures)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§Smoke test files import/header + dynamic-prop pattern)
  </read_first>
  <action>
    Create `test/components/stack.test.ts`, `grid.test.ts`, `surface.test.ts`. Each imports only `'../../src/components/<component>/<component>'` plus only the helpers its block uses (drop unused imports — `noUnusedLocals` is enforced). Lift the matching `describe` block verbatim from layout-primitives.test.ts (stack keeps its dynamic `direction` prop-update assertion; grid keeps the auto-fill default; surface keeps the default-variant assertion). Do NOT add new behavior — this is split + light hardening only (Pitfall 1). Keep smoke tier per D-04 (these are stateless layout primitives).
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/stack.test.ts test/components/grid.test.ts test/components/surface.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist, each importing exactly one `../../src/components/<component>/<component>` barrel.
    - `vitest run --project jsdom` over the three files exits 0.
    - Each file's assertions match the corresponding lifted block (no dropped assertions).
  </acceptance_criteria>
  <done>stack, grid, surface have passing dedicated jsdom files.</done>
</task>

<task type="auto">
  <name>Task 2: Split panel, card into dedicated files and retire the grouped file (OQ-3)</name>
  <files>test/components/panel.test.ts, test/components/card.test.ts, test/components/layout-primitives.test.ts</files>
  <read_first>
    - test/components/layout-primitives.test.ts (am-panel block lines 72-94 incl. header/body slot + part exposure, am-card 96-101)
    - test/components/stack.test.ts (as created in Task 1 — file-header convention to match)
    - .planning/phases/01-test-coverage-ci-gates-foundation/01-PATTERNS.md (§Smoke test files — slot/part assertion pattern)
  </read_first>
  <action>
    Create `test/components/panel.test.ts` (lift the panel block: bordered reflection, header/body named slots, and the `part="header"`/`part="body"`/`part="panel"` exposure assertions) and `test/components/card.test.ts` (lift the default-slot assertion). Then delete `test/components/layout-primitives.test.ts` (OQ-3 — 1:1 invariant; this plan is the sole owner of these 5 components, so deletion is race-free). Confirm the full jsdom project still passes and no `am-*` layout component lost coverage.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom test/components/panel.test.ts test/components/card.test.ts 2>&1 | tail -12 && test ! -f test/components/layout-primitives.test.ts && echo "grouped-removed OK" && npx vitest run --project jsdom 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `test/components/panel.test.ts` asserts the three `part=` exposures and both named slots.
    - `test/components/card.test.ts` exists and passes.
    - `test/components/layout-primitives.test.ts` no longer exists.
    - The full `vitest run --project jsdom` exits 0.
  </acceptance_criteria>
  <done>All five layout primitives have dedicated 1:1 files and the grouped file is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test authoring only | No runtime code, network, or input surface is changed; new files run under the existing jsdom project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02a | Tampering | New jsdom test files executing in CI | low | accept | Test files add no runtime code and no new deps; reuse existing helpers; run in the read-only CI established by plan 01 |
</threat_model>

<verification>
- Five dedicated files exist and pass under `vitest run --project jsdom`.
- `layout-primitives.test.ts` is absent.
- No layout component loses an assertion relative to the original grouped file.
</verification>

<success_criteria>
stack, grid, surface, panel, card each have a dedicated 1:1 test file; the grouped layout file is retired with no coverage loss.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- New: `test/components/stack.test.ts`, `grid.test.ts`, `surface.test.ts`, `panel.test.ts`, `card.test.ts`.
- Removed: `test/components/layout-primitives.test.ts`.
</artifacts>

<output>
Create `.planning/phases/01-test-coverage-ci-gates-foundation/01-02-SUMMARY.md` when done.
</output>
