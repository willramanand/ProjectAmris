---
phase: 08-bundle-size-deferral
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/combobox/combobox.ts
  - test/browser/combobox-virtual.test.ts
  - test/components/combobox.test.ts
autonomous: true
requirements: [SIZE-01, SIZE-02, SIZE-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "combobox sources its `size` width-match middleware from the loaded floating-ui module via the controller's module-receiving getter (moved OFF the construction-time static array), and positions identically."
    - "At exactly VIRTUALIZE_ROW_THRESHOLD = 100 filtered options the list still uses repeat() (the swap is strictly ABOVE the threshold, `> 100`); at 101 it uses virtualize() once loaded, at 99 it uses repeat() — the boundary is unchanged from today — SIZE-02 boundary edge."
    - "During the cold-chunk window (virtualizer not yet resolved) above the threshold, combobox renders the fully-functional unwindowed repeat() fallback and swaps to virtualize() after loadVirtualizer() resolves via requestUpdate() — SIZE-02 precision/adjacency edge (repeat↔virtualize swap)."
    - "ARIA posinset/setsize computed from the ABSOLUTE index are identical in both the repeat() and virtualize() render paths (no drift across the swap)."
    - "SIZE-05: combobox's non-critical init (ResizeObserver / non-essential listener attach) is moved off the constructor/first-paint path onto an interaction-gate or rAF/microtask; if the host disconnects before the scheduled callback fires, the deferred init does not double-run or leak (teardown-guarded) — SIZE-05 concurrency edge."
  artifacts:
    - "combobox with zero static @floating-ui/dom and @lit-labs/virtualizer runtime imports"
  key_links:
    - "combobox controller middleware moves from the static field-init array (lines 154-169) to `middleware: (mod) => [mod.size({ apply({rects,elements}) { ...width match... } })]`."
    - "combobox `_renderOptionList` (lines 717-733) swaps `this._virtualize ? this._virtualize({...}) : repeat(...)`; `_virtualize` set via `loadVirtualizer().then(m => { this._virtualize = m.virtualize; this.requestUpdate(); })`."
    - "prefetchFloating() on trigger intent; prefetchVirtualizer() near threshold / on popup open (D-01/D-04)."
  prohibitions:
    - "MUST NOT change the VIRTUALIZE_ROW_THRESHOLD boundary or the `> threshold` comparison (behavior-preserving)."
    - "MUST NOT use requestIdleCallback for the SIZE-05 deferral (Safari 16.4 floor; interaction-gate + rAF/microtask only — D-09)."
    - "MUST NOT change combobox's observable behavior, filtering, ARIA, or CEM surface (surface-freeze)."
    - "MUST NOT introduce innerHTML/eval in any fallback render path (Lit templates only)."
    - "MUST NOT add manualChunks tuning."
---

<objective>
Fully defer combobox's two heavy deps: route its `size` middleware through the loaded floating-ui module (SIZE-01), swap its `virtualize()` render site to the prefetch + `repeat()`-fallback pattern (SIZE-02), and apply the bounded SIZE-05 idle-init sweep (combobox is one of the two measured Phase-7 hotspots). combobox owns its file end-to-end so it can be executed in parallel with the other Wave-2 overlays.

Purpose: combobox is the reference implementation for the repeat↔virtualize swap (`_renderOptionList`) that data-grid and select mirror, and a measured hotspot for SIZE-05.
Output: combobox with zero static floating-ui/virtualizer runtime imports, cold-chunk fallback proven, non-critical init deferred.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
@src/internal/helpers/lazy-load.ts
@src/internal/helpers/virtualize-support.ts
</context>

## Artifacts this phase produces (touched by this plan)

- Consumes `loadFloating`/`prefetchFloating`/`loadVirtualizer`/`prefetchVirtualizer` from `src/internal/helpers/lazy-load.ts` (08-01).
- Consumes the controller's module-receiving middleware getter (08-01).
- Extends `test/browser/combobox-virtual.test.ts` with a cold-chunk `repeat()` fallback assertion.

<tasks>

<task type="auto">
  <name>Task 1: combobox floating-ui — `size` middleware from the loaded module + prefetch</name>
  <files>src/components/combobox/combobox.ts, test/components/combobox.test.ts</files>
  <read_first>
    - src/components/combobox/combobox.ts (static imports at lines 5-6 — `virtualize`, `size as sizeMiddleware`; the FloatingPositionController field-init at lines 154-169 with the static `sizeMiddleware(...)` array; the trigger open path)
    - src/internal/controllers/floating-position.ts (module-receiving middleware getter option from 08-01)
    - src/components/popover/popover.ts (getter-form analog)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (combobox/select assignment lines 122-146)
  </read_first>
  <action>
    Remove the static `size as sizeMiddleware` import from `combobox.ts` (line 6; keep the `virtualize` import handling for Task 2). Move the controller's `middleware` from the construction-time static array (lines 160-168) to the module-receiving getter form: `middleware: (mod) => [mod.size({ apply({ rects, elements }) { Object.assign(elements.floating.style, { width: ` + '`${rects.reference.width}px`' + ` }); } })]`. This is required because the field-init array evaluates BEFORE the module loads. Hang `prefetchFloating()` off the existing trigger `pointerenter`/`focus` listener (D-01/D-03). Reveal stays gated on first position (D-02). <!-- planner-discipline-allow: @floating-ui/dom -->
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/combobox.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - After build, `combobox.ts` has no static runtime `@floating-ui/dom` import; the `size` middleware is built from `mod.size` inside the getter.
    - `combobox.test.ts` passes unchanged (open/close, filtering, width-match behavior-preserving).
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>combobox sources `size` from the loaded module via the getter, prefetches on intent, positions with matched width identically, no 0,0 frame.</done>
</task>

<task type="auto">
  <name>Task 2: combobox virtualizer — deferred import + repeat() fallback swap</name>
  <files>src/components/combobox/combobox.ts, test/browser/combobox-virtual.test.ts</files>
  <read_first>
    - src/components/combobox/combobox.ts (static `virtualize` import at line 5; `_renderOptionList` at lines 717-733 — the canonical repeat↔virtualize swap; the `VIRTUALIZE_ROW_THRESHOLD` import at lines 14-15)
    - src/internal/helpers/lazy-load.ts (loadVirtualizer / prefetchVirtualizer)
    - src/internal/helpers/virtualize-support.ts (VIRTUALIZE_ROW_THRESHOLD = 100 at line 66; ARIA helpers computed from absolute index at 73-94)
    - test/browser/combobox-virtual.test.ts (existing virtual behavior spec to extend)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Pattern 2 lines 186-192; Pitfall V1 lines 257-260; code example lines 312-320)
  </read_first>
  <action>
    Remove the static `virtualize` import (line 5). Add a `_virtualize` state field (the loaded directive, initially undefined). In `_renderOptionList` keep the `filtered.length > VIRTUALIZE_ROW_THRESHOLD` branch but swap its body to `this._virtualize ? this._virtualize({ items, keyFunction, renderItem }) : repeat(filtered, o => o, (o, i) => this._renderOption(o, i, filtered.length))` — the existing `repeat()` path is the D-05 cold-cross AND fetch-failure fallback, fully functional (unwindowed). Set `_virtualize` via `loadVirtualizer().then(m => { this._virtualize = m.virtualize; this.requestUpdate(); })`, kicked by `prefetchVirtualizer()` when the popup opens near/above threshold (D-04). Keep `renderItem` bodies byte-identical; ARIA posinset/setsize already compute from the absolute index so both paths match. <!-- planner-discipline-allow: @lit-labs/virtualizer -->
    Extend `combobox-virtual.test.ts` with a cold-chunk assertion: with >100 options and the virtualizer not yet resolved, the list renders all options functionally via repeat() (correct ARIA, selectable), then swaps to the windowed virtualize() render after load.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --project browser test/browser/combobox-virtual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At 99 filtered options → repeat(); at 100 → repeat() (swap is strictly `> 100`); at 101 with the chunk resolved → virtualize(). The boundary matches today's behavior.
    - Cold-chunk case: above threshold before load, the list is fully functional (unwindowed repeat, correct posinset/setsize), then swaps to virtualize() after `requestUpdate()`.
    - After build, `combobox.ts` has no static `@lit-labs/virtualizer` import.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>combobox defers the virtualizer, falls back to a functional repeat() render on a cold/failed chunk, and swaps to virtualize() on resolve with identical ARIA; threshold behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: combobox SIZE-05 — bounded idle/deferred non-critical init</name>
  <files>src/components/combobox/combobox.ts, test/components/combobox.test.ts</files>
  <read_first>
    - src/components/combobox/combobox.ts (the constructor / firstUpdated / connectedCallback — locate ResizeObserver / MutationObserver / non-essential global listener attach done on the construction or first-paint path)
    - .planning/codebase/CONCERNS.md (autoUpdate/ResizeObserver churn — SIZE-05 sweep candidates)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Pattern 4 lines 202-208; Pitfall SIZE5-1 lines 272-275 — no requestIdleCallback)
    - src/internal/controllers/floating-position.ts (the existing autoUpdate-on-open gating discipline this sweep mirrors)
  </read_first>
  <action>
    Identify combobox's non-critical init that currently runs on the constructor / first-paint path (e.g. an observer or non-essential listener that is only needed after first interaction). Move it off the critical path using an interaction-gate (attach on first open/focus, mirroring the existing autoUpdate-on-open discipline) OR a double-`requestAnimationFrame`/microtask for "after first paint" work (D-09). Guard teardown so that if the host disconnects before the scheduled callback fires, the deferred init does not run twice or leak a listener/observer (use the existing disconnect/teardown path). Do NOT use `requestIdleCallback` (unsupported at the Safari 16.4 floor). Keep the change narrow — this is the bounded D-08 sweep, not a broad audit. Add a short code comment documenting the deferral discipline. <!-- planner-discipline-allow: requestIdleCallback -->
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/combobox.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `combobox.test.ts` passes unchanged — the deferred init still runs (on interaction/after paint) and behavior is preserved (filtering, sizing, selection, teardown).
    - No `requestIdleCallback` appears in `combobox.ts` (grep confirms).
    - Disconnecting the host before the scheduled callback does not throw or leave a dangling observer/listener (teardown test / existing disconnect coverage passes).
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>combobox's non-critical init is deferred off first paint via interaction-gate/rAF, teardown-guarded against double-run/leak, no requestIdleCallback, behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → deferred chunk fetch | combobox triggers the shared floating-ui and virtualizer `import()`s at open time. |
| user interaction → deferred init scheduling | non-critical init is gated on interaction/rAF; interruption/disconnect is an availability concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering (XSS) | repeat() cold-chunk fallback render | low | mitigate | Fallback stays a Lit `repeat()` template — no innerHTML/eval on any degraded path. |
| T-08-10 | Denial of Service | virtualizer chunk fetch fails above threshold | low | mitigate | repeat() fallback keeps the full list functional (unwindowed) — free failure mode (D-05). |
| T-08-11 | Denial of Service | deferred init interrupted by disconnect | low | mitigate | teardown-guarded scheduling — no double-run or leaked observer/listener. |

No package-manager installs occur in this phase — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `npm run build` green; `combobox.test.ts` unchanged and passing.
- `npx vitest run --project browser test/browser/combobox-virtual.test.ts` — threshold + cold-chunk fallback pass.
- `combobox.ts` free of static floating-ui/virtualizer imports and `requestIdleCallback`; `npm run diff:surface` clean.
</verification>

<success_criteria>
- combobox carries zero static floating-ui/virtualizer runtime imports.
- repeat↔virtualize swap is behavior-preserving at 99/100/101 and on a cold/failed chunk.
- Non-critical init deferred off first paint, teardown-safe, no requestIdleCallback.
- Surface frozen.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-04-SUMMARY.md` when done.
</output>
