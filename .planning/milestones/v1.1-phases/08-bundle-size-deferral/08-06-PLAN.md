---
phase: 08-bundle-size-deferral
plan: 06
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/data-grid/data-grid.ts
  - test/browser/data-grid-virtual.test.ts
  - test/components/data-grid.test.ts
autonomous: true
requirements: [SIZE-02, SIZE-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "data-grid's unconditional virtualize() render site swaps to `this._virtualize ? this._virtualize({...}) : repeat(...)`; the cold-chunk/fetch-failure repeat() render is fully functional (unwindowed) and swaps to virtualize() on resolve via requestUpdate() — SIZE-02 adjacency edge."
    - "aria-rowindex is computed from the ABSOLUTE sorted index and is identical across the repeat() and virtualize() paths (no drift across the swap) — SIZE-02 precision edge."
    - "SIZE-05: data-grid's non-critical init (ResizeObserver/observer/listener attach) is moved off the constructor/first-paint path onto interaction-gate or rAF/microtask; a disconnect before the scheduled callback fires does not double-run or leak — SIZE-05 concurrency edge."
    - "data-grid carries zero static @lit-labs/virtualizer runtime import after the swap."
  artifacts:
    - "data-grid with a deferred virtualizer + repeat() fallback and deferred non-critical init"
  key_links:
    - "data-grid.ts:516 unconditional virtualize() → `this._virtualize ? this._virtualize({...}) : repeat(sorted, keyFn, renderItem)`; `_virtualize` set via `loadVirtualizer().then(m => { this._virtualize = m.virtualize; this.requestUpdate(); })`."
    - "prefetchVirtualizer() near threshold / on grid mount (D-04)."
  prohibitions:
    - "MUST NOT change the keyFunction/renderItem bodies (byte-identical rows) or the absolute-index ARIA computation."
    - "MUST NOT use requestIdleCallback for the SIZE-05 deferral (Safari 16.4 floor — D-09)."
    - "MUST NOT change data-grid's observable behavior (sort/select/focus) or CEM surface (surface-freeze)."
    - "MUST NOT introduce innerHTML/eval in the fallback render path."
---

<objective>
Defer data-grid's virtualizer (SIZE-02) and apply the bounded SIZE-05 idle-init sweep — data-grid is the second measured Phase-7 hotspot. data-grid renders `virtualize()` UNCONDITIONALLY today, so this is the site where the repeat() fallback most changes the common-case cold render; the ARIA row indices already compute from the absolute sorted index, so the swap is behavior-preserving.

Purpose: complete SIZE-02 for the last render site and land the second of the two bounded SIZE-05 sweeps.
Output: data-grid with a deferred virtualizer + functional repeat() fallback and deferred non-critical init.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
@src/internal/helpers/lazy-load.ts
@src/components/combobox/combobox.ts
</context>

## Artifacts this phase produces (touched by this plan)

- Consumes `loadVirtualizer`/`prefetchVirtualizer` from `src/internal/helpers/lazy-load.ts` (08-01).
- Extends `test/browser/data-grid-virtual.test.ts` with a cold-chunk `repeat()` fallback assertion.

<tasks>

<task type="auto">
  <name>Task 1: data-grid virtualizer — deferred import + repeat() fallback swap</name>
  <files>src/components/data-grid/data-grid.ts, test/browser/data-grid-virtual.test.ts</files>
  <read_first>
    - src/components/data-grid/data-grid.ts (the unconditional `virtualize({ items: sorted, keyFunction, renderItem })` at lines 516-524 inside `render()`; the `@lit-labs/virtualizer` static import; the `getRowId`/`indexById`/absolute-index (`absIndex`) usage that drives `aria-rowindex`)
    - src/components/combobox/combobox.ts (`_renderOptionList` repeat↔virtualize swap — the reference implementation from 08-04)
    - src/internal/helpers/lazy-load.ts (loadVirtualizer / prefetchVirtualizer)
    - src/internal/helpers/virtualize-support.ts (ariaRowindex computed from absolute index at lines 92-94)
    - test/browser/data-grid-virtual.test.ts (existing virtual behavior spec to extend)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Pattern 2 lines 186-192; Pitfall V1 lines 257-260)
  </read_first>
  <action>
    Remove the static `@lit-labs/virtualizer` import from `data-grid.ts`. Add a `_virtualize` state field (initially undefined). Replace the unconditional `virtualize({...})` at line 516 with `this._virtualize ? this._virtualize({ items: sorted, keyFunction, renderItem }) : repeat(sorted, keyFunction, renderItem)` — keeping the existing `keyFunction`/`renderItem` bodies (lines 517-524) byte-identical. Set `_virtualize` via `loadVirtualizer().then(m => { this._virtualize = m.virtualize; this.requestUpdate(); })`, kicked by `prefetchVirtualizer()` near threshold / on grid mount (D-04). The `repeat()` path is the D-05 cold-cross AND fetch-failure fallback; `aria-rowindex` from the absolute index is identical in both paths so the swap is behavior-preserving. Import `repeat` from `lit/directives/repeat.js` if not already imported. <!-- planner-discipline-allow: @lit-labs/virtualizer -->
    Extend `data-grid-virtual.test.ts` with a cold-chunk assertion: before the virtualizer resolves, the grid body renders all rows via repeat() with correct `aria-rowindex`, then swaps to the windowed virtualize() render after load.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --project browser test/browser/data-grid-virtual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cold-chunk case: the grid renders a functional unwindowed repeat() body with correct `aria-rowindex` (absolute-index based), then swaps to virtualize() after `requestUpdate()`.
    - `aria-rowindex` for a given row is identical whether rendered via repeat() or virtualize().
    - After build, `data-grid.ts` has no static `@lit-labs/virtualizer` import.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>data-grid defers the virtualizer, falls back to a functional repeat() render on a cold/failed chunk, swaps to virtualize() on resolve with identical row ARIA; sort/select/focus behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: data-grid SIZE-05 — bounded idle/deferred non-critical init</name>
  <files>src/components/data-grid/data-grid.ts, test/components/data-grid.test.ts</files>
  <read_first>
    - src/components/data-grid/data-grid.ts (constructor / firstUpdated / connectedCallback — locate ResizeObserver / observer / non-essential listener attach on the construction or first-paint path)
    - .planning/codebase/CONCERNS.md (data-grid render cost + observer churn — SIZE-05 candidate)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Pattern 4 lines 202-208; Pitfall SIZE5-1 lines 272-275 — no requestIdleCallback)
    - src/components/combobox/combobox.ts (the sibling SIZE-05 sweep from 08-04 for the interaction-gate/rAF idiom)
  </read_first>
  <action>
    Identify data-grid's non-critical init that runs on the constructor/first-paint path (e.g. an observer or listener only needed after first interaction/paint). Move it off the critical path via an interaction-gate (attach on first interaction) OR a double-`requestAnimationFrame`/microtask for after-first-paint work (D-09). Guard teardown so a disconnect before the scheduled callback fires does not double-run or leak. Do NOT use `requestIdleCallback`. Keep the change narrow (bounded D-08 sweep). Add a short comment documenting the discipline. <!-- planner-discipline-allow: requestIdleCallback -->
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/data-grid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `data-grid.test.ts` passes unchanged — the deferred init still runs and sort/select/focus/resize behavior is preserved.
    - No `requestIdleCallback` appears in `data-grid.ts` (grep confirms).
    - Disconnecting the host before the scheduled callback does not throw or leave a dangling observer/listener.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>data-grid's non-critical init is deferred off first paint via interaction-gate/rAF, teardown-guarded, no requestIdleCallback, behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → deferred chunk fetch | data-grid triggers the virtualizer `import()` on demand. |
| user interaction → deferred init scheduling | non-critical init gated on interaction/rAF; interruption/disconnect is an availability concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-15 | Tampering (XSS) | data-grid repeat() cold-chunk fallback | low | mitigate | Fallback stays a Lit `repeat()` template — no innerHTML/eval. |
| T-08-16 | Denial of Service | virtualizer chunk fetch fails on grid mount | low | mitigate | repeat() fallback renders the full grid functionally (unwindowed) — D-05. |
| T-08-17 | Denial of Service | deferred init interrupted by disconnect | low | mitigate | teardown-guarded scheduling — no double-run or leaked observer/listener. |

No package-manager installs occur in this phase — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `npm run build` green; `data-grid.test.ts` unchanged and passing.
- `npx vitest run --project browser test/browser/data-grid-virtual.test.ts` — cold-chunk fallback + ARIA parity pass.
- `data-grid.ts` free of static virtualizer import and `requestIdleCallback`; `diff:surface` clean.
</verification>

<success_criteria>
- data-grid carries zero static virtualizer runtime import; repeat↔virtualize swap behavior-preserving with identical row ARIA.
- Non-critical init deferred off first paint, teardown-safe, no requestIdleCallback.
- Surface frozen.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-06-SUMMARY.md` when done.
</output>
