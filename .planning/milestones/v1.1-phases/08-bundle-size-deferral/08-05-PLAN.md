---
phase: 08-bundle-size-deferral
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/select/select.ts
  - src/internal/helpers/virtualize-support.ts
  - test/browser/virtualize-smoke.test.ts
  - test/components/select.test.ts
autonomous: true
requirements: [SIZE-01, SIZE-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "select sources its `size` width-match middleware from the loaded floating-ui module via the controller's module-receiving getter and positions identically."
    - "select's `virtualize()` render site swaps to the prefetch + repeat()-fallback pattern; the cold-chunk repeat() render is fully functional and swaps to virtualize() on resolve — SIZE-02 adjacency edge."
    - "virtualize-support.ts no longer statically imports the runtime `virtualizerRef` symbol from @lit-labs/virtualizer — so importing virtualize-support.ts does NOT pull the virtualizer into a component's static graph; scrollVirtualizerToIndex resolves virtualizerRef from the loaded module (or degrades to a safe no-op) — SIZE-02 empty edge (the helper import is the hidden static-graph leak)."
    - "scrollVirtualizerToIndex remains a safe no-op when no virtualizer is attached yet (below threshold or before load) so callers stay safe during the fallback window."
  artifacts:
    - "select with zero static @floating-ui/dom and @lit-labs/virtualizer runtime imports"
    - "virtualize-support.ts free of a static runtime @lit-labs/virtualizer import"
  key_links:
    - "select controller middleware getter `(mod) => [mod.size({...width match...})]` (select.ts:274-289 → getter)."
    - "select virtualize render site (select.ts:990-992) swaps `this._virtualize ? this._virtualize({...}) : repeat(...)` with `loadVirtualizer().then(...requestUpdate())`."
    - "virtualize-support.ts virtualizerRef access routed through loadVirtualizer() (or a lazily-resolved symbol) rather than a top-level runtime import."
  prohibitions:
    - "MUST NOT bump the exact @lit-labs/virtualizer 2.1.1 pin (deferral changes import timing only)."
    - "MUST NOT change the VIRTUALIZE_ROW_THRESHOLD value or the ARIA helpers (behavior-preserving)."
    - "MUST NOT change select's observable behavior or CEM surface (surface-freeze)."
    - "MUST NOT introduce innerHTML/eval in the fallback path."
---

<objective>
Fully defer select's floating-ui `size` middleware and virtualizer render site (SIZE-01 + SIZE-02), and close the hidden static-graph leak in `virtualize-support.ts` — its top-level runtime `virtualizerRef` import would otherwise keep `@lit-labs/virtualizer` in the static graph of every component that imports the helper, near-zeroing the SIZE-02 win.

Purpose: select mirrors combobox's dual deferral; virtualize-support.ts is the shared helper whose static virtualizer import must also become deferred for the byte win to be real.
Output: select with zero static floating-ui/virtualizer imports; virtualize-support.ts with no static runtime virtualizer import; scrollVirtualizerToIndex still a safe no-op during the fallback window.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
@src/internal/helpers/lazy-load.ts
@src/components/combobox/combobox.ts
</context>

## Artifacts this phase produces (touched by this plan)

- Consumes `loadFloating`/`prefetchFloating`/`loadVirtualizer`/`prefetchVirtualizer` from `src/internal/helpers/lazy-load.ts` (08-01).
- Modifies `virtualize-support.ts` to remove its static runtime `@lit-labs/virtualizer` import (a shared internal helper imported by data-grid/combobox/select).

<tasks>

<task type="auto">
  <name>Task 1: select — `size` middleware from the loaded module + virtualizer deferral + prefetch</name>
  <files>src/components/select/select.ts, test/components/select.test.ts, test/browser/virtualize-smoke.test.ts</files>
  <read_first>
    - src/components/select/select.ts (static imports at lines 3-4 — `size`, `virtualize`; the FloatingPositionController field with `size` middleware ~lines 274-289; the `virtualize()` render site at ~line 990-992; the trigger open path)
    - src/components/combobox/combobox.ts (the sibling analog migrated in 08-04 — getter middleware + `_renderOptionList` repeat↔virtualize swap)
    - src/internal/helpers/lazy-load.ts (loadFloating/prefetchFloating/loadVirtualizer/prefetchVirtualizer)
    - src/internal/helpers/virtualize-support.ts (VIRTUALIZE_ROW_THRESHOLD; ARIA helpers computed from absolute index)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (combobox/select assignment lines 122-146)
  </read_first>
  <action>
    Remove the static `size` and `virtualize` imports from `select.ts` (lines 3-4). Move the controller's `size` middleware to the module-receiving getter form `middleware: (mod) => [mod.size({ apply({ rects, elements }) { ...width match... } })]` (mirroring combobox 08-04). Swap the `virtualize()` render site to `this._virtualize ? this._virtualize({...}) : repeat(...)` with `_virtualize` set by `loadVirtualizer().then(m => { this._virtualize = m.virtualize; this.requestUpdate(); })`; prefetch floating on trigger intent and virtualizer near threshold / on popup open (D-01/D-03/D-04). Keep `renderItem`/`keyFunction` bodies byte-identical and the reveal gated on first position. <!-- planner-discipline-allow: @floating-ui/dom --> <!-- planner-discipline-allow: @lit-labs/virtualizer -->
    Extend `virtualize-smoke.test.ts` with a select cold-chunk fallback assertion (functional repeat() above threshold before load, swap to virtualize() after).
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/select.test.ts && npx vitest run --project browser test/browser/virtualize-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - After build, `select.ts` has no static runtime `@floating-ui/dom` or `@lit-labs/virtualizer` import.
    - `select.test.ts` passes unchanged (open/close, filtering, width-match, selection behavior-preserving).
    - Cold-chunk: above threshold before load the select popup renders a functional unwindowed repeat(); swaps to virtualize() after resolve with identical ARIA.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>select sources `size` from the loaded module, defers the virtualizer with a functional repeat() fallback, prefetches on intent, and positions identically; no 0,0 frame; surface frozen.</done>
</task>

<task type="auto">
  <name>Task 2: virtualize-support.ts — defer the static virtualizerRef import (close the static-graph leak)</name>
  <files>src/internal/helpers/virtualize-support.ts, test/browser/virtualize-smoke.test.ts</files>
  <read_first>
    - src/internal/helpers/virtualize-support.ts (the static runtime import at line 44 — `virtualizerRef, type VirtualizerHostElement`; `scrollVirtualizerToIndex` at 110-117 which uses `virtualizerRef`; the JSDoc noting it is already a no-op when no virtualizer is attached, lines 104-106)
    - src/internal/helpers/lazy-load.ts (loadVirtualizer — the module the symbol should come from)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (virtualize-support assignment lines 110-118 — expose a helper resolving virtualizerRef from the loaded module, keep the type-only import where possible, do NOT bump the 2.1.1 pin)
  </read_first>
  <action>
    Change `virtualize-support.ts` so it no longer statically imports the RUNTIME `virtualizerRef` symbol from `@lit-labs/virtualizer/virtualize.js` (keep `type VirtualizerHostElement` as a type-only import — erased at build). Route `scrollVirtualizerToIndex` to resolve `virtualizerRef` from the loaded module: since it is already documented as a safe no-op when no virtualizer is attached, make it read the loaded module's `virtualizerRef` only when a virtualizer is present (e.g. via the memoized `loadVirtualizer()` result cached at first attach, or guard on the symbol being resolvable). If a fully-synchronous symbol read is unavoidable, keep the function a no-op until the module has been loaded by a render-site swap (callers already invoke it unconditionally and tolerate the no-op). Do NOT bump the `2.1.1` pin; do NOT change `VIRTUALIZE_ROW_THRESHOLD` or the ARIA helpers. <!-- planner-discipline-allow: @lit-labs/virtualizer -->
  </action>
  <verify>
    <automated>npm run build && npx vitest run --project browser test/browser/virtualize-smoke.test.ts && node scripts/assert-no-bundled-lit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - After build, `virtualize-support.ts` (and thus any non-virtualizing consumer that imports it) has NO static runtime `@lit-labs/virtualizer` import in its dist static graph — the virtualizer appears only behind a dynamic `import()`.
    - `scrollVirtualizerToIndex` remains a safe no-op when no virtualizer is attached (existing data-grid/select scroll behavior unchanged once loaded).
    - The virtualize-smoke browser spec passes (scroll-to-index still works after the virtualizer loads).
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>virtualize-support.ts carries no static runtime virtualizer import; scrollVirtualizerToIndex resolves the symbol lazily / stays a safe no-op; the 2.1.1 pin and ARIA/threshold behavior are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → deferred chunk fetch | select and the shared helper trigger the virtualizer/floating-ui `import()`s on demand. |
| build → shipped artifact | virtualize-support.ts must not statically pull the virtualizer into non-virtualizing entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering (XSS) | select repeat() cold-chunk fallback | low | mitigate | Fallback stays a Lit `repeat()` template — no innerHTML/eval. |
| T-08-13 | Denial of Service | virtualizer chunk fetch fails on select popup | low | mitigate | repeat() fallback keeps the popup functional (unwindowed) — D-05. |
| T-08-14 | Tampering (integrity) | virtualize-support static import leaks virtualizer into every consumer's graph | medium | mitigate | Deferred symbol resolution; deep-import purity assertion (08-07) verifies absence in non-virtualizing entries. |

No package-manager installs occur in this phase — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `npm run build` green; `select.test.ts` unchanged and passing.
- `npx vitest run --project browser test/browser/virtualize-smoke.test.ts` — select cold-chunk fallback + scroll-to-index pass.
- select.ts and virtualize-support.ts free of static virtualizer/floating-ui runtime imports; `assert-no-bundled-lit` clean; `diff:surface` clean.
</verification>

<success_criteria>
- select carries zero static floating-ui/virtualizer runtime imports; positioning + virtualize swap behavior-preserving.
- virtualize-support.ts no longer leaks the virtualizer into non-virtualizing static graphs; scrollVirtualizerToIndex safe.
- Surface frozen; 2.1.1 pin intact.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-05-SUMMARY.md` when done.
</output>
