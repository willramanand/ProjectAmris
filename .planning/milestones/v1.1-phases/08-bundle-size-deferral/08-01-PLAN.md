---
phase: 08-bundle-size-deferral
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/internal/helpers/lazy-load.ts
  - src/internal/controllers/floating-position.ts
  - src/components/popover/popover.ts
  - .size-limit.json
  - test/browser/floating-position.test.ts
  - test/browser/overlay-no-zero-frame.test.ts
  - test/browser/registration-smoke.test.ts
  - api/size.baseline.json
autonomous: true
requirements: [SIZE-01, SIZE-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "loadFloating() returns one memoized module promise: two concurrent first-opens share a single import() (no double network/parse) — SIZE-01 empty/adjacency edge."
    - "An overlay opened before its prefetch promise resolves (cold cross) still positions correctly and never paints a frame at 0,0 — SIZE-01 adjacency edge."
    - "The open→position→focus→autoUpdate ordering contract is preserved after start() becomes async: focus/autoUpdate never run ahead of the first computePosition — SIZE-01 ordering edge."
    - "An imported component still calls customElements.define at runtime after the refactor (registration is not shaken away) — SIZE-03 registration edge (D-10 registration-smoke canary)."
    - "popover positions identically in real Chromium layout to the pre-deferral behavior (viewport-anchored, offset from trigger)."
    - "The re-scoped brotli size metric shows @floating-ui/dom leaving popover's initial (synchronous) payload — the deferral win posts report-only."
  artifacts:
    - src/internal/helpers/lazy-load.ts
    - test/browser/overlay-no-zero-frame.test.ts
    - test/browser/registration-smoke.test.ts
    - "New report-only overlay deep-import entry in .size-limit.json"
  key_links:
    - "floating-position.ts start() awaits loadFloating() before the first computePosition, then starts autoUpdate (D-01/D-02)."
    - "popover prefetchFloating() hangs off the existing trigger pointerenter/focus listener (D-03)."
    - "popover reveal stays gated on first computePosition writing left/top (hidden-until-positioned, D-02)."
  prohibitions:
    - "MUST NOT change any component's observable behavior, public API, or CEM surface (surface-freeze; diff:surface stays clean)."
    - "MUST NOT bundle or duplicate Lit (external array stays byte-frozen; no-bundled-Lit assertion clean)."
    - "MUST NOT paint an overlay at 0,0 before it is first positioned."
    - "MUST NOT add manualChunks tuning (that is PERF-V2-01, deferred)."
    - "MUST NOT edit the vite.config.ts external array (snapshot-frozen by test/no-bundled-lit.test.ts)."
    - "MUST NOT flip any size/count budget from report-only to enforcing (that is Phase 11)."
---

<objective>
Prove the floating-ui deferral end-to-end on ONE overlay path (popover): create the shared memoized dynamic-import loader, convert `FloatingPositionController` to await it, migrate popover to build its middleware from the loaded module, and verify the byte win is real, positioning is byte-for-byte identical, and no 0,0 frame appears — before expanding to the other overlays. Closes the SIZE-01 architecture and stands up the SIZE-03 registration canary.

Purpose: The loader → controller → overlay → measurement path is the phase's architectural spine (per D-06 and RESEARCH Pattern 1). Proving it once on the best early-context tokens catches an architectural dead-end after one commit instead of after eight migrated components.
Output: `src/internal/helpers/lazy-load.ts`, a deferred `FloatingPositionController`, a deferred popover, the no-0,0-frame + registration-smoke browser specs, a report-only overlay size entry, and a re-written size baseline showing the win.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
</context>

## Artifacts this phase produces (created by this plan)

- `src/internal/helpers/lazy-load.ts` (NEW internal helper) exporting: `loadFloating()`, `prefetchFloating()`, `loadVirtualizer()`, `prefetchVirtualizer()` — module-level promise-memoized loaders. Never re-exported from `src/index.ts` / `src/index.all.ts`; invisible to the CEM by construction.
- New `FloatingPositionController` middleware-getter shape: `middleware?: (mod: typeof import('@floating-ui/dom')) => Middleware[]` (internal controller API, non-exported).
- `test/browser/overlay-no-zero-frame.test.ts` (NEW spec) — asserts the hidden-until-positioned invariant.
- `test/browser/registration-smoke.test.ts` (NEW spec) — asserts `customElements.get('am-…')` is defined after import (SIZE-03).
- New report-only `.size-limit.json` overlay deep-import entry (e.g. `dist/components/popover/index.js`) making the SIZE-01 "positioning absent from non-overlay entries" delta legible.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end floating-ui deferral — loader + controller + popover, one path</name>
  <files>src/internal/helpers/lazy-load.ts, src/internal/controllers/floating-position.ts, src/components/popover/popover.ts</files>
  <read_first>
    - src/internal/controllers/floating-position.ts (the controller being modified — the full lifecycle, lines 1-131: static import at 2-12, `start()` at 94-104, `_updatePosition` at 116-130, `middleware` option at 54)
    - src/internal/helpers/virtualize-support.ts (the internal-helper JSDoc/boundary convention to MIRROR for lazy-load.ts: "registers no custom element … never appears on the frozen CEM", deps stay external)
    - src/components/popover/popover.ts (the tracer overlay — getter middleware form at lines 60-73, `onPositioned` arrow readback, the trigger open path)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Code Examples lines 279-310; Pitfall F1 lines 245-249; Pitfall CP1 lines 251-255 for why the controller always runs autoUpdate)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (the memoized-loader idiom lines 33-46; controller assignment lines 64-106; popover assignment lines 150-158)
    - vite.config.ts (the `external` array at line 241 — MUST stay byte-identical; chunk naming at 245)
  </read_first>
  <behavior>
    - loadFloating() called twice returns the identical Promise instance (module-level `??=` cache; concurrent callers dedupe to one import()).
    - prefetchFloating() is fire-and-forget (returns void) and warms the same cache loadFloating() awaits.
    - After start() (now async), the controller awaits loadFloating(), then calls mod.autoUpdate(...) which schedules mod.computePosition(...) with the base offset→flip→shift stack plus the host getter's `mod.*` middleware.
    - popover builds its arrow middleware from the loaded module: `middleware: (mod) => this.arrow && this.arrowEl ? [mod.arrow({ element: this.arrowEl })] : []`.
    - Opening popover positions the panel at a viewport-anchored non-zero rect offset from the trigger; the panel is never painted at 0,0 (visibility gated on first computePosition).
  </behavior>
  <action>
    Create `src/internal/helpers/lazy-load.ts` following the RESEARCH Code Example (lines 280-293) and the `virtualize-support.ts` internal-helper JSDoc convention: four exports — `loadFloating()` / `prefetchFloating()` / `loadVirtualizer()` / `prefetchVirtualizer()` — each a module-level `promise ??= import(...)` cache. Use bare/relative dynamic-import specifiers ONLY (the consumer's bundler resolves them; deps stay external) — never a computed or origin-qualified module path. Include both the floating-ui and virtualizer loaders now so downstream plans consume this one file. <!-- planner-discipline-allow: @floating-ui/dom -->
    In `floating-position.ts`: remove the runtime named imports (`computePosition`, `autoUpdate`, `flip`, `shift`, `offset`) from the static `@floating-ui/dom` import; keep the `type`-only imports (`Middleware`, `MiddlewareData`, `Placement`, `Strategy` — erased at build). Make `start()` `async`: after `this.stop()`, `const mod = await loadFloating()`, then resolve reference/floating, keep the `if (!reference || !floating) return;` early-out, and call `mod.autoUpdate(...)` scheduling `_updatePosition(mod, reference, floating)`. Thread `mod` into `_updatePosition` and use `mod.offset`/`mod.flip`/`mod.shift`/`mod.computePosition`, preserving the exact base stack and the `?? 4` / `?? 'bottom-start'` / `?? []` defaults. Change the `middleware` option type from `Resolvable<Middleware[]>` to `(mod: typeof import('@floating-ui/dom')) => Middleware[]`. <!-- planner-discipline-allow: @floating-ui/dom -->
    In `popover.ts`: remove the static `arrow` import (keep `type Placement`); change the middleware getter to receive `mod` and call `mod.arrow(...)`; leave `onPositioned` unchanged. Hang `prefetchFloating()` off the existing trigger `pointerenter`/`focus` listener (D-01/D-03). Confirm the panel reveal stays gated on the first computePosition writing `left/top` (D-02) — do not reveal before position resolves.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --project browser test/browser/floating-position.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (tsc strict + vite) with no static runtime `@floating-ui/dom` import left in `floating-position.ts` or `popover.ts` (only the deferred loader + type-only imports remain).
    - `npx vitest run --project browser test/browser/floating-position.test.ts` passes: the opened popover has a non-zero, viewport-anchored rect offset from its trigger (real Chromium layout), proving positioning is behavior-preserving after the async seam.
    - `npm run diff:surface` exits 0 (CEM/public surface unchanged).
    - A focused inspection of `lazy-load.ts` shows every loader uses a static bare specifier inside `import()` and a module-level `??=` cache (concurrent callers share one promise).
  </acceptance_criteria>
  <reversibility rating="reversible">Prefetch wiring and the loader boundary are local; re-tuning or re-inlining is cheap (D-01/D-06 loader path is reversible).</reversibility>
  <done>lazy-load.ts exists with four memoized loaders; the controller awaits the floating-ui module and starts autoUpdate only after it resolves; popover prefetches on intent, builds arrow from the loaded module, and positions identically in Chromium; build + browser positioning + diff:surface all green.</done>
</task>

<task type="auto">
  <name>Task 2: SIZE-03 registration canary + no-0,0-frame invariant spec</name>
  <files>test/browser/registration-smoke.test.ts, test/browser/overlay-no-zero-frame.test.ts, test/browser/floating-position.test.ts</files>
  <read_first>
    - test/browser/floating-position.test.ts (the existing browser popover spec + `waitForPosition` helper at lines 24-30 — the analog to extend)
    - test/no-bundled-lit.test.ts (the report-only assertion convention + jsdom lane style)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (registration-smoke example lines 322-329; Pitfall SE1 lines 267-270; Wave 0 Gaps lines 407-413)
    - test/helpers.ts (fixture / shadowQuery / waitForUpdate helpers used by browser specs)
  </read_first>
  <action>
    Create `test/browser/registration-smoke.test.ts`: import a representative component module for its side effect (`import '../../src/components/select/select';` and one or two more, e.g. popover, data-grid) and assert `customElements.get('am-select')` (etc.) is defined — proving a `sideEffects`/tree-shake change cannot silently shake away `@customElement` registration (Pitfall SE1, SIZE-03). Report-only in intent; a real failure is a genuine regression.
    Create `test/browser/overlay-no-zero-frame.test.ts`: open popover in real Chromium and assert the hidden-until-positioned invariant deterministically — the floating panel is NOT painted at viewport 0,0 at any point during the open transition (it stays `visibility:hidden` / unrevealed until the first computePosition writes `left/top`), and once revealed sits at a non-(0,0) viewport-anchored rect. Poll frames across the async loader gap so the assertion covers the cold-cross window. This is the Pitfall F1 guard.
    Extend `floating-position.test.ts` with one assertion that focus lands on the correct post-open target element (ordering contract preserved after start() became async).
  </action>
  <verify>
    <automated>npx vitest run --project browser test/browser/registration-smoke.test.ts test/browser/overlay-no-zero-frame.test.ts test/browser/floating-position.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `registration-smoke.test.ts` passes: `customElements.get('am-select')` (and the other imported tags) is defined after import.
    - `overlay-no-zero-frame.test.ts` passes: no polled frame observes the popover panel painted at (0,0); the revealed panel rect is non-zero and viewport-anchored.
    - The extended focus assertion in `floating-position.test.ts` passes (focus lands correctly after the async open).
  </acceptance_criteria>
  <done>Both new browser specs exist and pass; the ordering/focus assertion passes; the SIZE-03 registration canary and the Pitfall F1 no-0,0 guard are in the browser lane.</done>
</task>

<task type="auto">
  <name>Task 3: Make the deferral win legible — overlay size entry + re-baseline</name>
  <files>.size-limit.json, api/size.baseline.json</files>
  <read_first>
    - .size-limit.json (the existing entry shape — `name`/`path`/`ignore:["lit"]`/`limit`; entries are core/full/button/data-grid/first-load-composite)
    - scripts/size-baseline.mjs (the `--write`/`--check` modes lines 16-21; brotli unit; floating-ui NOT ignored lines 11-14)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Open Question 1 lines 355-358; Assumption A1/A2 lines 348-349 — size-limit excludes async chunks from an entry's initial size; that exclusion IS the win)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (.size-limit.json assignment lines 205-207)
  </read_first>
  <action>
    Add ONE report-only overlay deep-import entry to `.size-limit.json` mirroring the existing entry shape (`ignore:["lit"]`, a comfortable `limit` so nothing red-builds this phase) pointing at a built overlay entry, e.g. `dist/components/popover/index.js`, named `"popover (overlay deep import)"`. This makes the SIZE-01 "positioning absent from non-overlay entries" delta directly measurable. Do NOT add `manualChunks`; do NOT touch the `ignore` lists on other entries; do NOT flip anything to enforcing.
    Rebuild and re-`--write` the committed size baseline so it reflects the popover-path deferral (build-artifact drift is expected — floating-ui moves to a `chunks/*` async chunk). Confirm the floating-ui bytes drop out of the measured initial size of the overlay/full entries (empirically confirms Assumption A1).
  </action>
  <verify>
    <automated>npm run build && node scripts/size-baseline.mjs --write && node scripts/size-baseline.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `.size-limit.json` gains exactly one new report-only overlay entry; all existing entries are byte-unchanged except intended baseline drift.
    - `node scripts/size-baseline.mjs --check` exits 0 and the report shows the `full bundle` (and new overlay entry) initial brotli size DROPPED versus the pre-deferral committed baseline — the floating-ui deferral win is a real number (report-only).
    - `node scripts/assert-no-bundled-lit.mjs` prints zero inlined-Lit markers (Lit still external after the chunk-graph shift).
  </acceptance_criteria>
  <done>The overlay size entry exists; the re-written baseline records a measurable brotli reduction on the overlay/full entries; no-bundled-Lit is clean; everything stays report-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → deferred chunk fetch | The consumer's bundler resolves the `import()` specifier and fetches `chunks/floating-*.js` at overlay-open time over the network. |
| build → shipped artifact | `dist/**` chunk graph changes; integrity of what ships (Lit externalized, registration intact) is a supply-chain concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering / DoS | lazy-load.ts dynamic import() specifier | medium | mitigate | Bare/relative static specifiers only inside `import()`; deps stay `external` so the consumer bundler resolves them — never a computed/origin-qualified path (no eval/dynamic-code). |
| T-08-02 | Denial of Service | overlay open on flaky network (chunk fetch fails/slow) | low | mitigate | hidden-until-positioned reveal (D-02) means a slow/failed fetch shows no broken 0,0 overlay; the panel simply stays unrevealed until position resolves. |
| T-08-03 | Tampering (integrity) | Lit bundled/duplicated or registration shaken away | high | mitigate | no-bundled-Lit assertion (Task 3) + registration-smoke spec (Task 2) + frozen vite `external` snapshot (untouched) — block on this (ASVS L1 threshold = high). |

No package-manager installs occur in this phase (no npm/pip/cargo add) — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `npm run build && npx vitest run --project browser` — popover positioning + no-0,0 + registration-smoke green.
- `node scripts/size-baseline.mjs --check` — overlay/full initial brotli size dropped (report-only).
- `node scripts/assert-no-bundled-lit.mjs` — zero inlined-Lit markers.
- `npm run diff:surface` — CEM unchanged.
</verification>

<success_criteria>
- The shared memoized loader exists; the controller defers floating-ui end-to-end; popover positions identically in Chromium with no 0,0 frame.
- The SIZE-03 registration canary and Pitfall F1 no-0,0 guard are in the browser lane.
- The re-scoped size metric records a real brotli reduction (report-only); Lit stays external; CEM unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-01-SUMMARY.md` when done.
</output>
