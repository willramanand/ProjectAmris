---
phase: 08-bundle-size-deferral
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/tooltip/tooltip.ts
  - src/components/color-picker/color-picker.ts
  - test/browser/overlay-no-zero-frame-tooltip.test.ts
  - test/browser/overlay-no-zero-frame-color-picker.test.ts
  - test/components/tooltip.test.ts
  - test/components/color-picker.test.ts
autonomous: true
requirements: [SIZE-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "tooltip builds its arrow middleware from the loaded floating-ui module and positions identically; no static floating-ui runtime import remains."
    - "color-picker keeps its EXACT one-shot positioning (single computePosition, NO autoUpdate) — routed through loadFloating() directly, not the controller — so no reposition loop is introduced (Pitfall CP1)."
    - "Both overlays warm the chunk via prefetchFloating() on trigger intent and never paint at 0,0 before first position."
  artifacts:
    - "tooltip and color-picker with zero static @floating-ui/dom runtime imports"
  key_links:
    - "tooltip getter receives `mod` and calls `mod.arrow(...)` (mirrors popover from 08-01)."
    - "color-picker `_updatePosition` awaits loadFloating() then does ONE `mod.computePosition(...)` with `mod.offset/flip/shift` — no autoUpdate wrapper."
  prohibitions:
    - "MUST NOT migrate color-picker onto FloatingPositionController — the controller always runs autoUpdate; that would add a continuous reposition loop = observable behavior change (Pitfall CP1)."
    - "MUST NOT change any component's observable behavior, public API, or CEM surface (surface-freeze)."
    - "MUST NOT paint an overlay at 0,0 before it is first positioned."
    - "MUST NOT edit the vite.config.ts external array."
---

<objective>
Expand the proven floating-ui deferral to two more overlays with DISTINCT positioning contracts: tooltip (autoUpdate + arrow, mirrors the popover tracer) and color-picker (one-shot, NO autoUpdate — the research-corrected exception that MUST route through the loader directly, not the controller). Contributes to SIZE-01 "all overlays behavior-preserving; positioning absent from non-overlay entries."

Purpose: color-picker is the phase's most behavior-sensitive migration (Pitfall CP1). Keeping it a distinct task with its own acceptance criteria guards against the D-06 over-read that would add a reposition loop.
Output: tooltip and color-picker with no static floating-ui runtime import, both prefetch-on-intent, positioning byte-for-byte preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bundle-size-deferral/08-CONTEXT.md
@.planning/phases/08-bundle-size-deferral/08-RESEARCH.md
@.planning/phases/08-bundle-size-deferral/08-PATTERNS.md
@src/internal/helpers/lazy-load.ts
@src/components/popover/popover.ts
</context>

## Artifacts this phase produces (touched by this plan)

- Consumes `loadFloating()` / `prefetchFloating()` from `src/internal/helpers/lazy-load.ts` (created in 08-01).
- Consumes the module-receiving middleware-getter shape on `FloatingPositionController` (from 08-01) for tooltip.
- Creates two NEW per-overlay no-0,0-frame browser specs — `test/browser/overlay-no-zero-frame-tooltip.test.ts` and `test/browser/overlay-no-zero-frame-color-picker.test.ts` — mirroring the popover spec from 08-01 (`overlay-no-zero-frame.test.ts`). Each overlay gets its own file so no two Wave-2 plans write the same test file (parallel-worktree safe).

<tasks>

<task type="auto">
  <name>Task 1: tooltip — arrow middleware from the loaded module + prefetch</name>
  <files>src/components/tooltip/tooltip.ts, test/components/tooltip.test.ts, test/browser/overlay-no-zero-frame-tooltip.test.ts</files>
  <read_first>
    - src/components/tooltip/tooltip.ts (the `arrow` static import at line 3; the existing getter middleware form ~line 61; the trigger `pointerenter`/`focus` open path)
    - src/components/popover/popover.ts (the just-migrated analog from 08-01 — getter receives `mod`, `mod.arrow(...)`, prefetch on intent)
    - src/internal/helpers/lazy-load.ts (loadFloating / prefetchFloating signatures)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (popover/tooltip assignment lines 150-158)
  </read_first>
  <action>
    Remove the static `arrow` import from `tooltip.ts` (keep `type Placement`). Change the controller middleware getter to receive the loaded module and call `mod.arrow({ element: this.arrowEl })` where it currently calls `arrow(...)` — mirroring popover exactly. Leave `onPositioned` arrow-readback unchanged. Hang `prefetchFloating()` off the existing trigger `pointerenter`/`focus` listener (D-03). No other changes. <!-- planner-discipline-allow: @floating-ui/dom -->
    Create a NEW `test/browser/overlay-no-zero-frame-tooltip.test.ts` (mirroring 08-01's popover `overlay-no-zero-frame.test.ts`) with a tooltip case asserting the tip is never painted at 0,0 before first position. Do NOT extend 08-01's popover spec — this is its own file so parallel Wave-2 plans never write the same test file.
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/tooltip.test.ts && npx vitest run --project browser test/browser/overlay-no-zero-frame-tooltip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - After build, `tooltip.ts` has no static runtime `@floating-ui/dom` import (grep of the source shows only the deferred loader + `type Placement`).
    - `tooltip.test.ts` passes unchanged (behavior-preserving) and the browser no-0,0 tooltip case passes.
    - `npm run diff:surface` exits 0.
  </acceptance_criteria>
  <done>tooltip sources its arrow middleware from the loaded module, prefetches on intent, positions identically, and never paints at 0,0.</done>
</task>

<task type="auto">
  <name>Task 2: color-picker — one-shot positioning through the loader (NOT the controller)</name>
  <files>src/components/color-picker/color-picker.ts, test/components/color-picker.test.ts, test/browser/overlay-no-zero-frame-color-picker.test.ts</files>
  <read_first>
    - src/components/color-picker/color-picker.ts (the static import at line 4; the ONE-SHOT `_updatePosition` at lines 535-543 with NO autoUpdate; the `updated()` `if (this._open) this._updatePosition()` trigger; the trigger open path)
    - .planning/phases/08-bundle-size-deferral/08-RESEARCH.md (Pitfall CP1 lines 251-255 — the controller always wraps positioning in autoUpdate; color-picker positions one-shot; the "/ shared loader" branch of D-06 is the correct one)
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (color-picker assignment lines 170-187 — route through loadFloating() directly)
    - src/internal/helpers/lazy-load.ts (loadFloating / prefetchFloating)
  </read_first>
  <action>
    Remove the static `@floating-ui/dom` import from `color-picker.ts` (line 4). Rewrite `_updatePosition` to `await loadFloating()` and then perform the SAME single `mod.computePosition(this._trigger, this._panel, { placement:'bottom-start', strategy:'fixed', middleware:[mod.offset(4), mod.flip(), mod.shift({ padding: 8 })] })`, writing `left/top` exactly as today — one shot, NO autoUpdate wrapper, NO FloatingPositionController. Do not change the `updated()` gating or the reveal timing. Hang `prefetchFloating()` off the trigger intent (D-03). <!-- planner-discipline-allow: @floating-ui/dom -->
    Create a NEW `test/browser/overlay-no-zero-frame-color-picker.test.ts` (mirroring 08-01's popover `overlay-no-zero-frame.test.ts`) with a color-picker case (open panel never at 0,0 before first position). Its own file — do NOT extend 08-01's popover spec or the tooltip spec from Task 1.
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/components/color-picker.test.ts && npx vitest run --project browser test/browser/overlay-no-zero-frame-color-picker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - color-picker `_updatePosition` calls `mod.computePosition` exactly once per open (no autoUpdate loop) — verified by reading the migrated method and by `color-picker.test.ts` passing unchanged.
    - After build, `color-picker.ts` has no static runtime `@floating-ui/dom` import.
    - The browser no-0,0 color-picker case passes; `npm run diff:surface` exits 0.
    - color-picker is NOT wired to `FloatingPositionController` (grep confirms no `FloatingPositionController` import in color-picker.ts).
  </acceptance_criteria>
  <reversibility rating="costly">Part of the D-06 inline-migration set (rated costly in CONTEXT). Guard is the surface-diff gate + the unchanged component test + the one-shot-preserving acceptance above.</reversibility>
  <done>color-picker defers floating-ui via the loader while preserving its exact one-shot, no-autoUpdate positioning; no controller involvement; behavior and surface unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer bundler → deferred chunk fetch | tooltip/color-picker trigger the shared floating-ui `import()` at open time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering / DoS | color-picker/tooltip import() specifier | medium | mitigate | Reuse `loadFloating()`'s bare static specifier; no computed module path; deps stay external. |
| T-08-05 | Denial of Service | color-picker one-shot open on flaky network | low | mitigate | Reveal gated on the single computePosition resolving — a failed fetch shows no broken 0,0 panel. |
| T-08-06 | Tampering (integrity) | accidental controller migration adds reposition loop | medium | mitigate | Distinct-task acceptance forbids `FloatingPositionController` in color-picker; component test + surface-diff guard. |

No package-manager installs occur in this phase — no supply-chain install checkpoint required.
</threat_model>

<verification>
- `npm run build` green; `tooltip.test.ts` + `color-picker.test.ts` unchanged and passing.
- `npx vitest run --project browser test/browser/overlay-no-zero-frame-tooltip.test.ts test/browser/overlay-no-zero-frame-color-picker.test.ts` — tooltip + color-picker no-0,0 cases pass (each in its own spec file).
- `npm run diff:surface` — CEM unchanged.
</verification>

<success_criteria>
- tooltip and color-picker carry zero static floating-ui runtime imports and position identically.
- color-picker retains one-shot no-autoUpdate positioning (no controller, no reposition loop).
- No 0,0 frame on either; surface frozen.
</success_criteria>

<output>
Create `.planning/phases/08-bundle-size-deferral/08-02-SUMMARY.md` when done.
</output>
