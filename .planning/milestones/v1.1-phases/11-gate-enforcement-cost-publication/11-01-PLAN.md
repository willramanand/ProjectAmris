---
phase: 11-gate-enforcement-cost-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/size-baseline.mjs
  - .github/workflows/ci.yml
  - test/size-baseline.test.ts
autonomous: true
requirements: [GATE-01, GATE-03]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `node scripts/size-baseline.mjs --enforce` exits 1 when any entry or marginal row has a POSITIVE brotli delta beyond tolerance (a regression), and exits 0 when every row is same-or-shrunk (GATE-01)."
    - "`node scripts/size-baseline.mjs --check` still exits 0 even on drift — the report-only mode is preserved unchanged."
    - "Exit code 2 stays reserved for a usage error only (an unknown mode argument); it is never used to signal a size regression."
    - "The `size` CI job runs `--enforce` and carries `continue-on-error: true` during the soak, so a size regression annotates the job red WITHOUT flipping the CI workflow-run conclusion to failure (GATE-03)."
    - "`release.yml` still publishes only on `workflow_run.conclusion == 'success'`; the soaking size gate never blocks a publish (verified: the release workflow is unchanged this plan)."
  artifacts:
    - scripts/size-baseline.mjs
    - test/size-baseline.test.ts
    - .github/workflows/ci.yml
  key_links:
    - "size-baseline.mjs `--enforce` exit code -> CI `size` job step -> CI workflow-run conclusion -> release.yml `workflow_run` publish gate"
  prohibitions:
    - "A size gate must never fail-OPEN: exit 0 (pass) while a real positive-delta regression exists in the diff result."
    - "A permanent error-suppressor must never remain on the enforcing `size` job — the `continue-on-error` wrapper is soak-temporary and documented for removal; leaving it forever is a fake report-only that masks a real regression."
---

<objective>
Flip the per-entry brotli size baseline guard from report-only to enforcing (GATE-01), wired end-to-end as a soak-staged CI job that annotates red on a regression but cannot block a publish (GATE-03). This is the phase TRACER: the thinnest path through every layer this phase touches — script exit code, CI job, and the release critical path — proving the enforce->soak->CI pattern that GATE-02 (perf counts) reuses.

Purpose: Lock in the banked Phase 8 size wins so a future regression red-builds CI, while the soak wrapper keeps flaky characterization off the release path.
Output: `scripts/size-baseline.mjs` gains an `--enforce` mode; the `size` CI job runs it under a temporary `continue-on-error: true` soak; a unit test pins the exit-code decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gate-enforcement-cost-publication/11-RESEARCH.md
@.planning/phases/11-gate-enforcement-cost-publication/11-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's contribution)

- **New CLI flag:** `--enforce` on `node scripts/size-baseline.mjs` (third mode alongside `--write` / `--check`).
- **New exported pure helper:** `enforceSizeExitCode(result, { tolerance })` in `scripts/size-baseline.mjs` — mirrors `cem-diff.mjs` `releaseGateExitCode`, returns `1` on regression / `0` otherwise, so the exit decision is unit-testable without spawning a process.
- **New threshold constant:** `SIZE_TOLERANCE` (brotli-byte regression margin) in `scripts/size-baseline.mjs`.
- **New test file:** `test/size-baseline.test.ts`.
- **CI change:** `size` job step flips `size-baseline.mjs --check` -> `--enforce`; job gains a soak-temporary `continue-on-error: true`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — pin the size enforce exit-code decision (RED first)</name>
  <files>test/size-baseline.test.ts</files>
  <read_first>
    - scripts/size-baseline.mjs (the module under test — exports `diff`, `measure`, `formatReport`; `diff()` returns `{ rows, hasDrift }` where each row carries `name`, `base`, `current`, `delta`, `status`)
    - test/deep-import-purity.test.ts (analog: imports pure functions from a `.mjs` script into a Vitest spec, asserts behavior, no browser)
    - api/size.baseline.json (the committed metric shape: `{ unit, entries: {name: brotliBytes}, marginal: {name: bytes} }`)
  </read_first>
  <behavior>
    - Test: a synthetic `diff()` result with one row `delta: +200` (positive) -> `enforceSizeExitCode(result, { tolerance: 0 })` returns 1.
    - Test: a row with `delta: -200` (a shrink) -> returns 0.
    - Test: a row with `delta: 0` (unchanged) -> returns 0.
    - Test: a row with `delta: +8` and `tolerance: 16` -> returns 0 (within margin); `delta: +40` and `tolerance: 16` -> returns 1.
    - Test: rows with `delta: null` (added/removed entry) are treated per the ceiling rule — an `added` entry with a real positive current is NOT a regression on its own delta (delta null); assert the documented handling matches the implementation Task 2 lands.
  </behavior>
  <action>Create `test/size-baseline.test.ts` as a Vitest spec in the default jsdom project. Import `{ diff, enforceSizeExitCode }` from `../scripts/size-baseline.mjs` (the helper is added in Task 2 — this import is RED until then). Build synthetic `diff()`-shaped result objects (arrays of rows with `name`, `delta`, `status`) rather than running a real build, so the spec is pure and fast. Assert the regression-ceiling contract: a positive delta beyond tolerance returns exit 1; a zero or negative delta returns 0; a tolerance margin absorbs a small positive delta. Do NOT assert on wall-clock or on `measure()` output. Reference GATE-01.</action>
  <acceptance_criteria>
    - `test/size-baseline.test.ts` exists and imports `enforceSizeExitCode` and `diff` from `../scripts/size-baseline.mjs`.
    - Before Task 2, `npx vitest run test/size-baseline.test.ts` FAILS on the missing `enforceSizeExitCode` export (proves the test bites).
    - The spec contains at least one assertion each for: positive-delta -> 1, negative-delta -> 0, zero-delta -> 0, within-tolerance-positive -> 0.
    - No browser/`@vitest/browser` import; runs under the jsdom project.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/size-baseline.test.ts</automated>
  </verify>
  <done>The spec exists and fails only because `enforceSizeExitCode` is not yet exported (RED).</done>
</task>

<task type="tracer">
  <name>Task 2: TRACER — add `--enforce` to size-baseline.mjs and wire the soak-staged CI size gate end-to-end</name>
  <files>scripts/size-baseline.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/size-baseline.mjs (the file being modified — current CLI at lines 138-166 ends `process.exit(0); // REPORT-ONLY`; `USAGE` at line 136; `diff()` export at line 102; brotli-exact comment at lines 58-59)
    - scripts/cem-diff.mjs (the in-repo ENFORCING analog — pure `releaseGateExitCode` at lines 131-132; thin isMain block prints report, computes code, `process.exit(code)` at lines 165-184; exit 2 = usage only)
    - .github/workflows/ci.yml (the `size` job at lines 115-153; the report-only step "Brotli size baseline (report-only)" at lines 141-142; the report-only-discipline comment at lines 134-137; Node-22 pin at lines 120-124)
    - .github/workflows/release.yml (VERIFY-ONLY — the `workflow_run.conclusion == 'success'` publish gate at line 23; do NOT edit)
    - api/size.baseline.json (committed baseline the enforce mode reads)
  </read_first>
  <action>In `scripts/size-baseline.mjs`: add a `SIZE_TOLERANCE` constant set to `Math.max(16, Math.round(0.005 * base))` semantics — i.e. a per-row margin of the larger of 16 brotli bytes or 0.5% of the baseline value (Pitfall 1: survives a size-limit patch bump on the Node-22-pinned lane). Add an exported pure `enforceSizeExitCode(result, { tolerance })` that returns 1 when any row has `delta != null && delta > (per-row tolerance)`, else 0 — regression-ceiling semantics (a shrink or unchanged row passes; RESEARCH Pattern 2 recommends ceiling for size). Extend the CLI mode guard to accept `--enforce` as a third valid mode; keep `--write` and `--check` behavior byte-identical (check still exits 0 on drift). For `--enforce`: run the existing `measure()` + `diff(load(BASELINE_PATH), current)`, print `formatReport(result)` to stdout, and when `enforceSizeExitCode` returns non-zero print the offending rows (name + `+<delta>B`) to stderr and `process.exit(1)`; otherwise `process.exit(0)`. When the baseline file is absent, keep the existing first-generation write-and-exit-0 behavior in every mode. Update `USAGE` to list `[--write|--check|--enforce]`. Keep exit 2 for the unknown-mode usage error. Do NOT touch `measure()` / `diffMap()` / the `--write` single-writer path — enforce mode READS the committed baseline only, never writes it.

In `.github/workflows/ci.yml` `size` job: replace the "Brotli size baseline (report-only)" step that runs `node scripts/size-baseline.mjs --check` with an enforcing step named "Brotli size regression gate (enforcing, soaking)" that runs `node scripts/size-baseline.mjs --enforce`. Add `continue-on-error: true` at the `size` JOB level (a sibling of `runs-on:`) with an inline comment that it is SOAK-ONLY and removed post-soak size-first. Leave the `Bundle-size budget` (`npm run size`) coarse backstop, the no-bundled-Lit assertion, and the `--why` attribution steps untouched. Do NOT add any bare error-suppressor to the step. Leave `release.yml` unmodified — it is the lever GATE-03 relies on (a `continue-on-error` job that goes red keeps the workflow conclusion `success`, so publish stays reachable during soak).</action>
  <reversibility rating="costly">The `--enforce` flip changes CI behavior on regression; reverting is a git revert of two files, but a red build annotation is visible to consumers in the interim. Adding the `continue-on-error` soak wrapper is itself reversible; REMOVING it later (hard-blocking) is a deferred one-way decision, flagged not done in this plan.</reversibility>
  <acceptance_criteria>
    - `npx vitest run test/size-baseline.test.ts` PASSES (Task 1 goes GREEN).
    - `npm run build` then `node scripts/size-baseline.mjs --enforce` exits 0 against the committed clean `dist/` (no regression on HEAD).
    - Feeding a synthetic baseline whose entries are all lower than the current build (a forced positive delta) makes `--enforce` exit 1 and print the offending row(s) to stderr — demonstrated by a temp-file run, not committed.
    - `node scripts/size-baseline.mjs --check` still exits 0 even when the diff shows drift (report-only preserved).
    - `node scripts/size-baseline.mjs --bogus` exits 2.
    - `grep -q 'size-baseline.mjs --enforce' .github/workflows/ci.yml` succeeds; `grep -q 'continue-on-error: true' .github/workflows/ci.yml` succeeds within the `size` job.
    - `.github/workflows/release.yml` is unchanged (`git diff --exit-code .github/workflows/release.yml` clean).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/size-baseline.test.ts && npm run build && node scripts/size-baseline.mjs --enforce && node scripts/size-baseline.mjs --check && grep -q 'size-baseline.mjs --enforce' .github/workflows/ci.yml</automated>
  </verify>
  <done>`--enforce` exits 1 on a positive-delta regression and 0 on a clean/shrunk build; `--check` stays report-only; the `size` CI job runs `--enforce` under a documented soak `continue-on-error`; `release.yml` is untouched. The single size happy-path works end-to-end and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR/push -> CI runner | A PR contributor can propose edits to `ci.yml` / gate scripts; the runner executes them read-only (`permissions: contents: read`). |
| CI workflow conclusion -> release.yml | `release.yml` publishes only on `workflow_run.conclusion == 'success'`; the size job's blocking-ness is the GATE-03 lever. |
| committed `api/size.baseline.json` -> gate script | The enforce mode consumes the committed baseline; a self-writing gate would be able to approve its own regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | `scripts/size-baseline.mjs --enforce` | high | mitigate | `test/size-baseline.test.ts` asserts exit-1 on positive delta and exit-0 on shrink/zero; single enforcing exit path mirrors `cem-diff.mjs`; no fail-open. |
| T-11-02 | Repudiation | `ci.yml` `size` job soak wrapper | medium | mitigate | `continue-on-error` is soak-temporary and comment-documented for removal; repo convention forbids a permanent error-suppressor (ci.yml:134-137). |
| T-11-03 | Tampering | committed `api/size.baseline.json` | medium | mitigate | enforce mode READS only; baseline changes solely via explicit `--write` (single-writer discipline). |
| T-11-04 | Elevation of Privilege | `release.yml` publish credential | low | accept | gates run under `contents: read`; only `release.yml` carries `packages: write`; unchanged this plan. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — all tooling already in package.json). |
</threat_model>

<flagged_assumptions>
- **GATE-01 edge (unclassified probe row) — brotli determinism / tolerance.** Assumption A5/Pitfall 1: brotli byte counts are exact over an unchanged `dist/` on the pinned Node-22 + package-locked size-limit toolchain. The gate uses a small tolerance (`max(16 B, 0.5%)` per row) so a size-limit PATCH bump does not red-build a zero-code PR; a Node-MAJOR bump requires an explicit baseline re-commit. If zero-tolerance exact-match is preferred, it is a one-flag change.
- **GATE-03 edge (unclassified probe row) — Assumption A1 (empirical).** A job-level `continue-on-error: true` FAILURE keeps `workflow_run.conclusion == 'success'`, so `release.yml` still fires during soak. This is the one behavior that must be confirmed empirically with one throwaway CI run before the soak is trusted. If GitHub treats a `continue-on-error` job failure as a workflow failure, fall back to a separate non-gating workflow (RESEARCH Open Question 3).
- **Deferred one-way decision.** Removing `continue-on-error` from the `size` job (making the size gate hard-blocking) is a post-soak decision — size is deterministic so its soak is short, but the removal is not performed in this plan. Rated one-way; surfaced, not executed.
</flagged_assumptions>

<verification>
- `npx vitest run test/size-baseline.test.ts` green.
- `npm run build && node scripts/size-baseline.mjs --enforce` exits 0 on HEAD; a synthetic regression forces exit 1.
- `node scripts/size-baseline.mjs --check` exits 0 (report-only intact); `--bogus` exits 2.
- `ci.yml` `size` job runs `--enforce` under a soak `continue-on-error`; `release.yml` unchanged.
</verification>

<success_criteria>
GATE-01 satisfied: a per-entry brotli size regression now returns a non-zero exit from `--enforce` and red-annotates the `size` CI job. GATE-03 (size portion) satisfied: the enforcing job is soak-staged so it cannot block a publish. The enforce->soak->CI pattern is proven end-to-end for reuse by GATE-02.
</success_criteria>

<output>
Create `.planning/phases/11-gate-enforcement-cost-publication/11-01-SUMMARY.md` when done.
</output>
