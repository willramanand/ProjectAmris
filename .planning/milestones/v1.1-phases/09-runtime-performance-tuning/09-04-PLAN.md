---
phase: 09-runtime-performance-tuning
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - api/perf.baseline.json
  - api/perf.baseline.untuned.json
autonomous: true
requirements: [RPERF-01, RPERF-02, RPERF-03, RPERF-04]
estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The committed api/perf.baseline.json is regenerated on the FULLY-TUNED code and now contains all three new count keys (sortComputes, filterCalls, middlewareBuilds) plus the unchanged update/updated/render/nodes(+computePosition/repositions) keys — the trustworthy forward reference Phase 11 sets enforcing thresholds against. (RPERF-01/02/03)"
    - "A committed api/perf.baseline.untuned.json (perf-diff-compatible, same shape) preserves the untuned 'before' values of the three new count keys so `node scripts/perf-diff.mjs api/perf.baseline.untuned.json api/perf.baseline.json` emits a DURABLE, re-runnable machine diff of the count improvement — the before/after proof that outlives the ephemeral per-plan SUMMARYs and that Phase 11 needs to flip these budgets from report-only to enforcing (RESEARCH Methodology step 2 + Open Question 3; Pitfall 1). (RPERF-01/02/03)"
    - "That machine diff shows the three new keys dropping untuned to tuned (sortComputes N to 1 per render-seq; filterCalls 4-5 to 1 per keystroke; middlewareBuilds per-tick to per-config) and NO other count metric changed for any scenario — self-proving the untuned baseline differs from the tuned baseline ONLY in the three tuned keys (the lifecycle/node/computePosition/reposition invariant). (RPERF-01/02/03)"
    - "The capstone SUMMARY records that untuned to tuned delta table (all three targets) plus report-only wall-clock (median + mean+3sigma band) per scenario — a single committed phase-level record, not scattered across the three per-plan SUMMARYs. (RPERF-01/02/03)"
    - "Lit lifecycle (update/updated/render) and DOM node counts are IDENTICAL to the pre-Phase-9 baseline for every scenario — proof the three memos changed no observable render structure. (RPERF-01/02/03)"
    - "All three a11y snapshot specs (data-grid, combobox, popover) are green and unchanged — the tuning stripped no aria-*/role/focusability. (RPERF-04)"
    - "The full suite is green: test:run (jsdom+browser+perf), test:browser (regression gate incl. no-0,0 frame + CR-01 retry), and no size regression (size) — no bytes moved this phase. (all)"
    - "The frozen v1.0 CEM public surface and the byte-snapshot-guarded vite external array / .size-limit.json are untouched. (all)"
  artifacts:
    - "api/perf.baseline.json — regenerated to the final tuned state with the three new count keys (the go-forward baseline Phase 11 enforces against)"
    - "api/perf.baseline.untuned.json — NEW committed durable 'before' snapshot: the tuned baseline structure with ONLY the three new count keys overridden to their untuned per-plan-recorded values; perf-diff-compatible so the count improvement is a re-runnable machine diff"
  key_links:
    - "test:perf -> api/perf.json (all-tuned) -> perf-diff.mjs --write -> api/perf.baseline.json (committed, tuned)"
    - "per-plan SUMMARY untuned counts (09-01/02/03 RED) overlaid on the tuned baseline structure -> api/perf.baseline.untuned.json (committed)"
    - "perf-diff.mjs api/perf.baseline.untuned.json api/perf.baseline.json -> durable machine count-drop diff -> capstone SUMMARY delta table + Phase 11 reference"
  prohibitions:
    - "Do NOT re-run any tuning — this is verification + baseline finalization only."
    - "Do NOT touch the frozen vite external array / .size-limit.json / CEM baseline."
    - "Do NOT flip any budget from report-only to enforcing — that is Phase 11 (GATE-*)."
    - "api/perf.baseline.untuned.json must differ from api/perf.baseline.json ONLY in sortComputes/filterCalls/middlewareBuilds — copy every lifecycle/node/computePosition/reposition/wall-clock value verbatim from the tuned baseline; never hand-edit them."
---

<objective>
Verification capstone (Wave 3) — the single owner of the committed perf baselines
(the tuned api/perf.baseline.json AND the durable untuned api/perf.baseline.untuned.json).
Runs only after all three tunings (Plans 01-03) land. Because api/perf.json is
gitignored and each tuning plan demonstrated its own untuned-to-tuned drop only
ephemerally in its per-plan SUMMARY, this plan (a) finalizes the trustworthy forward
baseline on the fully-tuned code, (b) mints a durable untuned "before" baseline so the
count improvement is a re-runnable machine diff, and proves the phase-level acceptance
bar in one place: three count improvements demonstrated + durably captured,
lifecycle/node counts and a11y DOM byte-identical, full suite green, and no size regression.

Baseline decision (planner discretion, per CONTEXT.md's "Claude's Discretion" baseline
clause): TWO committed baselines are produced. (1) api/perf.baseline.json is regenerated
to the FINAL TUNED state — the forward reference Phase 11 gates against. (2)
api/perf.baseline.untuned.json preserves the untuned "before" counts for the three new
keys so the count improvement survives as a DURABLE, re-runnable machine diff
(`perf-diff.mjs untuned tuned`) rather than only as prose in the three ephemeral
per-plan SUMMARYs — the before/after proof Phase 11 needs (RESEARCH Methodology step 2 +
Open Question 3; Pitfall 1: a tuned-only baseline reports "No count drift" by
construction and leaves no machine evidence of the win). The untuned baseline is
assembled from the tuned baseline structure with ONLY the three count keys overridden to
the untuned values each tuning plan recorded during its RED phase — valid because the
lifecycle/node/computePosition/reposition counts are a byte-identical phase invariant, so
nothing else legitimately differs, and the assembly is self-verified by the diff.

Purpose: lock in a trustworthy post-Phase-9 measurement artifact, preserve a durable
machine before/after, and gate the whole phase. Output: regenerated
api/perf.baseline.json + NEW committed api/perf.baseline.untuned.json + a capstone
SUMMARY delta table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-runtime-performance-tuning/09-CONTEXT.md
@.planning/phases/09-runtime-performance-tuning/09-RESEARCH.md
@scripts/perf-diff.mjs
@api/perf.baseline.json
@test/perf/harness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: regenerate the tuned perf baseline + mint the durable untuned "before" baseline + aggregate the phase perf delta</name>
  <files>api/perf.baseline.json, api/perf.baseline.untuned.json</files>
  <read_first>
    - scripts/perf-diff.mjs:24-33 (usage/modes — note `<baseline> <current>` diffs ANY two baseline-shaped files, report-only exit 0), 54-67 (reduceToBaseline), 184-203 (--write mode + defaults), 205-223 (diff mode).
    - api/perf.baseline.json (current committed shape — per-scenario counts + wallClock{median,band} + throttle.profile + repeats; the untuned baseline is a copy of this shape with three counts overridden).
    - 09-RESEARCH.md "Perf Re-measurement Methodology" (steps 1-5 — capture the untuned "before" FIRST) + Open Question 3 (re-capture untuned, then diff) + Pitfall 1/F-1 (a tuned-only baseline shows "No count drift" — the untuned baseline is the durable machine "before") + "Runtime State Inventory".
    - The three per-plan SUMMARYs (09-01/02/03-SUMMARY.md) for the recorded untuned "before" counts (sortComputes, filterCalls, middlewareBuilds).
  </read_first>
  <action>
    On the merged, fully-tuned working tree run `npm run test:perf` to produce a fresh api/perf.json containing all four scenarios with the new count keys at their TUNED values (data-grid.sortComputes, combobox.filterCalls, overlay.middlewareBuilds) plus the unchanged update/updated/render/nodes (and computePosition/repositions for overlay) keys. Then regenerate the committed tuned baseline with `node scripts/perf-diff.mjs --write api/perf.json api/perf.baseline.json` and confirm it now contains the three new keys. Running `npm run perf:diff` against this just-written tuned baseline will report no drift (expected, report-only exit 0) — which is exactly why a separate durable "before" is needed.
    Next, mint the durable untuned baseline: copy the freshly-written api/perf.baseline.json to api/perf.baseline.untuned.json, then override ONLY three values with the untuned "before" counts recorded in the 09-01/02/03 SUMMARYs — set data-grid.counts.sortComputes, combobox.counts.filterCalls, and overlay.counts.middlewareBuilds to their pre-memo values. Change nothing else: every lifecycle/node/computePosition/reposition/wall-clock/throttle/repeats value stays byte-identical to the tuned baseline (they are a proven phase invariant, so nothing else may differ). Self-verify by running `node scripts/perf-diff.mjs api/perf.baseline.untuned.json api/perf.baseline.json`: it MUST report drift on exactly the three new count keys (each dropping untuned to tuned) and on NO other count metric in any scenario — that diff is the durable, re-runnable machine proof of the count improvement Phase 11 will reference. Commit both baseline files.
    In the capstone SUMMARY, record the untuned-to-tuned delta table for all three targets (taken from that machine diff) plus the report-only wall-clock (median + mean+3sigma band) per scenario, and explicitly confirm update/updated/render + nodes (and computePosition/repositions for overlay) are IDENTICAL across the two baselines, with the button control scenario staying flat as the noise control.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:perf && node scripts/perf-diff.mjs --write api/perf.json api/perf.baseline.json && npm run perf:diff</automated>
    <automated>cd C:/repos/ProjectAmris && grep -q sortComputes api/perf.baseline.json && grep -q filterCalls api/perf.baseline.json && grep -q middlewareBuilds api/perf.baseline.json && echo "tuned baseline OK: three new count keys present"</automated>
    <automated>cd C:/repos/ProjectAmris && test -f api/perf.baseline.untuned.json && grep -q sortComputes api/perf.baseline.untuned.json && grep -q filterCalls api/perf.baseline.untuned.json && grep -q middlewareBuilds api/perf.baseline.untuned.json && echo "untuned baseline OK: three new count keys present"</automated>
    <automated>cd C:/repos/ProjectAmris && node scripts/perf-diff.mjs api/perf.baseline.untuned.json api/perf.baseline.json | grep -cE '^ +~ (sortComputes|filterCalls|middlewareBuilds):' | grep -qx 3 && echo "untuned-vs-tuned diff shows exactly the three count-key drops"</automated>
    <automated>cd C:/repos/ProjectAmris && ! node scripts/perf-diff.mjs api/perf.baseline.untuned.json api/perf.baseline.json | grep -qE '^ +~ (update|updated|render|nodes|computePosition|repositions):' && echo "no lifecycle/node/position metric drifted between the two baselines"</automated>
  </verify>
  <acceptance_criteria>
    - api/perf.baseline.json contains sortComputes (data-grid), filterCalls (combobox), and middlewareBuilds (overlay) at their tuned values (grep gate passes).
    - api/perf.baseline.untuned.json exists, is committed, and contains the same three keys at their untuned "before" values (grep gate passes).
    - `node scripts/perf-diff.mjs api/perf.baseline.untuned.json api/perf.baseline.json` shows exactly the three count keys dropping untuned to tuned and NO other count metric changed in any scenario (the durable machine before/after; self-verify gates pass).
    - `npm run perf:diff` exits 0 with "No count drift" against the newly written tuned baseline (expected — the untuned baseline, not this diff, is the durable "before").
    - The capstone SUMMARY tabulates untuned-to-tuned deltas for all three targets + report-only wall-clock per scenario, as a single committed phase-level record.
    - update/updated/render + nodes (and computePosition/repositions for overlay) are byte-identical across the two baselines and equal the pre-Phase-9 baseline values; button scenario flat.
  </acceptance_criteria>
  <reversibility rating="reversible">Baselines are measurement artifacts; revert = restore the prior api/perf.baseline.json and delete api/perf.baseline.untuned.json.</reversibility>
  <done>The committed tuned baseline reflects the final tuned state with all three new count keys; a committed api/perf.baseline.untuned.json preserves the untuned "before" so the count improvement is a durable, re-runnable machine diff; the capstone SUMMARY tabulates the demonstrated deltas; lifecycle/node counts byte-identical across both baselines.</done>
</task>

<task type="auto">
  <name>Task 2: full-suite regression gate + no-size-regression + frozen-surface confirmation</name>
  <files>api/perf.baseline.json</files>
  <read_first>
    - package.json:71-84 (scripts: test:run, test:browser, test:perf, size, perf:diff, assert:no-lit).
    - .planning/phases/08-bundle-size-deferral/08-PATTERNS.md (the frozen vite external array is byte-snapshot-guarded; do not touch .size-limit.json).
    - 09-CONTEXT.md code_context ("no .size-limit.json / vite.config.ts external changes; the frozen array is byte-snapshot-guarded").
  </read_first>
  <action>
    Run the full regression gate on the merged tree: `npm run test:run` (jsdom + browser + perf all green), then `npm run test:browser` explicitly (the shared-controller blast-radius gate — floating-position, overlay-focus, data-grid-virtual, combobox-virtual, the Phase-8 no-0,0-frame specs, the CR-01 cold-load retry spec, and the three new a11y snapshot specs all green). Confirm no size regression with `npm run size` (no bytes move this phase) and that the no-bundled-Lit assertion still holds via `npm run assert:no-lit`. Do a source check that vite.config.ts `external` array and .size-limit.json are unmodified this phase (git diff shows no change to them). Record the gate results in the SUMMARY. Do NOT flip any budget to enforcing (Phase 11).
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npm run test:run</automated>
    <automated>cd C:/repos/ProjectAmris && npm run test:browser</automated>
    <automated>cd C:/repos/ProjectAmris && npm run size && npm run assert:no-lit</automated>
    <automated>cd C:/repos/ProjectAmris && git diff --quiet -- .size-limit.json vite.config.ts && echo "frozen size/external surface untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` green (all three lanes).
    - `npm run test:browser` green — the regression gate incl. no-0,0 frame, CR-01 retry, and the three a11y snapshot specs.
    - `npm run size` shows no regression; `npm run assert:no-lit` passes.
    - `git diff --quiet -- .size-limit.json vite.config.ts` passes (frozen surface untouched).
  </acceptance_criteria>
  <reversibility rating="reversible">Verification only — no source change beyond the baseline artifact from Task 1.</reversibility>
  <done>The full suite is green, no size regression, the frozen external/size surface and CEM are untouched, and no budget was flipped to enforcing.</done>
</task>

</tasks>

<flagged_assumptions>
No new unresolved edges are introduced by this verification-only plan. All 11
specless edge-probe rows are dispositioned in Plans 01-03 (authored as
must_haves.truths or flagged N/A); this capstone confirms the aggregate result.
</flagged_assumptions>

<artifacts_this_phase_produces>
This plan introduces NO new source symbols. It produces two committed measurement
artifacts:
- api/perf.baseline.json — regenerated to the final tuned state, now containing
  sortComputes / filterCalls / middlewareBuilds (created in Plans 01-03).
- api/perf.baseline.untuned.json — NEW durable "before" baseline: the tuned baseline
  structure with only those three count keys overridden to their untuned values (from
  the 09-01/02/03 SUMMARYs), so the count improvement is a re-runnable machine diff for
  Phase 11.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + baseline-artifact regeneration only; no source behavior change, no input path, no network, no new deps. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Tampering | committed perf baselines (tuned + untuned) | low | accept | Both are report-only measurement artifacts under api/ (outside package.files — never ship); the tuned baseline is regenerated only via the explicit `--write` single-writer path, and the untuned baseline is written once by this sole-owner capstone (no cross-plan writer) and self-verified against the tuned baseline (perf-diff.mjs T-07-07). |

No packages installed/upgraded — Package Legitimacy Gate N/A. No high-severity threats — nothing to block on.
</threat_model>

<verification>
- `npm run test:perf` green; committed tuned baseline regenerated with the three new count keys (grep gate).
- api/perf.baseline.untuned.json committed with the three keys at untuned values; `perf-diff.mjs untuned tuned` shows exactly those three dropping and no other count-metric drift (durable machine before/after).
- `npm run perf:diff` exits 0 ("No count drift" vs the tuned baseline — expected).
- `npm run test:run` + `npm run test:browser` green (full regression + a11y snapshots + CR-01 retry).
- `npm run size` + `npm run assert:no-lit` pass; `.size-limit.json`/`vite.config.ts` untouched.
- Capstone SUMMARY tabulates untuned-to-tuned deltas for sortComputes/filterCalls/middlewareBuilds + report-only wall-clock; confirms lifecycle/node counts byte-identical across both baselines.
</verification>

<success_criteria>
The committed post-Phase-9 perf baseline is trustworthy and forward-useful (contains
the new count keys at tuned values), and a committed untuned baseline preserves the
"before" so the count improvement is a durable, re-runnable machine diff (not just
prose in the per-plan SUMMARYs); all three count improvements are demonstrated and
aggregated in the capstone SUMMARY; lifecycle/node counts and the a11y DOM are
byte-identical across the phase; the full suite is green with no size regression and
the frozen surface intact. RPERF-01/02/03/04 verified at the phase level; nothing
flipped to enforcing (Phase 11).
</success_criteria>

<output>
Create `.planning/phases/09-runtime-performance-tuning/09-04-SUMMARY.md` when done.
</output>
