---
phase: 07-measurement-baselines-budgets
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-03]
files_modified:
  - scripts/perf-diff.mjs
  - test/perf/harness.ts
  - api/perf.baseline.json
autonomous: true
requirements: [MEAS-03]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A named low-end target profile (single CPU-throttle multiplier + single network tier) chosen from the measured candidate-grid data is pinned in harness config (THROTTLE_PROFILE), reflecting the worst-case-cellular intent (D-01, MEAS-03); the candidate grid (4x/6x CPU x Slow/Fast-3G) and the data-derived rationale for the pick are documented."
    - "scripts/perf-diff.mjs (zero-dependency clone of cem-diff.mjs) diffs the committed api/perf.baseline.json against a fresh api/perf.json keyed by scenario+metric, sorts every compared array, treats wall-clock samples as volatile/report-only, and exits 0 unconditionally this phase (report-only, D-08)."
    - "A first-generation api/perf.baseline.json is committed recording, per scenario, the count metrics plus wall-clock median + mean+3σ band (D-07) — the variance band Phase 11 sets enforcing thresholds outside of."
    - "perf-diff is deterministic and idempotent: a zero-delta run (baseline == current counts) reports no drift and exits 0, and diff rows are key-sorted so re-runs do not spuriously differ (MEAS-05 ordering / MEAS-02 boundary edges)."
    - "First-generation / empty-baseline behavior is defined: when api/perf.baseline.json is absent, perf-diff (or the harness write path) creates it and reports 'new baseline' rather than erroring (MEAS-05 empty edge)."
    - { statement: "The committed baseline is single-writer: a run reads the committed baseline and writes only its own api/perf.json report artifact, so parallel CI runs cannot corrupt the committed baseline (MEAS-02/03 concurrency edge).", verification: backstop }
  prohibitions:
    - "MUST NOT gate on wall-clock — counts are the gated metric, wall-clock median + band is report-only (Pitfall 15, D-06)."
    - "MUST NOT flip perf-diff to enforcing this phase — exit 0 unconditionally (D-08); the enforcing flip is Phase 11."
    - "MUST NOT pin the profile by guess — the single named profile is chosen from the measured candidate grid (MEAS-03)."
    - "MUST NOT throttle on WebKit/Firefox — CDP throttling is Chromium-only (D-11)."
  artifacts:
    - scripts/perf-diff.mjs
    - api/perf.baseline.json
    - test/perf/harness.ts (pinned THROTTLE_PROFILE)
  key_links:
    - "api/perf.json (Plan 03) → perf-diff input diffed against committed api/perf.baseline.json."
    - "cem-diff.mjs report-only spine (Plan 01 proved the inversion) → cloned for perf-diff.mjs."
    - "Measured candidate-grid data → the data-derived THROTTLE_PROFILE pin (MEAS-03)."
---

<objective>
Close the loop on the perf lane: clone cem-diff into a report-only perf-diff, pin the low-end target profile from measured data (MEAS-03), and commit the first-generation perf baseline.

Purpose: MEAS-03 requires the profile be chosen from real data, not guessed — this plan runs the candidate grid, picks the single named profile that best separates heavy from light components, and freezes it. The committed baseline + report-only diff mirror the proven size-baseline/cem-diff pattern so Phase 11 can flip it to enforcing.
Output: `scripts/perf-diff.mjs`, a data-derived pinned `THROTTLE_PROFILE`, and a committed `api/perf.baseline.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
@scripts/cem-diff.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/perf-diff.mjs (zero-dep, report-only clone of cem-diff)</name>
  <files>scripts/perf-diff.mjs</files>
  <read_first>
    - scripts/cem-diff.mjs (whole file — load(), key-by-stable-id normalization, sorted arrays, diffNameArrays/formatReport, isMain guard; INVERT the enforcing exit to report-only exit 0 per D-08)
    - scripts/size-baseline.mjs (Plan 01 — the report-only inversion already applied; mirror its --check/--write mode split)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"scripts/perf-diff.mjs" (exact clone target)
    - api/perf.json (Plan 03 — the current-metrics shape to diff)
  </read_first>
  <action>
    Create scripts/perf-diff.mjs as a zero-dependency clone of cem-diff.mjs. Key each row by a stable identifier (scenario name + metric name — never array position), sort every compared array, and strip/segregate volatile fields: count metrics are the deterministic gated numbers that appear in the drift diff; wall-clock median/mean/sd/band and raw samples are volatile and reported-only (never trigger drift this phase). Load the committed api/perf.baseline.json and a fresh api/perf.json, produce a structured { added, removed, changed, hasDrift } result, print a formatReport-style summary, and exit 0 unconditionally (report-only, D-08) — keep the isMain guard and usage error exit 2, and a --check mode. When api/perf.baseline.json is absent, report a new-baseline message and exit 0 (empty edge). The `perf:diff` npm script already exists as a Plan 00 stub pointing at this file — do NOT edit package.json (Plan 00 owns it).
  </action>
  <verify>
    <automated>node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json; test $? -eq 0 && node -e "const s=require('fs').readFileSync('scripts/perf-diff.mjs','utf8'); if(!/process\.exit\(0\)/.test(s)) throw new Error('not report-only exit 0');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json` exits 0 (report-only).
    - Rows are keyed by scenario+metric and every compared array is sorted (deterministic).
    - Count metrics appear in drift; wall-clock median/band/samples are report-only and never cause drift.
    - Absent baseline reports a new-baseline message and exits 0; usage error exits 2.
  </acceptance_criteria>
  <precondition>api/perf.json exists (Plan 03 emitted it via a test:perf run).</precondition>
  <reversibility rating="reversible">New standalone script.</reversibility>
  <done>A report-only perf-diff mirrors the cem-diff spine: counts gate-visible, wall-clock report-only, exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the low-end target profile from measured candidate-grid data (MEAS-03)</name>
  <files>test/perf/harness.ts</files>
  <read_first>
    - test/perf/harness.ts (Plan 03 — placeholder THROTTLE_PROFILE, NETWORK_TIERS, applyThrottle)
    - .planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md D-01 (candidate grid 4x/6x CPU x Slow/Fast-3G, centered on 6x+Slow-3G; pin ONE named profile that best separates heavy from light)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Pattern 1" and Assumptions A1
    - .planning/phases/07-measurement-baselines-budgets/07-03-SUMMARY.md (Plan 03 candidate-grid observations)
  </read_first>
  <action>
    Run the candidate grid — CPU-throttle {4x, 6x} x network {Slow-3G, Fast-3G} — against the four scenarios (at least the heavy data-grid/combobox/overlay vs the light button control) and pick the single profile whose numbers best separate heavy from light components (the profile where the heavy/light contrast is clearest and stable). Pin that single named profile in harness.ts THROTTLE_PROFILE as one CPU multiplier + one network tier, keeping the worst-case-cellular intent (D-01). Document, in a harness.ts comment, the candidate grid measured, the chosen profile, and the data-derived rationale for the pick (which separation drove it). The numeric pick is data-derived per MEAS-03; the intent baked in is worst-case cellular. Profile is reversible (re-pin from a re-run).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('test/perf/harness.ts','utf8'); if(!/cpuRate\s*:\s*[46]/.test(s)) throw new Error('cpuRate not pinned to a grid value'); if(!/(Slow-3G|Fast-3G)/.test(s)) throw new Error('network tier not pinned'); if(!/candidate grid|separates|rationale/i.test(s)) throw new Error('pin rationale not documented');"</automated>
  </verify>
  <acceptance_criteria>
    - THROTTLE_PROFILE pins exactly one CPU multiplier (a grid value) and one network tier.
    - A harness.ts comment documents the candidate grid, the chosen profile, and the data-derived rationale.
    - The pinned profile reflects the worst-case-cellular intent (D-01).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <precondition>Plan 03's harness + scenarios run under throttle; candidate-grid observations available.</precondition>
  <reversibility rating="reversible">Config value; re-pin from re-run data (D-01 explicitly reversible).</reversibility>
  <done>A single named low-end profile chosen from measured data is pinned in harness config — MEAS-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the first-generation api/perf.baseline.json</name>
  <files>api/perf.baseline.json</files>
  <read_first>
    - api/perf.json (fresh metrics under the now-pinned profile)
    - api/custom-elements.baseline.json (committed-baseline convention)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"api/perf.baseline.json"
    - .planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md D-07 (median + mean+3σ band)
  </read_first>
  <action>
    Regenerate api/perf.json with a clean throttled run under the pinned THROTTLE_PROFILE, then copy it to a committed first-generation api/perf.baseline.json recording per scenario the count metrics plus wall-clock median + mean+3σ band (D-07). Confirm perf-diff reports zero drift when diffing the committed baseline against a fresh same-profile run (counts stable). The baseline is the single committed source of truth; each future run reads it and writes only its own api/perf.json report artifact (single-writer — parallel CI runs cannot corrupt the committed baseline). api/ is outside package.files so nothing ships.
  </action>
  <verify>
    <automated>npm run build && npm run test:perf && node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json && node -e "const b=require('./api/perf.baseline.json'); const j=JSON.stringify(b); for(const s of ['data-grid','combobox','overlay','button']){ if(j.indexOf(s)<0) throw new Error('baseline missing '+s); } if(j.indexOf('band')<0) throw new Error('no mean+3sigma band');"</automated>
  </verify>
  <acceptance_criteria>
    - api/perf.baseline.json is committed with all four scenarios' counts + wall-clock median + mean+3σ band.
    - perf-diff against a fresh same-profile run reports zero count drift and exits 0.
    - api/ is not in package.files.
  </acceptance_criteria>
  <precondition>THROTTLE_PROFILE pinned (Task 2); test:perf emits api/perf.json.</precondition>
  <reversibility rating="reversible">Committed baseline data; regenerable.</reversibility>
  <done>A committed first-generation perf baseline (counts + variance band) exists and diffs clean — the input Phase 11 flips to enforcing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed api/perf.baseline.json → CI perf-diff | Trusted in-repo baseline vs fresh per-run artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | Parallel CI runs corrupting the committed baseline | low | mitigate | Single-writer discipline: runs read the committed baseline, write only their own api/perf.json; the baseline changes only via an explicit committed regeneration. |
| T-07-08 | Repudiation | A guessed profile making later gates meaningless | medium | mitigate | Profile pinned from the measured candidate grid with documented rationale (MEAS-03), not guessed. |
| T-07-02 | Tampering | perf-diff reads only trusted in-repo JSON | low | accept | No external input (RESEARCH Security Domain V5). |
</threat_model>

<verification>
- `node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json` exits 0 (report-only, zero count drift on a same-profile re-run).
- THROTTLE_PROFILE pins one grid CPU multiplier + one tier with documented data-derived rationale.
- api/perf.baseline.json committed with counts + mean+3σ band per scenario.
</verification>

<success_criteria>
MEAS-03 is closed: a data-chosen named low-end profile is pinned, and the perf lane has a committed baseline + report-only diff mirroring the size lane — ready for Phase 11's enforcing flip.
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-04-SUMMARY.md` when done. Record the pinned profile (CPU multiplier + tier), the candidate-grid numbers behind the pick, and the committed count baselines + variance bands.
</output>
