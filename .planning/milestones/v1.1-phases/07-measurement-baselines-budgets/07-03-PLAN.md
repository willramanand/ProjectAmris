---
phase: 07-measurement-baselines-budgets
plan: 03
type: execute
wave: 2
depends_on: [07-00]
files_modified:
  - test/perf/harness.ts
  - test/perf/data-grid.perf.test.ts
  - test/perf/combobox.perf.test.ts
  - test/perf/overlay.perf.test.ts
  - test/perf/button.perf.test.ts
  - vitest.config.ts
autonomous: true
requirements: [MEAS-02]

estimate:
  tokens: 68000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The perf harness runs on the Chromium-only Vitest Browser + Playwright perf project under CDP CPU + network throttling and emits, per scenario, count metrics (Lit render/update/updated call counts, FloatingPositionController._updatePosition invocations, DOM node counts) plus wall-clock timings to api/perf.json via the Node writeMetrics command (MEAS-02)."
    - "The four D-06 scenarios exist: data-grid (render + sort), combobox (filter-per-keystroke), overlay = am-popover (open + reposition), and button (light control / noise floor)."
    - "Count instrumentation wraps only first-party prototypes — the component's own update/updated/render hooks and FloatingPositionController.prototype._updatePosition — plus a MutationObserver on the overlay panel style attribute as a zero-instrumentation reposition cross-check; no @floating-ui/dom or Lit export is patched."
    - "Each scenario runs 5 repeats and the harness computes median (the reported number) and a mean+3σ variance band (D-07); count metrics are asserted stable (identical) across all 5 repeats (determinism), while wall-clock is the noisy band-protected value."
    - "The throttle is proven live: throttled wall-clock for a scenario is measurably greater than an unthrottled control run (not a silent no-op)."
    - "am-popover is the representative overlay because it routes through FloatingPositionController (the count wrap works) — color-picker and rich-select bypass the controller and are deliberately NOT chosen (Pitfall 6); the choice is documented in harness.ts."
  prohibitions:
    - "MUST NOT source any perf number from the jsdom project — perf lane is Chromium-only (Pitfall 1/2)."
    - "MUST NOT patch @floating-ui/dom or Lit exports to count — wrap first-party prototypes / observe style mutations."
    - "MUST NOT import test/setup.ts or any mock symbol into a perf spec (native APIs only)."
    - "MUST NOT write files from inside a browser-mode spec — persist via the Node writeMetrics command."
    - "MUST NOT gate on wall-clock — counts are the gated numbers, wall-clock is report-only (Pitfall 15, D-06)."
    - "MUST NOT pick a controller-bypassing overlay (color-picker / rich-select) for the overlay scenario."
  artifacts:
    - test/perf/harness.ts
    - test/perf/data-grid.perf.test.ts
    - test/perf/combobox.perf.test.ts
    - test/perf/overlay.perf.test.ts
    - test/perf/button.perf.test.ts
    - vitest.config.ts (finalized perf project + writeMetrics)
    - api/perf.json (emitted metrics)
  key_links:
    - "cdp() throttle path (Plan 00 A2) → applyThrottle in harness.ts."
    - "FloatingPositionController.prototype._updatePosition (floating-position.ts:116-130) → the computePosition count chokepoint."
    - "writeMetrics command → api/perf.json → consumed by Plan 04 perf-diff + baseline."
---

<objective>
Build the throttled runtime-perf harness on the Chromium-only perf project: instrument first-party prototypes for engine-independent count metrics, throttle CPU + network via CDP, run the four D-06 scenarios 5x, and persist {counts, wall-clock} to api/perf.json.

Purpose: MEAS-02 is the runtime half of the measurement mandate. Counts are the numbers Phases 9 and 11 gate on, so they must be deterministic and instrumented on code the harness owns (never library internals). Wall-clock is captured under real throttling but stays report-only.
Output: `test/perf/harness.ts` (throttle + instrument + summarize), four `*.perf.test.ts` scenarios, and the finalized perf project + writeMetrics command emitting api/perf.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
@src/internal/controllers/floating-position.ts
@test/browser/floating-position.test.ts
@test/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build test/perf/harness.ts (throttle, instrument, summarize)</name>
  <files>test/perf/harness.ts</files>
  <read_first>
    - test/perf/_spike.cdp.test.ts (Plan 00 — the proven cdp() throttle path + pinned config key)
    - src/internal/controllers/floating-position.ts (lines 85-130 — FloatingPositionController class, _updatePosition chokepoint at 116-130, the Object.assign(floating.style,...) at 128 the MutationObserver watches)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Pattern 1" (THROTTLE_PROFILE, NETWORK_TIERS, applyThrottle), §"Pattern 2" (count instrumentation), §"Pattern 3" (summarize median + mean+3σ)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"test/perf/harness.ts"
    - test/helpers.ts (fixture, shadowQuery, waitForUpdate — type-only / side-effect-free helpers; do NOT import setup.ts)
  </read_first>
  <action>
    Create test/perf/harness.ts exporting: THROTTLE_PROFILE (a single named low-end profile — a placeholder centered on the harsh 6x CPU + Slow-3G corner this phase; Plan 04 re-pins it from measured data per MEAS-03), NETWORK_TIERS (Slow-3G / Fast-3G constants from RESEARCH — flagged A1, tunable), and applyThrottle() using cdp() to send Emulation.setCPUThrottlingRate and Network.emulateNetworkConditions (Chromium-only, D-11). Add instrument helpers that wrap first-party prototypes only: countLifecycle(Ctor) wrapping the component's own update/updated/render hooks (public ReactiveElement API), a wrap of FloatingPositionController.prototype._updatePosition to count computePosition invocations, and countRepositions(panel) using a MutationObserver on the panel style attribute as a zero-instrumentation cross-check of the Object.assign(floating.style,...) at floating-position.ts:128; every wrapper restores the original in teardown. Add summarize(samples) returning { median, mean, sd, band: mean+3*sd, samples } (D-07). Document in a header comment that am-popover is the representative overlay because it routes through FloatingPositionController (Pitfall 6 — color-picker/rich-select bypass it and are excluded). Never patch @floating-ui/dom or Lit; never import setup.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('test/perf/harness.ts','utf8'); if(s.indexOf('THROTTLE_PROFILE')<0||s.indexOf('applyThrottle')<0||s.indexOf('summarize')<0) throw new Error('missing export'); if(s.indexOf('setup')>=0 && /from ['\"].*setup/.test(s)) throw new Error('imports setup.ts'); if(s.indexOf('_updatePosition')<0) throw new Error('no controller chokepoint wrap');"</automated>
  </verify>
  <acceptance_criteria>
    - harness.ts exports THROTTLE_PROFILE (with cpuRate + network), NETWORK_TIERS, applyThrottle, an instrument/count API, and summarize.
    - applyThrottle sends both Emulation.setCPUThrottlingRate and Network.emulateNetworkConditions via cdp().
    - Count wrappers target only component lifecycle hooks + FloatingPositionController.prototype._updatePosition + a style MutationObserver; no @floating-ui/dom or Lit export is referenced for patching.
    - harness.ts imports no setup.ts / mock symbol; `npx tsc --noEmit` passes.
    - A header comment documents am-popover as the representative overlay and why (controller-routed, Pitfall 6).
  </acceptance_criteria>
  <precondition>Plan 00 pinned the cdp() config key (or fallback) and the perf project exists.</precondition>
  <reversibility rating="reversible">New test helper module.</reversibility>
  <done>The harness provides throttle, first-party count instrumentation, and the median+band summarizer the scenarios consume.</done>
</task>

<task type="auto">
  <name>Task 2: Write the four D-06 scenario specs</name>
  <files>test/perf/data-grid.perf.test.ts, test/perf/combobox.perf.test.ts, test/perf/overlay.perf.test.ts, test/perf/button.perf.test.ts</files>
  <read_first>
    - test/perf/harness.ts (Task 1 — applyThrottle, instrument, summarize)
    - test/browser/data-grid-virtual.test.ts (grid fixture, columns/rows assignment, waitForUpdate, sort interaction analog)
    - test/browser/floating-position.test.ts (am-popover fixture, waitForPosition async-settle for the overlay scenario)
    - test/browser/combobox-virtual.test.ts (combobox filter-per-keystroke analog)
    - .planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md D-06 (scenario set) and D-07 (5 repeats, median + mean+3σ)
  </read_first>
  <action>
    Create the four scenario specs, each importing only the component under test + the side-effect-free helpers (never setup.ts). data-grid.perf.test.ts: mount am-data-grid with a representative row set, measure render then a sort interaction. combobox.perf.test.ts: mount am-combobox, measure filter-per-keystroke work. overlay.perf.test.ts: mount am-popover (trigger manual, placement bottom-start, offset 8 — the documented representative overlay), measure open then reposition, using both the _updatePosition count and the MutationObserver cross-check. button.perf.test.ts: mount am-button as the light control / noise-floor contrast. Each spec: applyThrottle(THROTTLE_PROFILE) first, then run the scenario 5 times (D-07) collecting per-repeat counts (lifecycle calls, computePosition calls where applicable, DOM node counts) and wall-clock, summarize to median + mean+3σ, assert count metrics are identical across the 5 repeats (determinism), and hand the assembled metrics object to the writeMetrics command target for api/perf.json. Assert the throttle is live (throttled wall-clock greater than an unthrottled control measurement).
  </action>
  <verify>
    <automated>npm run build && npx vitest run --project perf test/perf/data-grid.perf.test.ts test/perf/combobox.perf.test.ts test/perf/overlay.perf.test.ts test/perf/button.perf.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four scenario specs pass on the perf project under throttle.
    - Each scenario runs 5 repeats and asserts count metrics identical across repeats (deterministic).
    - The overlay scenario asserts the _updatePosition count and the MutationObserver reposition count agree.
    - Each spec asserts throttled wall-clock exceeds an unthrottled control (throttle proven live).
    - No spec imports setup.ts or any mock symbol.
  </acceptance_criteria>
  <precondition>`npm run build` done; Plan 00 perf project + cdp() path available.</precondition>
  <reversibility rating="reversible">New scenario test files.</reversibility>
  <done>The four D-06 scenarios measure count + wall-clock under real throttling, with deterministic counts.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the perf project + writeMetrics command emitting api/perf.json</name>
  <files>vitest.config.ts, test/perf/harness.ts</files>
  <read_first>
    - vitest.config.ts (Plan 00 perf project scaffold + writeMetrics stub)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Pattern 4" (writeMetrics BrowserCommand, called via commands.writeMetrics in-spec)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"writeMetrics custom command + perf project"
  </read_first>
  <action>
    Finalize the writeMetrics Node BrowserCommand (from vitest/node, writeFileSync of JSON with a trailing newline) so the scenarios persist their assembled metrics to api/perf.json (browser specs cannot touch the filesystem — this is the only persistence channel). Confirm the perf project include glob covers test/perf/**/*.perf.test.ts, keeps Chromium-only instances, has no setupFiles, and carries the pinned cdp() write+exec grant. Ensure a single full `npm run test:perf` run writes a well-formed api/perf.json containing all four scenarios' { counts, wallClock: { median, mean, sd, band } }. api/ is outside package.files so nothing ships.
  </action>
  <verify>
    <automated>npm run build && npm run test:perf && node -e "const m=require('./api/perf.json'); const j=JSON.stringify(m); for(const s of ['data-grid','combobox','overlay','button']){ if(j.indexOf(s)<0) throw new Error('missing scenario '+s); } if(j.indexOf('median')<0||j.indexOf('band')<0) throw new Error('missing summary fields');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:perf` writes api/perf.json via the writeMetrics command.
    - api/perf.json contains all four scenarios, each with count metrics and wall-clock median + mean+3σ band.
    - The perf project is Chromium-only, no setupFiles, with the cdp() grant pinned.
    - api/ is not in package.files (nothing ships).
  </acceptance_criteria>
  <reversibility rating="reversible">Config finalization of an already-scaffolded project.</reversibility>
  <done>A single test:perf run emits a complete api/perf.json — the input Plan 04 diffs and baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| perf spec (in-page) → writeMetrics (Node) | Metrics cross from the browser page to a server-side file write |
| perf project → browser server (cdp) | Elevated write+exec grant scoped to this project only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-EP | Elevation of Privilege | cdp() write+exec grant | low | mitigate | Scoped to the Chromium-only perf project; never on the shipped build; CI perf job inherits read-only `permissions: contents: read`. |
| T-07-05 | Tampering | Non-deterministic counts silently drifting a future gate | medium | mitigate | Counts asserted identical across 5 repeats; instrumentation on first-party prototypes only (engine-independent); wall-clock stays report-only. |
| T-07-06 | Info disclosure | Perf harness / metrics leaking into the tarball | low | mitigate | test/perf and api/ are outside package.files (["dist","README.md"]). |
</threat_model>

<verification>
- `npm run test:perf` passes on the Chromium perf project under throttle and writes api/perf.json.
- api/perf.json has all four scenarios with counts + wall-clock median + mean+3σ band.
- Counts identical across 5 repeats; throttled wall-clock > unthrottled control.
</verification>

<success_criteria>
MEAS-02 is closed: a reproducible throttled perf harness emits deterministic count metrics + report-only wall-clock for the four D-06 scenarios to a machine-readable api/perf.json.
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-03-SUMMARY.md` when done. Record the candidate-grid observations (which throttle corner best separates heavy from light components) to inform Plan 04's profile pin.
</output>
