---
phase: 07-measurement-baselines-budgets
plan: 05
type: execute
wave: 2
depends_on: [07-00]
files_modified:
  - vite.config.ts
  - scripts/attribution-check.mjs
autonomous: true
requirements: [MEAS-05]

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A dev-only bundle-attribution report is available: rollup-plugin-visualizer is wired into vite.config.ts gated behind an env flag (default off, so normal builds stay clean), and @size-limit/esbuild-why provides a --why cost breakdown on the existing size lane (MEAS-05, D-09)."
    - "The attribution confirms highlight.js is absent from every shipped chunk — highlight.js is already a devDependency with zero src/ imports, so MEAS-05 is confirm-only, not a dependency move (D-09, Pitfall 7)."
    - "A check (scripts/attribution-check.mjs) asserts highlight.js appears in zero shipped chunks — present in the report for exactly zero of the shipped entries (MEAS-05 boundary edge: absent from all, not merely 'mostly')."
    - "The visualizer env flag defaults off; the normal `npm run build` output is byte-unchanged when the flag is unset (no dev tool leaks into the shipped artifact)."
    - { statement: "Shared-chunk attribution lists each module against its owning chunk without double-counting a module that lands in a shared chunk (MEAS-05 adjacency edge), consistent with the marginal-over-core size metric.", verification: backstop }
  prohibitions:
    - "MUST NOT defer or move highlight.js — it is a Storybook-only devDep already absent from shipped chunks; do not chase a phantom (Pitfall 7)."
    - "MUST NOT leave the visualizer enabled by default — env-gate it so normal builds stay clean and no report ships."
    - "MUST NOT add any of the attribution tools to dependencies or package.files — dev-only."
    - "MUST NOT touch the vite.config.ts external array (snapshotted by Plan 02)."
  artifacts:
    - vite.config.ts (env-gated visualizer plugin)
    - scripts/attribution-check.mjs
    - package.json (size:why + visualize wiring)
  key_links:
    - "Installed rollup-plugin-visualizer + @size-limit/esbuild-why (Plan 00) → the attribution report + --why breakdown."
    - "Attribution data → the highlight.js-absent confirmation."
---

<objective>
Wire the dev-only bundle-attribution report (rollup-plugin-visualizer, env-gated) and the size-lane --why breakdown (@size-limit/esbuild-why), and confirm highlight.js ships in no chunk.

Purpose: MEAS-05 makes "what pulls in X" answerable and confirms the one known phantom (highlight.js) truly ships nowhere. It is confirm-only — highlight.js is already a devDep with zero src imports — so the deliverable is the attribution capability plus a passing absence check, not a dependency change.
Output: an env-gated visualizer in vite.config.ts, a size:why wiring, and `scripts/attribution-check.mjs` asserting highlight.js absence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the env-gated rollup-plugin-visualizer and size:why breakdown</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (plugins array line 200 `plugins: [stripLitCssComments()]`; rollupOptions.plugins line 221; DO NOT touch external line 220)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Standard Stack" (visualizer env-gated; esbuild-why matches size-limit 13) and Assumptions A4 (report may be HTML or JSON)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"vite.config.ts (MODIFY: visualizer)"
    - package.json (the visualize/size:why stubs already exist from Plan 00 — reference only; do NOT edit package.json here)
  </read_first>
  <action>
    Add rollup-plugin-visualizer to vite.config.ts gated behind an env flag (e.g. process.env.VISUALIZE) — insert it conditionally into the existing plugins array so it only runs when the flag is set; normal builds emit no report. Prefer a machine-readable output (JSON template) if available so the attribution can be asserted programmatically; fall back to the HTML treemap plus a size-limit --why grep if JSON is not exposed (A4). The visualize (VISUALIZE=1 build) and size:why (size-limit with the @size-limit/esbuild-why --why breakdown) npm scripts already exist as Plan 00 stubs — do NOT edit package.json (Plan 00 owns it). If a Plan 00 stub command needs adjustment to match the actual visualizer wiring, defer that to Plan 06 (the sole later package.json owner). Do NOT modify the external array (line 220) — it is snapshotted by Plan 02 and must stay byte-stable. Keep the attribution tools dev-only and out of package.files.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs'); const before=fs.readFileSync('dist/amris.js'); process.exit(0);" && VISUALIZE=1 npm run build && node -e "const s=require('fs').readFileSync('vite.config.ts','utf8'); if(s.indexOf('visualizer')<0) throw new Error('visualizer not wired'); if(!/VISUALIZE|process\.env/.test(s)) throw new Error('not env-gated'); const p=require('./package.json'); if(!p.scripts['size:why']||!p.scripts['visualize']) throw new Error('scripts missing');"</automated>
  </verify>
  <acceptance_criteria>
    - rollup-plugin-visualizer is present in vite.config.ts and only active when the env flag is set (default off).
    - A normal `npm run build` (flag unset) emits no visualizer report.
    - size:why runs size-limit with the esbuild-why --why breakdown; visualize runs the flagged build.
    - vite.config.ts external array is unchanged (byte-stable).
  </acceptance_criteria>
  <precondition>Plan 00 installed rollup-plugin-visualizer + @size-limit/esbuild-why.</precondition>
  <reversibility rating="reversible">Conditional plugin + scripts; removable.</reversibility>
  <done>A dev-only, env-gated attribution report and a size-lane --why breakdown are available.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm highlight.js is absent from every shipped chunk</name>
  <files>scripts/attribution-check.mjs</files>
  <read_first>
    - scripts/cem-diff.mjs (isMain + report-only exit shape)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Standard Stack" (highlight.js already devDep, zero src imports) and §"Specifics" / Pitfall 7 (confirm-only)
    - .planning/phases/07-measurement-baselines-budgets/07-VALIDATION.md (attribution-check.mjs per-task map row)
  </read_first>
  <action>
    Create scripts/attribution-check.mjs (zero-dependency) that reads the attribution output (the visualizer JSON if exposed, else greps every dist/**/*.js and the size-limit --why output) and asserts highlight.js appears in zero shipped chunks — the boundary is absent from ALL entries, not merely most. Report which shipped chunks were scanned and that none references highlight.js. This is a confirm-only check (Pitfall 7 — do not defer a phantom). Keep the isMain guard; exit 0 report-only this phase (D-08), and exit non-zero only on a usage error. The `attribution:check` npm script already exists as a Plan 00 stub pointing at this file — do NOT edit package.json (Plan 00 owns it).
  </action>
  <verify>
    <automated>VISUALIZE=1 npm run build && node scripts/attribution-check.mjs && node -e "const cp=require('child_process'); const out=cp.execSync('node scripts/attribution-check.mjs',{encoding:'utf8'}); if(!/highlight\.js/i.test(out)) throw new Error('check does not mention highlight.js'); if(/found highlight\.js in a shipped chunk/i.test(out)) throw new Error('highlight.js leaked into a chunk');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/attribution-check.mjs` confirms highlight.js is in zero shipped chunks and exits 0.
    - The check scans every shipped chunk (not a sample) and names highlight.js explicitly in its report.
    - The check is confirm-only (no dependency move) and report-only exit 0 this phase.
  </acceptance_criteria>
  <precondition>Attribution output available (VISUALIZE=1 build or size:why run).</precondition>
  <reversibility rating="reversible">New standalone check script.</reversibility>
  <done>Attribution confirms highlight.js ships nowhere — MEAS-05 satisfied confirm-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev attribution tools → build output | Env-gated, dev-only; must not enter the shipped tarball |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-ID | Info disclosure | Visualizer report / tool leaking into the published tarball | low | mitigate | Env-gate the visualizer (default off); keep both tools in devDependencies; package.files stays ["dist","README.md"]. |
| T-07-09 | Tampering | A future refactor pulling highlight.js into a shipped chunk | low | mitigate | attribution-check.mjs asserts highlight.js absent from every chunk (confirm-only now, seeds a Phase-11 guard). |
</threat_model>

<verification>
- `VISUALIZE=1 npm run build && node scripts/attribution-check.mjs` exits 0 and confirms highlight.js absent from all chunks.
- Normal `npm run build` (flag unset) emits no visualizer report; external array unchanged.
</verification>

<success_criteria>
MEAS-05 is closed: a dev-only, env-gated attribution report + --why breakdown exist and confirm highlight.js ships in no chunk (confirm-only, no dependency move).
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-05-SUMMARY.md` when done. Note the attribution output format used (JSON vs HTML+why grep) and confirm highlight.js absence.
</output>
