---
phase: 07-measurement-baselines-budgets
plan: 06
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05]
files_modified:
  - .github/workflows/ci.yml
  - tachometer/data-grid.json
  - tachometer/combobox.json
  - tachometer/overlay.json
  - package.json
autonomous: true
requirements: [MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05]

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new report-only `perf` CI job (Node 20, Chromium installed) runs `npm run test:perf` then `node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json`, posting numbers on every PR and exiting 0 (nothing red-builds this phase, D-08)."
    - "The existing `size` CI job (Node 22, preserving the version split) gains report-only steps: the re-scoped size-limit run, `node scripts/size-baseline.mjs --check`, `node scripts/assert-no-bundled-lit.mjs`, and the @size-limit/esbuild-why --why breakdown — all report-only."
    - "Both new report-only signals mirror the proven cem-diff report-only→enforcing pattern; the enforcing flip is explicitly Phase 11 (size first → runtime counts → wall-clock stays report-only)."
    - "tachometer is added as a committed local-only, ungated A/B config for the heavy components (data-grid, combobox, overlay) — never referenced by any CI job and excluded from package.files (D-10)."
    - "The Node-version split is preserved: size job stays Node 22 (size-limit@13 needs >=22.18), the perf job is pinned Node 20 and documented (Pitfall 5)."
    - "CI jobs inherit the repo's least-privilege `permissions: contents: read`; no new job gains write/publish scope."
  prohibitions:
    - "MUST NOT add continue-on-error or `|| true` to fake report-only — the diff scripts already exit 0 by design (D-08)."
    - "MUST NOT flip any job to enforcing this phase — report-only only; enforcing is Phase 11."
    - "MUST NOT reference tachometer from any CI job — it is local-only, ungated (D-10)."
    - "MUST NOT grant any new CI job packages:write / publish scope — inherit read-only permissions."
    - "MUST NOT move the size job off Node 22 or the perf job onto Node 22 — preserve the split."
    - "MUST NOT ship tachometer config — keep package.files ['dist','README.md']."
  artifacts:
    - .github/workflows/ci.yml (perf job + re-scoped size job)
    - tachometer/*.json (local-only A/B configs)
    - package.json (finalized report-only scripts)
  key_links:
    - "All Plan 01-05 scripts + baselines → the report-only CI jobs that run them."
    - "Node-version split (size 22 / perf 20) → prevents size-limit@13 breakage + baseline drift."
---

<objective>
Wire every measurement signal into CI as report-only jobs (numbers post on every PR, nothing red-builds), preserve the Node-version split, and land the local-only tachometer A/B config.

Purpose: D-08 requires Phase 7 to be measure + baseline + report-only wiring only — the flip to enforcing is Phase 11. This plan is the single owner of `.github/workflows/ci.yml` for the phase (avoiding cross-plan file conflicts) and closes the CI footprint for all five MEAS requirements.
Output: a report-only `perf` job, a re-scoped `size` job with report-only guard steps, committed tachometer configs, and finalized report-only npm scripts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the report-only perf CI job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (whole file — the `browser` job lines 35-51 for the Chromium install pattern `npx playwright install chromium`; top-level `permissions: contents: read` lines 10-11)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Code Examples" (perf job YAML) and §"Pitfall 5" (Node pin) and Open Question 3 (sibling job)
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §".github/workflows/ci.yml (MODIFY)"
  </read_first>
  <action>
    Add a sibling `perf` job to .github/workflows/ci.yml mirroring the `browser` job: checkout, setup-node Node 20 with npm cache (pin + document the Node choice per Pitfall 5), npm ci, `npx playwright install chromium`, then a report-only step running `npm run test:perf` followed by `node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json`. No continue-on-error and no `|| true` — perf-diff exits 0 by design (D-08). Keep it a sibling job (not folded into `browser`) to isolate the CDP write+exec privilege and the Chromium-only constraint (Open Question 3). It inherits the top-level read-only permissions.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!/^\s{2}perf:/m.test(y)) throw new Error('no perf job'); if(y.indexOf('test:perf')<0||y.indexOf('perf-diff.mjs')<0) throw new Error('perf steps missing'); if(/continue-on-error/.test(y.split('perf:')[1].split(/^\s{2}\w+:/m)[0]||'')) throw new Error('report-only must not use continue-on-error'); if(!/node-version:\s*20/.test(y)) throw new Error('perf job not pinned Node 20');"</automated>
  </verify>
  <acceptance_criteria>
    - A `perf` job exists (sibling, not folded into browser) with Node 20, Chromium install, npm ci.
    - It runs `npm run test:perf` then `node scripts/perf-diff.mjs api/perf.baseline.json api/perf.json`.
    - No continue-on-error / `|| true` (report-only is achieved by the script exiting 0).
    - The job inherits the read-only top-level permissions (no new write scope).
  </acceptance_criteria>
  <reversibility rating="reversible">New CI job; removable.</reversibility>
  <done>A report-only perf job posts throttled numbers on every PR without red-building.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the size CI job with report-only guard steps</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the `size` job lines 83-100 — Node 22 comment at 88-90 to preserve; `npm run size` at 100)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Code Examples" (size job additions) and §"Pitfall 5"
    - package.json (size, size:why, size:baseline, assert:no-lit scripts)
  </read_first>
  <action>
    Extend the existing `size` job (keep Node 22 — do not change the version split; the comment at lines 88-90 explains why size-limit@13 needs >=22.18). After the existing `npm run size` step add report-only steps: `node scripts/size-baseline.mjs --check`, `node scripts/assert-no-bundled-lit.mjs`, and the `@size-limit/esbuild-why` --why breakdown (via the size:why script). All are report-only — the scripts exit 0 by design; do not add continue-on-error. Keep the job under the top-level read-only permissions.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const size=y.split(/^\s{2}size:/m)[1]||''; const block=size.split(/^\s{2}\w+:/m)[0]; if(!/node-version:\s*22/.test(block)) throw new Error('size job must stay Node 22'); if(block.indexOf('size-baseline.mjs')<0) throw new Error('no size-baseline check'); if(block.indexOf('assert-no-bundled-lit.mjs')<0) throw new Error('no no-bundled-lit check'); if(!/size:why|esbuild-why|--why/.test(block)) throw new Error('no why breakdown');"</automated>
  </verify>
  <acceptance_criteria>
    - The size job stays Node 22 and gains report-only steps: size-baseline.mjs --check, assert-no-bundled-lit.mjs, and the --why breakdown.
    - No step uses continue-on-error; report-only is achieved by the scripts exiting 0.
    - The Node-version split (size 22 / perf 20 / rest 20) is intact.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive CI steps; removable.</reversibility>
  <done>The size job posts the re-scoped brotli baseline, the no-bundled-Lit guard, and the --why breakdown, all report-only.</done>
</task>

<task type="auto">
  <name>Task 3: Add the local-only tachometer A/B config and finalize report-only scripts</name>
  <files>tachometer/data-grid.json, tachometer/combobox.json, tachometer/overlay.json, package.json</files>
  <read_first>
    - .planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md D-10 (tachometer local-only, ungated, never CI, never a gate)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Standard Stack" (tachometer 0.7.2) and §"Recommended Project Structure" (tachometer/ outside package.files)
    - package.json (files array lines 48-51 — must stay ["dist","README.md"]; scripts block)
  </read_first>
  <action>
    Add committed tachometer A/B config files under tachometer/ for the heavy components (data-grid, combobox, overlay) as a local-only, ungated before/after micro-benchmark harness for Phases 8-9 (D-10). Do NOT reference tachometer from any CI job or npm script that CI runs — a local convenience only. The seven report-only npm scripts were created as stubs by Plan 00 (test:perf, size:baseline, perf:diff, assert:no-lit, size:why, visualize, attribution:check); as the sole later package.json owner, verify each resolves against its now-existing target script and adjust only a stub command that needs its final form (e.g. cross-platform env handling for visualize). Confirm package.files stays ["dist","README.md"] so tachometer/ and api/ never ship. Confirm nothing in CI red-builds (all diff/guard scripts exit 0).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(JSON.stringify(p.files)!==JSON.stringify(['dist','README.md'])) throw new Error('files array changed'); const need=['test:perf','size:baseline','perf:diff','size:why','visualize']; for(const s of need){ if(!p.scripts[s]) throw new Error('missing script '+s); } const fs=require('fs'); if(!fs.existsSync('tachometer/data-grid.json')) throw new Error('no tachometer config'); const y=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(/tachometer/.test(y)) throw new Error('tachometer must not be in CI');"</automated>
  </verify>
  <acceptance_criteria>
    - tachometer/ contains A/B configs for data-grid, combobox, and overlay.
    - No CI job or CI-run npm script references tachometer (local-only, D-10).
    - All report-only npm script names resolve.
    - package.files stays ["dist","README.md"] (tachometer/ + api/ never ship).
  </acceptance_criteria>
  <reversibility rating="reversible">New local config + script finalization.</reversibility>
  <done>tachometer is a committed local-only A/B harness, all report-only scripts resolve, and nothing red-builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI jobs → repo permissions | New jobs must inherit read-only, no publish scope |
| tachometer config → published tarball | Local-only; must not ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-CI | Elevation of Privilege | New CI jobs gaining write/publish scope | low | mitigate | Jobs inherit the top-level `permissions: contents: read`; no packages:write; no publish step (mirrors the existing PR-triggered jobs). |
| T-07-10 | Tampering | A flaky report-only job silently red-building a publish later | low | mitigate | Report-only by script design (exit 0), never continue-on-error; enforcing flip deferred to Phase 11 staged off the release critical path (GATE-03). |
| T-07-ID | Info disclosure | tachometer config shipping in the tarball | low | mitigate | tachometer/ outside package.files (["dist","README.md"]); never referenced by CI. |
</threat_model>

<verification>
- ci.yml has a report-only `perf` job (Node 20) and a re-scoped `size` job (Node 22) with size-baseline/no-bundled-lit/--why steps, none using continue-on-error.
- No CI job references tachometer; package.files unchanged; all report-only scripts resolve.
- New jobs inherit read-only permissions.
</verification>

<success_criteria>
The phase CI footprint is complete and report-only: size + perf numbers post on every PR, nothing red-builds, the Node split is preserved, and tachometer is a committed local-only A/B harness — all five MEAS requirements are wired end-to-end.
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-06-SUMMARY.md` when done. Note that all gates are report-only and the enforcing flip is Phase 11 (size first → runtime counts → wall-clock stays report-only).
</output>
