---
phase: 07-measurement-baselines-budgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .size-limit.json
  - scripts/size-baseline.mjs
  - api/size.baseline.json
autonomous: true
requirements: [MEAS-01, MEAS-04]

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `node scripts/size-baseline.mjs` reproducibly captures a committed per-entry brotli (on-the-wire) bundle-size baseline for every delivered-payload entry (MEAS-01)."
    - ".size-limit.json reports brotli (no `gzip:true` on any entry) and no longer ignores @floating-ui/dom on any delivered-payload entry — the Phase-8 deferral win becomes visible (MEAS-04, D-02); `lit` stays ignored as a peer dep."
    - "The baseline includes the expanded metric set (D-02): the 4 re-scoped JS entries, a dist/styles/tokens.css standalone-brotli entry, a first-load composite = core+button+input+dialog (D-05), and a marginal-cost-over-core value per component so shared-chunk moves are not double-counted."
    - "size-baseline.mjs is deterministic: re-running on an unchanged dist/ produces byte-identical brotli counts (MEAS-01 idempotency / MEAS-02 precision edge — brotli byte counts are exact integers)."
    - "A zero-delta run (baseline == current) prints a clean report and exits 0 with no false failure (MEAS-04 boundary — report-only this phase, D-08); diff rows are deterministically key-sorted by entry name so re-runs do not spuriously differ (MEAS-05 ordering edge)."
    - "First-generation / empty-baseline behavior is defined: when api/size.baseline.json is absent, the script writes it and reports 'new baseline' rather than erroring (MEAS-05 empty edge)."
  prohibitions:
    - "MUST NOT keep @floating-ui/dom in `ignore` on any delivered-payload entry."
    - "MUST NOT keep `gzip:true` on any entry — brotli is the on-the-wire unit used consistently everywhere."
    - "MUST NOT stop ignoring `lit` — it is a peer dependency and is never shipped."
    - "MUST NOT flip any size limit to enforcing this phase — report-only exit 0 only (D-08)."
    - "MUST NOT change the frozen v1.0 public component surface — this is measurement wiring only."
  artifacts:
    - scripts/size-baseline.mjs
    - api/size.baseline.json
    - .size-limit.json (re-scoped)
  key_links:
    - "Committed size baseline + report-only diff spine → cloned by Plan 04 perf-diff.mjs."
    - "@floating-ui/dom counted in delivered payload → makes the Phase-8 deferral win measurable."

# --- Artifacts this phase produces (phase-level inventory) ---
# scripts/size-baseline.mjs, scripts/perf-diff.mjs, scripts/assert-no-bundled-lit.mjs
# test/perf/harness.ts + test/perf/{data-grid,combobox,overlay,button}.perf.test.ts
# writeMetrics BrowserCommand (vitest.config.ts perf project)
# api/perf.baseline.json, api/size.baseline.json
# re-scoped .size-limit.json entries (brotli, floating-ui counted, tokens.css, composite, marginal)
# new report-only CI jobs (perf) + re-scoped size job (.github/workflows/ci.yml)
# devDependencies: rollup-plugin-visualizer@7, @size-limit/esbuild-why@13, tachometer@0.7.2
# THROTTLE_PROFILE pinned low-end profile (harness config), tachometer/ local-only A/B config
# COVERAGE.md
---

<objective>
Stand up the committed brotli size baseline — the tracer that proves the whole "config to measurement script to committed baseline to report-only diff" spine end-to-end on the size lane, which every later diff (perf-diff, gate flips) clones.

Purpose: MEAS-01 is the universal prerequisite: without a reproducible committed size baseline, no Phase-8 byte win can be defended. Leading with it proves the report-only baseline architecture on the simplest layer (no CDP, no browser) before the perf harness expands off it.
Output: A re-scoped brotli `.size-limit.json`, a zero-dependency `scripts/size-baseline.mjs` (clone of the cem-diff shape), and a committed first-generation `api/size.baseline.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md
@.planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md
@.planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md
@scripts/cem-diff.mjs
@.size-limit.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Re-scope .size-limit.json to brotli and create scripts/size-baseline.mjs end-to-end</name>
  <files>.size-limit.json, scripts/size-baseline.mjs, api/size.baseline.json</files>
  <read_first>
    - scripts/cem-diff.mjs (whole file — clone the load()/normalize/diff/formatReport/isMain shape; INVERT the enforcing exit to report-only exit 0 per D-08)
    - .size-limit.json (current 4 entries — each has ignore of lit + @floating-ui/dom and gzip true)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Code Examples" (re-scoped .size-limit.json + size-baseline.mjs skeleton) and §"Pitfall 3"
    - .planning/phases/07-measurement-baselines-budgets/07-PATTERNS.md §"scripts/size-baseline.mjs" and §".size-limit.json (MODIFY)"
    - api/custom-elements.baseline.json (committed-baseline convention)
  </read_first>
  <action>
    Re-scope .size-limit.json: on all 4 existing delivered-payload entries (core bundle, full bundle, button, data-grid) remove the gzip flag (size-limit v13 defaults to brotli) and remove @floating-ui/dom from every ignore array while keeping lit ignored (D-02, Pitfall 2/3). Leave the limit values as report-only placeholders this phase (enforcing values land in Phase 11). Create scripts/size-baseline.mjs as a zero-dependency Node script cloning the cem-diff.mjs structure: run size-limit with the --json flag via execFileSync to read each entry's brotli byte count, build a map of entry name to brotli bytes, load the committed api/size.baseline.json (if present), diff current vs baseline keyed by entry name with sorted output, print a formatReport-style summary, and exit 0 unconditionally (report-only, D-08) — keep the isMain guard and a usage/--check/--write mode split (exit 2 on usage error only). When api/size.baseline.json is absent, write it and report a new-baseline message (empty-baseline edge). The `size:baseline` npm script already exists as a Plan 00 stub pointing at this file — do NOT edit package.json (Plan 00 owns it; this plan only creates the script file). This is the tracer: one full path from re-scoped config through the script to a committed baseline and a report-only diff.
  </action>
  <verify>
    <automated>npm run build && node scripts/size-baseline.mjs --write && node scripts/size-baseline.mjs --check && node -e "const e=require('./.size-limit.json'); for(const x of e){ if(x.gzip) throw new Error('gzip on '+x.name); const ig=x.ignore||[]; if(ig.indexOf('@floating-ui/dom')>=0) throw new Error('floating-ui still ignored on '+x.name); }"</automated>
  </verify>
  <acceptance_criteria>
    - No entry in .size-limit.json has the gzip flag; no delivered-payload entry ignores @floating-ui/dom; every entry still ignores lit.
    - `node scripts/size-baseline.mjs --check` exits 0 against the freshly-written baseline (idempotent: byte-identical on unchanged dist).
    - The script exits 0 even when drift exists (report-only, D-08) and exits 2 only on a usage error.
    - When api/size.baseline.json is absent the script writes it and reports a new-baseline message.
    - Diff output is deterministically sorted by entry name.
  </acceptance_criteria>
  <precondition>`npm run build` has produced dist/**/*.js and dist/styles/tokens.css (the measurement inputs).</precondition>
  <reversibility rating="reversible">Config re-scope + new script; revertable to the current .size-limit.json.</reversibility>
  <done>The brotli baseline spine works end-to-end: re-scoped config, size-baseline.mjs, committed api/size.baseline.json, deterministic report-only diff.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the baseline to tokens.css, the first-load composite, and marginal-over-core</name>
  <files>.size-limit.json, scripts/size-baseline.mjs, api/size.baseline.json</files>
  <read_first>
    - scripts/size-baseline.mjs (Task 1 output)
    - .planning/phases/07-measurement-baselines-budgets/07-RESEARCH.md §"Code Examples" (tokens.css brotli via zlib; composite path array; marginal-over-core note; Open Question 2 recommends the arithmetic diff)
    - .planning/phases/07-measurement-baselines-budgets/07-CONTEXT.md D-02 and D-05
    - package.json (build:tokens-css produces dist/styles/tokens.css, line 64; exports it at line 36)
  </read_first>
  <action>
    Extend the baseline metric set per D-02/D-05. Add a first-load composite entry to .size-limit.json whose path is the array of dist/amris-core.js, dist/components/button/index.js, dist/components/input/index.js, and dist/components/dialog/index.js with ignore of lit (D-05 — light + form control + overlay, exercises the floating-ui deferral target via dialog). In scripts/size-baseline.mjs add: (a) a standalone tokens.css brotli measurement using node:zlib brotliCompressSync on dist/styles/tokens.css (size-limit does not measure the CSS asset as a JS entry), recorded as a tokens.css baseline entry; and (b) a marginal-cost-over-core value per component computed arithmetically as the component entry brotli minus the core bundle brotli (Open Question 2 — arithmetic diff, brotli-consistent, over size-limit import syntax), recorded so shared-chunk moves are not double-counted (Pitfall 2). Regenerate and commit api/size.baseline.json. All new metrics are brotli integers; the composite and marginal values are derived deterministically so re-runs are byte-identical.
  </action>
  <verify>
    <automated>npm run build && node scripts/size-baseline.mjs --write && node scripts/size-baseline.mjs --check && node -e "const b=require('./api/size.baseline.json'); const s=JSON.stringify(b); if(s.indexOf('tokens.css')<0) throw new Error('no tokens.css entry'); if(!/composite/i.test(s)) throw new Error('no composite entry'); if(!/marginal/i.test(s)) throw new Error('no marginal metric');"</automated>
  </verify>
  <acceptance_criteria>
    - api/size.baseline.json contains a tokens.css entry, a first-load composite entry, and a marginal-over-core value per component.
    - The composite entry path array is exactly core + button + input + dialog and ignores lit only.
    - The marginal value equals component-entry brotli minus core-bundle brotli (arithmetic, brotli-consistent).
    - `node scripts/size-baseline.mjs --check` exits 0 (byte-identical re-run).
  </acceptance_criteria>
  <precondition>`npm run build` has produced dist/styles/tokens.css and the component deep-import entries.</precondition>
  <reversibility rating="reversible">Additive metrics + config entry; removable.</reversibility>
  <done>The committed size baseline covers the full expanded D-02 metric set (JS entries + tokens.css + composite + marginal), all brotli, all deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/** build artifact → size-baseline.mjs | The script reads only in-repo, freshly-built artifacts |
| committed api/size.baseline.json → CI diff | Trusted in-repo baseline compared against a fresh measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | .size-limit.json ignore re-scope hiding a shipped byte | medium | mitigate | Removing @floating-ui/dom from ignore makes the delivered payload honest; a separate no-bundled-Lit guard (Plan 02) covers the one still-ignored dep (lit). Baseline committed + diffed in CI. |
| T-07-02 | Tampering | size-baseline.mjs reads only trusted in-repo JSON + own emitted JSON | low | accept | No external input; V5 input-validation surface is a committed baseline the repo owns (RESEARCH Security Domain). |
</threat_model>

<verification>
- `npm run build && node scripts/size-baseline.mjs --check` exits 0 (idempotent brotli baseline).
- .size-limit.json: zero gzip flags, zero delivered-payload @floating-ui/dom ignores, lit still ignored.
- api/size.baseline.json committed with JS entries + tokens.css + composite + marginal metrics.
</verification>

<success_criteria>
MEAS-01 is closed: a reproducible, committed, brotli per-entry size baseline exists via size-baseline.mjs, and the MEAS-04 size-limit re-scope (floating-ui counted, brotli) is in place — the report-only baseline spine the perf lane clones.
</success_criteria>

<output>
Create `.planning/phases/07-measurement-baselines-budgets/07-01-SUMMARY.md` when done. Record the measured brotli numbers per entry and the marginal-over-core values (they seed Phase 11's enforcing limits).
</output>
