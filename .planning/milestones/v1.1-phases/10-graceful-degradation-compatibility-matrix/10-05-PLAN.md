---
phase: 10-graceful-degradation-compatibility-matrix
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/components/number-field/number-field.ts
  - src/components/input-otp/input-otp.ts
  - src/components/radio/radio.ts
  - src/components/rich-select/rich-select.ts
  - src/components/select/select.ts
  - test/capabilities-off-constructor.batch-a2.test.ts
  - test/form-fallback-integration.batch-a2.test.ts
autonomous: true
requirements: [COMPAT-02, COMPAT-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With ElementInternals PRESENT (the default/above-floor state), the fallback hidden-input NEVER attaches on number-field, input-otp, radio-group, rich-select, or select — internals?.setFormValue continues to be the only channel; the XOR gate is proven per-component (COMPAT-03 concurrency edge: no double-submit)."
    - "With hasFormAssociation() forced false AND the fallback enabled, each value-bearing component in this batch submits FormData matching its above-floor value (FormData parity), and required/pattern constraints (where present) block invalid native submit."
    - "With hasFormAssociation() forced false AND the fallback disabled (the default), each component fires warnBelowFloorOnce exactly once (globally deduped) instead of throwing or silently submitting nothing new."
    - "Below the floor, all 6 attach sites across these 5 files (number-field, input-otp, both radio classes, rich-select, select) construct, connect, and render without throwing (COMPAT-02)."
  artifacts:
    - test/capabilities-off-constructor.batch-a2.test.ts
    - test/form-fallback-integration.batch-a2.test.ts
  key_links:
    - "Each component's updated()/willUpdate() calls, when this.internals is null: isFormFallbackEnabled() ? syncFormFallback(this, {...}) : warnBelowFloorOnce(tagName)"
    - "Each value-bearing component's disconnectedCallback calls teardownFormFallback(this)"
  prohibitions:
    - "Above-floor behavior (event cadence, DOM structure, a11y tree, validation timing) must not change on any of these components — the freeze holds except for the below-floor branch, which only executes when internals is null."
    - "No component wires BOTH the ElementInternals setFormValue path and the hidden-input fallback in the same render — exactly one channel executes, gated on internals presence."
---

<objective>
Combined COMPAT-02 + COMPAT-03 rollout, batch A2 (5 of 15 remaining files):
apply the FULL combined pattern (guarded attach + below-floor fallback/warn
wiring, in one edit per file) to number-field, input-otp, radio (both
`am-radio` and `am-radio-group`), rich-select, and select.

This is the second of the two batch-A rollout plans (batch A1 — am-input,
button, checkbox, combobox, color-picker, date-picker — is Plan 04). The
original single batch-A plan was split into 04/05 to keep each plan's context
budget in the smart zone. Batch A1 and batch A2 own DISTINCT test files
(this plan writes only `*.batch-a2.test.ts`), so they run genuinely in
parallel in wave 2 with zero file overlap — no two wave-2 plans append to the
same test-array file, which is what makes the parallel-worktree merge-back
collision-free.

The combined-per-file pattern is already proven end-to-end by Plan 01
(am-input) and re-proven in Plan 04's batch-A1 tracer; this plan re-establishes
it on `number-field` (a constrained numeric control) as its own tracer, then
mechanically expands to the remaining 4 files.

Purpose: bring 5 more form-associated components (6 attach sites, counting both
radio classes) to full degradation capability, on this plan's own distinct
test files.
Output: number-field, input-otp, radio (both classes), rich-select, and select
fully degradation-capable; this batch's own jsdom capability-off +
fallback-integration test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-CONTEXT.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-RESEARCH.md
@.planning/phases/10-graceful-degradation-compatibility-matrix/10-PATTERNS.md
@src/components/input/input.ts
@src/internal/helpers/capabilities.ts
@src/internal/helpers/attach-internals-safe.ts
@src/internal/helpers/form-participation.ts
@test/capabilities-off-constructor.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: number-field.ts — combined rollout on a constrained numeric control (tracer) + seed batch-a2 test files</name>
  <files>src/components/number-field/number-field.ts, test/capabilities-off-constructor.batch-a2.test.ts, test/form-fallback-integration.batch-a2.test.ts</files>
  <read_first>
    - src/components/number-field/number-field.ts:71 (attachInternals site) — grep `internals\|_internals\|setFormValue\|setValidity` in this file for the naming convention, exact call sites, its `required`/`min`/`max`/`step` constraints, and how its numeric value is serialized (mirror the SAME serialization onto the fallback input, coercing to a string via `String(this.value)` before passing to `syncFormFallback`'s `value: string` parameter).
    - src/components/input/input.ts (Plan 01) and Plan 04's batch-A1 components as the reference for the exact combined-transform shape (widen field, `attachInternalsSafe(this)`, null-safe call sites, below-floor fallback/warn branch in `updated()`, `disconnectedCallback` teardown).
    - src/internal/helpers/form-participation.ts (Plan 02 — `isFormFallbackEnabled`, `syncFormFallback`, `teardownFormFallback`, `warnBelowFloorOnce`; confirm `value` is a string and `required`/`pattern` are optional keys).
    - test/capabilities-off-constructor.test.ts (Plan 01 — the constructor-no-throw scaffold shape to mirror in this batch's own batch-a2 file).
    - 10-RESEARCH.md Pattern 2 (the two naming conventions: `this.internals` vs `this._internals`) and the "16 attach sites" table.
  </read_first>
  <action>
    Apply the combined transform to number-field.ts: widen the internals field to `ElementInternals | null`, replace the raw `this.attachInternals()` call with `attachInternalsSafe(this)` (import from `'../../internal/helpers/attach-internals-safe.js'`), null-safe every `.setFormValue(...)`/`.setValidity(...)` call site with `?.` (preserve every `setValidity({})` reset branch), and add the below-floor fallback/warn branch in `updated()`: when `!this.internals` (or `!this._internals`), call `isFormFallbackEnabled() ? syncFormFallback(this, { name: this.name, value: String(this.value), required: this.required, disabled: this.disabled }) : warnBelowFloorOnce('am-number-field')` (import from `'../../internal/helpers/form-participation.js'`). `min`/`max`/`step` have no direct HTML hidden-input constraint equivalent to project natively, so mirror only `required` — same documented below-floor validation limit as textarea's minlength/maxlength (RESEARCH.md Q4). Add a `disconnectedCallback()` override calling `teardownFormFallback(this)`.

    Create test/capabilities-off-constructor.batch-a2.test.ts (jsdom) mirroring Plan 01's constructor-no-throw scaffold: a top-level `const FORM_TAGS = ['am-number-field'];` array with a loop generating one describe/it per tag, forcing `hasFormAssociation()` false via the capabilities.ts shim and asserting the tag constructs/connects/renders without throwing below the floor. Task 2 appends this batch's remaining tags to the SAME batch-a2 file. Create test/form-fallback-integration.batch-a2.test.ts (jsdom) mirroring Plan 04's fallback-integration scaffold: a top-level `const FALLBACK_TAGS = ['am-number-field'];` array with a loop generating the 3 cases per tag (fallback-on numeric-value parity via `FormData.get(name)` equal to the stringified value; fallback-off warn; above-floor XOR). Both files are owned solely by this plan.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/capabilities-off-constructor.batch-a2.test.ts test/form-fallback-integration.batch-a2.test.ts src/components/number-field</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - number-field.ts: internals field is `ElementInternals | null`, constructor calls `attachInternalsSafe(this)`, all setter call sites `?.`-guarded, below-floor fallback/warn branch + teardown present.
    - test/capabilities-off-constructor.batch-a2.test.ts and test/form-fallback-integration.batch-a2.test.ts pass for am-number-field: constructs without throw below the floor; fallback mirrors its numeric value as a string with FormData parity; XOR holds above the floor.
    - Existing above-floor am-number-field specs stay green unchanged.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Component-local wiring behind existing null-checks.</reversibility>
  <done>number-field.ts is fully degradation-capable, re-proving the combined pattern on a constrained numeric control and seeding this batch's own distinct test files.</done>
</task>

<task type="auto">
  <name>Task 2: Combined rollout — input-otp, radio (both classes), rich-select, select (4 files, 5 attach sites)</name>
  <files>src/components/input-otp/input-otp.ts, src/components/radio/radio.ts, src/components/rich-select/rich-select.ts, src/components/select/select.ts, test/capabilities-off-constructor.batch-a2.test.ts, test/form-fallback-integration.batch-a2.test.ts</files>
  <read_first>
    - src/components/input-otp/input-otp.ts:73 (attachInternals site), src/components/rich-select/rich-select.ts:141, src/components/select/select.ts:311 — grep `attachInternals\|setFormValue\|setValidity\|_internals\|internals` in each file for exact call sites and the naming convention (`internals` vs `_internals`) in use.
    - src/components/radio/radio.ts:1-45 (`AmRadio` — the individual radio, `internals` field at line 40, constructor at 42-45, NO ValidationController/setFormValue visible near the constructor — grep the rest of the file for where `AmRadio` reports its checked state, likely via `setFormValue`/`setValidity` elsewhere or via the parent `AmRadioGroup`), 255-270 (`AmRadioGroup` — `internals` field + constructor at 264-266, WITH ValidationController wiring). Radio.ts contains TWO classes needing the guarded-attach edit; only `AmRadioGroup` (the value-bearing, form-participating one) needs the below-floor fallback/warn branch — confirm by grepping which class calls `setFormValue` before deciding whether `AmRadio` itself needs the fallback branch too (if `AmRadio` never calls `setFormValue`, treat it like button.ts: guarded-attach + warn-only, no fallback mirror).
    - test/capabilities-off-constructor.batch-a2.test.ts, test/form-fallback-integration.batch-a2.test.ts (Task 1 created both — append here; owned solely by this plan).
    - 10-RESEARCH.md "16 attach sites (research-verified line numbers)" table for the authoritative site list.
  </read_first>
  <action>
    Apply the identical combined transform from Task 1 to input-otp.ts, rich-select.ts, select.ts (each: widen field type, `attachInternalsSafe(this)`, null-safe every setter/getter call site with `?.`, add the below-floor fallback/warn branch mirroring the component's own value/name/required semantics, add `disconnectedCallback` teardown). For radio.ts, apply the guarded-attach half to BOTH `AmRadio` (line 44) and `AmRadioGroup` (line 266); add the below-floor fallback/warn branch to whichever class(es) actually call `setFormValue` (per the read_first investigation) — if only `AmRadioGroup` is value-bearing, `AmRadio` gets guard+warn-only like button.ts.

    Extend test/capabilities-off-constructor.batch-a2.test.ts's `FORM_TAGS` array with `'am-input-otp'`, `'am-radio'`, `'am-radio-group'`, `'am-rich-select'`, `'am-select'` (this batch's full tag set is now 6: number-field + these 5). Extend test/form-fallback-integration.batch-a2.test.ts's `FALLBACK_TAGS` with the value-bearing subset (input-otp, radio-group, rich-select, select — excluding whichever radio class turned out to be guard+warn-only per the investigation), covering fallback-on parity + fallback-off warn + above-floor XOR for each.
  </action>
  <verify>
    <automated>cd C:/repos/ProjectAmris && npx vitest run --project jsdom test/capabilities-off-constructor.batch-a2.test.ts test/form-fallback-integration.batch-a2.test.ts src/components/input-otp src/components/radio src/components/rich-select src/components/select</automated>
    <automated>cd C:/repos/ProjectAmris && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - input-otp.ts, rich-select.ts, select.ts: internals field is `ElementInternals | null`, constructor calls `attachInternalsSafe(this)`, every setter/getter call site is `?.`-guarded, below-floor fallback/warn branch + `disconnectedCallback` teardown present.
    - radio.ts: BOTH `AmRadio` and `AmRadioGroup` have guarded-attach; the value-bearing class(es) additionally have the fallback/warn branch.
    - test/capabilities-off-constructor.batch-a2.test.ts covers all 6 batch-A2 tags (am-number-field, am-input-otp, am-radio, am-radio-group, am-rich-select, am-select) and every one constructs/connects/renders without throwing below the floor.
    - test/form-fallback-integration.batch-a2.test.ts covers every value-bearing control wired in this plan.
    - Existing above-floor jsdom specs for these components stay green unchanged.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Component-local wiring behind existing null-checks; revert per-file independently.</reversibility>
  <done>input-otp, radio (both classes), rich-select, and select are fully degradation-capable; combined with number-field this plan completes batch A2's 5 files / 6 attach sites, on its own distinct test files. Plan 06 (batch B) finishes the remaining 4 components.</done>
</task>

</tasks>

<flagged_assumptions>
Specless edge-probe fallback — unresolved edges dispositioned (no silent drops):
- [COMPAT-03 concurrency] AUTHORED as must_haves.truth + Task 1/2 XOR test cases: with ElementInternals present, the fallback never attaches on any of these components.
- [COMPAT-02 unclassified] AUTHORED as must_haves.truth: all 6 attach sites across these 5 files construct/connect/render without throwing below the floor.
</flagged_assumptions>

<artifacts_this_phase_produces>
NEW symbols/files this plan introduces (excluded from source-drift verification):
- Below-floor fallback/warn branches + `disconnectedCallback` teardown overrides in number-field.ts, input-otp.ts, rich-select.ts, select.ts, and the value-bearing radio class.
- `attachInternalsSafe(this)` call sites replacing raw `attachInternals()` in number-field.ts, input-otp.ts, radio.ts (both classes), rich-select.ts, select.ts.
- test/capabilities-off-constructor.batch-a2.test.ts, test/form-fallback-integration.batch-a2.test.ts — new jsdom specs owned solely by this plan (no shared-array append with any other plan).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Light-DOM hidden `<input>` mirrors ↔ batch-A2 value-bearing components' enclosing `<form>` | Each value-bearing component below the floor appends its own mirror, following the same one-way (component → input) sync discipline established in Plan 02. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08b | Repudiation / double-submit | All value-bearing batch-A2 components' FormData | high | mitigate | Each component's below-floor branch is gated on `!this.internals` (or `!this._internals`) — the same guard that already prevents `setFormValue` above the floor — so the two channels are structurally exclusive per component. Task 1/2's XOR test cases re-verify this per component. |
| T-10-09b | Tampering | radio checked-state omission | low | accept | An unselected radio below the floor must be ABSENT from FormData (matching native radio semantics) — implemented via teardown-on-deselect rather than syncing an empty value, avoiding a divergent present-but-empty state native forms never produce. |

Highest severity present: T-10-08b (high) — mitigated by the shared internals-presence guard; no threat left unmitigated at high severity.
</threat_model>

<verification>
- `npx vitest run --project jsdom test/capabilities-off-constructor.batch-a2.test.ts test/form-fallback-integration.batch-a2.test.ts src/components/number-field src/components/input-otp src/components/radio src/components/rich-select src/components/select` green — no regression across this batch's touched components.
- `npx tsc --noEmit` clean.
- This batch's constructor-no-throw file covers 6 tags; combined with Plan 01 (am-input), Plan 04 (batch A1, 5 tags), and Plan 06 (batch B, 4 tags), all 16 form-associated tags are covered across the four partitioned files.
</verification>

<success_criteria>
5 more form-associated components (6 attach sites, counting both radio classes) are fully degradation-capable: guarded ElementInternals attach (COMPAT-02) plus, where value-bearing, an XOR-gated, FormData-parity-proven hidden-input fallback with one-time dev warning (COMPAT-03). Combined with Plan 04, batch A is complete (11 files); Plan 06 finishes the final 4 components. Each rollout plan owns distinct test files, so all three run collision-free in parallel wave 2.
</success_criteria>

<output>
Create `.planning/phases/10-graceful-degradation-compatibility-matrix/10-05-SUMMARY.md` when done.
</output>
